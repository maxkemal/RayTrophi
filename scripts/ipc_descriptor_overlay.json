{
  "rig.get_bone_envelope": {
    "summary": "Read the resolved tapered envelope profile for one bone segment",
    "returns": "Bone profile, override flag and rig revision",
    "tags": "rig|weights|envelope|bone|profile|read",
    "related": "rig.apply_bone_envelope|rig.set_envelope_overlay|rig.get_weight_map",
    "notes": "Returns a stored per-bone override or category-derived defaults. Radii are fractions of canonical character height; start/end extensions are fractions of bone length. Detailed humanoid digit roles default to 30% of extremity_radius, clamped to .005, so closely spaced fingers do not inherit a palm-sized capsule. The bone must own an outgoing deforming segment.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "bone": {
        "type": "string",
        "required": true
      }
    }
  },
  "rig.apply_bone_envelope": {
    "summary": "Store one tapered bone envelope and rebuild all authored skin weights",
    "returns": "ok",
    "tags": "rig|weights|envelope|bone|profile|apply",
    "related": "rig.get_bone_envelope|rig.preview_envelope_weights|rig.get_weight_map",
    "notes": "Atomically stores the per-bone start/end radius, axial extensions and falloff, then recomputes every registered flat bind part against all competing envelopes. Strongest-four normalization, backend invalidation, native persistence and one heavy undo step match apply_envelope_weights. Radii .005..0.5 character height; extensions 0..0.75 bone length; falloff .5..8.",
    "undoable": true,
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "bone": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "start_radius": {
        "type": "float",
        "required": true
      },
      "end_radius": {
        "type": "float",
        "required": true
      },
      "start_extension": {
        "type": "float",
        "required": true
      },
      "end_extension": {
        "type": "float",
        "required": true
      },
      "falloff": {
        "type": "float",
        "required": true
      }
    }
  },
  "rig.get_envelope_overlay": {
    "summary": "Read the transient viewport envelope-overlay state",
    "returns": "Visibility, character and current torso/limb/extremity radius and falloff values",
    "tags": "rig|weights|envelope|overlay|viewport|read",
    "related": "rig.set_envelope_overlay|rig.preview_envelope_weights|rig.get_binding",
    "notes": "Read-only transient view state. The overlay is not saved in native projects and does not change skin weights. Capsule geometry is evaluated from the current displayed skeleton pose while radii remain fractions of canonical rest height."
  },
  "rig.set_envelope_overlay": {
    "summary": "Show or hide anatomical influence capsules in the viewport",
    "returns": "ok",
    "tags": "rig|weights|envelope|overlay|viewport|display",
    "related": "rig.get_envelope_overlay|rig.preview_envelope_weights|rig.get_binding",
    "notes": "Transient display control shared with Edit Bone UI. When visible=true, validates an authored bound rig and draws every parent-owned capsule: cyan for ordinary segments and amber for the active bone. It follows the current displayed pose, performs no weight mutation and creates no undo entry. Settings use the same ranges as preview_envelope_weights.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "visible": {
        "type": "boolean",
        "required": true
      },
      "torso_radius": {
        "type": "float",
        "required": false,
        "default": 0.16
      },
      "limb_radius": {
        "type": "float",
        "required": false,
        "default": 0.065
      },
      "extremity_radius": {
        "type": "float",
        "required": false,
        "default": 0.05
      },
      "falloff": {
        "type": "float",
        "required": false,
        "default": 2.0
      }
    }
  },
  "rig.preview_envelope_weights": {
    "summary": "Preview bounded anatomical capsule weights on an authored bound rig",
    "returns": "Envelope report with counts, radii and fallback vertices; scene unchanged",
    "tags": "rig|weights|envelope|capsule|skin|preview",
    "related": "rig.apply_envelope_weights|rig.get_weight_map|rig.weight_stats|rig.get_binding",
    "notes": "Read-only recomputation over canonical flat P_orig positions of every part in the authored binding registry. Each parent-owned bone segment receives a height-relative capsule radius selected from anatomy roles: torso, limb, hand/foot extremity or detailed-hand digit. Digit radius is max(.005, extremity_radius * .3) until a per-bone profile overrides it. Influence is zero outside the capsule, falls smoothly inside, keeps the strongest four and normalizes through the shared weight contract. Vertices outside every capsule use the nearest segment and are counted as fallback_vertices, so preview never invents unweighted rows. Edit Bone UI enables the separate transient viewport overlay after a successful preview; the IPC operation itself leaves view state unchanged. Does not support arbitrary imported skins or parts missing canonical bind positions. Limits: 256 parts, 2M vertices and 4096 joints. Settings: torso_radius .02..0.50, limb_radius .01..0.30, extremity_radius .005..0.20 of character height; falloff .5..8.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "torso_radius": {
        "type": "float",
        "required": false,
        "default": 0.16
      },
      "limb_radius": {
        "type": "float",
        "required": false,
        "default": 0.065
      },
      "extremity_radius": {
        "type": "float",
        "required": false,
        "default": 0.05
      },
      "falloff": {
        "type": "float",
        "required": false,
        "default": 2.0
      }
    }
  },
  "rig.apply_envelope_weights": {
    "summary": "Replace authored bound-mesh weights with bounded anatomical capsule weights",
    "returns": "ok",
    "tags": "rig|weights|envelope|capsule|skin|apply",
    "related": "rig.preview_envelope_weights|rig.get_weight_map|rig.weight_stats|rig.get_binding",
    "notes": "Recomputes from canonical flat P_orig using the same deterministic core as preview, then swaps all authored bound-part skin weights atomically in one heavy undo step. CPU, raster, Vulkan and OptiX animation geometry are invalidated; native project geometry persists the resulting weights. Requires the current nonnegative rig_revision from preview/get_binding. Imported skins and missing authored parts reject. Radius/falloff ranges and fallback semantics match preview.",
    "undoable": true,
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "torso_radius": {
        "type": "float",
        "required": false,
        "default": 0.16
      },
      "limb_radius": {
        "type": "float",
        "required": false,
        "default": 0.065
      },
      "extremity_radius": {
        "type": "float",
        "required": false,
        "default": 0.05
      },
      "falloff": {
        "type": "float",
        "required": false,
        "default": 2.0
      }
    }
  },
  "rig.preview_human_walk": {
    "summary": "Validate and describe a humanoid in-place walk recipe",
    "returns": "Recipe plan with frames, duration, world-scaled motion values and requirements",
    "tags": "rig|animation|walk|gait|recipe|preview|humanoid",
    "related": "rig.create_human_walk_clip|rig.get_anatomy|rig.get_pose_state|rig.get_controls",
    "notes": "Read-only validation for the first quick-motion recipe. Requires active Pose on an owned bound humanoid with canonical pelvis, left/right ankle roles and left/right arm/leg chains. Recipe values are height fractions, except cadence in steps/minute, fps and cycles. body_motion 0..1 controls pelvis/torso naturalization and defaults to .75 when omitted. Reports planted foot orientation, pelvis sway and availability of upper-spine counter rotation, head stabilization and shoulder-girdle motion from optional mapped roles. It does not mutate pose, clips, controls or history. The v1 recipe has no root motion, path, footprints, terrain query or contact channel.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "fps": {
        "type": "float",
        "required": true
      },
      "cadence": {
        "type": "float",
        "required": true
      },
      "cycles": {
        "type": "integer",
        "required": true
      },
      "stride": {
        "type": "float",
        "required": true
      },
      "step_height": {
        "type": "float",
        "required": true
      },
      "body_bounce": {
        "type": "float",
        "required": true
      },
      "arm_swing": {
        "type": "float",
        "required": true
      },
      "body_motion": {
        "type": "float",
        "required": false,
        "default": 0.75
      }
    }
  },
  "rig.create_human_walk_clip": {
    "summary": "Generate a loopable editable humanoid walk clip",
    "returns": "ok; selects the new authored bone clip",
    "tags": "rig|animation|walk|gait|recipe|generate|humanoid|clip",
    "related": "rig.preview_human_walk|rig.get_pose_state|rig.insert_pose_keys|rig.create_controls",
    "notes": "Owned bound humanoid in Pose mode, matching revision, no render job or pending preview. Generates every frame through canonical anatomy-derived two-bone limb IK and shared joint limits, then stores ordinary rigid local bone keys. Feet retain planted orientation with swing toe pitch; the pelvis transfers weight laterally, tilts and yaws while mapped spine bones counter-rotate. Optional mapped head and clavicle roles stabilize gaze and involve the shoulder girdle. body_motion 0..1 controls pelvis/torso naturalization and defaults to .75 when omitted. Reusing a name atomically replaces only an editable rig-authored clip for the same character in one undo step; imported clips still return rig_pose_clip_name_conflict. The clip is in-place and actor-placement independent; no persistent IK targets or controls are added. Height fractions: stride .05..0.80, step_height 0..0.25, body_bounce 0..0.15; arm_swing/body_motion 0..1, cadence 20..300 steps/min, cycles 1..8, fps 1..120. Maximum 1000 frames and 100000 frame-joint samples. Native project persistence stores it. Errors include rig_walk_requires_humanoid, rig_walk_requires_humanoid_roles, rig_walk_requires_humanoid_limbs, rig_walk_invalid_recipe, rig_walk_sample_limit, rig_walk_invalid_height, rig_pose_clip_name_conflict, rig_pose_preview_active and rig_edit_stale_revision.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "name": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "fps": {
        "type": "float",
        "required": true
      },
      "cadence": {
        "type": "float",
        "required": true
      },
      "cycles": {
        "type": "integer",
        "required": true
      },
      "stride": {
        "type": "float",
        "required": true
      },
      "step_height": {
        "type": "float",
        "required": true
      },
      "body_bounce": {
        "type": "float",
        "required": true
      },
      "arm_swing": {
        "type": "float",
        "required": true
      },
      "body_motion": {
        "type": "float",
        "required": false,
        "default": 0.75
      }
    }
  },
  "rig.create_aim_control": {
    "summary": "Add a one-bone aim control from a canonical anatomy role",
    "returns": "ok",
    "tags": "pose|IK|aim|look|head|wing|target|roll",
    "related": "rig.get_anatomy|rig.create_controls|rig.get_controls|rig.set_ik_target|rig.insert_ik_key|rig.bake_ik_channels",
    "notes": "Owned bound rig, matching revision, no render job/preview. role must name an existing canonical anatomy role. The service appends role.aim and derives its local aim axis from the first nonzero direct child, falling back to local +Y; a perpendicular roll-up axis is stored explicitly. The world target controls direction and pole_world controls roll. Local translation is preserved, quaternion blend occurs once, then shared joint limits may leave angular residual. Aim controls support IK/FK blend, target keys and bake; position contacts, spline shape and tip-orientation channels are rejected. Persistent anatomy version 5 stores solver, aim_axis and up_axis. Duplicate/overlapping driven bones fail. Errors include rig_ik_unknown_role, rig_ik_invalid_aim_axes, rig_ik_aim_target_at_origin, rig_ik_overlapping_controls and rig_edit_stale_revision.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "role": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.set_ik_spline": {
    "summary": "Preview a cubic spline guide for a multi-joint IK control",
    "returns": "ok; transient pose preview",
    "tags": "pose|IK|spline|shape|spine|neck|tail|bezier",
    "related": "rig.get_controls|rig.select_control|rig.insert_ik_key|rig.apply_pose_preview|rig.cancel_pose_preview|rig.bake_ik_channels",
    "notes": "Active owned bound Pose, no render job, matching rig_revision. Requires a multi-joint chain control. points_world contains exactly two finite world-space cubic Bezier interior points; [] allowed only when enabled=false. Root is the live chain anchor, end is target_world. Enabling activates IK and uses full blend if it was zero. A 257-sample curve seeds arc-length chain stations, then bounded FABRIK preserves bone lengths and anchor and blends once before joint limits. Shape is a guide, not exact curve adherence. Unreachable endpoints straighten to reach; collapsed curves fail rig_ik_degenerate_spline when solving. Default handles approximate current end tangents, so review activation preview. Apply creates one undo step; Auto Key and insert_ik_key store spline state/points in version 2 IK channels. Existing version 1 channels load unchanged. Contacts retain curve points; bake writes solved bones. Errors: rig_ik_invalid_spline, rig_ik_spline_requires_chain, rig_ik_degenerate_spline, rig_edit_stale_revision.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "points_world": {
        "type": "array",
        "required": true,
        "description": "Two [x,y,z] interior world points; empty only to disable/clear shape."
      },
      "enabled": {
        "type": "bool",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.create_chain_control": {
    "summary": "Add a multi-joint FABRIK control from a canonical anatomy chain",
    "returns": "ok",
    "tags": "pose|IK|chain|spine|neck|tail|fabrik",
    "related": "rig.get_anatomy|rig.create_controls|rig.get_controls|rig.set_ik_target|rig.bake_ik_channels",
    "notes": "Owned bound rig, matching revision, no render job/preview. chain names an existing anatomy chain of 4..64 distinct consecutive rigid bones with nonzero segments. Appends a control with that name, preserving limb controls. Duplicate names/overlapping driven bones fail. Shared create_controls service publishes one undo step and persistent anatomy version 4; re-query revision. Target solving fixes the root, preserves local translations/lengths, blends once, then shared joint limits may leave residual. Pole seeds bending when solving a changed endpoint; this is not a spline curve editor. Same target/orientation/FK/key/contact/bake services as limb IK. Errors: rig_ik_unknown_chain, rig_ik_invalid_chain, rig_ik_overlapping_controls, rig_ik_duplicate_control, rig_ik_zero_length, rig_edit_stale_revision.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "chain": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.get_ik_channels": {
    "summary": "Inspect persistent IK keys and contact intervals in the selected clip",
    "returns": "ok",
    "tags": "pose|IK|timeline|contact|bake",
    "related": "rig.get_ik_channels|rig.insert_ik_key|rig.set_ik_contact_interval|rig.bake_ik_channels",
    "notes": "Active owned bound Pose, no render job, selected editable clip required. Version 1/2 schema (v2 adds spline_enabled and two spline_world points), seconds, world-space target/pole/unit quaternion [w,x,y,z], orientation_enabled, enabled and blend. Contacts [start,end) override keys; live controls override saved channels temporarily. Saved channels evaluate in authored playback as well as Pose.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      }
    }
  },
  "rig.insert_ik_key": {
    "summary": "Key the effective limb IK control at the current frame",
    "returns": "ok",
    "tags": "pose|IK|timeline|contact|bake",
    "related": "rig.get_ik_channels|rig.insert_ik_key|rig.set_ik_contact_interval|rig.bake_ik_channels",
    "notes": "Requires active owned bound Pose, no preview/render job, selected editable clip and matching rig_revision. Captures effective handles/blend/orientation/enabled, replacing an existing key at the same time. Before first key no control; after last hold; positions/pole/blend linear, orientation slerp, spline points linear when both keys carry them, orientation/spline activation stepped; IK ramps between enabled endpoints with positive interpolated blend. Time is frame/Pose fps; clip duration extends. Live override for this limb clears. One undo step, native persistence. Max 256 channels/10000 keys+contacts. Errors: rig_pose_clip_required, rig_ik_unknown_control, rig_edit_stale_revision, rig_pose_time_limit.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      }
    }
  },
  "rig.remove_ik_key": {
    "summary": "Remove one IK control key at the current frame",
    "returns": "ok",
    "tags": "pose|IK|timeline|key|remove",
    "related": "rig.get_ik_channels|rig.insert_ik_key|rig.clear_ik_channels",
    "notes": "Requires active owned bound Pose, no preview/render job, selected editable clip and matching rig_revision. Removes only the exact current-frame key for the named control; contact intervals and keys at other times remain unchanged. Empty channels are removed. Missing keys fail rig_edit_no_change. One undo step and native persistence."
  },
  "rig.set_ik_contact_interval": {
    "summary": "Capture the current achieved limb pose for a timed contact",
    "returns": "ok",
    "tags": "pose|IK|timeline|contact|bake",
    "related": "rig.get_ik_channels|rig.insert_ik_key|rig.set_ik_contact_interval|rig.bake_ik_channels",
    "notes": "Same mutation guards as insert_ik_key. Frames become seconds using Pose fps. Captures CURRENT achieved world tip and pole, optional achieved orientation; full IK, half-open [start_frame,end_frame). Exact same interval replaces capture; overlapping intervals fail rig_ik_contact_overlap; adjacent intervals valid. End must exceed start. Outside interval keyed controls resume, otherwise FK. One undo step and native persistence; clears limb live pin. Errors include rig_ik_invalid_contact_interval.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "start_frame": {
        "type": "integer",
        "required": true
      },
      "end_frame": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.clear_ik_channels": {
    "summary": "Remove all saved IK keys and contact intervals for one limb",
    "returns": "ok",
    "tags": "pose|IK|timeline|contact|bake",
    "related": "rig.get_ik_channels|rig.insert_ik_key|rig.set_ik_contact_interval|rig.bake_ik_channels",
    "notes": "Same mutation guards as insert_ik_key. Clears only selected clip/control and its live override, preserving bone channels and clip duration. One undo step. Missing channel fails rig_edit_no_change.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      }
    }
  },
  "rig.bake_ik_channels": {
    "summary": "Bake saved IK channels into a new bone clip over an inclusive frame range",
    "returns": "ok",
    "tags": "pose|IK|timeline|contact|bake",
    "related": "rig.get_ik_channels|rig.insert_ik_key|rig.set_ik_contact_interval|rig.bake_ik_channels",
    "notes": "Same mutation guards as insert_ik_key. Samples original bone clip -> saved IK -> joint limits -> all bone keys at every frame, using current fixed actor placement. Excludes transient live/FK edits. Creates/selects a new named clip with no IK channels; original unchanged. Existing bone channels outside range are copied. At most 1001 frames and 100000 bone samples; frames nonnegative <=1000000, ticks <=1000000. One undo step/native persistence. Errors: rig_ik_bake_limit, rig_ik_channels_empty, rig_pose_clip_name_conflict, invalid_clip_name.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "name": {
        "type": "string",
        "required": true
      },
      "start_frame": {
        "type": "integer",
        "required": true
      },
      "end_frame": {
        "type": "integer",
        "required": true
      }
    }
  },
  "$comment": "Hand-written half of the IPC method descriptors. Parameters, types, defaults and capabilities are extracted from the dispatch code by gen_ipc_descriptors.py - do NOT repeat them here unless you are adding a description, an enum hint or correcting a type the parser could only see as 'any'. Keys: summary (required, one line, present tense), notes, tags (extra search synonyms; the domain and the method name are added automatically), related (pipe-separated), returns, undoable, access, params{name:{description,enum,type,required,default}}.",
  "rig.get_joint_limit_view": {
    "summary": "Inspect the selected joint's rest-relative rotation frame, limits and IK/FK state",
    "returns": "character, bone, rig_revision, owned, has_rule, rule, neutral_world, joint_world, twist_degrees, swing_degrees, outside_limits, ik_driven, pose_source, limit_space",
    "tags": "joint|limits|overlay|hinge|ball|IK|FK",
    "related": "rig.get_joint_profile|rig.set_joint_limits|rig.set_joint_limit_overlay",
    "notes": "Read-only, no render job. Full bone keys required. neutral_world is the current parent joint global composed with canonical local rest rotation, anchored at the current joint origin; joint_world is the actual evaluated joint transform. Both are row-major 16-float world matrices, independent of inverse skin offsets. Angles in degrees use the same rest-relative swing/twist convention as joint rules; IK-driven state follows active Pose controls. Imported rigs can be inspected but mutations require ownership. Missing hierarchy/parent or unstable transform fails explicitly. No envelope or skin weight editing is implied.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "bone": {
        "type": "string",
        "required": true
      }
    }
  },
  "rig.set_joint_limits": {
    "summary": "Atomically update an existing hinge/ball joint's angular limits",
    "returns": "ok",
    "tags": "joint|limits|hinge|ball|authoring|undo",
    "related": "rig.get_joint_limit_view|rig.get_joint_profile|rig.set_joint_profile",
    "notes": "Owned rig, no render job or outstanding pose preview, matching nonnegative rig_revision. Existing hinge/ball rule required. Finite degrees: minimum -180..0, maximum 0..180, swing 0..180. Hinge does not use swing for evaluation but stores the validated field. Axis, enabled and lock_translation remain unchanged. Same core/profile history transaction as inspector settings, one undo step; limits persist in native anatomy, evaluator invalidates once. Re-query revision after success. Errors include rig_not_owned, rig_edit_stale_revision, rig_joint_rule_required, rig_joint_limits_not_applicable, rig_joint_invalid_range, rig_joint_no_change, rig_pose_preview_active and scene_locked. Rest/bind/weights are not modified.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "bone": {
        "type": "string",
        "required": true
      },
      "minimum": {
        "type": "float",
        "required": true
      },
      "maximum": {
        "type": "float",
        "required": true
      },
      "swing": {
        "type": "float",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.get_joint_limit_overlay": {
    "summary": "Inspect transient active-joint axes and limits display/edit settings",
    "returns": "visible, edit, scope=active_joint",
    "tags": "joint|limits|overlay|viewport",
    "related": "rig.set_joint_limit_overlay|rig.get_joint_limit_view",
    "notes": "Read-only transient session state, not project metadata. Detailed axes/limits display is scoped to the active joint. It does not enable or disable joint enforcement. No envelope display is included."
  },
  "rig.set_joint_limit_overlay": {
    "summary": "Show selected joint axes/limits and enable viewport limit handles",
    "returns": "ok",
    "tags": "joint|limits|overlay|viewport|authoring",
    "related": "rig.get_joint_limit_overlay|rig.get_joint_limit_view|rig.set_joint_limits",
    "notes": "Transient flags; no project modification or history entry. edit=true requires visible=true, no render job and no pose preview; pauses playback. Edit mode gives angular boundary handles priority over FK/IK/actor gizmos. Viewport handles preview bounds visually; release calls set_joint_limits once, Escape discards. Unknown/unsupported rule or imported rig remains display-only. Rule activation/axis changes use existing joint-profile service. Errors: rig_joint_overlay_hidden, rig_pose_preview_active, scene_locked. No separate UI business logic or persistent preview state.",
    "params": {
      "visible": {
        "type": "bool",
        "required": true
      },
      "edit": {
        "type": "bool",
        "required": true
      }
    }
  },
  "rig.get_controls": {
    "summary": "Inspect owned bound-rig IK, chain and aim controls",
    "returns": "character, rig_revision, controls, display_contract, selected_control, handle, coordinate_space, contact_kind, target_keys",
    "tags": "pose|IK|FK|contact|controls",
    "related": "rig.create_controls|rig.create_chain_control|rig.create_aim_control|rig.select_control|rig.set_ik_target|rig.set_ik_fk|rig.match_ik_to_fk|rig.match_fk_to_ik|rig.set_ik_contact|rig.get_pose_state",
    "notes": "Owned bound rig, no render job. Rows report solver, bones, root/mid/tip keys, enabled, blend, contact, target_world, pole_world, tip_world, target_error_world, aim_error_degrees, aim_axis/up_axis, orientation state, spline state/guide, length_actor and placement-scaled length_world. Each row has a display descriptor with semantic, side/color role and target/pole/FK shape, level, scale and channel metadata. The top-level display_contract version 2 declares hybrid_anatomical_clamped visual sizing from length_world, a screen_constant_minimum hit space, Primary/Secondary/Deform levels, built-in shape/color vocabularies and the contact marker. Presentation is derived by the shared RigControlDisplay core from canonical anatomy roles. Two-bone rows also report root/mid fk_handles in world space. Display data does not alter solver state or deform bones. Residual is measured after IK blend and joint rules. Limb definitions persist in anatomy v3, full chains in v4 and aim controls in v5; runtime handle selection is transient. Position contacts apply to limb/chain controls. Timed target keys and bake support all solver types.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      }
    }
  },
  "rig.create_controls": {
    "summary": "Create or replace canonical limb, chain and aim definitions",
    "returns": "ok",
    "tags": "pose|IK|controls|setup",
    "related": "rig.get_controls|rig.create_chain_control|rig.create_aim_control|rig.set_mode|rig.set_ik_target|rig.set_ik_fk",
    "notes": "Owned bound rig, no render job or pending preview. Null/omitted controls derive anatomy _arm/_leg chains; humanoid arms skip clavicle, legs start at index zero. Supply [{name,root,mid,tip}] for explicit custom limbs; add chain:[root,...,tip] for FABRIK (4..64 bones, mid = second bone). Aim rows use solver:aim, the same bone in root/mid/tip, and finite orthonormal aim_axis/up_axis vectors. [] clears definitions unless saved channels reference them. At most 256 controls/4096 joints, rigid bones, no duplicate names or shared driven joints. Errors include rig_ik_invalid_controls, rig_ik_invalid_solver, rig_ik_invalid_aim_axes, rig_ik_invalid_chain, rig_ik_zero_length, rig_ik_overlapping_controls, rig_ik_duplicate_control, rig_ik_invalid_name, unknown_bone, rig_ik_no_limb_chains, rig_edit_stale_revision, rig_revision_overflow and rig_edit_no_change. One undo step persists definitions/revision and resets handles; undo restores prior runtime state. Rest/bind/weights remain unchanged. Re-query revision after success.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "controls": {
        "type": "array|null",
        "required": false,
        "description": "null/omitted = derive anatomy limbs; array = replace; [] = clear."
      }
    }
  },
  "rig.select_control": {
    "summary": "Select an IK target, pole, aim roll, orientation or spline handle",
    "returns": "ok",
    "tags": "pose|IK|selection|viewport",
    "related": "rig.get_controls|rig.set_ik_target|rig.select_bone",
    "notes": "Active Pose, no render job or pending preview. Transient selection, no undo/project modification. Empty control returns to bone FK gizmo. Handle is target or pole (Translate), orientation (Rotate), or spline_0/spline_1 (Translate, chain controls only). For aim controls target sets direction and pole is the roll-up handle; independent orientation is unsupported. Errors: rig_pose_mode_required, rig_pose_preview_active, rig_ik_unknown_control, rig_ik_invalid_handle. Successful bone selection returns to the FK bone gizmo, discards an uncommitted IK preview and preserves committed IK/contacts. Re-select a control to resume its target gizmo.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "handle": {
        "type": "string",
        "required": false,
        "default": "target"
      }
    }
  },
  "rig.set_ik_target": {
    "summary": "Preview a limb target and bend pole in world space",
    "returns": "ok; transient pose preview",
    "tags": "pose|IK|target|pole",
    "related": "rig.get_controls|rig.apply_pose_preview|rig.cancel_pose_preview|rig.set_pose_auto_key",
    "notes": "Active owned bound Pose, no render job, matching revision. target_world/pole_world are finite 3-number world points. First activation matches the current pose, enables IK. Replaceable preview: FK input -> parent-first limb solve -> joint rules -> skin. Blends once, preserves segment lengths/translations. Unreachable targets clamp to reach; collinear pole falls back to current bend then a deterministic perpendicular. Inspect get_controls.target_error_world and get_pose_state.limit_hits. Apply creates one undo step; Auto Key writes IK control channels and undriven FK bones; Cancel discards preview. Noncontact handles reset on frame change; contacts survive scrub during Pose. Native files store definitions, bone keys and explicit IK channels; unkeyed live targets remain transient. Errors include rig_ik_invalid_target, rig_ik_unknown_control, rig_ik_invalid_placement and rig_edit_stale_revision.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "target_world": {
        "type": "vec3",
        "required": true
      },
      "pole_world": {
        "type": "vec3",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.set_ik_fk": {
    "summary": "Preview IK/FK blend with matched activation and pose-preserving FK return",
    "returns": "ok; transient pose preview",
    "tags": "pose|IK|FK|blend",
    "related": "rig.get_controls|rig.set_ik_target|rig.apply_pose_preview|rig.cancel_pose_preview",
    "notes": "Active owned bound Pose, no render job, matching revision. Finite blend 0..1. Activation matches current target/pole, avoiding positional snap. 0 disables IK/contact and copies achieved limb rotations into FK input. Local quaternions blend once. Apply/cancel/Auto Key use common pose transaction. Direct FK edits of driven joints fail rig_ik_bone_driven; un-driven pelvis/root remain editable for world contacts. Pose mirror requires FK (rig_ik_switch_to_fk). Errors include rig_ik_invalid_blend, rig_ik_unknown_control and rig_edit_stale_revision.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "blend": {
        "type": "float",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.match_ik_to_fk": {
    "summary": "Match a two-bone IK control to the visible FK pose and switch fully to IK",
    "returns": "ok; transient pose preview",
    "tags": "pose|IK|FK|match|snap",
    "related": "rig.get_controls|rig.match_fk_to_ik|rig.apply_pose_preview|rig.cancel_pose_preview",
    "notes": "Active owned bound Pose, no render job, matching revision. Only canonical two-bone limb controls are accepted. Captures the visible tip position, bend plane and world tip orientation, disables contact, enables orientation hold and switches to full IK without changing the visible pose. Apply commits through the shared pose transaction; Cancel restores the prior state. Chain/aim controls fail rig_ik_match_requires_two_bone.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.match_fk_to_ik": {
    "summary": "Bake a visible two-bone IK result into FK and switch fully to FK",
    "returns": "ok; transient pose preview",
    "tags": "pose|IK|FK|match|snap",
    "related": "rig.get_controls|rig.match_ik_to_fk|rig.apply_pose_preview|rig.cancel_pose_preview",
    "notes": "Active owned bound Pose, no render job, matching revision. Only canonical two-bone limb controls are accepted. Copies the achieved root/mid/tip local rotations into the FK input, clears contact and disables IK, preserving the visible pose. Apply commits through the shared pose transaction; Cancel restores the prior state. Chain/aim controls fail rig_ik_match_requires_two_bone.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.set_ik_contact": {
    "summary": "Preview a world-position contact captured from the achieved limb pose",
    "returns": "ok; transient pose preview",
    "tags": "pose|IK|contact|plant",
    "related": "rig.get_controls|rig.set_ik_target|rig.apply_pose_preview|rig.cancel_pose_preview|rig.insert_pose_keys",
    "notes": "Active owned bound Pose, no render job, matching revision. true captures achieved tip position/current pole, enables full IK and pins position across frame scrub/root FK edits. false releases the scrub pin, retaining current-frame IK; blend=0 returns to FK with pose preserved. Apply creates one undo step; Auto Key writes control channels for IK edits and undriven FK bone channels; use bake for solved IK bones. Optional tip orientation is controlled by set_ik_orientation and recaptured with the contact. No inferred ground; timed contacts use set_ik_contact_interval. Contacts clear on mode exit/clip selection; key controls or bake solved bones for native persistence. Inspect residual and limit_hits.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "enabled": {
        "type": "bool",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "rig.mirror_pose": {
    "summary": "Preview paired rest-relative FK motion on the opposite side",
    "returns": "ok; transient preview",
    "tags": "pose|FK|mirror",
    "related": "rig.get_anatomy|rig.apply_pose_preview|rig.cancel_pose_preview",
    "notes": "Active owned bound Pose, no render job or pending preview; switch IK to FK first. Copies local rest-relative motion through joint rest orientation bases across actor-local x/y/z, preserving target rest origins/lengths. Uses anatomy pairs, no name guessing. selected requires one side per pair; both sides fail rig_mirror_ambiguous_pair. Directional copy with empty bones selects all pairs. Apply commits one history step, Auto Key writes changed channels; Cancel discards preview. Matching nonnegative revision required.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "bones": {
        "type": "array",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      },
      "direction": {
        "type": "string",
        "required": false,
        "default": "selected"
      },
      "axis": {
        "type": "string",
        "required": false,
        "default": "x"
      }
    }
  },
  "agent.discover": {
    "summary": "Identify the application and list every capability domain, agent role and coverage metric",
    "notes": "First call for a new agent session. registered_methods is how many methods are dispatched; documented_coverage is the share that carries a hand-written summary. A documented_coverage below 1.0 means agent.describe will answer some methods with parameters but no explanation.",
    "tags": "bootstrap|handshake|capabilities|introspection",
    "related": "agent.list_methods|agent.describe|agent.search_capabilities",
    "returns": "DiscoveryInfo"
  },
  "agent.list_methods": {
    "summary": "List every registered IPC method, optionally filtered to one domain",
    "notes": "The full list is around 30 KB. Cache it once per session instead of calling it per step.",
    "tags": "catalogue|introspection",
    "related": "agent.describe|agent.search_capabilities",
    "returns": "MethodSummary[]",
    "params": {
      "domain": {
        "description": "Domain to restrict the listing to, e.g. 'fluid'. Omit for every method."
      }
    }
  },
  "agent.describe": {
    "summary": "Return the full parameter schema, capability and related methods for one IPC method",
    "notes": "Parameters come from the dispatch code itself, so they are exact. `documented: false` means the schema is real but nobody has written the prose yet - trust the parameters, be careful with intent.",
    "tags": "schema|introspection|parameters",
    "related": "agent.list_methods|agent.get_examples",
    "returns": "MethodDescriptor",
    "params": {
      "method": {
        "description": "Exact method name, e.g. 'fluid.create_domain'"
      }
    }
  },
  "agent.search_capabilities": {
    "summary": "Search workflow recipes and method metadata for a plain-language goal",
    "notes": "Recipes are the valuable half: each one is an ordered list of calls that is known to work end to end. Prefer following a recipe over assembling calls from the method hits.",
    "tags": "search|howto|recipe|workflow|goal",
    "related": "agent.get_examples|agent.describe",
    "returns": "SearchResult",
    "params": {
      "query": {
        "description": "Plain language goal, e.g. 'make a wooden object burn'"
      }
    }
  },
  "agent.get_examples": {
    "summary": "Return runnable call sequences for a method or a named workflow recipe",
    "tags": "example|sample|recipe",
    "related": "agent.search_capabilities|agent.describe",
    "returns": "ExampleSet",
    "params": {
      "method": {
        "description": "Method to show examples for, e.g. 'fluid.create_domain'"
      },
      "workflow": {
        "description": "Recipe id from agent.search_capabilities, e.g. 'combustion_setup'"
      }
    }
  },
  "agent.get_state_summary": {
    "summary": "Compact snapshot of scene, lights, camera, timeline and viewport measurement state",
    "notes": "The viewport block is a MEASUREMENT and is only present when frame capture is on and a frame exists; when it is absent that means 'not measured', never 'measured zero'. Turn capture on with viewport.capture before relying on it.",
    "tags": "state|context|snapshot|scene|verify",
    "related": "viewport.status|render.probe|scene.list_objects",
    "returns": "StateSummary",
    "params": {
      "include_probe": {
        "description": "Sample the last viewport frame for luminance and black/NaN fractions. Costs a full-frame scan.",
        "type": "bool"
      }
    }
  },
  "agent.roles": {
    "summary": "Describe the manager/controller/worker agent roles and their delegation chain",
    "notes": "Informational only. Access is decided by the security capability on the connection token, not by the role an agent claims.",
    "tags": "role|hierarchy|delegation|multi-agent",
    "returns": "RoleSet"
  },
  "agent.chat_send": {
    "summary": "Post a message into the Studio Agent Chat panel",
    "notes": "This WRITES to the user interface, so it needs the AgentChat capability - it is the one agent.* method that is not read-only. Use type='activity' for per-call progress, 'thought' for reasoning, 'error' for failures; the panel colours and filters them separately.",
    "tags": "chat|message|ui|report|progress",
    "related": "agent.chat_poll",
    "returns": "bool",
    "params": {
      "sender": {
        "description": "Display name shown in the panel, e.g. 'RayTrophi Agent'"
      },
      "content": {
        "description": "Message text"
      },
      "type": {
        "description": "Message kind",
        "type": "string",
        "enum": "reply|activity|thought|error",
        "default": "reply"
      },
      "payload": {
        "description": "Optional structured data appended to the message"
      },
      "image_base64": {
        "description": "Optional base64 image; the panel notes the attachment"
      }
    }
  },
  "agent.send_prompt": {
    "summary": "Queue a task for another agent to pick up on its next poll, addressed by agent id or 'all'",
    "notes": "Returns queued:true, NOT delivered - nothing has run yet and the target agent may never poll. Do not report the work as done on the strength of this call; ask for the result, or check the scene yourself. The target must match the id the other agent polls with (agent.chat_poll agent_id), and 'all' broadcasts.",
    "tags": "agent|delegate|multi-agent|prompt|task|handoff",
    "related": "agent.chat_poll|agent.chat_send|agent.roles",
    "verify_with": [
      "agent.get_state_summary"
    ]
  },
  "agent.chat_poll": {
    "summary": "Take the user prompts queued in the Agent Chat panel and mark the agent as alive",
    "notes": "Polling is also the panel's heartbeat: it shows the agent as online for a few seconds after each call. An agent that blocks on a long model turn should keep polling from a second thread, or the panel reports it offline.",
    "tags": "chat|poll|prompt|heartbeat",
    "related": "agent.chat_send",
    "returns": "QueuedPrompt[]"
  },
  "version": {
    "summary": "Return the RayTrophi Studio version string"
  },
  "batch": {
    "summary": "Execute several IPC calls in order and return one result array",
    "notes": "Each child call is authorized on its own; nesting a batch inside a batch is refused. A failing child does not stop the rest.",
    "tags": "sequence|bulk|transaction",
    "params": {
      "calls": {
        "description": "Array of {method, params} objects",
        "type": "array",
        "required": true
      }
    }
  },
  "undo": {
    "summary": "Undo the last recorded scene command",
    "tags": "history",
    "related": "redo|undo_description"
  },
  "redo": {
    "summary": "Redo the last undone scene command",
    "tags": "history",
    "related": "undo|redo_description"
  },
  "undo_description": {
    "summary": "Name of the command that undo would revert",
    "tags": "history"
  },
  "redo_description": {
    "summary": "Name of the command that redo would reapply",
    "tags": "history"
  },
  "request_render": {
    "summary": "Ask the viewport to render another frame",
    "tags": "viewport|refresh"
  },
  "reset_accumulation": {
    "summary": "Restart progressive sample accumulation in the viewport",
    "notes": "Post-process changes must NOT reset accumulation - only changes to scene, camera or lighting should.",
    "tags": "viewport|samples|refresh"
  },
  "scene.list_objects": {
    "summary": "List every object in the scene by name",
    "notes": "Covers both flat SoA meshes and legacy facade objects, so it is the reliable inventory call.",
    "tags": "inventory|objects|browse",
    "related": "scene.object_info|scene.object_exists",
    "returns": "string[]"
  },
  "scene.object_exists": {
    "summary": "Report whether an object with this name exists",
    "returns": "bool",
    "related": "scene.list_objects"
  },
  "scene.object_info": {
    "summary": "Report an object's triangle/vertex counts and its local and world bounding box",
    "returns": "ObjectInfo",
    "related": "scene.list_objects",
    "notes": "has_bounds false means the object carries no geometry, and the bound fields are then ABSENT rather than zeroed - a zeroed box would read as a point at the origin. world_size/world_center are derived from the world box and are what an assembly check needs (does this leg actually reach that rail). 'meshes' counts how many meshes carry the name: a multi-material import is one object spread over several meshes, and the counts are the aggregate, not a first-hit sample."
  },
  "scene.add_primitive": {
    "summary": "Create a primitive mesh object and return its final name",
    "notes": "The name is made unique by suffix, so the returned name may differ from the requested one - always use the returned name afterwards.",
    "tags": "create|mesh|cube|sphere|plane|cylinder|torus",
    "related": "scene.delete|scene.set_transform|material.assign",
    "returns": "string",
    "undoable": true,
    "params": {
      "type": {
        "description": "Primitive shape",
        "enum": "cube|sphere|plane|cylinder|torus"
      },
      "name": {
        "description": "Requested name; a numeric suffix is added if it is taken"
      },
      "size": {
        "description": "Edge length or radius in metres; must be positive"
      }
    }
  },
  "scene.delete": {
    "summary": "Delete an object from the scene",
    "undoable": true,
    "tags": "remove"
  },
  "scene.duplicate": {
    "summary": "Duplicate an object and return the new object's name",
    "undoable": true,
    "returns": "string",
    "tags": "copy|clone"
  },
  "scene.get_transform": {
    "summary": "Return an object's translation, rotation, scale and full matrix",
    "returns": "TransformInfo",
    "related": "scene.set_transform|scene.get_world_transform",
    "notes": "This is the AUTHORED transform and a solver never writes it: a rigid body can fall for a thousand steps while this keeps returning the spawn pose. To measure physics use scene.get_world_transform."
  },
  "scene.get_world_transform": {
    "summary": "Return an object's SIMULATED world pose: the solver's motion composed onto the authored transform",
    "notes": "The rigid solver bakes its motion into the mesh vertices and never writes a transform, so scene.get_transform reports the spawn pose for a body that has been falling for hundreds of steps. Every physics measurement - free fall, buoyancy draft, restitution, settling - has to read this one. The result carries simulated: false when no solver has contributed, which is not the same as the body having stayed still.",
    "tags": "transform|physics|simulated|pose|measure|verify",
    "related": "scene.get_transform|physics.step|physics.add_body",
    "verify_with": [
      "scene.get_transform"
    ]
  },
  "scene.set_transform": {
    "summary": "Set an object's transform, either as a matrix or as translation/rotation/scale components",
    "notes": "Send `matrix`, or any combination of translation/rotation/scale - components you omit keep their current value. Rotation is in degrees.",
    "tags": "move|rotate|scale|position|placement",
    "related": "scene.get_transform",
    "undoable": true,
    "params": {
      "name": {
        "description": "Object name"
      },
      "matrix": {
        "description": "Row-major 4x4 matrix; wins over the component form when both are sent"
      },
      "translation": {
        "description": "World position in metres"
      },
      "rotation": {
        "description": "Euler angles in degrees"
      },
      "scale": {
        "description": "Per-axis scale factors"
      }
    }
  },
  "scene.import_model": {
    "summary": "Import a model file (glTF/FBX/OBJ) into the scene",
    "tags": "load|asset|mesh"
  },
  "select.object": {
    "summary": "Select an object by name, optionally adding to the current selection",
    "tags": "selection"
  },
  "select.light": {
    "summary": "Select a light by index, optionally adding to the current selection",
    "tags": "selection"
  },
  "select.deselect_object": {
    "summary": "Remove one object from the selection",
    "tags": "selection"
  },
  "select.all_objects": {
    "summary": "Select every object in the scene",
    "tags": "selection"
  },
  "select.clear": {
    "summary": "Clear the selection",
    "tags": "selection"
  },
  "select.list": {
    "summary": "List the currently selected objects and lights",
    "returns": "SelectionEntry[]",
    "tags": "selection"
  },
  "camera.get": {
    "summary": "Return camera position, target, up vector, field of view, focus distance, aperture and the physical exposure state",
    "notes": "EXPOSURE IS A RELATIVE MODEL, NOT AN ABSOLUTE PHOTOMETRIC ONE. The textbook formula 1/(1.2*2^EV100) assumes scene radiance in cd/m2; this engine's light intensities are arbitrary, so the multiplier is a ratio against a calibrated baseline (0.00003125) chosen to avoid a black viewport. Do not 'fix' it into the absolute formula - every scene would go black and the symptom would read as 'too dark', not 'wrong formula'. 'iso_value', 'shutter_seconds', 'f_number' and 'exposure_factor' are DERIVED read-only outputs, not settings: a preset INDEX is not a measurement, and exposure_factor is the number actually handed to the shaders. 'aperture' is the depth-of-field dial and does NOT affect exposure; 'fstop_preset_index' is the exposure/lens dial and does NOT affect depth of field. PRIORITY ORDER, and it is the most common reason these dials look dead: the exposure MODE now lives in the post chain, not on the camera. Read post.get_exposure 'mode' - only 'physical' reads ISO/shutter/f-stop; under 'manual' and 'auto_histogram' they are NOT read and the camera term is 1.0. Set it first with post.configure_exposure {\"settings\":{\"mode\":\"physical\"}}. The camera's own auto_exposure / use_physical_exposure flags are LEGACY: syncDisplay forces both in physical mode, so toggling them does not change the Vulkan or Rendered image. These setters still SUCCEED because they really do write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor for the multiplier ACTUALLY applied (it is g_display_post.camera_exposure, so it reads 1.0 whenever the mode is not physical)."
  },
  "camera.set_position": {
    "summary": "Move the camera to a world position",
    "tags": "view|move"
  },
  "camera.set_target": {
    "summary": "Aim the camera at a world position",
    "tags": "view|look|aim"
  },
  "camera.set_fov": {
    "summary": "Set the camera field of view in degrees",
    "tags": "view|lens|zoom"
  },
  "camera.set_focus_distance": {
    "summary": "Set the depth-of-field focus distance in metres",
    "tags": "view|lens|dof|blur"
  },
  "camera.set_aperture": {
    "summary": "Set the camera aperture; larger values give shallower depth of field",
    "notes": "The AMOUNT, not the switch: writing an aperture does not turn depth of field on. Call camera.set_depth_of_field first (or read camera.get 'depth_of_field'), then read 'effective_lens_radius' - it is 0 whenever the lens is off, and that, not 'aperture', is what every renderer samples. The aperture is the twin of the f-number: camera.get 'f_number' is derived from it through the focal length, and the panel f-stop dial writes both.",
    "tags": "view|lens|dof|blur|bokeh"
  },
  "lights.list": {
    "summary": "List the scene lights with index, name, type and position",
    "returns": "LightSummary[]",
    "tags": "lighting"
  },
  "lights.get": {
    "summary": "Return every property of one light",
    "returns": "LightInfo",
    "tags": "lighting"
  },
  "lights.add": {
    "summary": "Add a light of the given type at a world position and return its name",
    "tags": "lighting|create|illuminate",
    "related": "lights.set_intensity|lights.set_color|world.set_mode",
    "undoable": true,
    "params": {
      "type": {
        "description": "Light type",
        "enum": "point|directional|spot|area"
      }
    }
  },
  "lights.delete": {
    "summary": "Delete a light by index",
    "undoable": true,
    "tags": "lighting|remove"
  },
  "lights.rename": {
    "summary": "Rename a light",
    "tags": "lighting"
  },
  "lights.set_position": {
    "summary": "Move a light to a world position",
    "tags": "lighting|move"
  },
  "lights.set_direction": {
    "summary": "Set the direction a directional or spot light points in",
    "tags": "lighting|aim"
  },
  "lights.set_color": {
    "summary": "Set a light's RGB colour",
    "tags": "lighting|colour"
  },
  "lights.set_intensity": {
    "summary": "Set a light's intensity",
    "tags": "lighting|brightness|exposure"
  },
  "lights.set_visible": {
    "summary": "Show or hide a light without deleting it",
    "tags": "lighting|visibility"
  },
  "lights.set_param": {
    "summary": "Set one numeric light parameter by name (radius, spot_angle, spot_falloff, width, height)",
    "notes": "spot_angle and spot_falloff apply to spot lights only; width and height to area lights only. The call fails rather than silently ignoring a mismatch.",
    "tags": "lighting|softness|cone",
    "params": {
      "param": {
        "enum": "intensity|radius|width|height|spot_angle|spot_falloff",
        "description": "Which light property to set. Not every key applies to every light type - a key the light does not have is refused, not ignored."
      }
    }
  },
  "world.get": {
    "summary": "Return world mode, background colour, sun angles and atmosphere settings",
    "returns": "WorldState",
    "tags": "environment|sky"
  },
  "world.set_mode": {
    "summary": "Switch the world background between flat colour, HDRI and physical sky",
    "tags": "environment|sky|background|hdri",
    "params": {
      "mode": {
        "description": "Background model",
        "enum": "solid|hdri|nishita"
      }
    }
  },
  "world.set_background_color": {
    "summary": "Set the flat background colour used in solid world mode",
    "tags": "environment|sky|colour"
  },
  "world.set_sun_elevation": {
    "summary": "Set the sun elevation angle of the physical sky",
    "tags": "environment|sky|sun|time-of-day"
  },
  "world.set_sun_azimuth": {
    "summary": "Set the sun compass angle of the physical sky",
    "tags": "environment|sky|sun|direction"
  },
  "world.set_sun_intensity": {
    "summary": "Set the sun intensity of the physical sky",
    "tags": "environment|sky|sun|brightness"
  },
  "world.set_sun_size": {
    "summary": "Set the sun's angular size; larger values give softer shadows",
    "tags": "environment|sky|sun|shadow|softness"
  },
  "world.set_atmosphere_intensity": {
    "summary": "Set the strength of atmospheric scattering in the physical sky",
    "tags": "environment|sky|haze|fog"
  },
  "world.get_thermal": {
    "summary": "Return the ambient thermal condition every uncoupled substance relaxes toward",
    "notes": "Distinct from world.get: that is the render sky, this is WorldThermalState -- the room temperature (ambient_kelvin), the Kelvin-per-normalized-unit calibration a substance's MSF temperature is read against (kelvin_per_unit), the passive-cooling multiplier (convection_coefficient) and the pyrolysis rate scale (oxygen_availability). Formerly no scripting surface existed for any of this -- see docs/dev/SIMULATION_NODE_OBJECT_MODEL.md section 7 item 1.",
    "returns": "WorldThermalInfo",
    "tags": "simulation|thermal|ambient|world|msf",
    "related": "world.set_thermal|sim_graph.list"
  },
  "world.set_thermal": {
    "summary": "Set the ambient thermal condition every uncoupled substance relaxes toward",
    "notes": "Every field optional: omit a key to leave it unchanged. oxygen_availability is clamped to 0..1 rather than rejected. A domain's own thermal_override_enabled (SimulationGridDomainDesc) still wins over this where it is set -- this call changes only the default a domain has not overridden.",
    "tags": "simulation|thermal|ambient|world|msf",
    "related": "world.get_thermal",
    "params": {
      "ambient_kelvin": {
        "description": "Room temperature everything relaxes toward, in Kelvin. Must be positive."
      },
      "kelvin_per_unit": {
        "description": "Kelvin per normalized solver unit -- the calibration MSF temperature reads through. Must be positive."
      },
      "convection_coefficient": {
        "description": "Scales every substance's passive cooling toward ambient: 1 = authored, higher = draughty, 0 = a perfect thermos."
      },
      "oxygen_availability": {
        "description": "0..1, scales pyrolysis burn rate; 0 smothers combustion entirely. Clamped, not rejected, if out of range."
      }
    }
  },
  "timeline.get_frame": {
    "summary": "Return the current timeline frame",
    "tags": "time|playhead",
    "related": "timeline.set_frame"
  },
  "timeline.set_frame": {
    "summary": "Move the timeline playhead to a frame",
    "notes": "This is how simulations are advanced from a script: stepping the frame runs the solvers that are live for that frame.",
    "tags": "time|playhead|simulate|advance",
    "related": "timeline.get_frame|sim_cache.bake"
  },
  "render.start": {
    "summary": "Render one frame at the given sample count and write it to an image file",
    "notes": "Blocking work runs on the render job; poll render.status for progress. The written image can be read back, so visual checks can be automated.",
    "tags": "output|image|final|save",
    "related": "render.status|render.cancel|render.start_sequence",
    "params": {
      "output_path": {
        "description": "Absolute path of the image file to write"
      },
      "spp": {
        "description": "Samples per pixel"
      }
    }
  },
  "render.start_sequence": {
    "summary": "Render a frame range to an output directory",
    "tags": "animation|output|batch",
    "related": "render.sequence_status|render.cancel_sequence"
  },
  "render.status": {
    "summary": "Report render job state, progress and current sample count",
    "returns": "RenderJobInfo",
    "tags": "progress"
  },
  "render.sequence_status": {
    "summary": "Report sequence render progress and current frame",
    "returns": "SequenceInfo",
    "tags": "progress|animation"
  },
  "render.cancel": {
    "summary": "Cancel the running single-frame render",
    "tags": "abort"
  },
  "render.cancel_sequence": {
    "summary": "Cancel the running sequence render",
    "tags": "abort|animation"
  },
  "render.probe": {
    "summary": "Measure a region of the last viewport frame: mean/min/max luminance, black fraction, NaN fraction and a histogram",
    "notes": "This is the measurement an agent verifies its own work with. `available: false` means no frame was captured - it is not a dark scene. A non-zero nan_fraction means a shader produced invalid pixels.",
    "tags": "measure|verify|luminance|black|nan|check",
    "related": "viewport.capture|viewport.render_frames|viewport.status",
    "returns": "ProbeInfo",
    "params": {
      "x": {
        "description": "Region origin in pixels"
      },
      "y": {
        "description": "Region origin in pixels"
      },
      "width": {
        "description": "Region width in pixels; 0 means to the right edge"
      },
      "height": {
        "description": "Region height in pixels; 0 means to the bottom edge"
      },
      "threshold": {
        "description": "Luminance at or below which a pixel counts as black"
      }
    }
  },
  "render.volume_stats": {
    "summary": "Return volume traversal counters for the last frame",
    "tags": "diagnostics|performance|volume"
  },
  "render.volume_counters": {
    "summary": "Enable or disable volume instrumentation counters",
    "tags": "diagnostics|performance|volume"
  },
  "render.volume_tables": {
    "summary": "Report the published volume SSBO state per backend (render / raster viewport), including whether the simulation compute device is that backend's own device",
    "returns": "VolumeTablesInfo",
    "tags": "diagnostics|volume|backend|verify",
    "notes": "The raster viewport can be a SECOND VulkanBackendAdapter with its own VkDevice and its own volume table. A 'viewport' row with instance_count 0 while 'render' is non-zero means the realtime viewport was never handed the volumes and will draw none — render.volume_stats cannot see this, because a pass that never ran counts zero exactly like a scene with no volumes. sim_device_is_this_backends false means live dense gas addresses are suppressed for that backend and it falls back to the per-adapter NanoVDB upload (a frozen grid, not a fault).",
    "related": "render.volume_stats|viewport.status"
  },
  "viewport.status": {
    "summary": "Report viewport backend, shading mode, resolution, sample count, capture state and whether a frame is available",
    "returns": "ViewportStatusInfo",
    "tags": "measure|verify|backend|samples",
    "related": "viewport.capture|render.probe"
  },
  "viewport.shading": {
    "summary": "Report which viewport shading mode is on screen and whether the interactive raster viewport exists",
    "notes": "interactive_available false means this build has no raster viewport (no Vulkan), so 'rendered' is the only selectable mode - a rejected viewport.set_shading there is the machine, not a bad request.",
    "tags": "viewport|shading|display|measure",
    "related": "viewport.set_shading|viewport.status"
  },
  "viewport.preview_lighting": {
    "summary": "Report realtime/material raster lighting mode, shadow allocation, world background and Nishita direct-sun status",
    "notes": "Canonical modes are 'scene' and 'three_point'. The default 'scene' reads the renderer's own light buffer; 'three_point' is the fixed material-inspection rig. Scene draws canonical world color/HDRI/Nishita behind geometry and uses it for ambient/specular. Both HDRI and Physical Sky use generated diffuse irradiance, GGX roughness prefilter mips and a split-sum BRDF LUT when world_ibl_ready is true; world_ibl_source says which producer filled them ('hdri' = uploaded environment texture, 'sky' = Physical Sky baked to an equirect, 'none' = neither), and world_ibl_fallback reports the bounded per-fragment cone path. world_sky_capture_supported false means the bake shader is missing, so Physical Sky cannot leave that fallback. The sun disc is excluded from the bake because the sun is delivered as an analytic directional light; near-mirror surfaces still sample it directly. Nishita contributes a direct sun with its own directional atlas tile. 'shadowed_light_count' counts scene lights only; 'world_sun_shadow' reports the reserved sun tile. A gap from 'scene_light_count' means later lights still illuminate without an atlas tile. A gap between 'scene_light_count' and 'scene_light_total' means the preview light loop itself is clamped. 'material_preview_active' false means the mode is stored but the material raster viewport is not on screen. DISPLAY TRANSFORM: Rendered and raster preview use PostProcess/ColorMath.h. display_* reports the GPU transport; post.get reports user gain, while post.get_exposure reports the resolved adaptive gain. None/linear means clipping; Reinhard is explicit. Raster preview uses the resolved EV from the Rendered HDR meter and does not meter its own display-encoded image. ★ 'world_background_source' is what the sky pass ACTUALLY draws, read from the adapter that drives the raster viewport and NOT OR-ed across backends: 'color' (world is a flat colour), 'hdri', 'sky', 'solid_fallback' (HDRI mode but no environment texture reached this adapter -- the background degrades to the flat world colour) or 'sky_analytic_fallback' (Physical Sky mode but no atmosphere LUT on this device -- a crude gradient). The two *_fallback values are the failure mode where Vulkan RT renders every world mode correctly while the realtime viewport sits on a solid background; world_ibl_ready cannot tell them apart because it is OR-ed over both adapters. It is 'not_drawn' whenever the preset is not 'scene': three_point skips the sky pass entirely, so there is no background to measure and reporting 'color' there would turn 'could not measure' into 'measured a flat colour'.",
    "tags": "viewport|preview|lighting|scene|lights|measure",
    "related": "viewport.set_preview_lighting|viewport.set_shading|lights.list"
  },
  "viewport.set_preview_lighting": {
    "summary": "Set realtime/material raster lighting to the default scene path or the three-point inspection rig",
    "notes": "'scene' uses scene lights plus the canonical world background/ambient; 'three_point' isolates material inspection from scene lighting. Directional, point, spot and area lights share a bounded shadow atlas; Nishita adds a direct world sun with one reserved directional tile. Physical Sky uses the Vulkan atmosphere LUT when available and an analytic fallback otherwise. HDRI uses generated irradiance, GGX prefilter mips and a BRDF LUT when ready, with an explicitly reported raw-environment fallback. Resets accumulation and only takes visible effect in 'material' shading mode.",
    "tags": "viewport|preview|lighting|scene|lights|shading",
    "related": "viewport.preview_lighting|viewport.set_shading|viewport.get_screenshot",
    "params": {
      "preset": {
        "enum": "three_point|scene",
        "description": "Canonical mode name. Realtime/real_lights aliases map to scene; legacy classic/studio/outdoor aliases map to three_point."
      }
    },
    "verify_with": [
      "viewport.preview_lighting"
    ]
  },
  "viewport.quality": {
    "summary": "Report the raster viewport quality preset and, as a value, whether scatter proxy substitution is active",
    "notes": "Read values rather than matching on the preset name. 'scatter_lod_split' changes geometry submission. Scene shadows keep one fixed 4096 atlas so preset changes never destroy an image referenced by an in-flight frame; shadow_tile_resolution, shadow_tile_capacity, shadow_light_budget and shadow_pcf_samples report the live bounded contract. directional_shadow_cascades is 2 for Performance and 3 otherwise; directional lights and the Physical Sky sun consume that many atlas tiles and select the smallest camera-centred projection containing the receiver. A scene light beyond the budget still illuminates but has no atlas shadow. 'scene_pbr_shader' reports the comparison shader (GGX for Scene). The material parity strings are an explicit capability contract: opaque_core_parity is RT-aligned; material_graph_surface is bounded because pointiness/named attributes/AO/time inputs are neutral in raster; clearcoat is an iridescent lobe using the RT thin-film model; subsurface is a radius/scale profile approximation; translucency is a bounded thin-surface approximation; surface_anisotropy is unsupported because the current ABI fields are also legacy water wave controls; transparency is unsorted_alpha; transmission is screen_space_thickness with frozen opaque color/depth, bounded screen-space reflection, closed-mesh front/back thickness, RT texture/graph/IOR/Fresnel/Beer/roughness/dispersion semantics and environment fallback; overlapping or off-screen continuation/reflection remains bounded; resin_interior is a procedural bounded approximation; sdf_surface=shared_nanovdb_depth_pbr means realtime reads Vulkan RT's same field/transform/iso threshold and writes real raster depth, while lighting and environment continuation remain bounded rather than recursive. 'raster_viewport_available' false means the preset is stored but nothing on this machine reads it. Volume shadow budgets are volume_shadow_tile_resolution, volume_shadow_depth_layers and volume_shadow_steps (32/8/16 Performance, 128/12/48 Balanced or Auto, 256/16/64 Quality or Full). They describe configured budgets, not shader availability or measured GPU cost. Scene lighting shares a layered optical-depth shadow atlas between raster surfaces, SDF receivers and gas/VDB; lights outside the existing shadow budget remain unshadowed on surfaces. Up to 16 intersecting media are integrated per shadow ray; scalar transmittance and fixed depth layers are bounded approximations.",
    "tags": "viewport|quality|lod|proxy|scatter|measure",
    "related": "viewport.set_quality|viewport.frame_telemetry"
  },
  "viewport.set_quality": {
    "summary": "Set the raster viewport quality preset; 'full' disables scatter proxy substitution entirely",
    "notes": "The preset controls both geometry and bounded Scene quality. Performance/Balanced/Quality select 256/512/1024 shadow tiles, 4/8/16 shadowed scene lights and 9/9/25 receiver PCF samples; Auto currently maps to Balanced. The atlas remains 4096 and is not reallocated. 'full' additionally turns scatter proxy substitution OFF so every visible instance draws its own mesh; frustum culling stays on. Rebuilds the raster scene and resets accumulation, so render frames again before probing.",
    "tags": "viewport|quality|lod|proxy|scatter|full|performance",
    "related": "viewport.quality|viewport.frame_telemetry|viewport.render_frames",
    "params": {
      "preset": {
        "enum": "auto|performance|balanced|quality|full",
        "description": "Canonical preset name. 'no_proxy' and 'full (no proxy)' are accepted aliases for full because that is what the panel combo reads."
      }
    },
    "verify_with": [
      "viewport.quality",
      "viewport.frame_telemetry"
    ]
  },
  "viewport.frame_telemetry": {
    "summary": "Report raster/Realtime viewport frame presentation telemetry: per-stage host timings, async frame-slot counters and stale-present count",
    "notes": "'available' false means NO raster frame has ever been presented (Rendered mode, no Vulkan viewport backend, or nothing drawn yet) - the missing fields are ABSENCE, not zeros. GEOMETRY SUBMISSION: 'gpu_culling' false while 'global_instance_buffer' is true means the scene is drawn with NO frustum culling and NO scatter proxy - every instance of every mesh, wherever the camera looks. That combination is a FAULT that looks CORRECT on screen and only shows up as cost, so check it before trusting any raster timing. 'visible_triangles'/'full_triangles'/'proxy_triangles'/'full_instances'/'proxy_instances' are read back from the GPU when gpu_culling is on and are therefore ONE FRAME BEHIND; identical on a still camera, one frame late while it moves. 'scatter_triangle_target' is a TARGET, not a hard cap: GPU culling converges a distance threshold toward it across frames, so a fast camera move can exceed it for a frame or two. 'async_present' false means the driver refused persistent frame slots and the old synchronous readback path is live; 'image_readback_ms' is non-zero ONLY there. COUNTERS COUNT BLOCKS, NOT CALLS (corrected 2026-09-02; earlier numbers are not comparable). 'stale_presents' means NO new frame could be consumed during a whole render pass - the viewer really is seeing pixels from before this pass. It used to be incremented by the opportunistic second consume that runs right after submit, which can essentially never succeed, so it tracked frame count in healthy async operation and read as 'the ring is permanently behind'. A one-frame delivery delay is what the async ring BUYS, not a fault. 'slot_waits' likewise counts only waits that actually blocked on an unsignalled fence; read it together with 'slot_wait_ms', which is the honest number and stays 0.0 when nothing blocked. viewport.capture(enabled=true) FORCES synchronous presentation so a probe reads the frame just recorded, so timings taken with capture on are not the interactive timings. 'resource_drains' counts host blocks caused by RESOURCE MUTATION (a buffer/descriptor edit that had to wait for submitted frames), and as of 2026-09-02 it increments ONLY when the wait actually blocked on an unsignalled fence - it used to count every call, including waits on already-signalled fences, so it reported a CALL COUNT and not a cost. Read it per submitted frame: above about 1 per frame means some edit path mutates GPU-read state every frame and the two-slot ring is producing no overlap at all, which shows up as 'stale_presents' tracking 'frames_submitted'. Numbers from before that fix are not comparable. 'host_read_ms' was ALSO looking at the wrong place until 2026-09-02: it timed only the opportunistic post-submit consume (which essentially never succeeds), so it read 0.00 and made the GPU-to-CPU readback look free. It now covers the top-of-pass consume that performs the real invalidate + full-frame memcpy. Any architecture decision about removing the readback needs THIS number, measured at the target resolution - the copy scales with pixel count, so a figure taken at 1680x945 does not describe 4K. DISPLAY PATH (added 2026-09-02): every timing above ends when the backend's renderProgressive returns, and the frame is NOT finished there - the main loop still rebuilds the display surface and uploads it to the SDL texture. Those two passes are 'display_post_ms' and 'display_texture_upload_ms', with 'display_loop_period_ms' as the real main-loop period; they are published ONLY on frames that were actually presented (skipped idle/background frames would otherwise average in as free). They are reported even when 'available' is false, because Rendered mode goes down the SAME display path - check 'display_available'. 'display_post_was_noop_copy' true means color processing was a no-op and the pass degenerated to a plain full-frame copy: still a full-frame cost, but REMOVABLE work rather than needed work. MEASURED 2026-09-02 at 3840x2160, material shading, Vulkan viewport: host_read_ms 3.06 + present_ms 2.79 = 5.84 ms of a 10.57 ms frame (55%), i.e. two 31.6 MB CPU copies per frame; the copy scales with pixel count, so numbers taken at 1080p are ~1/4 of these and must not be compared across resolutions.",
    "tags": "viewport|realtime|telemetry|performance|measure|frame|latency|present",
    "related": "viewport.status|perf.list|render.probe",
    "verify_with": [
      "viewport.status"
    ]
  },
  "viewport.get_screenshot": {
    "summary": "Return the captured viewport frame as a base64 JPEG, for a vision-capable model to look at",
    "notes": "Requires viewport.capture(enabled=true) and at least one rendered frame; an empty result means NOT CAPTURED, never 'the screen is empty'. Looking is not measuring - pair it with render.probe when the question is numeric (is there a black band, did the frame change), because a model reading a JPEG cannot tell 0.001 luminance from 0.",
    "tags": "viewport|screenshot|image|vision|look|see",
    "related": "viewport.capture|render.probe|viewport.render_frames",
    "requires": [
      "viewport.capture",
      "viewport.render_frames"
    ],
    "verify_with": [
      "render.probe"
    ]
  },
  "viewport.set_shading": {
    "summary": "Switch the viewport shading mode (solid, material, rendered, matcap) and optionally the matcap preset",
    "notes": "Resets accumulation like the panel buttons do, so probe AFTER switching - otherwise you measure the frame from the mode you left. Fails loudly when a mode is unavailable instead of silently falling back to rendered.",
    "tags": "viewport|shading|display|solid|rendered|matcap|preview",
    "related": "viewport.shading|viewport.render_frames|render.probe",
    "params": {
      "mode": {
        "enum": "solid|material|rendered|matcap",
        "description": "Canonical mode name. 'preview' is accepted as an alias for material because the panel button reads Preview."
      },
      "matcap_preset": {
        "description": "0..9, meaningful in matcap mode; -1 leaves it unchanged."
      }
    }
  },
  "viewport.capture": {
    "summary": "Turn viewport frame capture on or off",
    "notes": "Capture must be on before render.probe or the state summary can measure anything.",
    "tags": "measure|verify|enable",
    "related": "render.probe|viewport.render_frames"
  },
  "viewport.render_frames": {
    "summary": "Render a fixed number of viewport frames and report timing and convergence",
    "notes": "Use this to converge the viewport deliberately before probing it, instead of guessing how long to wait. Over IPC the captured frame is refreshed between calls by the display loop, so viewport.capture -> viewport.render_frames -> render.probe works. It does NOT work inside a single script.run_file: the script holds the main thread, the display loop never runs, and viewport.status frame_available stays false however many frames were rendered - measured 2026-08-19.",
    "tags": "measure|converge|samples|wait",
    "related": "render.probe|viewport.capture|viewport.status"
  },
  "material.list": {
    "summary": "List every material in the scene",
    "tags": "shading",
    "returns": "string[]"
  },
  "material.create": {
    "summary": "Create a material of the given type and return its name",
    "tags": "shading|create",
    "params": {
      "type": {
        "description": "Material type, e.g. 'principled'"
      }
    }
  },
  "material.assign": {
    "summary": "Assign an existing material to an object",
    "tags": "shading|apply"
  },
  "material.info": {
    "summary": "Return a material's full parameter set",
    "tags": "shading",
    "returns": "MaterialInfo"
  },
  "material.of_object": {
    "summary": "List the materials used by one object",
    "tags": "shading",
    "returns": "string[]"
  },
  "material.get": {
    "summary": "Read one material parameter of an object",
    "tags": "shading"
  },
  "material.set": {
    "summary": "Set one material parameter on an object (base_color, roughness, metallic, emission, ior, transmission, ...)",
    "notes": "Colour parameters take a 3-element array, scalars take a number. An unknown parameter name is an error, not a silent no-op.",
    "tags": "shading|colour|roughness|metallic|emission",
    "related": "material.get|material.info",
    "params": {
      "param": {
        "enum": "base_color|bubble_film|bubble_ior|dust_color_a|dust_color_b|dust_style|emission|emission_strength|ior|is_bubble|metallic|opacity|resin_color|resin_density|resin_dirt|resin_dirt_color|resin_inclusion|resin_inclusion_scale|resin_object_space|resin_roughness|resin_shard|resin_shard_hue|roughness|shard_shape|specular|transmission|uv_offset_x|uv_offset_y|uv_scale_x|uv_scale_y",
        "description": "Which Principled BSDF property to set. Colour keys (base_color, emission, resin_color, dust_color_a/b, resin_dirt_color) need an RGB value; the rest need a scalar. roughness, metallic, specular, transmission and opacity are clamped to [0,1]; ior to [1,10]."
      }
    }
  },
  "material.get_param": {
    "summary": "Read one material parameter directly by material name (not through an object)",
    "notes": "material.get reads through an OBJECT and averages nothing but still names an object; this names the material ASSET, which is what you want when several materials share one flat mesh (e.g. a carafe's glass body + brass cap + rubber foot) and you need the one that actually has that name.",
    "tags": "shading",
    "related": "material.set_param|material.get|material.info"
  },
  "material.set_param": {
    "summary": "Set one material parameter directly by material name (base_color, roughness, metallic, emission, ior, transmission, ...)",
    "notes": "material.set edits through an OBJECT: every Principled BSDF that object's flat meshes reference gets the same value, which is wrong the instant one object carries several DIFFERENT materials — a carafe's glass body + brass cap + rubber foot is one flat mesh, three materials, and object.set would smear transmission=1 onto the cap and foot too. This edits exactly the named material asset and nothing else that happens to share its object. Colour parameters take a 3-element array, scalars take a number; an unknown parameter name is an error, not a silent no-op. Not undoable, unlike material.set.",
    "tags": "shading|colour|roughness|metallic|emission",
    "related": "material.get_param|material.set|material.set_texture",
    "params": {
      "param": {
        "enum": "base_color|bubble_film|bubble_ior|dust_color_a|dust_color_b|dust_style|emission|emission_strength|ior|is_bubble|metallic|opacity|resin_color|resin_density|resin_dirt|resin_dirt_color|resin_inclusion|resin_inclusion_scale|resin_object_space|resin_roughness|resin_shard|resin_shard_hue|roughness|shard_shape|specular|transmission|uv_offset_x|uv_offset_y|uv_scale_x|uv_scale_y",
        "description": "Which Principled BSDF property to set. Colour keys (base_color, emission, resin_color, dust_color_a/b, resin_dirt_color) need an RGB value; the rest need a scalar. roughness, metallic, specular, transmission and opacity are clamped to [0,1]; ior to [1,10]."
      }
    }
  },
  "material.set_texture": {
    "summary": "Bind an image file to a material texture slot",
    "tags": "shading|texture|image"
  },
  "material.clear_texture": {
    "summary": "Clear one material texture slot",
    "tags": "shading|texture"
  },
  "material.textures": {
    "summary": "List a material's populated texture slots with the identity of each bound texture",
    "notes": "Returns {slot, texture} pairs. 'texture' is the texture's identity - a file path, or an 'embedded_<import>_<slot>_<type>' cache key for a texture that lived inside the model file. Two materials reporting the SAME texture share one decoded image: intended when a map is reused, a defect when the two materials came from different source files.",
    "tags": "shading|texture"
  },
  "fluid.create_domain": {
    "summary": "Create an APIC liquid or gas grid domain over a world-space box",
    "notes": "voxel_size sets both resolution and cost: halving it multiplies cell count by eight. A liquid domain does nothing until fluid.seed or a flow_source fills it. Changing voxel_size later invalidates the bake.",
    "tags": "simulation|liquid|water|smoke|fire|create|domain|grid",
    "related": "fluid.seed|fluid.set_param|flow_source.create|fluid.list_domains",
    "returns": "FluidDomainInfo",
    "params": {
      "name": {
        "description": "Domain name; also the object name in the scene"
      },
      "type": {
        "description": "Solver family",
        "enum": "fluid|gas"
      },
      "domain_min": {
        "description": "World-space AABB minimum in metres"
      },
      "domain_max": {
        "description": "World-space AABB maximum in metres"
      },
      "voxel_size": {
        "description": "Grid cell size in metres. Drives resolution, cost and the smallest feature the solver can resolve."
      }
    }
  },
  "fluid.list_domains": {
    "summary": "List every fluid and gas domain with its full settings",
    "tags": "simulation|inventory",
    "related": "fluid.get",
    "returns": "FluidDomainInfo[]"
  },
  "fluid.get": {
    "summary": "Return one fluid or gas domain's complete settings and live counters",
    "tags": "simulation",
    "returns": "FluidDomainInfo"
  },
  "fluid.remove_domain": {
    "summary": "Delete a fluid or gas domain",
    "tags": "simulation|remove"
  },
  "fluid.seed": {
    "summary": "Fill a liquid domain with particles - a given box, or the domain's lower half",
    "notes": "Omit seed_min and seed_max to fill the bottom half of the domain; there is no fixed default box any more, because the old one was derived from nothing and seeded zero particles in any domain that did not contain it. A region that does not overlap the domain is now refused with both boxes in the error, instead of succeeding having created nothing. particles_per_cell of 4 is the standard APIC density; a thin jet seeded below that has no interior cells and therefore no pressure field, which reads as 'water that will not splash'. persistent=true re-seeds the box every reset.",
    "tags": "simulation|liquid|water|fill|particles|initial",
    "related": "fluid.create_domain|fluid.clear|flow_source.create",
    "params": {
      "seed_min": {
        "description": "World-space box minimum in metres; omit to derive from the domain"
      },
      "seed_max": {
        "description": "World-space box maximum in metres; omit to derive from the domain"
      },
      "particles_per_cell": {
        "description": "Particle density per grid cell; 4 is standard"
      },
      "replace": {
        "description": "Clear existing particles first"
      },
      "persistent": {
        "description": "Re-apply this seed after every reset"
      }
    },
    "verify_with": [
      "fluid.get"
    ]
  },
  "fluid.clear": {
    "summary": "Remove the particles from a fluid domain",
    "tags": "simulation|reset"
  },
  "fluid.reset": {
    "summary": "Reset every fluid and gas simulation to frame zero",
    "tags": "simulation|reset|rewind"
  },
  "fluid.step": {
    "summary": "Advance the fluid solver by one timestep",
    "notes": "For scripted work prefer timeline.set_frame, which advances every solver consistently.",
    "tags": "simulation|advance"
  },
  "fluid.set_param": {
    "summary": "Update any field of a fluid or gas domain: bounds, voxel size, solver backend, viscosity, granular constitutive settings, render mode and surface material",
    "notes": "Overlay semantics - fields you do not send keep their value. Changing voxel_size or the bounds invalidates the bake for that domain.",
    "requires": [
      "fluid.create_domain"
    ],
    "next": [
      "timeline.set_frame"
    ],
    "verify_with": [
      "fluid.get",
      "render.probe"
    ],
    "invalidates": [
      "simulation_cache"
    ],
    "tags": "simulation|configure|viscosity|granular|render-mode|backend",
    "related": "fluid.get|fluid.set_substance_material"
  },
  "fluid.set_substance_material": {
    "summary": "Bind a substance (its physical identity, phase and representation) to a fluid domain",
    "notes": "Phase is what the matter IS; representation is how it is solved and drawn. They are separate axes - setting one does not imply the other.",
    "tags": "simulation|substance|material|phase|msf",
    "related": "msf.substances|fluid.set_splat_material|fluid.set_param",
    "params": {
      "domain": {
        "description": "Fluid domain name"
      },
      "substance": {
        "description": "Substance id from msf.substances"
      },
      "phase": {
        "description": "Physical phase",
        "enum": "liquid|solid|gas"
      },
      "representation": {
        "description": "How the substance is solved and rendered"
      }
    }
  },
  "fluid.set_splat_material": {
    "summary": "Set the material used for particle splat rendering of a domain",
    "tags": "simulation|render|appearance"
  },
  "fluid.get_combustion": {
    "summary": "Read a liquid domain's combustion settings",
    "tags": "simulation|fire|burn",
    "related": "fluid.set_combustion"
  },
  "fluid.set_combustion": {
    "summary": "Configure a liquid domain as a combustible fuel: ignition temperature, heat release, smoke yield and evaporation",
    "tags": "simulation|fire|burn|fuel|ignite|combustion",
    "related": "gas.set_settings|fluid.set_substance_material"
  },
  "gas.create_domain": {
    "summary": "Create a gas grid domain over a world-space box",
    "notes": "Same handler as fluid.create_domain with type defaulting to gas.",
    "tags": "simulation|smoke|fire|create|domain",
    "related": "gas.set_settings|flow_source.create",
    "returns": "FluidDomainInfo"
  },
  "gas.list_domains": {
    "summary": "List every gas and fluid domain with its settings",
    "tags": "simulation|inventory"
  },
  "gas.get": {
    "summary": "Return one gas domain's complete settings and live counters",
    "tags": "simulation"
  },
  "gas.remove_domain": {
    "summary": "Delete a gas domain",
    "tags": "simulation|remove"
  },
  "gas.get_settings": {
    "summary": "Read a gas domain's solver settings: fire, buoyancy, turbulence, quality and resource budget",
    "tags": "simulation|fire|smoke",
    "related": "gas.set_settings"
  },
  "gas.set_settings": {
    "summary": "Configure a gas domain's solver: fire and ignition, burn rate, heat release, buoyancy, turbulence, vorticity and resource budget",
    "notes": "This is the SOLVER. Appearance - colour, emission, density scale - lives in gas.set_shader, and the two are read back separately; a fire that simulates but looks wrong is usually a shader setting, not a solver setting.",
    "requires": [
      "gas.create_domain"
    ],
    "next": [
      "gas.set_shader",
      "flow_source.create",
      "timeline.set_frame"
    ],
    "verify_with": [
      "gas.get_settings",
      "render.probe"
    ],
    "invalidates": [
      "simulation_cache"
    ],
    "tags": "simulation|fire|smoke|ignite|burn|buoyancy|turbulence",
    "related": "gas.get_settings|gas.set_shader|flow_source.create",
    "params": {
      "fire_enabled": {
        "description": "Run combustion in this domain"
      },
      "ignition_temperature": {
        "description": "Temperature at which fuel ignites, in Kelvin"
      },
      "burn_rate": {
        "description": "How fast fuel is consumed once lit"
      },
      "heat_release": {
        "description": "Temperature added per unit of fuel burned"
      },
      "smoke_generation": {
        "description": "Smoke produced per unit of fuel burned"
      }
    }
  },
  "gas.get_shader": {
    "summary": "Read a gas domain's volume shading settings",
    "tags": "render|appearance|volume",
    "related": "gas.set_shader",
    "notes": "Reports the ray-march budget too (voxel_step_multiplier, max_steps, shadow_steps/stride/strength)."
  },
  "gas.set_shader": {
    "summary": "Set a gas domain's volume appearance and ray-march budget: preset, density, absorption/scattering, blackbody emission and the temperature range it maps, plus sample spacing and step ceilings",
    "notes": "temperature_min/max define the window mapped to emission colour; a flame outside that window renders black however hot it is. voxel_step_multiplier is the primary sample spacing in VOXELS (0.5 = one sample every other voxel), so it follows domain resolution rather than a world constant; max_steps is a strict per-ray ceiling and is the dial that actually bounds cost. The realtime raster viewport applies its own ceiling on top (96/256/512 by viewport quality preset), so raising max_steps past it changes RT and the final render but not the viewport.",
    "tags": "render|appearance|volume|fire|colour|emission|blackbody|quality|raymarch|steps",
    "related": "gas.get_shader|gas.set_settings"
  },
  "gas.set_param": {
    "summary": "Update any field of a gas domain (same overlay setter as fluid.set_param)",
    "tags": "simulation|configure"
  },
  "gas.step": {
    "summary": "Advance the gas solver by one timestep",
    "tags": "simulation|advance"
  },
  "gas.reset": {
    "summary": "Reset every gas and fluid simulation to frame zero",
    "tags": "simulation|reset"
  },
  "gas.clear": {
    "summary": "Clear a gas domain's fields",
    "tags": "simulation|reset"
  },
  "gas.pressure_pulse": {
    "summary": "Inject a pressure pulse into a gas domain, as an explosion or blast wave",
    "tags": "simulation|explosion|blast|shockwave|pressure"
  },
  "gas.structural_impulse_stats": {
    "summary": "Report the blast impulses the gas solver handed to the structural/fracture side",
    "tags": "diagnostics|blast|fracture|coupling"
  },
  "flow_source.create": {
    "summary": "Create an emitter that feeds a fluid or gas domain with mass, heat, fuel or particles",
    "notes": "An emitter belongs to a domain by name. With a parent_object it follows that object, and velocity_space decides whether its velocity is read in world or parent-local space.",
    "tags": "simulation|emitter|source|inject|pour|jet|flame|inflow",
    "related": "flow_source.update|flow_source.list|fluid.create_domain|gas.set_settings",
    "returns": "FlowSourceInfo",
    "params": {
      "name": {
        "description": "Emitter name",
        "type": "string"
      },
      "domain": {
        "description": "Target fluid or gas domain name",
        "type": "string"
      },
      "source_mode": {
        "description": "Emission shape/source kind",
        "type": "string"
      },
      "source_object": {
        "description": "Object whose surface or volume emits",
        "type": "string"
      },
      "parent_object": {
        "description": "Object the emitter follows",
        "type": "string"
      },
      "position": {
        "description": "Emitter position; parent-local when parented",
        "type": "vec3"
      },
      "velocity": {
        "description": "Emitted velocity in metres per second",
        "type": "vec3"
      },
      "velocity_space": {
        "description": "Frame the velocity is expressed in",
        "type": "string"
      },
      "radius": {
        "description": "Emission radius in metres",
        "type": "float"
      },
      "temperature": {
        "description": "Injected temperature in Kelvin",
        "type": "float"
      },
      "fuel": {
        "description": "Injected fuel density",
        "type": "float"
      },
      "density": {
        "description": "Injected smoke or liquid density",
        "type": "float"
      },
      "fluid_substance": {
        "description": "Substance id emitted into a liquid domain",
        "type": "string"
      },
      "fluid_particles_per_second": {
        "description": "Liquid particle emission rate",
        "type": "float"
      },
      "enabled": {
        "description": "Emitter active",
        "type": "bool"
      }
    }
  },
  "flow_source.update": {
    "summary": "Update fields of an existing emitter, keeping everything you do not send",
    "notes": "Read-modify-write on purpose: sending only one field must not reset the rest.",
    "tags": "simulation|emitter|configure",
    "related": "flow_source.create|flow_source.get"
  },
  "flow_source.get": {
    "summary": "Return one emitter's full settings",
    "tags": "simulation|emitter",
    "returns": "FlowSourceInfo"
  },
  "flow_source.list": {
    "summary": "List every emitter with its settings",
    "tags": "simulation|emitter|inventory",
    "returns": "FlowSourceInfo[]"
  },
  "flow_source.remove": {
    "summary": "Delete an emitter",
    "tags": "simulation|emitter|remove"
  },
  "msf.substances": {
    "summary": "List the material substance library: every substance an object or fluid can be made of",
    "notes": "Substance ids from here are what fluid.set_substance_material and the combustion path expect.",
    "tags": "substance|material|library|wood|water|metal|thermochemistry",
    "related": "fluid.set_substance_material|msf.fields"
  },
  "msf.fields": {
    "summary": "Report the live material state fields (temperature, moisture, char, mass) per object",
    "notes": "This is the measurement side of burning and melting: mass loss, integrity and mass-conservation error are reported here.",
    "tags": "substance|thermal|burn|melt|measure|verify|temperature|moisture",
    "related": "msf.substances"
  },
  "physics.add_body": {
    "summary": "Give an object a rigid-body with a mass, motion type and collision shape",
    "tags": "simulation|rigid|collision|gravity|mass",
    "related": "physics.set_gravity|physics.get_body|physics.step",
    "params": {
      "object": {
        "description": "Object name"
      },
      "kind": {
        "description": "Body family, e.g. rigid"
      },
      "motion_type": {
        "description": "dynamic, kinematic or static"
      },
      "mass": {
        "description": "Mass in kilograms"
      },
      "shape": {
        "description": "Collision shape, e.g. box, sphere, mesh"
      }
    }
  },
  "physics.get_body": {
    "summary": "Return a rigid body's mass, damping, friction, restitution and motion type",
    "tags": "simulation|rigid"
  },
  "physics.remove_body": {
    "summary": "Remove an object's rigid body",
    "tags": "simulation|rigid|remove"
  },
  "physics.set_gravity": {
    "summary": "Set the world gravity vector in metres per second squared",
    "tags": "simulation|gravity"
  },
  "physics.step": {
    "summary": "Advance the physics solver by dt and move the playhead with it",
    "tags": "simulation|advance",
    "notes": "Advances the solver AND the timeline playhead, and claims the timeline until the user scrubs, plays or stops. It used to advance only the solver: the playhead stayed put, the frame loop found a rigid state that disagreed with the displayed frame, and it reset the runtime to the rest pose - erasing every scripted step while this call returned success. Check sim.control_state around a measurement to learn whether the claim was taken back.",
    "verify_with": [
      "sim.control_state"
    ],
    "next": [
      "scene.get_world_transform"
    ]
  },
  "physics.reset": {
    "summary": "Reset the rigid-body simulation to its initial state",
    "tags": "simulation|reset"
  },
  "physics.fracture_object": {
    "summary": "Shatter an object into Voronoi shards and return the shard objects",
    "notes": "site_count sets how many shards; exact_surface clips shards against the real surface instead of approximating it. The shards are ordinary objects afterwards - group them with physics.make_fracture_group so they hold together until something breaks them.",
    "tags": "destruction|shatter|break|voronoi|shards|debris",
    "related": "physics.make_fracture_group|physics.fracture_cluster_groups|physics.unfracture_object",
    "params": {
      "object": {
        "description": "Object to shatter",
        "type": "string",
        "required": true
      },
      "site_count": {
        "description": "Number of Voronoi sites, i.e. shards"
      },
      "pattern": {
        "description": "Fracture site distribution pattern"
      },
      "cluster_count": {
        "description": "Number of shard clusters to form"
      },
      "exact_surface": {
        "description": "Clip shards against the exact surface rather than an approximation"
      },
      "preview_gap": {
        "description": "Visual gap between shards in metres, for preview only"
      },
      "seed": {
        "description": "Random seed for reproducible shard layouts"
      }
    }
  },
  "physics.unfracture_object": {
    "summary": "Restore a fractured object back to its unbroken form",
    "tags": "destruction|undo|restore"
  },
  "physics.make_fracture_group": {
    "summary": "Bond shards into a fracture group that holds together until the break threshold is exceeded",
    "notes": "break_velocity is a velocity threshold, not an impulse - the group's mass is taken into account when the threshold is evaluated.",
    "tags": "destruction|group|bond|threshold|collapse",
    "related": "physics.fracture_object|physics.break_fracture_group|physics.apply_fracture_impulse"
  },
  "physics.break_fracture_group": {
    "summary": "Break a fracture group apart immediately with a given strength",
    "tags": "destruction|break|collapse"
  },
  "physics.apply_fracture_impulse": {
    "summary": "Apply an impulse at a point on a fracture group and report whether it broke",
    "tags": "destruction|impact|blast|impulse"
  },
  "physics.get_fracture_group": {
    "summary": "Report a fracture group's mass, integrity and break thresholds",
    "tags": "destruction|measure|verify"
  },
  "physics.fracture_cluster_groups": {
    "summary": "List or build the cluster groups of a fractured object",
    "tags": "destruction|group"
  },
  "forcefield.types": {
    "summary": "List the available force-field types",
    "tags": "simulation|wind|vortex|drag"
  },
  "forcefield.create": {
    "summary": "Create a force field of the given type and return its name",
    "tags": "simulation|wind|vortex|turbulence|gravity|create"
  },
  "forcefield.list": {
    "summary": "List the force fields in the scene",
    "tags": "simulation|inventory"
  },
  "forcefield.get": {
    "summary": "Return one force field's full settings",
    "tags": "simulation"
  },
  "forcefield.remove": {
    "summary": "Delete a force field",
    "tags": "simulation|remove"
  },
  "forcefield.set_param": {
    "summary": "Update a force field: shape, strength, falloff, noise, drag and which solvers it affects",
    "notes": "A force field is spatial - it is NOT attached to a domain. Which solvers feel it is decided by the affects_* flags.",
    "tags": "simulation|wind|turbulence|drag|configure",
    "related": "forcefield.evaluate"
  },
  "forcefield.evaluate": {
    "summary": "Evaluate the combined force fields at a world position and return the resulting acceleration",
    "notes": "The way to check a field really reaches a place, rather than inferring it from how a simulation looks.",
    "tags": "measure|verify|probe|force",
    "related": "forcefield.set_param"
  },
  "particle.list_systems": {
    "summary": "List the particle systems with their emitter, domain and collider counts",
    "tags": "particles|inventory"
  },
  "particle.emitters": {
    "summary": "List the particle emitters",
    "tags": "particles|inventory"
  },
  "particle.get_emitter": {
    "summary": "Return one particle emitter's full settings",
    "tags": "particles"
  },
  "particle.add_emitter": {
    "summary": "Add a particle emitter and return it",
    "tags": "particles|create|spawn|emit"
  },
  "particle.set_emitter": {
    "summary": "Update fields of a particle emitter, keeping what you do not send",
    "tags": "particles|configure"
  },
  "particle.remove_emitter": {
    "summary": "Remove a particle emitter",
    "tags": "particles|remove"
  },
  "particle.clear_emitters": {
    "summary": "Remove every particle emitter",
    "tags": "particles|remove"
  },
  "particle.clear_systems": {
    "summary": "Remove every particle system",
    "tags": "particles|remove"
  },
  "particle.clear": {
    "summary": "Delete the live particles without touching the emitters",
    "tags": "particles|reset"
  },
  "particle.spawn": {
    "summary": "Spawn one particle with an explicit position, velocity, size, mass and lifetime",
    "tags": "particles|manual|inject"
  },
  "particle.get_physics": {
    "summary": "Read the particle solver settings",
    "tags": "particles|solver"
  },
  "particle.set_physics": {
    "summary": "Set the particle solver: mode, quality, rest density, viscosity, cohesion, self-collision and grid deposit rates",
    "tags": "particles|solver|configure|sph"
  },
  "particle.stats": {
    "summary": "Report live particle counts and per-stage solver timings",
    "tags": "particles|measure|performance|verify"
  },
  "particle.step": {
    "summary": "Advance the particle solver by one timestep",
    "tags": "particles|advance"
  },
  "terrain.list": {
    "summary": "List the terrain objects",
    "tags": "landscape|inventory"
  },
  "terrain.get": {
    "summary": "Return one terrain's size, resolution, height scale and node-graph state",
    "tags": "landscape"
  },
  "terrain.create": {
    "summary": "Create a terrain heightfield of a given world size and resolution",
    "tags": "landscape|mountain|ground|create|heightmap",
    "related": "terrain.set_mesh_resolution|terrain.get|perf.list",
    "params": {
      "size": {
        "description": "World-space extent in metres"
      },
      "resolution": {
        "description": "Heightmap resolution in samples per side"
      },
      "height_scale": {
        "description": "Vertical scale in metres"
      }
    },
    "notes": "resolution is the FIELD grid: heights and every analysis product (slope, flow, erosion, analysisFields) live at that resolution. mesh_resolution is the separate VERTEX grid; 0 follows the field. Set it at creation rather than decimating afterwards - creating at 4096 and decimating still pays one full-resolution acceleration-structure build (measured 5.6 s Vulkan solid raster + 2.8 s Embree). With a 1024 mesh over a 4096 field the same build is 0.42 s + 0.15 s."
  },
  "terrain.remove": {
    "summary": "Delete a terrain",
    "tags": "landscape|remove"
  },
  "terrain.apply_preset": {
    "summary": "Apply a built-in terrain node preset and report links it could not wire",
    "tags": "landscape|preset|mountain|snow|river",
    "params": {
      "preset": {
        "description": "Preset name",
        "enum": "default|snow_layer|snowy_mountain_valley|river_network|biome_temperate|biome_lush|biome_alpine|biome_arid|biome_boreal|biome_foliage|geology_foundation"
      },
      "add_satmap": {
        "description": "Also add and wire a matching SatMap colorizer",
        "default": false
      }
    },
    "notes": "Returns wiring_faults: links the setup asked for and did not get. A refused link leaves the graph looking complete while the consumer falls back to a synthesized value, so an empty wiring_faults list is the only evidence the setup actually connected everything. wiring_fault_count > 0 means the resulting splat/biome masks are partly synthesized, not driven by the graph.",
    "verify_with": "nodes.list"
  },
  "terrain.list_satmap_presets": {
    "summary": "List data-driven multi-field SatMap preset recipes",
    "tags": "landscape|satmap|color|preset|flow|curvature"
  },
  "terrain.apply_satmap_preset": {
    "summary": "Build a SatMap node recipe from flow, slope, curvature and other terrain fields",
    "tags": "landscape|satmap|color|preset|flow|slope|curvature",
    "related": "terrain.list_satmap_presets|terrain.evaluate"
  },
  "terrain.erode": {
    "summary": "Run an erosion pass over a terrain; the hydraulic model brackets its droplet stages with the fluvial cycle - drainage-area feedback, lake spill and outlet incision, downstream sediment transport",
    "notes": "Every fluvial_* parameter defaults to leaving the solver setting alone, so tuning one dial does not reset the other twenty. The cycle is what produces a river hierarchy and deltas; with fluvial_cycle=0 erosion falls back to the local-slope droplet walk, which is statistically isotropic and leaves closed basins undrained. Check the result with terrain.erosion_stats rather than by eye.",
    "tags": "landscape|erosion|weathering|realism|river|delta|lake|sediment",
    "related": "terrain.erosion_stats|terrain.calculate_flow|terrain.evaluate",
    "verify_with": [
      "terrain.erosion_stats"
    ],
    "params": {
      "type": {
        "description": "Erosion model",
        "enum": "hydraulic|thermal|fluvial|wind"
      },
      "iterations": {
        "description": "Droplet count for the hydraulic model; 0 uses the model's own default"
      },
      "strength": {
        "description": "Erosion strength"
      },
      "talus_angle": {
        "description": "Repose angle for thermal erosion"
      },
      "fluvial_cycle": {
        "description": "Enable the landscape-evolution cycle (1/0); omit to keep the solver setting"
      },
      "fluvial_quality": {
        "description": "Convergence budget preset. Sets iteration count, relaxation passes, transport steps and pyramid depth ONLY - never the shape parameters - so raising it refines the same landscape instead of producing a different one. Applied before any explicit budget parameter in the same call, so you can pick a preset and override one dial of it.",
        "enum": "draft|balanced|high"
      },
      "fluvial_iterations": {
        "description": "Cycle iterations. Refines the solve; does NOT change how much material moves"
      },
      "fluvial_time_step": {
        "description": "Whole-cycle time multiplier: this is the dial for how much material moves"
      },
      "rain_rate": {
        "description": "Runoff depth per unit time; only its ratio to settling_velocity matters"
      },
      "orographic_rain": {
        "description": "Windward/lee rain split, 0..1. The second symmetry breaker after drainage area"
      },
      "rain_wind_degrees": {
        "description": "Prevailing wind bearing in degrees for orographic rain"
      },
      "incision_k": {
        "description": "Stream-power coefficient, in metres of incision at 1 km2 catchment and slope 1"
      },
      "stream_power_m": {
        "description": "Drainage-area exponent, near 0.5. This is what makes rivers cut faster than rills"
      },
      "stream_power_n": {
        "description": "Slope exponent, near 1.0"
      },
      "transport_k": {
        "description": "Sediment transport capacity coefficient, same units as incision_k"
      },
      "sediment_cover": {
        "description": "Cover effect 0..1: a bed already carrying its capacity is armoured"
      },
      "settling_velocity": {
        "description": "How readily suspended load drops. Larger settles sooner and shortens deltas"
      },
      "sediment_route_steps": {
        "description": "Cells of downstream sediment travel per iteration"
      },
      "drainage_refresh_interval": {
        "description": "Iterations between depression-fill and drainage-area re-solves"
      },
      "drainage_fill_passes": {
        "description": "GPU depression-fill budget per pyramid level. Too low leaves spurious lakes"
      },
      "drainage_accumulate_passes": {
        "description": "GPU area-accumulation budget. Too low under-counts long trunk rivers"
      },
      "drainage_coarsest_size": {
        "description": "Coarsest pyramid grid for the GPU drainage solve"
      },
      "flat_gradient": {
        "description": "Slope in m/m assumed for a depression-filled FLAT. A flat has no gradient of its own, so this is what water steers by there; 0 disables it and flat channels go back to straight, grid-locked and unable to incise"
      },
      "flat_resolve_passes": {
        "description": "GPU flat-resolution budget, one cell per pass: the widest flat in cells that resolves. Check unresolved_flat_cells in the stats, not the render"
      },
      "mass_wasting": {
        "description": "Enable in-loop landslides (1/0). Off means valley walls never collapse"
      },
      "repose_angle_degrees": {
        "description": "Angle of repose for mass wasting"
      },
      "mass_wasting_rate": {
        "description": "Fraction of the excess above repose shed per pass, 0..1"
      },
      "mass_wasting_steps": {
        "description": "Talus relaxation passes per iteration"
      },
      "hillslope_diffusion": {
        "description": "Creep coefficient in m2 over the whole cycle; rounds ridges, keeps channels"
      },
      "incision_safety": {
        "description": "Anti-pit limit: max fraction of the drop to the receiver cut in one step"
      },
      "deposition_safety": {
        "description": "Anti-spike limit: max fraction of the rise to the donor built in one step"
      },
      "max_step_meters": {
        "description": "Absolute per-step height change limit; 0 selects half a cell"
      },
      "lake_epsilon_meters": {
        "description": "Fill residue below this is not treated as standing water"
      },
      "headwater_area_km2": {
        "description": "Catchment area at which a cell counts as a channel"
      },
      "backend": {
        "description": "Compute backend; auto picks GPU when available"
      }
    }
  },
  "terrain.erosion_stats": {
    "summary": "Read the sediment mass ledger and drainage diagnostics of the last erosion run",
    "notes": "This is how an erosion result is checked without a screenshot, and the field to read FIRST is max_drainage_area_fraction: the largest catchment as a share of the map. Single digits mean the drainage graph is in fragments and no trunk river exists - a state that still renders as a convincing river network, which is why looking at the picture has repeatedly sent debugging the wrong way. max_drainage_area_km2 alone is NOT interpretable: 0.03 km2 is a shredded network on a 1 km terrain and a healthy trunk on a 100 m one. lake_cells and lake_area_fraction count ANY standing water including the depression-fill ladder's few-ulp lift, so they can report a quarter of the map and mean nothing; act on deep_lake_cells / deep_lake_area_fraction (10 cm threshold) and deepest_lake_meters instead. A high deep_lake_area_fraction on sloping terrain means the surface fed to the solver is genuinely full of pits - suspect the droplet stage, not the fill. mass_error_fraction away from zero means sediment transport is leaking. cycle_iterations == 0 is not a measurement, it is a cleared default.",
    "tags": "landscape|erosion|hydrology|measurement|validation|mass-balance|drainage|lakes",
    "related": "terrain.erode|terrain.calculate_flow|terrain.flow_authority|terrain.evaluate"
  },
  "terrain.evaluate": {
    "summary": "Evaluate the terrain node graph and bake the result into the heightfield",
    "tags": "landscape|bake|apply",
    "related": "terrain.evaluation_status|terrain.cancel_evaluation"
  },
  "terrain.evaluation_status": {
    "summary": "Report terrain evaluation progress and the node being processed",
    "tags": "landscape|progress"
  },
  "terrain.cancel_evaluation": {
    "summary": "Cancel the running terrain evaluation",
    "tags": "landscape|abort"
  },
  "terrain.sample_height": {
    "summary": "Sample terrain height at a world position",
    "tags": "landscape|measure|probe|placement"
  },
  "terrain.carve_river": {
    "summary": "Carve a river channel along a path into the terrain",
    "tags": "landscape|river|water|erosion"
  },
  "terrain.list_rivers": {
    "summary": "List the rivers carved into a terrain",
    "tags": "landscape|river|inventory"
  },
  "terrain.set_layer": {
    "summary": "Bind or edit one terrain layer slot",
    "notes": "Omit a field to leave it unchanged. material=\"\" CLEARS the slot; clearing a semantic overlay restores the built-in shading for that channel. overlay_strength applies to slots 4-7 only and is refused on 0-3, which are normalized against each other and have no independent strength.",
    "tags": "landscape|material|layer|splat|semantic|river",
    "related": "terrain.list_layers",
    "verify_with": "terrain.list_layers",
    "params": {
      "slot": {
        "description": "0-3 splat-weighted, 4-7 semantic overlay (4=Flow 5=Wetness 6=Ice 7=Hardness)"
      },
      "material": {
        "description": "Material name to bind; \"\" clears the slot; omit to leave unchanged"
      },
      "uv_scale": {
        "description": "Tiling scale; omit to leave unchanged"
      },
      "overlay_strength": {
        "description": "Overlay dial [0,1] for slots 4-7; omit to leave unchanged"
      }
    }
  },
  "terrain.slope_area_fit": {
    "summary": "Fit the slope-area relation over the channel network to test for real fluvial form",
    "notes": "Stream-power erosion gives S = k * A^-theta, so on log-log axes channel cells fall on a straight line. Noise, diffusion and sculpted relief scatter instead. This is the only measurement that separates 'an erosion pass ran' from 'the erosion pass produced fluvial form' - both render as plausible terrain. Read r_squared BEFORE concavity_index: a confident theta fitted to scatter is exactly the number that ends a debugging session early. Real landscapes sit near theta 0.4-0.6. MEASURED: the flow field is filled by terrain.calculate_flow ONLY. Evaluating the node graph does not fill it - the buffer ends up allocated, correctly sized and entirely zero, which used to sail past the size guard and answer too_few_channels, an empty input dressed as a statement about the landscape. Call terrain.calculate_flow first; status flow_field_empty now names that case and flow_peak reports what the fit actually read. status no_flow_field means the buffer is missing or the wrong size.",
    "tags": "landscape|hydrology|erosion|measurement|validation",
    "related": "terrain.calculate_flow|terrain.erosion_stats|terrain.flow_authority"
  },
  "terrain.flow_authority": {
    "summary": "Report which discharge field the graph's Flow node classified",
    "notes": "Two quantities were both called flow: DISCHARGE, the physical field the erosion sim measures against the terrain it is carving, and CHANNEL, a 0-1 selection of which cells read as a watercourse. Flow is now the single authority - measured discharge goes into it and every consumer reads out of it. source == 'derived_erosion_unwired' is the state nothing else reports: the graph HAS an erosion sim whose discharge is not wired into Flow, so channels come from bare geometry (no lakes, no infiltration, no erosion history) while the render shows the eroded surface, and the result still looks like a river network.",
    "tags": "landscape|hydrology|flow|measurement|authority",
    "related": "terrain.calculate_flow|terrain.erosion_stats|terrain.apply_preset"
  },
  "terrain.landform_stats": {
    "summary": "Measure the realised shape of a terrain: slope distribution, landform width, local-relief spread and hypsometry",
    "tags": "landscape|terrain|measure|diagnostic|shape|slope|relief",
    "related": "terrain.slope_area_fit|terrain.flow_authority|terrain.get",
    "notes": "Measured on the BAKED heightfield, never read back from a generator's own settings, so it can see the gap between what a node was asked for and what the terrain became. relief_window_meters / relief_window_relief are the relief-vs-window ladder itself; broad_growth is the relief added by its last doubling and is the sharpest read on whether the tile has landforms at its own scale - near 1.0 the field is already uncorrelated at a quarter of the tile and the map is one texture repeated (measured 1.05 with Feature Size 600 m on a 4096 m terrain, against 1.37 at 2048 m). landform_scale_meters is the ladder's growth knee, octave-quantised and NOT expected to equal the authored Feature Size. cliff_fraction is the area over 40 degrees - ground too steep to hold soil, so the share that must read as bare rock - and roughness_slope_ratio is cell-scale roughness on the steepest fifth over the gentlest fifth. Read those two TOGETHER: a terrain with no steep ground still scores a high ratio because its steepest fifth is only its least flat fifth. local_relief_ratio near 1 means every part of the map is equally rugged - no massifs, no basins. lowland_fraction below midland_fraction means the hypsometry is gaussian rather than depositional. spectrum_kink is macro-micro coherence in one number: the largest departure of any octave from the fitted power law. A landscape whose layers agree has ONE law from the grid to the landform, so a detail pass that does not match what it sits on, or a band guard quietly dropping octaves, shows up here and ONLY here - slope, relief and hypsometry all stay healthy while it is there. Under about 0.05 the scales are one landscape; spectrum_kink_meters names the window and spectrum_kink_signed says whether that scale is starved of detail (negative) or carrying too much (positive). realised_hurst is fitted only below landform_scale_meters; include the saturated windows and any exponent is dragged toward 0.5 regardless of the field.",
    "params": {
      "name": {
        "description": "Terrain name"
      }
    }
  },
  "terrain.calculate_flow": {
    "summary": "Reconstruct final-height flow diagnostics and report likely inland stalls",
    "notes": "Reconstructs a diagnostic accumulation field from the final heightmap, then returns channel cells with no greater-accumulation neighbour as inland_terminations. This does not inspect Watershed Analysis's authoritative direction/receiver raster (Hydraulic Erosion stopped publishing one when the compact port contract moved flow direction to its authoritative owner), so it detects wholesale depression-routing failures but is not an exact river-topology measurement. Use terrain.flow_authority to identify the graph's real source.",
    "tags": "landscape|water|hydrology|flow|measurement",
    "related": "terrain.erosion_stats|terrain.erode",
    "verify_with": "terrain.erosion_stats"
  },
  "terrain.import_heightmap": {
    "summary": "Import a heightmap image into a terrain",
    "tags": "landscape|import|heightmap"
  },
  "terrain.export_heightmap": {
    "summary": "Export a terrain heightfield to an image file",
    "tags": "landscape|export|heightmap"
  },
  "scatter.list_groups": {
    "summary": "List the scatter groups with their sources, counts and placement settings",
    "tags": "instancing|vegetation|inventory"
  },
  "scatter.create_group": {
    "summary": "Create a scatter group that instances objects over a target surface",
    "tags": "instancing|vegetation|forest|rocks|create"
  },
  "scatter.add_source": {
    "summary": "Add a source object that a scatter group may instance",
    "tags": "instancing|vegetation"
  },
  "scatter.fill": {
    "summary": "Populate a scatter group and report how many instances were spawned",
    "tags": "instancing|vegetation|populate|spawn"
  },
  "scatter.clear": {
    "summary": "Remove the instances of a scatter group, keeping the group",
    "tags": "instancing|reset"
  },
  "scatter.delete_group": {
    "summary": "Delete a scatter group",
    "tags": "instancing|remove"
  },
  "hair.list": {
    "summary": "List the hair grooms",
    "tags": "hair|fur|grass|inventory"
  },
  "hair.get": {
    "summary": "Return one hair groom's full settings",
    "tags": "hair"
  },
  "hair.create": {
    "summary": "Create a hair groom bound to a mesh",
    "tags": "hair|fur|grass|create"
  },
  "hair.remove": {
    "summary": "Delete a hair groom",
    "tags": "hair|remove"
  },
  "hair.rename": {
    "summary": "Rename a hair groom",
    "tags": "hair"
  },
  "hair.update": {
    "summary": "Update hair groom settings, keeping what you do not send",
    "tags": "hair|configure"
  },
  "hair.grow": {
    "summary": "Scale hair length by a factor",
    "tags": "hair|length|style"
  },
  "hair.trim": {
    "summary": "Trim hair strands",
    "tags": "hair|length|style"
  },
  "hair.comb": {
    "summary": "Comb hair strands towards a direction with a root stiffness",
    "tags": "hair|style|direction"
  },
  "hair.smooth": {
    "summary": "Smooth hair strand shapes",
    "tags": "hair|style"
  },
  "hair.restyle": {
    "summary": "Restyle a hair groom",
    "tags": "hair|style"
  },
  "hair.apply_preset": {
    "summary": "Apply a built-in hair preset to a groom",
    "tags": "hair|preset|style"
  },
  "hair.list_presets": {
    "summary": "List the available hair presets",
    "tags": "hair|preset"
  },
  "hair.bake": {
    "summary": "Bake a hair groom to its cached geometry",
    "tags": "hair|bake"
  },
  "hair.reset_simulation": {
    "summary": "Reset hair dynamics to the rest pose",
    "tags": "hair|simulation|reset"
  },
  "paint.ensure": {
    "summary": "Create or return the paint target (texture set) for an object",
    "tags": "texture|paint|setup"
  },
  "paint.get": {
    "summary": "Return an object's paint target: layers, channels and resolution",
    "tags": "texture|paint"
  },
  "paint.add_layer": {
    "summary": "Add a paint layer to an object's texture set",
    "tags": "texture|paint|layer"
  },
  "paint.remove_layer": {
    "summary": "Remove a paint layer",
    "tags": "texture|paint|layer"
  },
  "paint.duplicate_layer": {
    "summary": "Duplicate a paint layer",
    "tags": "texture|paint|layer"
  },
  "paint.move_layer": {
    "summary": "Reorder a paint layer in the stack",
    "tags": "texture|paint|layer"
  },
  "paint.update_layer": {
    "summary": "Set a paint layer's name, opacity, blend mode, visibility or lock",
    "tags": "texture|paint|layer"
  },
  "paint.merge_down": {
    "summary": "Merge a paint layer into the one below it",
    "tags": "texture|paint|layer"
  },
  "paint.flatten": {
    "summary": "Flatten every paint layer into one",
    "tags": "texture|paint|layer"
  },
  "paint.fill": {
    "summary": "Fill a paint layer channel with a value or colour",
    "tags": "texture|paint|fill"
  },
  "paint.clear_channel": {
    "summary": "Clear one channel of a paint layer",
    "tags": "texture|paint"
  },
  "paint.apply_mask": {
    "summary": "Apply a mask preset to a paint layer",
    "tags": "texture|paint|mask|wear|dirt"
  },
  "paint.list_mask_presets": {
    "summary": "List the available paint mask presets",
    "tags": "texture|paint|mask"
  },
  "paint.bake_height_to_normal": {
    "summary": "Bake a paint height channel into the normal channel",
    "tags": "texture|paint|bake|normal"
  },
  "paint.import_channel": {
    "summary": "Import an image into a paint layer channel",
    "tags": "texture|paint|import"
  },
  "paint.export_channel": {
    "summary": "Export a paint layer channel to an image file",
    "tags": "texture|paint|export"
  },
  "sculpt.get": {
    "summary": "Report an object's sculpt state: vertex count and mask range",
    "tags": "sculpt|mesh"
  },
  "sculpt.stroke": {
    "summary": "Apply a sculpt brush stroke to a mesh",
    "tags": "sculpt|mesh|deform|brush",
    "params": {
      "points": {
        "type": "array",
        "required": true,
        "description": "Stroke path: list of [x, y, z] world points. Required - a stroke with no points does nothing."
      }
    }
  },
  "sculpt.paint_mask": {
    "summary": "Paint the sculpt mask on a mesh",
    "tags": "sculpt|mask"
  },
  "sculpt.mask_operation": {
    "summary": "Run an operation over the sculpt mask (invert, clear, grow, ...)",
    "tags": "sculpt|mask"
  },
  "modifiers.get_stack": {
    "summary": "List an object's modifier stack",
    "tags": "mesh|modifier"
  },
  "modifiers.add": {
    "summary": "Add a modifier to an object and return the new stack entry",
    "tags": "mesh|modifier|subdivide|create"
  },
  "modifiers.remove": {
    "summary": "Remove a modifier from an object",
    "tags": "mesh|modifier|remove"
  },
  "modifiers.apply": {
    "summary": "Apply a modifier destructively into the mesh",
    "tags": "mesh|modifier|bake"
  },
  "modifiers.set_param": {
    "summary": "Set a modifier parameter: enabled, levels, render_levels or smooth_angle",
    "tags": "mesh|modifier|configure"
  },
  "nodes.graphs": {
    "summary": "List the node graphs that exist",
    "tags": "nodes|graph|inventory"
  },
  "nodes.create_graph": {
    "summary": "Create a node graph in a domain",
    "tags": "nodes|graph|create"
  },
  "nodes.remove_graph": {
    "summary": "Delete a node graph",
    "tags": "nodes|graph|remove"
  },
  "nodes.types": {
    "summary": "List every node type with its category and description",
    "tags": "nodes|catalogue"
  },
  "nodes.list": {
    "summary": "List the nodes of a graph with their inputs and outputs",
    "tags": "nodes|graph"
  },
  "nodes.add": {
    "summary": "Add a node of the given type to a graph",
    "tags": "nodes|graph|create"
  },
  "nodes.remove": {
    "summary": "Remove a node from a graph",
    "tags": "nodes|graph|remove"
  },
  "nodes.link": {
    "summary": "Connect one node's output to another node's input",
    "tags": "nodes|graph|connect|wire"
  },
  "nodes.link_by_key": {
    "summary": "Connect nodes using stable input and output port keys",
    "tags": "nodes|graph|connect|wire|port"
  },
  "nodes.list_ports": {
    "summary": "List stable node ports, exposure tiers, visibility and connection state",
    "tags": "nodes|graph|ports"
  },
  "nodes.set_port_visible": {
    "summary": "Show or hide an optional node port without changing evaluation",
    "tags": "nodes|graph|ports|configure"
  },
  "nodes.list_params": {
    "summary": "List a node's parameters, their types, values and whether they are driven by a link",
    "tags": "nodes|graph"
  },
  "nodes.get_param": {
    "summary": "Read one node parameter",
    "tags": "nodes|graph"
  },
  "nodes.set_param": {
    "summary": "Set one node parameter",
    "tags": "nodes|graph|configure"
  },
  "nodes.list_properties": {
    "summary": "List a node's non-parameter properties",
    "tags": "nodes|graph"
  },
  "nodes.get_property": {
    "summary": "Read one node property",
    "tags": "nodes|graph"
  },
  "nodes.set_property": {
    "summary": "Set one node property",
    "tags": "nodes|graph|configure"
  },
  "nodes.apply": {
    "summary": "Evaluate a node graph and apply its result, reporting errors and warnings",
    "tags": "nodes|graph|evaluate|bake"
  },
  "sim_graph.list": {
    "summary": "List the simulation node graphs with their scope, owner and whether the owner still exists",
    "notes": "owner_missing means the graph outlived the entity it was written for - it will not run.",
    "tags": "simulation|nodes|graph|inventory|scope"
  },
  "sim_graph.create": {
    "summary": "Create a simulation node graph for a scope and owner",
    "notes": "Scope and owner are mandatory and identify what the graph drives; a graph is not global.",
    "tags": "simulation|nodes|graph|create|scope"
  },
  "sim_graph.delete": {
    "summary": "Delete a simulation node graph",
    "tags": "simulation|nodes|remove"
  },
  "sim_graph.clear": {
    "summary": "Remove every node from a simulation graph",
    "tags": "simulation|nodes|reset"
  },
  "sim_graph.nodes": {
    "summary": "List a simulation graph's nodes, their channels, sources and restart requirements",
    "tags": "simulation|nodes|graph"
  },
  "sim_graph.add_node": {
    "summary": "Add a node of the given type to a simulation graph",
    "tags": "simulation|nodes|create"
  },
  "sim_graph.connect": {
    "summary": "Connect two nodes in a simulation graph",
    "tags": "simulation|nodes|connect|wire"
  },
  "sim_graph.set_node": {
    "summary": "Set a text-valued key on a simulation node",
    "tags": "simulation|nodes|configure"
  },
  "sim_graph.set_node_value": {
    "summary": "Set a numeric key on a simulation node",
    "tags": "simulation|nodes|configure"
  },
  "sim_graph.evaluate": {
    "summary": "Evaluate a simulation graph and report the commands it would issue, without applying them",
    "notes": "The graph DECLARES; the solver REPORTS. Evaluate first, then apply - that split is what makes a graph inspectable.",
    "tags": "simulation|nodes|evaluate|dry-run|inspect",
    "related": "sim_graph.apply|sim_graph.couplings"
  },
  "sim_graph.apply": {
    "summary": "Apply an evaluated simulation graph, reporting what was applied, refused or held",
    "tags": "simulation|nodes|apply|commit",
    "related": "sim_graph.evaluate"
  },
  "sim_graph.clear_overrides": {
    "summary": "Drop the overrides a simulation graph is holding",
    "tags": "simulation|nodes|reset"
  },
  "sim_graph.couplings": {
    "summary": "Report declared versus actually running couplings between simulation domains",
    "notes": "declared_not_running and running_not_declared are the two ways a graph and the live solvers can disagree.",
    "tags": "simulation|nodes|coupling|verify|diagnostics"
  },
  "sim_graph.domain_intersections": {
    "summary": "Report which force fields and colliders geometrically overlap a domain's box",
    "notes": "A MEASUREMENT, not a declared link - neither force fields nor colliders carry a domain field, because force is spatial. Bounding-volume overlap, not exact clipping: an obb collider's box ignores rotation (over-reports only, never hides a real intersection); mesh_sdf/convex/mesh_bvh colliders report measurable=false rather than a guessed non-intersection.",
    "tags": "simulation|nodes|forcefield|collider|measure|diagnostics"
  },
  "attr.list": {
    "summary": "List the attribute names that exist for an object/domain/world scope",
    "notes": "Unifies the old sim_graph.attributes (domain) and sim_graph.surface_attributes (object) into one surface that also covers world.",
    "tags": "simulation|attributes|discover",
    "related": "attr.stats"
  },
  "attr.stats": {
    "summary": "Measure one named attribute directly: count, min, max, mean",
    "notes": "available=false means the attribute could NOT be measured (no live particles, no MSF field, unknown name) - not the same as a value that measured zero. A world field is a single scalar: min/max/mean all read the same number.",
    "tags": "simulation|attributes|measure",
    "related": "attr.list"
  },
  "sim_cache.status": {
    "summary": "Report the simulation cache: valid range, frames in RAM, cache directory and the config signature",
    "notes": "The config signature is what decides whether a bake is still valid; when it changes, cached frames belong to a different setup.",
    "tags": "simulation|cache|bake|verify"
  },
  "sim_cache.bake": {
    "summary": "Bake a frame range of the simulation to the cache",
    "tags": "simulation|cache|bake"
  },
  "sim_cache.clear": {
    "summary": "Clear the simulation cache",
    "tags": "simulation|cache|reset"
  },
  "debris.configure": {
    "summary": "Enable and configure the ash/debris particle system",
    "tags": "fire|ash|debris|particles"
  },
  "debris.emit_ash": {
    "summary": "Emit ash debris of a given mass at a point",
    "tags": "fire|ash|debris|emit"
  },
  "debris.stats": {
    "summary": "Report ash debris counts, accepted mass and budget rejections",
    "tags": "fire|ash|measure"
  },
  "anim.characters": {
    "summary": "List the animated characters",
    "tags": "animation|inventory"
  },
  "anim.character": {
    "summary": "Return one character's animation state",
    "tags": "animation"
  },
  "anim.clips": {
    "summary": "List a character's animation clips with frame ranges and loop flags",
    "tags": "animation|clip"
  },
  "anim.source_clips": {
    "summary": "List the RAW imported animation clips with channel and key counts",
    "notes": "SceneData::animationDataList as the LOADER produced it - not a character's AnimationController state (that is anim.clips). This is the acceptance instrument for replacing the Assimp importer: the same file must yield identical channel and key counts on the old and the new path, and 'it still animates' is not a measurement. A channel is one animated node name; position_keys/rotation_keys/scaling_keys are summed over all channels. Times are in TICKS (duration_ticks, first_key_time, last_key_time); divide by ticks_per_second for seconds. first_key_time > last_key_time would mean a reader emitted unsorted keys - the sampling code assumes time-sorted keys.",
    "tags": "animation|clip|import|acceptance|assimp|gltf|channels|keys",
    "related": "anim.source_channels|anim.clips|scene.import_model",
    "returns": "Array of objects with name, model_name, duration_ticks, ticks_per_second, duration_seconds, start_frame, end_frame, position_channels, rotation_channels, scaling_channels, position_keys, rotation_keys, scaling_keys, first_key_time, last_key_time",
    "undoable": false
  },
  "anim.source_channels": {
    "summary": "Per-node key counts for one raw imported clip",
    "notes": "The breakdown behind anim.source_clips, sorted by node name so two runs diff without post-processing. Kept separate because a rigged character has hundreds of nodes and the clip totals answer most questions; reach for this when the totals match but the pose does not, which means the keys landed on different NODES.",
    "tags": "animation|clip|channels|bones|import|acceptance",
    "related": "anim.source_clips",
    "returns": "Array of objects with node_name, position_keys, rotation_keys, scaling_keys",
    "undoable": false,
    "params": {
      "clip": {
        "description": "Clip name; empty or omitted uses the first imported clip"
      }
    }
  },
  "anim.play": {
    "summary": "Play an animation clip on a character layer with a blend time",
    "tags": "animation|play|clip"
  },
  "anim.stop": {
    "summary": "Stop playback on a character layer with a blend-out time",
    "tags": "animation|stop"
  },
  "anim.set_paused": {
    "summary": "Pause or resume a character's animation",
    "tags": "animation|pause"
  },
  "anim.set_time": {
    "summary": "Set playback time in seconds on a character layer",
    "tags": "animation|time|scrub"
  },
  "anim.set_speed": {
    "summary": "Set playback speed on a character layer",
    "tags": "animation|speed"
  },
  "anim.set_loop": {
    "summary": "Set whether a character layer loops",
    "tags": "animation|loop"
  },
  "anim.status": {
    "summary": "Report a character layer's playback state",
    "tags": "animation|progress"
  },
  "anim.graph_status": {
    "summary": "Report a character's animation graph state",
    "tags": "animation|graph|statemachine"
  },
  "anim.set_graph_param": {
    "summary": "Set a float or bool parameter on a character's animation graph",
    "tags": "animation|graph|parameter"
  },
  "anim.trigger_graph_param": {
    "summary": "Fire a trigger parameter on a character's animation graph",
    "tags": "animation|graph|trigger"
  },
  "anim.insert_key": {
    "summary": "Insert a keyframe on an object channel at a frame",
    "tags": "animation|keyframe|channel",
    "params": {
      "channel": {
        "description": "Animated channel",
        "enum": "location|rotation|scale"
      }
    }
  },
  "anim.remove_key": {
    "summary": "Remove an object's keyframe at a frame",
    "tags": "animation|keyframe"
  },
  "anim.list_keys": {
    "summary": "List an object's keyframes",
    "tags": "animation|keyframe"
  },
  "post.get": {
    "summary": "Read the post-processing settings: exposure, gamma, tone mapping, saturation, vignette and stylize",
    "tags": "post|grade|look"
  },
  "post.set_exposure": {
    "summary": "Set post-process exposure",
    "notes": "Linear gain in [0,65504]. Multiplies the EV/physical/auto exposure selected by post.configure_exposure. Does not reset sample accumulation.",
    "tags": "post|brightness|grade"
  },
  "post.set_gamma": {
    "summary": "Set post-process gamma",
    "tags": "post|grade"
  },
  "post.set_tone_mapping": {
    "summary": "Select AgX, ACES Fitted, Uncharted, Hejl Filmic, Linear or Reinhard",
    "notes": "Shared CPU/CUDA/GLSL scene-linear Rec.709 implementation. ACES is a fitted approximation, not a full ACES/OCIO output transform. None/linear clips without tone mapping. Old AGX/ACES looks are intentionally replaced.",
    "tags": "post|tonemap|color",
    "params": {
      "type": {
        "enum": "agx|aces_fitted|uncharted|filmic|linear|reinhard"
      }
    }
  },
  "post.set_saturation": {
    "summary": "Set post-process saturation",
    "tags": "post|grade|colour"
  },
  "post.set_color_temperature": {
    "summary": "Set scene-linear Bradford white balance",
    "notes": "Finite [4000,25000] Kelvin, daylight-locus source white adapted to D65. 6500 K is exactly neutral. Applied before the view transform. Higher Kelvin warms.",
    "tags": "post|whitebalance|color"
  },
  "post.set_vignette_enabled": {
    "summary": "Enable or disable the vignette",
    "tags": "post|look"
  },
  "post.set_vignette_strength": {
    "summary": "Set vignette strength",
    "tags": "post|look"
  },
  "post.set_stylize_enabled": {
    "summary": "Enable or disable the stylize pass",
    "tags": "post|look|npr"
  },
  "post.set_stylize_strength": {
    "summary": "Set stylize pass strength",
    "tags": "post|look|npr"
  },
  "project.path": {
    "summary": "Return the current project file path",
    "tags": "project|file"
  },
  "project.open": {
    "summary": "Open a project file, replacing the current scene",
    "tags": "project|file|load"
  },
  "viewport.retry_device_recovery": {
    "summary": "Re-arms automatic viewport recovery after it gave up on repeated device losses.",
    "notes": "Clears the given_up state and schedules one more rebuild attempt. Deliberately manual: automatic recovery stops after max_streak consecutive losses because the loop can feed itself -- if the raster viewport is what trips the TDR, rebuilding it trips the TDR again, and each round resets the display driver. So calling this while the scene that caused the loss is still loaded most likely means another driver reset; change what the viewport is drawing first, or accept that cost knowingly. A manual viewport mode change in the UI re-arms recovery the same way, so a user at the panel is never stuck without IPC."
  },
  "viewport.device_recovery_status": {
    "summary": "Whether the Vulkan viewport came back after a TDR, and why it has not yet.",
    "notes": "Answers 'after a TDR the viewport never comes back, not even if I switch modes by hand'. Two separate defects produced that, and the fields separate them. First: the device-lost handler tore the viewport backend down and then fell through, in the SAME loop iteration with no continue or return between them, to an unconditional initializeViewportBackendIfAvailable() -- opening a new VkDevice while the driver was still resetting. Second: that function begins with `if (g_viewport_backend) return true`, so a non-null pointer is taken as proof of a working device; once a poisoned backend was stored, every later attempt short-circuited and no manual mode switch could ever rebuild it. Recovery is now behind a time gate and the main loop retries on its own rather than waiting for the user to change backends. Read consecutive_losses against max_streak FIRST: automatic recovery is budgeted precisely because the loop it enables can be self-feeding -- if the raster work is what trips the TDR, rebuilding the viewport reproduces the TDR, and a time gate only slows that down rather than stopping it. When consecutive_losses reaches max_streak, given_up goes true and nothing is retried until a manual viewport mode change or viewport.retry_device_recovery re-arms it; given_up=true with viewport_alive=false is therefore a DELIBERATE stop, not a hang. The streak counts losses that follow a successful rebuild, not failed rebuild attempts, because those are different faults: a rebuild that fails means the device will not come back, while a rebuild that succeeds and is then lost again means the viewport work itself is killing it. The streak clears only after a rebuilt viewport has survived 30 seconds, so an unrelated TDR hours later is not charged to an old streak. Read viewport_alive with rebuild_pending, never alone: alive=true plus pending=true means a backend object exists but recovery has not been confirmed. gate_remaining_ms > 0 means we are deliberately waiting for the driver to reset and the absence of a rebuild is correct, not a hang. rebuild_attempts climbing while viewport_alive stays false means the device is genuinely refusing to come back, which is a different problem from never having tried."
  },
  "render.optix_accum_status": {
    "summary": "Counts how often the OptiX accumulation buffer was WIPED, independently of the sample counter.",
    "notes": "Answers 'the sample counter climbs but the image never accumulates'. The two numbers are deliberately independent because they can disagree, and that disagreement is the finding. OptixWrapper reallocates and zeroes the float4 accumulation buffer whenever accumulation_valid is false; the resolution-change branch sets exactly that, and it does NOT reset accumulated_samples. So if the render resolution alternates between two values across frames, the buffer is zeroed every frame while the counter keeps counting: progress is reported that the image does not have, and each pass is displayed alone because the kernel takes its prev_samples==0 branch. Read wipe_count against accumulated_samples: a healthy progressive render wipes ONCE per camera move, so wipe_count should stay near 1 while samples climb. If wipe_count tracks the frame count, the buffer is being destroyed every pass. There are THREE wipe routes and they are counted separately because they are different mechanisms and only one of them is visible in wipe_count: the resolution branch and the lazy realloc both free and re-zero the buffer (wipe_count, wipe_resolution), while the camera-change branch uses cudaMemsetAsync instead (wipe_camera). Counting only the realloc would have shown wipe_count=0 while the camera path re-zeroed every single frame -- an instrument going quiet read as proof of absence. Note also that the camera branch DOES reset accumulated_samples while the resolution branch does not, so a climbing sample counter alongside a non-accumulating image points away from camera_changed and toward the resolution branch. Then read wipe_resolution and the last_wipe_from / last_wipe_to pairs: if those two sizes keep swapping, the caller is handing OptiX a different resolution on alternating frames and that is the root, not the accumulation code. available=false simply means the OptiX backend is not active and is not an error."
  },
  "project.autosave_now": {
    "summary": "Force-writes the session autosave now, ignoring the interval and the modified flag.",
    "notes": "Writes the session autosave immediately, ignoring both the interval and the modified flag. Reports the FULL autosave status back, not just success, because the useful question after a failure is 'why did it not write' and the answer is in those fields. Autosave was a lie until 2026-09-16: the preference (auto_save_enabled, default true), the restore path (StartupMode::RestoreAutosave) and the Hub's 'Recover the last autosaved scene session' button all existed and NOTHING EVER WROTE THE FILE, so the recovery offered was of a file that could not exist. The writer also has to protect project identity: ProjectManager::saveProject sets current_file_path, renames an Untitled project after the file, and adds the path to the recent list -- correct for a user save, catastrophic for an autosave, because the user's next Ctrl+S would go to autosave.rtp instead of their project. The autosave unit restores path, name and the is_modified flag around the write; is_modified in particular, so an autosave never makes unsaved work look saved and silently suppress the quit prompt. The same writer is called on VK_ERROR_DEVICE_LOST before the viewport backend is torn down.",
    "params": {
      "reason": {
        "description": "Tag recorded in last_reason so a later status read says what triggered the write. Defaults to 'manual'."
      }
    }
  },
  "project.autosave_status": {
    "summary": "Reads the autosave instrument without writing.",
    "notes": "Reads the autosave instrument without writing. Read write_count FIRST: if it is 0 while the app has been open longer than interval_sec, autosave is not running at all. Then read skipped_unmodified -- if THAT is growing while write_count stays 0, the broken thing is not autosave but the is_modified flag, which is why the two counters are separate rather than one 'healthy' boolean. seconds_until_next goes negative only between the due moment and the next tick. file_exists plus file_bytes describe the file on disk, so a status that claims success while file_bytes stays 0 means the write path is reporting an outcome it did not achieve. last_reason distinguishes an interval write from the forced one taken on VK_ERROR_DEVICE_LOST."
  },
  "project.save": {
    "summary": "Save the project, optionally to a new path",
    "tags": "project|file|save"
  },
  "script.run_file": {
    "summary": "Run a Python script file inside the application",
    "tags": "script|python|automation"
  },
  "templates.list": {
    "summary": "List the available scene templates",
    "tags": "template|starter|scene"
  },
  "templates.get": {
    "summary": "Return one template's metadata",
    "tags": "template"
  },
  "templates.open": {
    "summary": "Open a template as the current scene",
    "tags": "template|load|start"
  },
  "templates.prepare": {
    "summary": "Run a template's preflight without opening it",
    "tags": "template|preflight|validate"
  },
  "templates.validate": {
    "summary": "Validate a template and report the errors that would block it",
    "tags": "template|validate"
  },
  "templates.refresh": {
    "summary": "Rescan the template directories",
    "tags": "template|reload"
  },
  "templates.save_user": {
    "summary": "Save the current scene as a user template",
    "tags": "template|save"
  },
  "templates.delete_user": {
    "summary": "Delete a user template",
    "tags": "template|remove"
  },
  "templates.show_hub": {
    "summary": "Show the Template Hub window",
    "tags": "template|ui"
  },
  "templates.hide_hub": {
    "summary": "Hide the Template Hub window",
    "tags": "template|ui"
  },
  "templates.is_hub_visible": {
    "summary": "Report whether the Template Hub is visible",
    "tags": "template|ui"
  },
  "editor.get_state": {
    "summary": "Report which editors are open, the node editor's domain and the simulation graph scope being shown",
    "notes": "Editor state is a VALUE, so it travels over IPC. Panel drawing does not - that is the rt.ui exemption.",
    "tags": "ui|editor|state|context"
  },
  "editor.set_bottom_editor": {
    "summary": "Choose which editor fills the bottom dock",
    "tags": "ui|editor"
  },
  "editor.set_node_domain": {
    "summary": "Choose which domain's graph the node editor shows",
    "tags": "ui|editor|nodes",
    "params": {
      "name": {
        "description": "Node editor domain",
        "enum": "material|geometry|terrain"
      }
    }
  },
  "editor.set_sim_graph_scope": {
    "summary": "Choose which scope and owner the simulation node editor shows",
    "tags": "ui|editor|simulation|scope"
  },
  "addons.list": {
    "summary": "List the installed addons with their load state",
    "tags": "plugin|extension"
  },
  "addons.enable": {
    "summary": "Enable an addon module",
    "tags": "plugin|extension"
  },
  "addons.disable": {
    "summary": "Disable an addon module",
    "tags": "plugin|extension"
  },
  "addons.reload": {
    "summary": "Reload an addon module from disk",
    "tags": "plugin|extension|develop"
  },
  "ipc.admin.tokens.list": {
    "summary": "List the remote IPC access tokens and their capabilities",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.create": {
    "summary": "Create a remote IPC token with a capability mask and return the raw secret once",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.update": {
    "summary": "Update a token's name, capabilities, expiry or address allowlist",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.rotate": {
    "summary": "Rotate a token's secret and return the new one once",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.revoke": {
    "summary": "Revoke a remote IPC token",
    "tags": "security|admin|token"
  },
  "ipc.admin.sessions.list": {
    "summary": "List the connected IPC sessions with traffic counters",
    "tags": "security|admin|session"
  },
  "ipc.admin.sessions.get": {
    "summary": "Return one IPC session's details",
    "tags": "security|admin|session"
  },
  "ipc.admin.sessions.disconnect": {
    "summary": "Disconnect one IPC session",
    "tags": "security|admin|session"
  },
  "ipc.admin.sessions.disconnect_all": {
    "summary": "Disconnect every IPC session except the caller",
    "tags": "security|admin|session"
  },
  "ipc.admin.audit.list": {
    "summary": "Return recent IPC audit events: method, caller, outcome and duration",
    "tags": "security|admin|audit|log"
  },
  "ipc.admin.audit.clear": {
    "summary": "Clear the IPC audit log",
    "tags": "security|admin|audit"
  },
  "ipc.admin.audit.export": {
    "summary": "Export the IPC audit log to a JSONL file",
    "tags": "security|admin|audit|export"
  },
  "sim.control_state": {
    "summary": "Who is currently driving the solvers, and an epoch that changes whenever anything re-poses them",
    "notes": "Read epoch before and after a measurement. If it changed, the solvers were re-posed under you - the user scrubbed, playback ran, or a reset fired - and the numbers you just read are not a physics result. Without this a reverted pose and a body that never moved read exactly the same. driver says which of those it was; script_driving is true while physics.step holds the timeline, which it keeps only until the user scrubs, plays or stops. dropped_seeds names any fluid domain whose one-shot fluid.seed() (no persistent=true) the most recent auto-reset wiped and did NOT refill -- that auto-reset fires from a config-signature change ANYWHERE in the scene (e.g. an unrelated fluid.create_domain), so a domain's particles can go to zero with no error at all; check this field when that happens instead of assuming a reader fault.",
    "tags": "simulation|control|epoch|driver|timeline|measure|verify|fluid|seed",
    "related": "physics.step|timeline.set_frame|timeline.get_frame|scene.get_world_transform|fluid.seed|fluid.create_domain"
  },
  "perf.list": {
    "summary": "List every recorded build/render timing section, newest write first",
    "notes": "Sections are named by the code that performs the work. BUILD sections: terrain.graph.evaluate/.height/.aux_outputs/.finalize_mesh, terrain.mesh_fill/.create/.update/.publish_fields, terrain.splat_resize, Renderer::rebuildBVH(...), Renderer::rebuildBackendGeometry(GPU), accel.vulkan_solid.raster_geometry. PER-FRAME sections (added 2026-09-03), all prefixed loop.: loop.frame is the whole main-loop iteration and the others are its parts - loop.events (SDL event pump), loop.ui_draw (building every ImGui panel), loop.viewport_render (the backend's renderProgressive; loop.viewport_render_cpu for the legacy CPU path), loop.display_post (original_surface to display surface), loop.imgui_render (building draw lists), loop.present (SDL_RenderClear/Copy/RenderPresent - a vsync wait, if any, lands HERE, so a large value must be read as 'work OR waiting'), loop.throttle_sleep and loop.frame_tail (deferred geometry/BLAS/TLAS rebuilds and the rest of the iteration). WHY THIS EXISTS: before it, only the backend frame and two display copies were timed. Measured on a 695k-triangle interior at 1680x945, the backend frame was 1.2 ms while the loop period was 17.8-26 ms - about 95% of the frame was in code no instrument looked at, so a 142 ms frame could not be decomposed at all. READ loop.throttle_sleep BEFORE CONCLUDING ANYTHING: the main loop deliberately sleeps 16 ms (tier1) or 48 ms (tier2) when the app is not in tier0, and tier0 requires camera_moved, which is set ONLY from mouse/keyboard. An IPC-driven session therefore never reaches tier0, so every frame rate a script measures includes that sleep and is NOT the engine's throughput. Use max_ms to catch the rare slow frame - the mean dissolves it - and call perf.reset first to open a clean window. Each section carries last/total/max ms and a call count. 'rss_measured' says whether the working-set fields mean anything: the loop.* sections set it false because a per-frame scope must not pay for two GetProcessMemoryInfo calls, and their last_rss_delta_mb/rss_after_mb are ABSENCE, not a measurement of zero.",
    "tags": "performance|timing|profile|measure|slow|memory",
    "related": "perf.get|perf.reset|perf.set_logging",
    "returns": "PerfSection[]"
  },
  "perf.get": {
    "summary": "Read one timing section by name",
    "notes": "Returns found:false when the section has never been recorded. It never answers with zeros: a zeroed timing reads as 'measured, and it was free', which turns a missing measurement into a false one. 'rss_measured' false means the working-set fields are absence rather than zero - per-frame sections (loop.*) skip the working-set query so the instrument does not distort the section it measures. See perf.list for the loop.* section map and for why loop.throttle_sleep must be read before any frame-rate conclusion.",
    "tags": "performance|timing|profile|measure",
    "related": "perf.list",
    "returns": "PerfSection"
  },
  "perf.reset": {
    "summary": "Clear the timing counters before a measured run",
    "notes": "Write ordering (seq) stays monotonic across a reset, so a section recorded after it cannot be confused with a stale one. Changes no scene, render or file state - which is why it carries the read capability. Call it before a measured run: max_ms never decays, so one scene-load frame keeps dominating the table until it is cleared.",
    "tags": "performance|timing|profile|reset",
    "related": "perf.list|perf.get"
  },
  "perf.set_logging": {
    "summary": "Also mirror completed timing sections into the Scene Log",
    "notes": "Off by default. The registry is the readable surface - script and IPC callers cannot read the Scene Log, which is exactly why the previous profiler produced no usable numbers.",
    "tags": "performance|timing|profile|log",
    "related": "perf.list"
  },
  "terrain.set_mesh_resolution": {
    "summary": "Set the terrain vertex-grid resolution independently of the field (analysis) resolution",
    "notes": "0 means the mesh follows the field, which is the historical behaviour. The field grid (heights, slope, flow, erosion, every analysisFields entry) is untouched; only the triangle count changes, and analysis fields are resampled onto the coarser vertex grid rather than dropped. Measured at 4096^2: mesh fill is 380 ms but the acceleration structures built from its 33.5 M triangles cost 6.4 s (Vulkan solid raster) + 2.6 s (Embree), and that cost is linear in triangle count - a 1024 mesh over a 4096 field is roughly 18x cheaper end to end. Normals are still sampled from the field, so shading detail largely survives; the silhouette does not. A value above the field resolution is REFUSED rather than clamped.",
    "tags": "terrain|mesh|resolution|lod|performance|triangles|decimate",
    "related": "terrain.get|terrain.create|perf.list",
    "returns": "TerrainInfo",
    "params": {
      "name": {
        "description": "Terrain name",
        "required": true,
        "type": "string"
      },
      "mesh_resolution": {
        "description": "Vertices per side for the mesh grid, or 0 to follow the field resolution",
        "required": true,
        "type": "int"
      }
    }
  },
  "spline.list": {
    "summary": "List editable 2D spline sources and their curve types",
    "notes": "Returns names, plane, closed state and control-point counts. This is a read-only scene query.",
    "tags": "spline|curve|profile|control-point|authoring",
    "related": "spline.get|spline.set|spline.insert_point|spline.subdivide|spline.extrude",
    "returns": "SplineInfo[]"
  },
  "spline.create": {
    "summary": "Create an editable spline source through the canonical scene-object service",
    "notes": "Returns the unique scene name. The operation is undoable and produces the same SplineObject used by the viewport UI, Python and Geometry Nodes. primitive also accepts 'empty' (no points) and plane also accepts 'free' - the pair that starts a drawn route, since a profile is planar but a path is not.",
    "tags": "spline|curve|create|authoring|undo",
    "related": "spline.list|spline.get|scene.delete|nodes.create_graph",
    "returns": "string",
    "undoable": true,
    "params": {
      "primitive": {
        "description": "Initial spline shape",
        "type": "string",
        "enum": "circle|rectangle|open_line|open_arc",
        "default": "open_line"
      },
      "name": {
        "description": "Requested object name; made unique when necessary",
        "type": "string",
        "default": "Spline"
      },
      "plane": {
        "description": "Authoring plane",
        "type": "string",
        "enum": "xy|xz|yz",
        "default": "xy"
      }
    }
  },
  "spline.get": {
    "summary": "Read the versioned JSON authoring payload of a spline",
    "notes": "The payload includes curve type, plane, closed state, render transform, pivot_offset, optional cubic B-Spline knots and all point/handle data.",
    "tags": "spline|serialize|profile|curve",
    "related": "spline.set|spline.list",
    "returns": "SplinePayload",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      }
    }
  },
  "spline.set": {
    "summary": "Replace a spline authoring payload with validated JSON data",
    "notes": "Rejects unknown curve types, malformed points, invalid transforms, pivot_offset or knot vectors, and B-Splines with fewer than four controls.",
    "tags": "spline|serialize|edit|profile",
    "related": "spline.get|spline.insert_point|spline.subdivide",
    "returns": "bool",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "spline": {
        "description": "Versioned spline payload object",
        "required": true,
        "type": "object"
      }
    }
  },
  "spline.insert_point": {
    "summary": "Insert a control point on a spline segment at normalized parameter t",
    "notes": "Bezier insertion preserves the curve with De Casteljau; Linear insertion splits the segment; cubic B-Spline insertion uses shape-preserving Boehm knot insertion.",
    "tags": "spline|insert|subdivide|bezier|linear",
    "related": "spline.subdivide|spline.get",
    "returns": "{index:int}",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "segment": {
        "description": "Zero-based segment start index",
        "required": true,
        "type": "int"
      },
      "t": {
        "description": "Segment parameter in [0,1]",
        "required": true,
        "type": "float"
      }
    }
  },
  "spline.subdivide": {
    "summary": "Subdivide one or more spline segments with a fixed cut count",
    "notes": "Segments are processed in descending index order so a batch request remains stable. The operation is validated as one mutation.",
    "tags": "spline|subdivide|batch|curve",
    "related": "spline.insert_point|spline.get",
    "returns": "{last_index:int}",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "segments": {
        "description": "Segment start indices",
        "required": true,
        "type": "int[]"
      },
      "cuts": {
        "description": "Cuts per segment, from 1 to 128",
        "required": true,
        "type": "int"
      }
    }
  },
  "spline.extrude": {
    "summary": "Extrude an open spline endpoint to a new control point",
    "notes": "Only endpoint 0 or the final point is accepted, and closed splines are rejected.",
    "tags": "spline|extrude|endpoint|profile",
    "related": "spline.get|spline.insert_point",
    "returns": "{index:int}",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "endpoint": {
        "description": "0 for first endpoint or last point index",
        "required": true,
        "type": "int"
      },
      "position": {
        "description": "New control point position",
        "required": true,
        "type": "float[3]"
      }
    }
  },
  "spline.keyframe.insert": {
    "summary": "Capture spline object transform and control-point deformation at a timeline frame",
    "notes": "Point keys include Linear, Bezier and B-Spline controls, Bezier handles, radius/user data and color. Point count, curve type, open/closed state and knot topology must remain stable between keys.",
    "tags": "spline|animation|keyframe|deform|hose|cable|radius",
    "related": "spline.keyframe.list|spline.keyframe.remove|timeline.set_frame|spline.get",
    "returns": "bool",
    "params": {
      "name": {
        "description": "Spline object name",
        "type": "string",
        "required": true
      },
      "frame": {
        "description": "Non-negative timeline frame",
        "type": "int",
        "required": true
      },
      "object_transform": {
        "description": "Capture the spline object's location, rotation and scale",
        "type": "bool",
        "default": true
      },
      "points": {
        "description": "Capture all control positions, handles, radius/user data and colors",
        "type": "bool",
        "default": true
      }
    }
  },
  "spline.keyframe.remove": {
    "summary": "Remove spline transform and/or point channels from one timeline key",
    "notes": "Other keyframe domains stored at the same frame are preserved.",
    "tags": "spline|animation|keyframe|remove",
    "related": "spline.keyframe.insert|spline.keyframe.list",
    "returns": "bool",
    "params": {
      "name": {
        "description": "Spline object name",
        "type": "string",
        "required": true
      },
      "frame": {
        "description": "Timeline frame containing the key",
        "type": "int",
        "required": true
      },
      "object_transform": {
        "description": "Remove the object transform portion",
        "type": "bool",
        "default": true
      },
      "points": {
        "description": "Remove the control-point deformation portion",
        "type": "bool",
        "default": true
      }
    }
  },
  "spline.keyframe.list": {
    "summary": "List spline animation keys and the channels captured at each frame",
    "tags": "spline|animation|keyframe|list",
    "related": "spline.keyframe.insert|spline.keyframe.remove|timeline.set_frame",
    "returns": "SplineKeyInfo[]",
    "params": {
      "name": {
        "description": "Spline object name",
        "type": "string",
        "required": true
      }
    }
  },
  "spline.animation.self_test": {
    "summary": "Run the deterministic spline transform, control-point and radius interpolation self-test",
    "tags": "spline|animation|selftest|validation",
    "related": "spline.keyframe.insert|spline.keyframe.list",
    "returns": "{ok:bool,details:string}"
  },
  "mesh.profile.revolve.preview": {
    "summary": "Preview a partial or full revolve around a selected axis",
    "notes": "The radial side profile may be open or closed. start_angle/end_angle are radians; partial ranges keep their angular seams open.",
    "tags": "mesh|profile|revolve|screw|axis|preview",
    "related": "mesh.profile.revolve.commit|mesh.profile.revolve.self_test",
    "params": {
      "preset": {
        "description": "Built-in radial profile preset: cup or bottle",
        "type": "string"
      },
      "axis": {
        "description": "Revolution axis: x, y or z",
        "type": "string"
      },
      "start_angle": {
        "description": "Start angle in radians",
        "type": "float"
      },
      "end_angle": {
        "description": "End angle in radians; span must be <= 2*pi",
        "type": "float"
      },
      "radius_offset": {
        "description": "Added to profile X before revolving; use a positive value for torus-style closed profiles",
        "type": "float"
      },
      "axis_pivot": {
        "description": "Geometry-space revolution axis pivot [x, y, z]",
        "type": "float[3]"
      },
      "angle_segments": {
        "description": "Angular subdivisions",
        "type": "int"
      },
      "profile_samples": {
        "description": "Samples along the radial profile",
        "type": "int"
      }
    }
  },
  "mesh.profile.revolve.commit": {
    "summary": "Commit an undoable partial or full revolve mesh",
    "notes": "Uses the same open/closed profile, axis and angle contract as preview.",
    "tags": "mesh|profile|revolve|screw|axis|commit",
    "undoable": true,
    "related": "mesh.profile.revolve.preview|mesh.profile.revolve.self_test",
    "params": {
      "preset": {
        "description": "Built-in radial profile preset: cup or bottle",
        "type": "string"
      },
      "object": {
        "description": "Destination mesh object name",
        "type": "string"
      },
      "axis": {
        "description": "Revolution axis: x, y or z",
        "type": "string"
      },
      "start_angle": {
        "description": "Start angle in radians",
        "type": "float"
      },
      "end_angle": {
        "description": "End angle in radians; span must be <= 2*pi",
        "type": "float"
      },
      "radius_offset": {
        "description": "Added to profile X before revolving; use a positive value for torus-style closed profiles",
        "type": "float"
      },
      "axis_pivot": {
        "description": "Geometry-space revolution axis pivot [x, y, z]",
        "type": "float[3]"
      },
      "angle_segments": {
        "description": "Angular subdivisions",
        "type": "int"
      },
      "profile_samples": {
        "description": "Samples along the radial profile",
        "type": "int"
      }
    }
  },
  "mesh.profile.revolve.self_test": {
    "summary": "Run full closed and partial open-profile revolve core tests",
    "tags": "mesh|profile|revolve|screw|test"
  },
  "mesh.profile.loft.preview": {
    "summary": "Preview a loft between multiple closed spline sections",
    "tags": "mesh|profile|loft|spline|preview",
    "related": "mesh.profile.loft.commit|mesh.profile.loft.self_test",
    "params": {
      "sections": {
        "description": "Ordered spline object names",
        "required": true,
        "type": "string[]"
      },
      "samples_per_section": {
        "description": "Samples per loft ring",
        "type": "int"
      },
      "cap_start": {
        "description": "Generate the first cap",
        "type": "bool"
      },
      "cap_end": {
        "description": "Generate the final cap",
        "type": "bool"
      }
    }
  },
  "mesh.profile.loft.commit": {
    "summary": "Commit an undoable flat DNA SoA mesh generated from multiple spline sections",
    "tags": "mesh|profile|loft|spline|commit",
    "undoable": true,
    "related": "mesh.profile.loft.preview|mesh.profile.loft.self_test",
    "params": {
      "sections": {
        "description": "Ordered spline object names",
        "required": true,
        "type": "string[]"
      },
      "object": {
        "description": "Destination mesh object name",
        "required": true,
        "type": "string"
      },
      "samples_per_section": {
        "description": "Samples per loft ring",
        "type": "int"
      },
      "cap_start": {
        "description": "Generate the first cap",
        "type": "bool"
      },
      "cap_end": {
        "description": "Generate the final cap",
        "type": "bool"
      }
    }
  },
  "mesh.profile.loft.self_test": {
    "summary": "Run the deterministic profile loft core self-test",
    "tags": "mesh|profile|loft|test"
  },
  "geometry_cache.bake": {
    "summary": "Bake a fixed-topology mesh deformation clip from the canonical evaluated geometry",
    "notes": "Stores only local vertex positions. Bake is rejected if vertex count or index connectivity changes.",
    "tags": "geometry|cache|animation|bake|deformation|vertex",
    "related": "geometry_cache.status|geometry_cache.set_enabled|geometry_cache.clear|nodes.apply",
    "params": {
      "object_name": {
        "description": "Flat mesh / Geometry Graph host object",
        "required": true,
        "type": "string"
      },
      "start_frame": {
        "description": "First sampled timeline frame",
        "required": true,
        "type": "int"
      },
      "end_frame": {
        "description": "Last sampled timeline frame",
        "required": true,
        "type": "int"
      },
      "frame_step": {
        "description": "Distance between stored samples",
        "type": "int",
        "default": 1
      }
    }
  },
  "spline.skin.create": {
    "summary": "Create or update a non-destructive skin display on an open spline",
    "notes": "Reuses one linked preview host while parameters change. Builds Spline Object to Taper to Twist to Wave/Noise to Curve to Mesh to Output; an optional closed custom_profile drives the cross-section.",
    "tags": "spline|skin|bevel|cable|tube|curve-to-mesh|geometry-nodes",
    "related": "spline.get|spline.keyframe.insert|geometry_cache.bake|nodes.set_property",
    "params": {
      "spline": {
        "description": "Open spline path object",
        "required": true,
        "type": "string"
      },
      "output": {
        "description": "Requested output mesh name; blank derives one from the spline",
        "type": "string",
        "default": ""
      },
      "radius": {
        "description": "Global bevel radius",
        "type": "float",
        "default": 0.1
      },
      "path_samples": {
        "description": "Samples along the spline",
        "type": "int",
        "default": 48
      },
      "radial_segments": {
        "description": "Circular cross-section segments",
        "type": "int",
        "default": 12
      },
      "cap_start": {
        "description": "Cap the first endpoint",
        "type": "bool",
        "default": true
      },
      "cap_end": {
        "description": "Cap the final endpoint",
        "type": "bool",
        "default": true
      },
      "use_point_radius": {
        "description": "Multiply bevel radius by interpolated per-point Curve Radius",
        "type": "bool",
        "default": true
      },
      "custom_profile": {
        "description": "Optional closed spline used as the swept cross-section",
        "type": "string",
        "default": ""
      },
      "taper_start": {
        "description": "Radius scale at the first control",
        "type": "float",
        "default": 1.0
      },
      "taper_end": {
        "description": "Radius scale at the final control",
        "type": "float",
        "default": 1.0
      },
      "taper_falloff": {
        "description": "Taper interpolation exponent",
        "type": "float",
        "default": 1.0
      },
      "twist_start_degrees": {
        "description": "Profile rotation at the first control",
        "type": "float",
        "default": 0.0
      },
      "twist_end_degrees": {
        "description": "Profile rotation at the final control",
        "type": "float",
        "default": 0.0
      },
      "wave_amplitude": {
        "description": "Wave displacement amplitude",
        "type": "float",
        "default": 0.0
      },
      "wave_cycles": {
        "description": "Wave cycles along the controls",
        "type": "float",
        "default": 1.0
      },
      "wave_phase_degrees": {
        "description": "Wave phase in degrees",
        "type": "float",
        "default": 0.0
      },
      "wave_noise": {
        "description": "Deterministic noise amplitude",
        "type": "float",
        "default": 0.0
      },
      "wave_seed": {
        "description": "Deterministic noise seed",
        "type": "int",
        "default": 0
      },
      "wave_axis": {
        "description": "Local offset axis: 0=X, 1=Y, 2=Z",
        "type": "int",
        "default": 1
      }
    }
  },
  "spline.skin.finalize": {
    "summary": "Convert a spline's live skin display into an ordinary mesh",
    "notes": "Evaluates the current frame, removes the procedural Geometry Graph link and keeps the existing flat preview host as the final mesh.",
    "tags": "spline|skin|preview|convert|apply|mesh",
    "related": "spline.skin.create|spline.skin.clear|geometry_cache.bake",
    "params": {
      "spline": {
        "description": "Open spline source owning the live skin display",
        "required": true,
        "type": "string"
      }
    }
  },
  "spline.skin.clear": {
    "summary": "Remove a spline's non-destructive skin display",
    "notes": "Deletes only the linked preview host and graph; the editable spline source remains unchanged.",
    "tags": "spline|skin|preview|remove|clear",
    "related": "spline.skin.create|spline.skin.finalize",
    "params": {
      "spline": {
        "description": "Spline source owning the live skin display",
        "required": true,
        "type": "string"
      }
    }
  },
  "geometry_cache.status": {
    "summary": "Inspect deformation-cache range, memory, topology validity and source staleness",
    "tags": "geometry|cache|animation|status|memory",
    "related": "geometry_cache.bake|geometry_cache.set_enabled|geometry_cache.clear",
    "params": {
      "object_name": {
        "description": "Cached flat mesh object",
        "required": true,
        "type": "string"
      }
    }
  },
  "geometry_cache.set_enabled": {
    "summary": "Switch an existing geometry cache between cached and live procedural playback",
    "tags": "geometry|cache|animation|playback|toggle",
    "related": "geometry_cache.status|geometry_cache.bake",
    "params": {
      "object_name": {
        "description": "Cached flat mesh object",
        "required": true,
        "type": "string"
      },
      "enabled": {
        "description": "Use cached positions when true",
        "required": true,
        "type": "bool"
      }
    }
  },
  "geometry_cache.clear": {
    "summary": "Remove a baked deformation clip and restore live Geometry Graph evaluation",
    "tags": "geometry|cache|animation|clear",
    "related": "geometry_cache.bake|geometry_cache.status",
    "params": {
      "object_name": {
        "description": "Cached flat mesh object",
        "required": true,
        "type": "string"
      }
    }
  },
  "geometry_cache.self_test": {
    "summary": "Run fixed-topology position capture, interpolation and memory-accounting tests",
    "tags": "geometry|cache|animation|selftest|validation",
    "related": "geometry_cache.bake|geometry_cache.status"
  },
  "terrain.list_layers": {
    "summary": "List the eight terrain layer slots and what drives each one",
    "notes": "Slots 0-3 are weighted by the splat map and normalized against each other - they partition the surface. Slots 4-7 are weighted by the semantic map (Flow, Wetness, Ice, Hardness) and composited OVER that blend by their own unnormalized weight. bound=false on a semantic slot means the built-in shading still applies to that channel. TWO dead cases to check on a bound overlay: channel_coverage 0 means the graph never fills that channel (Auto Splat's semantic output writes Flow and hard-zeroes the other three, while Surface Composer writes all four); channel_constant true means min==max, a flat fill that reports full coverage yet selects nothing and washes the material evenly over the terrain - this is how an unwired Surface Composer Hardness input presents, falling back to a constant 0.45. Hardness is produced by Lithology or Strata.",
    "tags": "landscape|material|layer|splat|semantic",
    "related": "terrain.set_layer|material.list"
  },
  "terrain.sculpt": {
    "summary": "Run a terrain sculpt stroke (raise|lower|flatten|smooth|stamp) over a list of world-space dabs",
    "tags": "landscape|brush|authoring",
    "notes": "The panel brush and this method share TerrainManager::sculpt, so this is the only way to regression-test terrain sculpting. Read height_delta, NOT ok: the height field is metres/scale_y and a graph-authored landform routinely exceeds 1.0, so a bug that clamps the field reports success while flattening the ground under the brush. dt is the seconds one dab represents (the panel feeds 1/60); strength is metres per second at full falloff, so a visible bump needs either a large strength or many dabs. flatten samples its target from the surface under the first dab unless use_fixed_height is set.",
    "params": {
      "dabs": {
        "type": "array",
        "required": true,
        "description": "Stroke path: list of [world_x, world_z] pairs. One dab is a click, several are a drag; every dab must land on the tile or the call is refused."
      },
      "mode": {
        "type": "string",
        "enum": "raise|lower|flatten|smooth|stamp"
      },
      "dt": {
        "description": "Seconds one dab represents; the panel feeds one frame",
        "default": "0.0166667"
      },
      "strength": {
        "description": "Metres of height change per second at full falloff"
      },
      "radius": {
        "description": "Brush radius in metres"
      },
      "curve": {
        "description": "Falloff exponent, 0.25..4"
      }
    }
  },
  "terrain.paint_splat": {
    "summary": "Paint one terrain splat channel (layer) along a list of world-space dabs",
    "tags": "landscape|brush|paint",
    "notes": "Fails rather than initialising when the terrain has no splat map or layers: an auto-created layer stack would hide the missing setup step. coverage_before/after are the mean weight of the painted channel over the whole splat map, which is what separates 'the stroke ran' from 'the stroke painted'; a stroke fully outside the tile is refused, not silently ignored.",
    "params": {
      "dabs": {
        "type": "array",
        "required": true,
        "description": "Stroke path: list of [world_x, world_z] pairs, all of which must land on the tile."
      },
      "channel": {
        "description": "Splat channel / layer index 0..3"
      },
      "dt": {
        "description": "Seconds one dab represents; the panel feeds one frame",
        "default": "0.0166667"
      }
    }
  },
  "scatter.set_settings": {
    "summary": "Patch a scatter group's placement rules and its density / exclusion / scale masks",
    "notes": "Only the keys you pass are written; the rest are left alone. The two mask roles are NOT symmetric: density_mask INCLUDES probabilistically (its value is a placement probability, so 0.3 thins a stand), while exclusion_mask FORBIDS placement wherever the field is >= exclusion_threshold. exclusion_threshold is shared by exclusion_mask and splat_exclude_channel. Mask names are terrain analysis fields - get the ones a terrain actually has from terrain.list_fields, and build a combined mask upstream with a Publish Field node rather than expecting more mask slots here. splat channels are -1 (off) or 0..3 (RGBA); an out-of-range channel is refused, not clamped. Read the result back with scatter.list_groups.",
    "tags": "scatter|foliage|mask|density|exclusion|placement",
    "related": "scatter.list_groups|terrain.list_fields|scatter.fill",
    "params": {
      "group": {
        "type": "string",
        "description": "Scatter group id or name"
      },
      "target_count": {
        "type": "int",
        "description": "How many instances to aim for on the next fill"
      },
      "seed": {
        "type": "int",
        "description": "Placement seed; the same seed reproduces the same layout"
      },
      "min_distance": {
        "type": "float",
        "description": "Minimum spacing between instances, in metres"
      },
      "slope_max": {
        "type": "float",
        "description": "Reject ground steeper than this, in degrees"
      },
      "height_min": {
        "type": "float",
        "description": "Lowest world height that accepts an instance, in metres"
      },
      "height_max": {
        "type": "float",
        "description": "Highest world height that accepts an instance, in metres"
      },
      "density_mask": {
        "type": "string",
        "description": "INCLUDE field name; its value is a placement PROBABILITY, so 0.3 thins rather than cuts. Empty string turns it off"
      },
      "exclusion_mask": {
        "type": "string",
        "description": "EXCLUDE field name; placement is forbidden where the field is >= exclusion_threshold. Empty string turns it off"
      },
      "exclusion_threshold": {
        "type": "float",
        "description": "Reject at or above this value; drives BOTH exclusion_mask and splat_exclude_channel"
      },
      "scale_mask": {
        "type": "string",
        "description": "Field name that scales instances. Empty string turns it off"
      },
      "scale_mask_influence": {
        "type": "float",
        "description": "0 ignores scale_mask, 1 lets it drive scale fully"
      },
      "splat_include_channel": {
        "type": "int",
        "description": "Splat map channel used as an include weight: -1 off, 0..3 = RGBA"
      },
      "splat_exclude_channel": {
        "type": "int",
        "description": "Splat map channel used as an exclusion: -1 off, 0..3 = RGBA"
      }
    }
  },
  "terrain.list_fields": {
    "summary": "List the named analysis fields a terrain is currently publishing",
    "notes": "Measures the LIVE terrain: a name appears only because an output node (Terrain Fields Output, or a Publish Field node) actually wrote it during this evaluation. It is not a catalogue of what the node library could produce, so an empty result means the graph has not published anything yet - not that the feature is missing. These are the valid mask names for scatter.set_settings.",
    "tags": "terrain|field|mask|introspection",
    "related": "scatter.set_settings|terrain.list_layers",
    "params": {
      "terrain": {
        "type": "string",
        "description": "Terrain name, as reported by terrain.list"
      }
    }
  },
  "terrain.field_stats": {
    "summary": "Measure the values and coverage of one live terrain analysis field",
    "notes": "Reads the published field grid itself, not node settings or mesh attributes. Use this before judging a mask visually: min/max/mean and nonzero_fraction expose empty, saturated and constant fields; non_finite_count exposes poisoned data. histogram_bins is 0 to omit or 2..256. samples contains integer [x, y] field-grid coordinates and out-of-range coordinates are refused.",
    "tags": "terrain|field|mask|measurement|histogram|sample|diagnostic",
    "related": "terrain.list_fields|terrain.get|scatter.set_settings",
    "params": {
      "terrain": {
        "type": "string",
        "description": "Terrain name, as reported by terrain.list"
      },
      "field": {
        "type": "string",
        "description": "Live field name, as reported by terrain.list_fields"
      },
      "histogram_bins": {
        "type": "int",
        "description": "0 omits the histogram; otherwise 2..256 bins over the finite min/max range"
      },
      "samples": {
        "type": "array",
        "description": "Optional list of integer [x, y] field-grid coordinates"
      }
    }
  },
  "scene.pick_gpu": {
    "summary": "Pick the object under a screen position using the GPU, reading what the raster viewport actually drew",
    "notes": "Immune by construction to the CPU-geometry failure class that scene.pick_ray measures: it draws an object-ID target with the SAME vertex and instance buffers, the SAME draw loop and the SAME (unjittered) viewProj the raster frame used, then reads back one pixel. u,v are normalized screen coordinates in [0,1] with v UP -- never pixels, so the caller never has to know the viewport resolution. The pass runs only on this call and touches nothing the normal frame uses; cost is one frame of vertex work with the raster clipped to a single pixel. Read the three result fields separately and do NOT collapse them: 'ok' says the call ran, 'hit' says there was geometry at that pixel (false is a legitimate answer, not a failure), and 'reason' explains a non-run. The result is resolved by IDENTITY, never by name: the shader writes (mesh slot, instance slot) and the backend converts that to an index into the raster instance list, reported as 'instance_index'. This matters because the OptiX GPU pick was switched off precisely for resolving id -> name -> selection cache, and that cache was stale. GPU culling is deliberately bypassed, since the compacted instance buffer makes gl_InstanceIndex unmappable back to a scene instance.",
    "params": {
      "u": {
        "type": "number",
        "required": true,
        "description": "normalized X in [0,1]"
      },
      "v": {
        "type": "number",
        "required": true,
        "description": "normalized Y in [0,1], measured UP from the bottom"
      }
    }
  },
  "scene.pick_ray": {
    "summary": "Diagnose viewport object picking at a screen position without changing the selection",
    "notes": "Answers 'why did the click select the wrong object'. Selection is now GPU-first: the click path calls scene.pick_gpu's backend (an ID render pass on the raster viewport device) for IDENTITY, then hits ONLY the named object on the CPU for the surface record and the distance. This method is the CPU half's instrument and stays meaningful: compare its linear_object against scene.pick_gpu at the same u,v, because a disagreement between them is now a disagreement between what is DRAWN and what would be SELECTED. The legacy OptiX pick buffer (getPickedObjectId, ensurePickBuffers, params.pick_buffer and its kernel writes) has been removed outright, so there is exactly one GPU pick path and it is the raster one this method is compared against. This method builds the camera ray from normalized screen coordinates (u,v in [0,1], v up -- NOT pixels, so the caller never has to know the current viewport resolution) and then asks the two picking paths SEPARATELY: the CPU BVH, and a linear scan over scene.world.objects that ignores the BVH entirely. Read 'paths_agree' first. FALSE means the BVH disagrees with brute force, which is what a stale BVH looks like: its AABB pruning drops a small object and returns a larger neighbour, and because the interactive path queries the BVH FIRST and only falls back to the scan when the BVH misses, the wrong answer wins and the scan that would have corrected it never runs. Both hits missing is NOT agreement and is reported as paths_agree false. 'world_objects' catches the other root: if it is small or zero the objects never reached world.objects at all (flat SoA seen only through the facade, or scatter not expanded on Vulkan) and no picking path can find them. 'ray_divergence_deg' separates a third cause -- the render ray carries lens distortion and a random aperture sample while the raster viewport draws a deterministic pinhole, so with depth_of_field on the two differ per click and small objects are missed at random; 'interactive_fallback_ray' says which one selection would actually have used. Then read 'linear_handles_agree'. The interactive selection tries rec.triangle (facade) FIRST and rec.tri_mesh (flat SoA) second, and resolves the facade branch through a tri_to_index lookup; in this repo geometry is always flat SoA and the facade is the legacy path, so when both handles are populated and they name different objects the hit is correct while the SELECTION is wrong -- the exact shape of 'a small object is picked but the wrong one gets selected'. Two empty handles are not agreement and are reported as false. Read 'gate_blocks_selection' BEFORE any of the above: the ray can be perfectly healthy while selection is dead, because the click never reaches the pick code. Measured 2026-09-16 -- selection stopped working entirely in a session while this method reported paths_agree true, divergence 0 and a correct hit; the user had to restart the app because the gate could not be asked. The gate fields are the STICKY ones, the ones that stay true and are only cleared by a scene or project reload, which is what 'reopening fixed it' actually means: rigView.edit_mode (rig edit owns the mouse and handleMouseSelection returns immediately), hud_captured_mouse (an overlay swallowed the click and the flag is only reset on the next click that is also swallowed) and is_dragging. Per-frame values like WantCaptureMouse and ImGuizmo::IsOver are deliberately NOT published: sampled outside an actual click they are meaningless, and reporting them as the gate would be an instrument describing something it did not measure. Unlike scene.raycast, which takes an origin and direction you already know, this one constructs the ray the viewport would and reports the disagreement."
  },
  "scene.raycast": {
    "summary": "Reports what a ray hits: scene geometry, terrain, or the Y=0 ground plane. This is the VALUE behind the viewport's draw-on-surface tools - without it that workflow is reachable only by clicking, and therefore not testable.",
    "params": {
      "origin": {
        "type": "vec3",
        "description": "Ray origin in world space"
      },
      "direction": {
        "type": "vec3",
        "description": "Ray direction; normalized internally, must be non-zero"
      },
      "filter": {
        "type": "string",
        "description": "mesh_and_terrain (default) | terrain_only | ground_plane. An unknown value is REFUSED, not defaulted: silently widening a terrain_only query would report a mesh hit that looks correct"
      }
    },
    "notes": "Returns hit, kind (mesh|terrain|ground_plane|none), object (hit mesh name; empty for terrain and the ground plane), position, normal and distance. The mesh and terrain hits are compared by distance, so a mesh behind the terrain does not win.",
    "related": "spline.append_point|terrain.sample_height|scene.list_objects"
  },
  "spline.append_point": {
    "summary": "Appends a control point at the tail of an open spline, valid from ZERO points. spline.extrude needs two existing points to derive a tangent, so it can extend a curve but never build one.",
    "params": {
      "name": {
        "type": "string",
        "description": "Spline object name, as reported by spline.list"
      },
      "position": {
        "type": "vec3",
        "description": "Point position in the spline's LOCAL space"
      }
    },
    "notes": "Returns the inserted index. Pair with scene.raycast to reproduce the viewport draw-on-surface flow from script. Keyframe topology propagation runs only once the curve has more than two points, because the first two create the curve rather than change its shape.",
    "related": "spline.create|spline.extrude|scene.raycast|spline.get"
  },
  "terrain.road.list_profiles": {
    "summary": "Lists the built-in road profiles. 'Path' and 'road' are the SAME measurement under a different profile, not two solvers - a footpath is narrow, steep-capable and barely moves earth; a main road buys a shallow grade with earthworks.",
    "notes": "Returns id, display_name and the carve parameters each profile sets: road_width, shoulder_width, grading_falloff, foliage_margin, max_grade_percent, max_cut_meters, max_fill_meters. Ids: footpath, dirt_road, main_road.",
    "related": "terrain.road.assign_profile|terrain.road.list_assignments"
  },
  "terrain.road.assign_profile": {
    "summary": "Marks an existing SplineObject as a road under the named profile. Stores NO curve geometry - the curve stays owned by the spline.* surface.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name, as reported by spline.list"
      },
      "profile": {
        "type": "string",
        "description": "Profile id: footpath | dirt_road | main_road. An unknown id is REFUSED, never substituted - carving a main road where a footpath was asked for looks entirely plausible"
      }
    },
    "notes": "A spline that does not exist is refused: an assignment pointing at a missing curve would look identical to a working one in every listing. This is also what removes the graph explosion - Road Network reads the registry, so N roads are one node.",
    "related": "terrain.road.clear_profile|terrain.road.get_assignment|spline.list"
  },
  "terrain.road.clear_profile": {
    "summary": "Removes a road assignment. The SplineObject itself is untouched.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      }
    },
    "related": "terrain.road.assign_profile|terrain.road.list_assignments"
  },
  "terrain.road.get_assignment": {
    "summary": "Reads one road assignment back, including whether its curve still exists.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      }
    },
    "notes": "Returns spline_object, profile_id, crossing_mode, enabled, has_override and curve_exists. A setter with no readback cannot be tested.",
    "related": "terrain.road.assign_profile|terrain.road.get_diagnostics"
  },
  "terrain.road.list_assignments": {
    "summary": "Lists every road assignment in the scene.",
    "notes": "Same fields as terrain.road.get_assignment, one row per assignment.",
    "related": "terrain.road.get_assignment|terrain.road.get_diagnostics"
  },
  "terrain.road.set_crossing_mode": {
    "summary": "Sets how a road resolves where the ground cannot carry it. The solver applies the mode PER ROUTE SAMPLE, so only the span that needs a crossing gets one.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      },
      "mode": {
        "type": "string",
        "description": "auto | terrain | bridge | ford | tunnel. An unknown mode is refused. auto bridges over water when the Road Network node's Water pin is connected and otherwise behaves exactly as terrain; bridge also spans wherever the required fill exceeds max_fill_meters; tunnel bores wherever the required cut exceeds max_cut_meters; ford follows the bed and needs a Water field, reporting crossing_diagnostic when it has none"
      }
    },
    "related": "terrain.road.get_route|terrain.road.get_assignment",
    "notes": "Bridge and tunnel spans leave the terrain untouched and are straightened between their abutments; a tunnel also publishes no surface masks at all, so a forest still grows over it. Measure the result with terrain.road.get_route."
  },
  "terrain.road.set_enabled": {
    "summary": "Enables or disables one road without losing its profile assignment.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      },
      "enabled": {
        "type": "bool",
        "description": "false leaves the assignment in place but stops it carving"
      }
    },
    "related": "terrain.road.get_assignment|terrain.road.get_diagnostics"
  },
  "terrain.road.get_diagnostics": {
    "summary": "Reports assignment health: counts, assignments whose SplineObject is gone, and assignments naming a profile this build does not have.",
    "notes": "Returns assignment_count, enabled_count, dangling and unknown_profiles. A road that silently stops grading because its curve was deleted is the failure nobody files, so it is surfaced here and on the Road Network node.",
    "related": "terrain.road.list_assignments|terrain.field_stats"
  },
  "terrain.road.get_route": {
    "summary": "Reads the SOLVED route of one assigned road out of the terrain graph: sample positions, road and ground height, and the crossing each sample resolved to.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name of an assigned road"
      },
      "max_samples": {
        "type": "int",
        "description": "0 (default) returns only the counters. A positive value returns that many samples, decimated with both endpoints kept"
      }
    },
    "notes": "This is the measurement surface for crossings: bridge_samples, ford_samples and tunnel_samples are how a script proves a declared crossing actually resolved, and crossing_diagnostic names one that could not. It fails explicitly when no Road Network node has carved this assignment yet - an empty route and an unevaluated graph must not read the same.",
    "returns": "spline_object, terrain, sample_count, bridge_samples, ford_samples, tunnel_samples, length_meters, peak_cut_meters, peak_fill_meters, revision, crossing_diagnostic, samples[]",
    "tags": "terrain|road|measurement",
    "related": "terrain.road.build_mesh|terrain.road.set_crossing_mode|terrain.field_stats"
  },
  "terrain.road.build_mesh": {
    "summary": "Generates the optional road surface mesh from the solved route - the very samples that carved the terrain, never a second sampling of the curve.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name of an assigned road"
      },
      "object": {
        "type": "string",
        "description": "Scene object to write. Empty reuses the object this assignment already owns, or creates <spline>_RoadSurface"
      },
      "include_shoulder": {
        "type": "bool",
        "description": "Include the shoulder in the ribbon. The ditch is never meshed - a ditch is terrain"
      },
      "surface_offset": {
        "type": "float",
        "description": "Metres above the graded surface. At zero the continuous ribbon and the discretised heightfield interpenetrate and the road reads as torn"
      },
      "uv_meters_per_tile": {
        "type": "float",
        "description": "Metres of road per V tile"
      },
      "skip_tunnels": {
        "type": "bool",
        "description": "Split the ribbon at tunnel spans instead of drawing a deck through the mountain"
      }
    },
    "notes": "A rebuild REPLACES the geometry of the object the assignment owns, so repeated generation cannot leave a stack of stale roads behind. Publishes flat TriangleMesh / DNA SoA geometry. The terrain-only road stays valid without it: the mesh is a view of the route, never the authority for it.",
    "returns": "object, vertex_count, triangle_count, span_count, length_meters, replaced_existing",
    "tags": "terrain|road|geometry",
    "related": "terrain.road.clear_mesh|terrain.road.get_route"
  },
  "terrain.road.clear_mesh": {
    "summary": "Deletes the generated road surface and forgets the ownership record.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name of an assigned road"
      }
    },
    "notes": "The ownership record is cleared even when the object is already gone, so the next build is not blocked by a pointer to nothing. The terrain-only road is unaffected.",
    "related": "terrain.road.build_mesh"
  },
  "world.get_atmosphere": {
    "summary": "Return the physical atmosphere the sky LUT is baked from",
    "notes": "Separate from world.get on purpose: world.get is the SUN plus the two intensities, this is the MEDIUM. Every field here feeds makeAtmosphereLUTParamsGPU, so every one of them dirties the transmittance/SkyView LUT when it changes.",
    "returns": "WorldAtmosphereInfo",
    "tags": "environment|sky|atmosphere|nishita",
    "related": "world.set_atmosphere|world.get"
  },
  "world.set_atmosphere": {
    "summary": "Set the physical atmosphere parameters of the Nishita sky",
    "notes": "Every field optional: omit a key to leave it unchanged. Out-of-range values are REJECTED, not clamped, because a silently clamped planet radius or scale height reads as 'the sky looks odd' rather than as an error. Any changed field marks the atmosphere LUT dirty; the runtime rebuilds it on the GPU compute path (throttled to about 15 Hz while a value is being dragged) and falls back to a CPU bake only when that pipeline is missing. UNITS ARE ABSOLUTE: planet_radius/atmosphere_height/altitude are METRES, rayleigh_density and mie_density are SCALE HEIGHTS in metres (NOT densities, despite the field names), temperature is CELSIUS and scales both scale heights.",
    "tags": "environment|sky|atmosphere|nishita|scattering",
    "related": "world.get_atmosphere|world.set_atmosphere_intensity|world.set_sun_elevation",
    "params": {
      "air_density": {
        "description": "Rayleigh scattering multiplier (Blender 'Air'). 1 = Earth. Must be >= 0."
      },
      "dust_density": {
        "description": "Mie/aerosol scattering multiplier (Blender 'Dust'). Must be >= 0."
      },
      "ozone_density": {
        "description": "Ozone column multiplier; drives blue-hour saturation. Must be >= 0."
      },
      "ozone_absorption_scale": {
        "description": "Scales the ozone absorption coefficients on top of ozone_density. Must be >= 0."
      },
      "humidity": {
        "description": "0..1, dry to hazy. Rejected outside that range."
      },
      "temperature": {
        "description": "Air temperature in CELSIUS. Scales BOTH Rayleigh and Mie scale heights by (T+273.15)/288.15, so it moves the whole atmosphere profile, not just a tint."
      },
      "altitude": {
        "description": "Viewer height above sea level in METRES; the SkyView LUT is baked from this camera altitude."
      },
      "mie_anisotropy": {
        "description": "Henyey-Greenstein g for the Mie phase function; forward scattering as it approaches 1. Must be within (-1, 1)."
      },
      "planet_radius": {
        "description": "Ground sphere radius in METRES (Earth = 6360000). Must be >= 1000."
      },
      "atmosphere_height": {
        "description": "Atmosphere shell thickness above the ground in METRES (Earth = 60000). Must be >= 1000."
      },
      "rayleigh_scattering": {
        "description": "Per-channel Rayleigh scattering coefficients as [r, g, b], in 1/metre."
      },
      "mie_scattering": {
        "description": "Per-channel Mie scattering coefficients as [r, g, b], in 1/metre."
      },
      "rayleigh_density": {
        "description": "Rayleigh SCALE HEIGHT in metres (Earth = 8000). The name says density, the unit is a height. Must be >= 1."
      },
      "mie_density": {
        "description": "Mie SCALE HEIGHT in metres (Earth = 1200). Same naming trap as rayleigh_density. Must be >= 1."
      }
    }
  },
  "camera.set_auto_exposure": {
    "summary": "LEGACY flag - the exposure mode lives in post.configure_exposure; this does not change the image",
    "notes": "PRIORITY ORDER, and it is the most common reason these dials look dead: the exposure MODE now lives in the post chain, not on the camera. Read post.get_exposure 'mode' - only 'physical' reads ISO/shutter/f-stop; under 'manual' and 'auto_histogram' they are NOT read and the camera term is 1.0. Set it first with post.configure_exposure {\"settings\":{\"mode\":\"physical\"}}. The camera's own auto_exposure / use_physical_exposure flags are LEGACY: syncDisplay forces both in physical mode, so toggling them does not change the Vulkan or Rendered image. These setters still SUCCEED because they really do write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor for the multiplier ACTUALLY applied (it is g_display_post.camera_exposure, so it reads 1.0 whenever the mode is not physical)."
  },
  "camera.set_use_physical_exposure": {
    "summary": "LEGACY flag - use post.configure_exposure mode='physical' to actually read the ISO/shutter/f-stop chain",
    "notes": "PRIORITY ORDER, and it is the most common reason these dials look dead: the exposure MODE now lives in the post chain, not on the camera. Read post.get_exposure 'mode' - only 'physical' reads ISO/shutter/f-stop; under 'manual' and 'auto_histogram' they are NOT read and the camera term is 1.0. Set it first with post.configure_exposure {\"settings\":{\"mode\":\"physical\"}}. The camera's own auto_exposure / use_physical_exposure flags are LEGACY: syncDisplay forces both in physical mode, so toggling them does not change the Vulkan or Rendered image. These setters still SUCCEED because they really do write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor for the multiplier ACTUALLY applied (it is g_display_post.camera_exposure, so it reads 1.0 whenever the mode is not physical)."
  },
  "camera.set_iso_preset": {
    "summary": "Select the ISO preset by index; camera.get returns the resolved iso_value",
    "notes": "The index is not a measurement - read 'iso_value' from camera.get for the actual ISO. PRIORITY ORDER, and it is the most common reason these dials look dead: the exposure MODE now lives in the post chain, not on the camera. Read post.get_exposure 'mode' - only 'physical' reads ISO/shutter/f-stop; under 'manual' and 'auto_histogram' they are NOT read and the camera term is 1.0. Set it first with post.configure_exposure {\"settings\":{\"mode\":\"physical\"}}. The camera's own auto_exposure / use_physical_exposure flags are LEGACY: syncDisplay forces both in physical mode, so toggling them does not change the Vulkan or Rendered image. These setters still SUCCEED because they really do write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor for the multiplier ACTUALLY applied (it is g_display_post.camera_exposure, so it reads 1.0 whenever the mode is not physical)."
  },
  "camera.set_shutter_preset": {
    "summary": "Select the shutter-speed preset by index; camera.get returns shutter_seconds",
    "notes": "Index, not seconds. Read 'shutter_seconds' from camera.get for the resolved exposure time. This dial does NOT drive motion blur. PRIORITY ORDER, and it is the most common reason these dials look dead: the exposure MODE now lives in the post chain, not on the camera. Read post.get_exposure 'mode' - only 'physical' reads ISO/shutter/f-stop; under 'manual' and 'auto_histogram' they are NOT read and the camera term is 1.0. Set it first with post.configure_exposure {\"settings\":{\"mode\":\"physical\"}}. The camera's own auto_exposure / use_physical_exposure flags are LEGACY: syncDisplay forces both in physical mode, so toggling them does not change the Vulkan or Rendered image. These setters still SUCCEED because they really do write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor for the multiplier ACTUALLY applied (it is g_display_post.camera_exposure, so it reads 1.0 whenever the mode is not physical)."
  },
  "camera.set_fstop_preset": {
    "summary": "Select the f-stop preset by index; drives exposure, the physical aperture and Cinema lens imperfections",
    "notes": "The recorded debt is PAID (2026-09-06): this preset now writes the physical 'aperture' as well, so the panel f-stop dial, the viewport HUD triangle and this method all produce the same f-number - previously turning one left the others showing a different value. It still does NOT force blur on: the lens is sampled only while camera.set_depth_of_field is on, and camera.get 'effective_lens_radius' is the value every renderer actually uses. Index 0 is 'Custom' and leaves the aperture untouched. PRIORITY ORDER, and it is the most common reason these dials look dead: the exposure MODE now lives in the post chain, not on the camera. Read post.get_exposure 'mode' - only 'physical' reads ISO/shutter/f-stop; under 'manual' and 'auto_histogram' they are NOT read and the camera term is 1.0. Set it first with post.configure_exposure {\"settings\":{\"mode\":\"physical\"}}. The camera's own auto_exposure / use_physical_exposure flags are LEGACY: syncDisplay forces both in physical mode, so toggling them does not change the Vulkan or Rendered image. These setters still SUCCEED because they really do write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor for the multiplier ACTUALLY applied (it is g_display_post.camera_exposure, so it reads 1.0 whenever the mode is not physical)."
  },
  "camera.set_ev_compensation": {
    "summary": "Set exposure compensation in stops; this one applies in EVERY mode",
    "notes": "Unlike the preset dials this is always read, so it is the reliable way to move exposure without first arranging auto_exposure/use_physical_exposure. Rejected outside [-10, +10]."
  },
  "scene.export_gltf": {
    "summary": "Export the scene to glTF 2.0 (.glb or .gltf + sidecar .bin) and report measured cost",
    "notes": "Written directly from the in-memory flat SoA geometry - no Assimp round trip, no per-triangle facade. Blocks the main thread until the file is closed. The reply carries what was actually written plus a per-phase time breakdown and the writer's own peak heap (peak_writer_mb, NOT process RSS), so export cost is regressable from a script. .glb is capped at 4 GB by its 32-bit chunk length; use .gltf for anything larger.",
    "tags": "export|gltf|glb|save|interchange|asset",
    "related": "scene.import_model|project.save",
    "returns": "Object with path, meshes, primitives, triangles, vertices, nodes, instances, instanced_groups, materials, images, file_bytes, peak_writer_mb and seconds_total/collect/materials/plan/write",
    "undoable": false,
    "params": {
      "path": {
        "description": "Output file path; extension must be .glb or .gltf"
      },
      "geometry": {
        "description": "Write meshes"
      },
      "materials": {
        "description": "Write materials and textures"
      },
      "cameras": {
        "description": "Write the active camera as a glTF camera node"
      },
      "lights": {
        "description": "Write scene lights via KHR_lights_punctual"
      },
      "animations": {
        "description": "Write animation clips as glTF samplers/channels"
      },
      "skinning": {
        "description": "Write the skeleton, JOINTS_0/WEIGHTS_0 and inverse bind matrices"
      },
      "selected_only": {
        "description": "Export only the current multi-selection; fails if nothing is selected"
      },
      "bake_terrain_materials": {
        "description": "Flatten splat-blended terrain layers into export textures"
      },
      "terrain_bake_resolution": {
        "description": "Square resolution of each baked terrain texture"
      },
      "gpu_instancing": {
        "description": "Collapse scattered objects sharing one source mesh into EXT_mesh_gpu_instancing nodes"
      }
    }
  },
  "scene.export_estimate": {
    "summary": "Report what an export WOULD cost, without writing anything",
    "notes": "The same pre-export estimate the Export Settings panel shows, computed by the same function - so a script can catch the panel drifting away from the writer. It already did: the writer was fixed to read scatter from InstanceManager (on Vulkan scatter is NEVER expanded into world.objects), the panel's estimate was not, and it reported instances=0 for a scene the exporter then wrote a thousand instances of. Compare `instances` here against scene.export_gltf's measured `instances`; a gap means they have drifted again. Note `triangles` counts non-instanced geometry only - add `instance_triangles` for the total. `estimated_peak_mb` covers only geometry the writer must MATERIALISE (legacy Triangle facades); flat SoA meshes stream from where they live and cost no extra heap.",
    "tags": "export|gltf|estimate|cost|preflight|scatter|instances",
    "related": "scene.export_gltf|scatter.list_groups",
    "returns": "Object with objects, triangles, legacy_triangles, instances, unique_instance_sources, instance_triangles, materialised_instance_triangles, estimated_peak_mb",
    "undoable": false,
    "params": {
      "geometry": {
        "description": "Count meshes"
      },
      "materials": {
        "description": "Count materials and textures"
      },
      "cameras": {
        "description": "Count the active camera as a glTF camera node"
      },
      "lights": {
        "description": "Count scene lights"
      },
      "animations": {
        "description": "Count animation clips"
      },
      "skinning": {
        "description": "Count skinning data"
      },
      "selected_only": {
        "description": "Estimate the current multi-selection only"
      },
      "bake_terrain_materials": {
        "description": "Assume terrain layers get baked"
      },
      "terrain_bake_resolution": {
        "description": "Terrain bake resolution assumed by the estimate"
      },
      "gpu_instancing": {
        "description": "Assume EXT_mesh_gpu_instancing collapses scatter nodes"
      }
    }
  },
  "post.get_exposure": {
    "summary": "Read exposure settings, applied EV and HDR histogram",
    "notes": "Relative scene-linear luminance, not photometric EV100. GPU meters use a deterministic 128x128 sample grid and 256 weighted log2 bins. Raster preview shares the last resolved EV; it does not provide an HDR meter. meter_valid and meter_source report available data.",
    "returns": "object",
    "tags": "post|exposure|histogram",
    "related": "post.configure_exposure|post.reset_exposure"
  },
  "post.configure_exposure": {
    "summary": "Atomically configure manual, physical-camera or GPU-histogram exposure",
    "notes": "UI and Python call the same service. Mode is manual|physical|auto_histogram. EV in [-24,24]; min_ev <= max_ev. 0 <= low_percent < high_percent <= 100. key in [0.001,1], speed_up/down in (0,20] per second, center_weight in [0,1]. Non-finite values and unknown fields fail without mutation. locked=true captures current applied EV unless locked_ev is supplied. Final render freezes adaptation and rejects edits. No accumulation reset.",
    "params": {
      "settings": {
        "type": "object",
        "required": true,
        "description": "Exposure patch: mode, ev, min_ev, max_ev, low_percent, high_percent, key, speed_up, speed_down, center_weight, locked, locked_ev"
      }
    },
    "tags": "post|exposure|histogram",
    "verify_with": "post.get_exposure",
    "invalidates": "display",
    "returns": "object"
  },
  "post.reset_exposure": {
    "summary": "Reset exposure histogram and adaptation history",
    "notes": "Invalidates in-flight measurements by generation. Does not change settings or the explicit locked EV. Rejected while a final render is active.",
    "returns": "object",
    "tags": "post|exposure|reset",
    "verify_with": "post.get_exposure",
    "invalidates": "display"
  },
  "viewport.set_depth_of_field": {
    "summary": "Enable realtime raster depth of field and set its cost ceilings",
    "notes": "THE BLUR STRENGTH IS NOT HERE. The circle of confusion comes from the camera's own 'aperture' and 'focus_distance' with the same thin-lens formula the path tracer uses, so Rendered and Realtime cannot drift apart; 'max_coc_pixels' and 'max_taps' are COST ceilings. The default camera has aperture 0, so enabling this alone changes nothing - open a lens with camera.set_aperture first and set camera.set_focus_distance. Runs ONLY in Material shading mode (viewport.set_shading mode='material') and only on a perspective camera; Solid/Matcap content is display-referred and never enters the HDR pass. Values are REJECTED, not clamped. Read viewport.get_depth_of_field 'active' and 'inactive_reason' to see which of those gates is closed instead of guessing from an unchanged image.",
    "tags": "viewport|camera|post",
    "related": "viewport.get_depth_of_field|camera.set_aperture|camera.set_focus_distance|viewport.set_shading"
  },
  "viewport.get_depth_of_field": {
    "summary": "Report realtime depth-of-field settings plus whether it is ACTUALLY blurring",
    "notes": "'enabled' is the setting; 'active' is the measurement, and they are not the same question. 'active' is false whenever any gate is closed - the setting is off, the shading mode is not Material, the camera is orthographic, or the lens is shut (aperture 0) - and 'inactive_reason' names which one. A bare false would let a caller record 'measured no blur' when the truth is 'could not blur'.",
    "tags": "viewport|camera|post",
    "related": "viewport.set_depth_of_field|camera.get"
  },
  "camera.set_depth_of_field": {
    "summary": "Turn the camera lens on or off; off means a pinhole and nothing blurs",
    "notes": "THIS IS THE SWITCH, 'aperture' IS THE AMOUNT. Until 2026-09-06 the only off switch was aperture == 0, which is a sentinel rather than a value: no f-number produces a zero opening, so once the f-stop dial started writing the aperture there was no way back and depth of field could never be turned off again. Turning this off does NOT clear the aperture, so turning it back on restores exactly the previous blur. Enabling it on a camera whose aperture was never written seeds the aperture from the current f-number, otherwise the call would succeed and change nothing. Everything downstream (CPU renderer, OptiX, Vulkan RT, the realtime raster DoF pass, the viewport focus ring and the AF tolerance) reads camera.get 'effective_lens_radius', which is 0 while this is off.",
    "tags": "camera|lens|dof|blur|bokeh|pinhole|switch",
    "related": "camera.set_aperture|camera.set_focus_distance|camera.get|viewport.get_depth_of_field"
  },
  "viewport.set_af": {
    "summary": "Configure the viewfinder AF point grid, its area mode and the focus mode",
    "notes": "NOT a monitoring overlay: AF WRITES the camera. In AF-C (focus_mode 2) the selected point re-measures the scene every frame and overwrites camera.focus_distance, so a focus distance you set over IPC will be reverted while it is on - set focus_mode 0 (MF) first if you want to own the value. area_mode 0=Single 1=Zone9 2=Zone21 3=Wide 4=CenterWeighted; the grid is 3x3 for every mode except Zone21, which is 5x5, so selected_point is in [0,8] or [0,24] and an out-of-range index is REJECTED, not clamped. Nothing is drawn unless the Camera HUD is on; read viewport.get_af 'active' and 'inactive_reason' rather than inferring from an unchanged image.",
    "tags": "viewport|camera|focus|af|autofocus|viewfinder",
    "related": "viewport.get_af|camera.set_focus_distance|camera.set_depth_of_field|viewport.get_depth_of_field"
  },
  "viewport.get_af": {
    "summary": "Report the AF grid state, its point count, and why it is inactive if it is",
    "notes": "'active' is not the setting: the overlay also needs the Camera HUD on, a camera, a scene BVH (AF measures distance by casting rays), and no sequence render in progress - each closed gate is named in 'inactive_reason'. 'point_count' is derived from area_mode (25 for Zone21, otherwise 9) so selected_point can be validated before writing it.",
    "tags": "viewport|camera|focus|af|autofocus",
    "related": "viewport.set_af|camera.get"
  },
  "viewport.scene_load_guard": {
    "summary": "Report whether the scene-load Solid guard is active",
    "notes": "ON (the default) means opening a project or template drops Material/Rendered to Solid before touching the scene. Tearing a loaded scene down while a heavy viewport mode is bound loses the Vulkan device. OFF means openings run in whatever mode is bound - reproduce-the-fault mode, and every load logs a warning.",
    "tags": "viewport|shading|project|open|diagnostic",
    "related": "viewport.set_scene_load_guard|viewport.frame_telemetry|project.open"
  },
  "viewport.set_scene_load_guard": {
    "summary": "Turn the scene-load Solid guard on or off (off = deliberately reproduce the device-lost fault)",
    "notes": "Turn it OFF only to reproduce the project-open device-lost on purpose. With the guard ON the faulty path is never taken, so the stale-descriptor tripwire can never fire and its silence proves NOTHING - the switch and the thing being measured would otherwise be the same switch. Read the outcome as values from viewport.frame_telemetry: stale_descset_rebuilds > 0 names the root cause class, device_lost true means the driver was lost anyway. Deliberately not exposed in any panel: leaving the faulty path selectable from a menu would turn the rule into a preference.",
    "tags": "viewport|shading|project|open|diagnostic|device_lost",
    "related": "viewport.scene_load_guard|viewport.frame_telemetry|project.open",
    "params": {
      "enabled": {
        "description": "true restores the guard (openings drop to Solid). false reproduces the fault; leave it false only for the duration of a measurement."
      }
    }
  },
  "rayfusion.core_status": {
    "summary": "Report RayFusion probe control-plane availability and planned quality budgets",
    "notes": "Phase 0.1 only: renderer_available and gi_active are false until GPU tracing and raster composition are connected. planned_* values are budgets, not dispatched rays or measured performance. Reads the existing viewport quality preset; changes no settings.",
    "returns": "RayFusionCoreInfo",
    "access": "read",
    "tags": "rayfusion|gi|probe|development|status",
    "related": "rayfusion.validate_core|viewport.quality"
  },
  "rayfusion.validate_core": {
    "summary": "Run isolated native checks of the RayFusion probe cache and update scheduler",
    "notes": "Uses the real C++ core with local fixtures; checks world-cell reuse, ray budgets, stale scene/device/light/geometry completions, cross-field isolation, invalid packets and distance-moment visibility. Does not touch the scene or GPU. passed=true is not a rendered GI validation; gpu_tested remains false. Takes no parameters.",
    "returns": "RayFusionCoreValidation",
    "access": "read",
    "tags": "rayfusion|gi|probe|validation|qa",
    "related": "rayfusion.core_status"
  },
  "rayfusion.probe_field": {
    "summary": "RayFusion probe field as it actually reaches the GPU: grid, fill progress and which producer filled it.",
    "notes": "Step 1a. 'producer' is 'sky_bake' while the field is filled from the baked sky irradiance, which means the rendered image is meant to look IDENTICAL to the direct sky read - the counters, not the picture, are what show the pipe is live. 'uploaded' false with 'configured' true means the CPU field exists but no slot has reached the GPU, so shading still falls back to the global sky texture. 'valid' climbs toward 'total' over several frames because scheduling is budget-limited by 'budget_preset'. 'rejected' counts stale or duplicate tickets and should stay flat once the field is full; a climbing 'rejected' means results are arriving against a revision that has already been invalidated. Real indirect light needs probe rays, which need an acceleration structure in the raster viewport - until then this reports a working pipe, not GI. STEP 1b: 'producer' reports what actually ran - 'traced' (rays), 'sky_bake' (the step-1a bake), or 'none' - never the request; 'producer_traced_requested' is the request and 'producer_reason' explains any refusal. 'hit_fraction' is the share of probe rays that hit anything: if it stays 0.0 while geometry is clearly within range, the rays are not seeing the scene at all, and the result then looks EXACTLY like the sky-only producer - a correct-looking image that measures nothing. 'rejected_inside' counts probes born inside geometry; those publish with alpha 0 so the slot stops being re-traced while the shader falls back to the global read instead of going black. 'trace_ms' is the GPU dispatch plus readback for the last batch. Switch producers with rayfusion.set_probe_producer. STEP 1b-beta bounce keys: 'bounce_supported_materials' is the number of materials actually inside the single-diffuse-bounce subset, and it is the one to read first - 'bounce_unsupported_materials' alone cannot be checked against the scene, because a fully textured scene and a gate that rejects everything print the same number. When supported is 0 the bounce cannot change a single pixel and the image is identical to 1b-alpha, which is why 'bounce_reason' says so in words. 'bounce_rejected_textured', 'bounce_rejected_transparent', 'bounce_rejected_layered' and 'bounce_rejected_flagged' name the clause that rejected each material and overlap (a material can trip several), so they do not sum to the unsupported count. 'bounce_rejected_flag_bits' is the OR of the material flags that tripped the flag clause: its low bits are packed texture-channel selectors rather than material features, so a material that looks plain in the material panel can still be rejected here, and this field is what names the bit. STEP 1b-beta TEXTURE slice: 'bounce_shaded_hits' is the acceptance number and the only one measured on the GPU rather than counted off the CPU table -- how many probe rays actually landed on a material this slice could shade, during the last dispatch. 'bounce_hits' greater than zero with 'bounce_shaded_hits' at zero means every ray met an occluder and the published field is byte-identical to the bounce being off; the material counts cannot report that, because they describe what the slice COULD shade, not what it DID. 'bounce_alpha_tested' and 'bounce_alpha_occluded' measure the alpha cutout: shadow and primary rays no longer carry the opaque flag, so a masked leaf or curtain lets light through instead of shadowing like a solid sheet, and the candidate loop that costs is reported rather than assumed. Albedo, emission, opacity, metallic and specular textures are read at the bounce hit through the SAME bindless array and the same packed-channel policy the RT closest-hit uses, with the same UV transform; roughness, normal and height maps are deliberately not read (a diffuse lobe never reads roughness, and a probe texel is a hemisphere average, below the frequency of a normal map). transmission_tex, scalar opacity below 1 and transmission above 0 still fall outside the slice. INDIRECT SUN: 'bounce_sun_included' says whether the Physical Sky sun is carried as a directional bounce light. Until 2026-09-15 it was NOT, and the bounce could therefore only carry SKY light: an interior lit through a window went blue instead of taking the warm bounce off the sunlit floor. Measured against the RT reference on the same scene and settings, the ceiling ratio was 3.2/7.0/12.0 in R/G/B -- a blue-weighted excess, not a scalar gain, which is the signature of missing sun transport rather than a double-applied intensity. 'bounce_sun_tint_from_lut' says whether the sun tint came from the real transmittance LUT or from the shader's constant fallback; the fallback is visibly less warm at low sun elevations, so it is reported rather than applied silently. PER-FRAME CPU COST: 'bounce_prepare_ms' is how long the hit/material/light tables took to rebuild on the CPU. The emissive triangle scan behind it is now gated on an INPUT signature (geometry + instances + materials): measured 2026-09-15 it was 8.89 ms of a 9.21 ms total, every frame, in a scene with zero emissive triangles. 'bounce_emissive_cached' distinguishes 'the cache hit' from 'the scan never ran', which a bare 0.00 ms cannot. Read it beside rayfusion.scene_as 'signature_ms' (the AS change gate, also per frame): together they are what RayFusion spends before a single ray is cast. Measured 2026-09-08 in a foliage scene, the table build re-hashed each mesh's whole material-ID stream once per PLACEMENT, so the cost was multiplied by the instance count and one core sat at 100% while the GPU idled -- on a 16-thread machine that reads as '6% CPU', which is why the number has to be here rather than inferred from task-manager totals. WHY A HIT WAS NOT SHADED: 'bounce_shaded_hits' at zero is true but useless on its own, because five different exits produce the same zero. The named counters split it, and bounce_skipped_disabled + bounce_rejected_unresolved + bounce_rejected_unsupported_hit + bounce_rejected_degenerate + bounce_shaded_hits must equal bounce_hits -- if they do not, there is an unnamed exit left. 'bounce_backface_shaded' is NOT part of that sum: it is a SUBSET of the shaded hits, not an exit. RENAMED 2026-09-15 from 'bounce_skipped_backface' because the behaviour changed, not just the label: the front-face gate was REMOVED. Measured in a closed interior, 681 of 806 hits (84.5%) were back faces and every one of them returned black, so enabling the bounce cost CPU and changed almost nothing. The RT reference never rejected them (closesthit.rchit flips the normal on a back face) and rfBounceRadiance already flipped the normal toward the incoming ray, so the gate was discarding hits the shading function could handle correctly. A high value here now just means the scene is an interior, not that anything is wrong. 'bounce_rejected_unresolved' means the hit instance/index/material address could not be resolved at all -- a table problem, not a material one, and the CPU material counts cannot see it. 'bounce_rejected_unsupported_hit' is the per-RAY twin of the CPU-side material counts: it says the material the ray actually landed on is outside the slice, which is a different question from how many materials in the scene are.",
    "tags": "rayfusion|gi|probe|grid|status|development",
    "related": "rayfusion.core_status|rayfusion.validate_core|viewport.preview_lighting"
  },
  "rayfusion.scene_as": {
    "summary": "Acceleration structure residency in the raster viewport: BLAS/instance counts, bytes and build cost.",
    "notes": "Step 2. 'hardware_rt' false means this GPU or driver declined ray tracing, so every ray-based RayFusion step is blocked here and the UI disables them with this reason - 'cannot' and 'has not built yet' are different states and must not be shown the same way. 'as_bytes' is the memory the viewport keeps resident whether or not a ray is traced this frame; that residency, not the tracing, is the cost that has to be budgeted. REBUILD GATE: 'geometry_signature' (the mesh set) and 'instance_signature' (placements) are what trigger work, NOT 'built_geometry_generation' - that global counter is reported for reference only, because scene.delete removes an object from the drawn image without bumping it, and a generation-gated structure kept a DELETED object with no error. A change in the geometry signature rebuilds every BLAS; a change in only the instance signature refreshes the TLAS alone and increments 'tlas_only_refreshes'. 'signature_ms' is what the gate itself costs each frame. 'builds' rising while only the camera moves is a bug. 'instances_skipped' above zero means the instance cap was hit, so part of the raster image is NOT in the traced scene - probe rays read those directions as empty. 'meshes_skipped' counts meshes with no device address or too few vertices. Scatter impostor proxies are excluded on purpose: tracing a camera-facing billboard would put it into the world the rays see. A raster instance whose mesh is gone is skipped rather than traced against a stale BLAS. DELETE IS A HIDE, NOT AN ERASE: scene.delete sets the raster instance mask to 0 and leaves both the mesh and the instance resident so undo is instant, which is why the global geometry generation does not move. Those instances are counted in 'instances_hidden' and are excluded from the TLAS, so 'instance_count' FALLS on delete. 'blas_count' does NOT fall: BLAS residency mirrors raster residency (the hidden mesh still holds its vertex buffer), so it is a memory number, not a scene-content number. VRAM HANDOFF: while the viewport shows Rendered the dedicated viewport device releases this AS so the render backend (same GPU) gets the memory; 'yielded' is true then, 'yields' counts releases, and 'ready' false with that reason is the CORRECT state, not a failure. Returning to a raster mode lifts the gate and the AS rebuilds lazily. 'vram_usage_bytes'/'vram_budget_bytes' are the driver's device-local numbers for the WHOLE PROCESS (every VkDevice); 'vram_measured' false means VK_EXT_memory_budget is missing and the zeros are not a reading. Measure the handoff by reading this in Solid, switching to Rendered, waiting a frame, and reading again: usage must fall by about 'as_bytes'.",
    "tags": "rayfusion|gi|acceleration|blas|tlas|memory|status",
    "related": "rayfusion.probe_field|rayfusion.core_status"
  },
  "rayfusion.set_probe_bounce": {
    "summary": "Enable the bounded single diffuse bounce producer for RayFusion probes.",
    "notes": "Requires traced producer and ready hit/material tables. enabled must be a boolean; unknown keys are rejected. applied acknowledges the request, active reports the last published field. Read rayfusion.probe_field bounce_requested/ready/active/reason and unsupported material/light counts. Supports untextured opaque diffuse/emission, environment, point/directional lights only; unsupported hits remain opaque occluders. No volume, alpha, glass, area/spot or analytic sky-sun bounce. At most 192 rays per updated probe including secondary and shadow rays, within the existing quality ray ceiling. Developer switch is session-local. Disable for the 1b-alpha visibility comparison.",
    "related": "rayfusion.probe_field|rayfusion.set_probe_producer"
  },
  "rayfusion.set_probe_producer": {
    "summary": "Choose which producer fills the RayFusion probe field: traced rays (step 1b) or the sky bake (step 1a).",
    "notes": "This is an A/B MEASUREMENT lever on the live image, which is why it is Render and not Read. Step 1b swapped the probe PRODUCER while leaving the consumer (the ambient lookup in the fragment shader) untouched, so any visible difference is attributable to the producer alone - but that argument only holds if the old producer can still be selected on the same scene. Pass traced=false to get the step-1a sky bake back, traced=true for ray-traced visibility. The reply reports 'producer', which is what the field ACTUALLY runs afterwards, and it can differ from the request: with no hardware ray tracing, no built scene AS, or a missing rayfusion_probe_trace.spv, the request is refused and 'producer_reason' says why. Switching invalidates every probe on purpose - a half-swapped field would blend two producers and the comparison would measure the mixture. Read the result back with rayfusion.probe_field."
  },
  "rayfusion.set_probe_overlay": {
    "summary": "Show depth-tested probe markers inside the raster viewport",
    "notes": "Strict boolean enabled; session-local, default false. UI/Python/IPC use one service. Applied means accepted, not GPU success. probe_field reports overlay_requested, overlay_ready, overlay_markers and overlay_reason. Markers use scene depth and stay below UI panels; unavailable in Rendered mode. No per-frame marker work while disabled. Missing shader reports reason.",
    "related": "rayfusion.probe_field",
    "params": {
      "enabled": {
        "type": "boolean",
        "required": true,
        "description": "Enable depth-tested raster probe markers"
      }
    }
  },
  "rayfusion.set_probe_follow_camera": {
    "summary": "Scroll the fixed-budget probe window with camera translation",
    "notes": "Strict boolean enabled; session-local, default false. Rotation does not move the window. Existing world-cell values survive scrolling; only new cells are scheduled. Turning off freezes the current window. Does not perform scene fitting, density changes or relocation.",
    "related": "rayfusion.probe_field",
    "params": {
      "enabled": {
        "type": "boolean",
        "required": true,
        "description": "Follow camera position with a cell dead band"
      }
    }
  },
  "rayfusion.set_probe_grid": {
    "summary": "Move or reshape the RayFusion probe window, or hand placement back to auto-fit",
    "notes": "'auto_fit' (2026-09-15) is the DEFAULT and owns both shape and placement: the grid is sized to the traced scene bounds and re-fitted when they change. Send auto_fit=false, or any explicit 'counts'/'spacing'/'minimum'/'center', to take placement over -- an explicit grid alongside auto-fit would be accepted, live one frame and be silently rewritten, which reads as 'the request was forgotten' with no error anywhere. Read 'auto_fit_mode' back from rayfusion.probe_field: 'scene' means the whole traced scene fit the slot budget at usable density and follow-camera was turned off; 'camera_local' means it did not (measured 2026-09-15: hit_fraction 3.2%, mean_hit_distance 194 m -- the scene was far too large) so the entire budget is spent on a small volume around the camera instead. Partial edit: every key is optional and what you do not send keeps the value the field already has. The window was a BUILD CONSTANT until now (4x2x4 cells, spacing 3, nailed to the world origin), which is why every RayFusion image question so far has been confounded by coverage: in the measured room scene 6 of the 16 useful probes stood outdoors and hit_fraction read 0.105. A grid that needs a rebuild to change cannot be A/B'd against the thing it is blamed for. 'counts' is cells per axis (1..128 each) and their product may not exceed 'max_slots' from rayfusion.probe_field -- the texel buffer is allocated once for that ceiling so a grid change never reallocates a buffer an in-flight frame is reading. 'spacing' is world units per cell. Placement takes EITHER 'minimum' (the lowest cell index, integers, the same numbers probe_field reports) OR 'center' (world units, placed in the middle of the window and resolved against the FINAL spacing and counts) -- sending both is an error rather than a precedence rule. An explicit placement turns follow_camera OFF, because otherwise following would overwrite it on the next frame and the accepted request would appear to do nothing. Changing counts or spacing rebuilds the slot array and drops every measurement: the cells are different volumes of world, and carrying old packets across would keep a value measured for a different place. Moving only the placement scrolls, so world cells that stay inside the window keep their values and only newly exposed cells are rescheduled. Per-frame RAY cost does not scale with the grid -- it is bounded by the quality budget's probes-per-update -- but a larger window costs convergence time (more frames before valid reaches total) and a larger buffer copy per publish. The reply and rayfusion.probe_field report the APPLIED window, which is still the OLD one until the viewport produces a frame: toggle, move the camera, wait for traced_publishes to climb, THEN read. Scene fitting, density selection and relocating probes out of walls are not done here; this method is the lever those would eventually drive.",
    "related": "rayfusion.probe_field|rayfusion.set_probe_follow_camera|rayfusion.set_probe_producer",
    "verify_with": "rayfusion.probe_field",
    "params": {
      "counts": {
        "type": "array",
        "required": false,
        "description": "Cells per axis [x,y,z], each 1..128; the product may not exceed max_slots"
      },
      "spacing": {
        "type": "float",
        "required": false,
        "description": "World units per cell; probes sit at (cell + 0.5) * spacing"
      },
      "minimum": {
        "type": "array",
        "required": false,
        "description": "Lowest cell index [x,y,z] in cells (integers); mutually exclusive with center"
      },
      "center": {
        "type": "array",
        "required": false,
        "description": "World point [x,y,z] to place at the middle of the window; mutually exclusive with minimum"
      }
    }
  },
  "viewport.set_raster_gpu_instancing": {
    "summary": "Turn the raster viewport's GPU instancing, culling and LOD proxies on or off",
    "notes": "A/B lever, ON by default. OFF forces the per-mesh instance-buffer fallback -- which is what the REALTIME viewport ran unconditionally until 2026-09-08, because its buildRasterGeometry override carried a copy of the base class's tail and silently dropped the rebuildRasterInstanceLayout() call. One missing call cost two things at once, both measured on a 1000-instance foliage scene: (1) GPU culling and the scatter LOD proxies never engaged, so 45.9M triangles were submitted every frame while the proxies carried 30k of them against a 29.6M triangle target; (2) setRasterVisibleInstances fell to the per-mesh upload whose first statement is a full frame-ring drain, measured at 0.98 drains PER FRAME, so there was no CPU/GPU overlap at all. THE SCENE LOOKS CORRECT EITHER WAY -- this is a speed difference with no visual tell, which is exactly why it survived and why the switch has to exist: a fix that cannot be turned off destroys the measurement that would judge it. Read the outcome from viewport.frame_telemetry rather than from the image: 'gpu_culling' and 'global_instance_buffer' say whether it engaged, 'cull_mesh_count' is 0 whenever it did not, 'visible_triangles'/'proxy_triangles' say what was actually submitted, and 'resource_drains' delta over N frames says whether the pipeline is still draining every frame. This method reports the REQUEST; the telemetry reports what engaged, and they differ whenever the cull resources failed to build. Turning it off logs a warning, because a slow-but-correct viewport months later has no other trace.",
    "related": "viewport.frame_telemetry|viewport.set_quality|viewport.set_shading",
    "verify_with": "viewport.frame_telemetry",
    "params": {
      "enabled": {
        "type": "boolean",
        "required": true,
        "description": "True (default) allows the global instance buffer, GPU culling and LOD proxies; False forces the per-mesh fallback"
      }
    }
  },
  "anim.bind_clip": {
    "summary": "Bake an imported-rig clip onto a target with optional rest-basis retarget conversion and undo",
    "returns": "ClipBindingReport",
    "related": "anim.preview_clip_binding|anim.clips|anim.source_channels",
    "notes": "Default same_rig behavior is preserved. Preview runs the same clone/conversion used by bind; ready means mapping and numerical validation passed, not a live visual preview. rest_basis supports matching parent chains, rest-oriented rotation deltas, target rest translation plus scaled motion and relative uniform scale. No IK, contacts, role inference, reparenting or persisted mapping presets.",
    "verify_with": "anim.clips|anim.source_channels",
    "undoable": true,
    "params": {
      "source_character": {
        "description": "Imported source rig name"
      },
      "source_clip": {
        "description": "Canonical clip name owned by source_character"
      },
      "target_character": {
        "description": "Imported destination rig name"
      },
      "output_name": {
        "description": "Optional new unique canonical clip name"
      },
      "node_map": {
        "description": "Optional source unique node name -> target unique node name overrides. Full parent/helper chains must map consistently; unknown nodes and duplicate targets are rejected."
      },
      "mode": {
        "description": "same_rig copies absolute local TRS. rest_basis transfers rest-frame motion deltas for matching parent chains; positive uniform rest/animated scale required.",
        "enum": [
          "['same_rig', 'rest_basis']"
        ]
      },
      "translation_scale": {
        "description": "Positive world-space translation delta multiplier, at most 10000, only in rest_basis mode. Includes source/target parent rest unit conversion. Does not estimate limb proportions or solve contact."
      }
    }
  },
  "anim.force_state": {
    "summary": "Jump a character's state machine straight to a named state",
    "tags": "animation|statemachine",
    "notes": "Cancels any running transition. node_id 0 targets the first state machine. An unknown state name is reported, never silently ignored."
  },
  "anim.preview_clip_binding": {
    "summary": "Validate imported-rig clip mapping and optional rest-basis retarget conversion without scene mutation",
    "returns": "ClipBindingReport",
    "related": "anim.bind_clip|rig.list_characters|anim.source_clips|anim.source_channels",
    "notes": "Default same_rig behavior is preserved. Preview runs the same clone/conversion used by bind; ready means mapping and numerical validation passed, not a live visual preview. rest_basis supports matching parent chains, rest-oriented rotation deltas, target rest translation plus scaled motion and relative uniform scale. No IK, contacts, role inference, reparenting or persisted mapping presets.",
    "params": {
      "source_character": {
        "description": "Imported source rig name"
      },
      "source_clip": {
        "description": "Canonical clip name owned by source_character"
      },
      "target_character": {
        "description": "Imported destination rig name"
      },
      "node_map": {
        "description": "Optional source unique node name -> target unique node name overrides. Full parent/helper chains must map consistently; unknown nodes and duplicate targets are rejected."
      },
      "mode": {
        "description": "same_rig copies absolute local TRS. rest_basis transfers rest-frame motion deltas for matching parent chains; positive uniform rest/animated scale required.",
        "enum": [
          "['same_rig', 'rest_basis']"
        ]
      },
      "translation_scale": {
        "description": "Positive world-space translation delta multiplier, at most 10000, only in rest_basis mode. Includes source/target parent rest unit conversion. Does not estimate limb proportions or solve contact."
      }
    }
  },
  "anim.sample_clip_binding": {
    "summary": "Sample synchronized source and target model-space skeleton poses without changing scene playback",
    "returns": "ClipPosePreview {binding,time_seconds,duration_seconds,target_pose_source,source[],target[]}",
    "related": "anim.preview_clip_binding|anim.bind_clip|rig.select_bone",
    "notes": "Uses the same imported-rig mapping/retarget clone as bind. Returns all hierarchy nodes with unique name, parent name and row-major 16-float model-space world_transform. Seconds loop at clip duration using the existing sampler. Incompatible mapping preflight returns binding.ready=false with animated source and target bind fallback; invalid parameters/conversion return named errors. No controller, selection, history or scene mutation. No scene placement or contact IK. Optional source_pose_view and target_pose_view (animated/rest, default animated) independently choose canonical rest or sampled clip for each preview side. Does not change scene rig pose view, binding report or saved clips. Returns source_pose_source and target_pose_source. Invalid selectors return invalid_rig_pose_view.",
    "access": "Read",
    "params": {
      "source_character": {
        "description": "Imported source rig name"
      },
      "source_clip": {
        "description": "Canonical clip name owned by source_character"
      },
      "target_character": {
        "description": "Imported destination rig name"
      },
      "node_map": {
        "description": "Optional source unique node name -> target unique node name overrides. Full parent/helper chains must map consistently; unknown nodes and duplicate targets are rejected."
      },
      "mode": {
        "description": "same_rig copies absolute local TRS. rest_basis transfers rest-frame motion deltas for matching parent chains; positive uniform rest/animated scale required.",
        "enum": [
          "['same_rig', 'rest_basis']"
        ]
      },
      "translation_scale": {
        "description": "Positive world-space translation delta multiplier, at most 10000, only in rest_basis mode. Includes source/target parent rest unit conversion. Does not estimate limb proportions or solve contact."
      },
      "time_seconds": {
        "description": "Finite nonnegative time in seconds, looped by clip duration. Both skeletons sample the same time."
      },
      "source_pose_view": {
        "enum": [
          "rest",
          "animated"
        ]
      },
      "target_pose_view": {
        "enum": [
          "rest",
          "animated"
        ]
      }
    }
  },
  "anim.state_machines": {
    "summary": "Report the live state machines of a character's runtime animation graph",
    "tags": "animation|statemachine|inventory",
    "notes": "Reads the RUNTIME graph clone the renderer evaluates, not the editor asset. Each state carries pose_connected: false means that state resolves to an EMPTY pose, which freezes the character silently instead of erroring."
  },
  "rayfusion.reflections": {
    "summary": "Read per-pixel specular reflection settings and the last measured dispatch",
    "returns": "object",
    "tags": "raster|lighting|specular|reflection|same-frame",
    "related": "rayfusion.set_reflections|rayfusion.screen_gi|viewport.frame_timings",
    "notes": "This pass REPLACES the environment radiance lookup rather than adding a metal-only term: the fragment shader writes the split-sum weight (F0*brdf.x + brdf.y) into a G-buffer and the compute pass adds weight * (traced - env(R)). Two consequences that decide how to read these numbers. A ray that MISSES changes nothing at all -- the subtracted term is the same lookup, so the difference is exactly zero and there is no seam between traced and untraced regions by construction. And if the pass never runs, the image is identical to reflections being off, so 'ready' false is never a dark image. Because the weight is a Fresnel term, this is NOT metal-only: varnished wood, painted floor, ceramic and plastic inherit it through the same multiplier, and the gate is on the LOBE (roughness_gate plus weight_gate) -- a metallic>x gate would have excluded exactly those surfaces. 'shaded_hits' is the acceptance number and the only one measured on the GPU: 'rays' above zero with 'shaded_hits' at zero means every ray was rejected and the image is byte-identical to the pass being off, which 'gated_pixels' can never report because it only counts how many pixels WANTED a reflection. 'sky_misses' equal to 'rays' also means the image did not change, but for the opposite reason: there is nothing in the scene to reflect. Counters lag ONE FRAME -- they are zeroed in the frame command buffer, filled by the GPU and read by the CPU next frame, so a test that writes a setting and measures immediately sees the PREVIOUS batch. One bounce only: no mirror inside a mirror, and the hit surface contributes diffuse, emission and one direct light, not its own specular. Clearcoat is a second lobe and stays on the environment lookup."
  },
  "rayfusion.screen_gi": {
    "summary": "Read same-frame diffuse RTGI settings and last recorded dispatch",
    "returns": "object",
    "tags": "raster|lighting|diffuse|same-frame",
    "related": "rayfusion.set_screen_gi|viewport.frame_timings",
    "notes": "Reports MEASUREMENT as well as settings: mean_sky_visibility, mean_gi_luminance and full_confidence_fraction, sampled from 1/64 of pixels and ONE FRAME BEHIND (read without stalling the GPU); measured=false means no measurement yet, never 'zero'. full_confidence_fraction is the discriminator: when it is high the consumer skips the ambient fallback entirely, so the sky-visibility term does NOT apply to those pixels and excess brightness lies in the GI radiance instead. GI owns no history buffer, but sample directions rotate with the TAA accumulation index (temporal_seed_rotation), so noise resolves while TAA converges and is frozen when TAA is off. ready means recorded, not GPU-completed. primary_ray_budget excludes secondary visibility rays. Experimental depth-derived geometric normals; requires RT shadows."
  },
  "rayfusion.set_reflections": {
    "summary": "Replace per-pixel specular reflection settings",
    "returns": "object",
    "tags": "raster|lighting|specular|reflection",
    "related": "rayfusion.reflections|viewport.frame_timings",
    "notes": "Requires material viewport, scene lighting and a prefiltered environment -- the same gate the fragment shader uses to fill the weight, because the subtracted term is only valid on that path. Independent of RT shadows: the G-buffer comes from the main pass. Invalid settings reject atomically; unknown keys reject. samples is per PIXEL (1/2/4) and cost is linear in it. roughness_gate drops glossier pixels; weight_gate drops pixels whose split-sum weight is too small to see. Produce a viewport frame before reading rayfusion.reflections -- the counters describe the last dispatch, not this request.",
    "verify_with": "rayfusion.reflections"
  },
  "rayfusion.set_screen_gi": {
    "summary": "Replace same-frame diffuse RTGI settings",
    "returns": "object",
    "related": "rayfusion.screen_gi|viewport.frame_timings",
    "notes": "Runtime experiment. Requires material viewport, scene lighting and RT shadows. No GI-owned history buffer, but sample seeds rotate with the TAA accumulation index. Invalid settings reject atomically; unknown keys reject. Unsupported hit BSDFs use the established ambient fallback.",
    "verify_with": "rayfusion.screen_gi",
    "invalidates": "raster_frame",
    "params": {
      "enabled": {
        "description": "Enable same-frame diffuse RTGI"
      },
      "samples": {
        "enum": [
          "1",
          "2",
          "4"
        ]
      },
      "filter_radius": {
        "enum": [
          "0",
          "1",
          "2"
        ]
      },
      "max_distance": {
        "description": "Finite ray range in world units [0.1,10000]"
      }
    }
  },
  "rig.add_bone": {
    "summary": "Append a child joint to an owned unskinned rig without bound clips",
    "returns": "{ok:true}",
    "related": "rig.create|rig.set_rest_transform|rig.list_bones",
    "notes": "Parent is an existing full unique joint key. Preserves existing global bone indices; refreshes all derived rig representations, selects the new joint and increments persisted rig revision. Imported rigs, mesh-bound/weighted rigs and rigs with clips are rejected in this first pass. Service failures return named codes (Python ValueError / IPC code): rig_name_conflict, invalid_rig_name/invalid_bone_name, unknown_rig_template, unknown_character, unknown_parent_bone/unknown_bone, bone_name_conflict, rig_not_owned, rig_edit_requires_unskinned, rig_edit_requires_no_clips, invalid_rest_transform, rig_rest_requires_rigid_transform, scene_locked or history_not_bound. Shape/type errors use Python TypeError/ValueError and IPC invalid_parameter. Validation/staging happens before scene changes. Empty name requests automatic Joint1/Joint2 naming inside the staged core operation. Success selects the new child as the next parent; each addition is one undo command. Explicit duplicate names still return bone_name_conflict. Unskinned staging also scans actual canonical flat scene weight buffers for references to this rig hierarchy indices; references block with rig_edit_requires_unskinned even if weighted metadata is missing, including zero/invalid entries and extra rows. No weighted delete/remap/transfer is implemented.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned rig identity."
      },
      "name": {
        "description": "New authored joint name; full key becomes character_name."
      },
      "parent": {
        "description": "Existing full unique parent joint key."
      },
      "rest_transform": {
        "description": "Row-major 16-number local rest transform. Translation and proper rotation only; scale/shear/reflection rejected."
      }
    }
  },
  "rig.apply_pose_preview": {
    "summary": "Commit a pose preview and Auto Key IK controls or undriven FK bones",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional and only enables deformation preview. Imported rigs remain read-only. Mutations require active pose mode and no render job. Rest transforms and weights are never modified. Frame time uses scene FPS; create_pose_clip.fps controls clip ticks. Matrices are row-major rigid local transforms. Preview replaces the previous preview; frame changes and mode exit discard unkeyed overrides. Keys persist; clip selection and Auto Key are transient.",
    "undoable": true
  },
  "rig.bind_mesh": {
    "summary": "Bind actual flat mesh parts and publish initial weights as one undoable transaction",
    "returns": "{ok: bool}",
    "related": "rig.preview_bind|rig.get_binding|rig.weight_stats",
    "notes": "Initial bind only: authoring-owned meshless rig and static unskinned actual flat mesh parts. Existing authored clips are preserved and republished to the character controller/Ozz runtime; clips no longer prevent binding. Exact mesh name takes precedence over model:<importName> group. Preserves part names/topology/materials/UV; converts flat local P_orig/N_orig (fallback P/N) through inverse(actor placement)*source base into common rig bind space. All parts share actor Transform; inverse joint-global rest offsets and model inverse identity. Root anatomy role is nondeforming; segment weights belong to parent joint, nearest outgoing segment per joint, inverse squared distance regularized by extent*1e-4 (minimum 1e-9), strongest four merged/normalized via common contract, negligible entries pruned. No visibility/interior or alignment certification. Rejects existing skin, modifiers/geometry graphs/deltas, incomplete or invalid normals/geometry, reflected/singular transforms. Limits: 256 parts, 2M vertices, 100M vertex-segment evaluations, 4096 joints. Requires preview object from rig.preview_bind. Rebuilds weights and validates rig_revision/rig_token/mesh_token; never trusts submitted weights/can_bind. Stale rig/mesh tokens fail rig_bind_stale_preview before canonical/history mutation. Commit swaps cloned DNA geometry, transforms, owned context, global bone snapshot and affected source memberships. Refreshes CPU BVH and OptiX/Vulkan/raster influence geometry, invalidates animation groups; undo/redo restore state and schedule the same refresh. Explicit part registry persists to native project; missing/deleted part identity retained, not manufactured. No weighted rebind or second bind append. Errors additionally rig_bind_invalid_preview, rig_already_bound, api_not_bound, history_not_bound, scene_locked, rig_bind_failed. Python rt.rig.bind_mesh(character,mesh,preview).",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "preview": {
        "description": "Unmodified preview payload; generated weights are recomputed"
      }
    }
  },
  "rig.unbind_mesh": {
    "summary": "Detach all bound mesh parts while preserving the authored skeleton and clips",
    "returns": "{ok: bool}",
    "related": "rig.get_binding|rig.bind_mesh|rig.get_pose_state",
    "notes": "Owned rigs only. Restores existing parts to bind P_orig/N_orig, clears skin influences and explicit rig membership, but preserves flat topology/materials/UV, actor placement, skeleton hierarchy, anatomy, controls and AnimationData clips. Missing registered parts are removed from the binding registry without manufacturing geometry. One heavy undo step restores geometry, weights, membership and runtime clips. Errors: rig_not_bound, rig_not_owned, ambiguous_character, ambiguous_mesh_name, rig_revision_overflow, api_not_bound, history_not_bound, scene_locked, rig_unbind_failed. Python rt.rig.unbind_mesh(character).",
    "undoable": true,
    "access": "SceneWrite"
  },
  "rig.cancel_pose_preview": {
    "summary": "Discard a pose preview and restore the committed pose",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional and only enables deformation preview. Imported rigs remain read-only. Mutations require active pose mode and no render job. Rest transforms and weights are never modified. Preview state is transient and keys remain canonical AnimationData."
  },
  "rig.clear_selection": {
    "summary": "Clear the shared skeleton selection",
    "returns": "ok",
    "tags": "skeleton|joint|meshless|overlay",
    "related": "rig.list_characters|rig.list_bones|rig.select_bone|rig.get_selected_bone",
    "notes": "Works with skeleton-only and meshless animated imports. Selection/overlay are transient view state, not authoring edits; no undo. Names are import-prefixed. Joint world transforms are row-major 16 floats from evaluated hierarchy globals, never skin-offset matrices. pose_source is bind, graph, controller or ozz. Bind fallback is explicit per node. Independent skeleton root-motion placement and bind/pose editing are subsequent roadmap work. Clears complete multi-selection, active and anchor; pivot preference is retained. Successful selection releases the selected IK handle and discards its uncommitted preview; committed IK controls, timed channels and contacts remain active. Direct FK edits of IK-driven bones still require returning their control to FK."
  },
  "rig.commit_fit": {
    "summary": "Apply reviewed manual rest landmarks as one undoable rig edit",
    "returns": "{ok:true}",
    "related": "rig.preview_fit",
    "notes": "Requires same character/mesh, unchanged rig revision and exact current flat mesh content token. Recomputes validation; client can_commit cannot bypass it. Rejects outside-bounds/zero-length landmarks. Preserves model-space global joint rotations, converts world positions through inverse actor placement and derives canonical local rest transforms; rebuilds BoneData/skeleton/runtime through existing common stage/undo service. Native snapshot persists resulting hierarchy and anatomy, not transient drafts. No mesh mutation, binding, weights, automatic fit or verified surface interior. Errors: rig_fit_invalid_preview, rig_fit_stale_preview, rig_fit_landmarks_outside_bounds and preview/shared staged rig errors. Imported weighted rigs and clip-bound rigs remain gated. Targets also accept model:<importName> multipart groups from rig.list_fit_targets. Exact existing mesh names take precedence. Group processing preserves independent scene geometry/transforms and hashes combined viewport world positions/indices for stale preview detection. Group errors: unknown_mesh_group, ambiguous_mesh_group, mesh_group_has_no_flat_parts, mesh_group_invalid_geometry, mesh_group_too_large.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned unskinned clip-free rig name"
      },
      "mesh": {
        "description": "Exact flat mesh node name or model:<importName> group target"
      },
      "preview": {
        "description": "Preview payload from rig.preview_fit; landmarks are revalidated"
      }
    }
  },
  "rig.copy_from": {
    "summary": "Create a separate editable rest rig from a meshless unskinned source skeleton",
    "returns": "{character: string, bone_map: [{source_bone: string, target_bone: string}]}",
    "related": "rig.list_characters|rig.list_bones|rig.set_mode|rig.rename_bone|rig.reparent_bone|rig.delete_bone|anim.bind_clip",
    "notes": "Source canonical hierarchy is authoritative. Copies skeleton joints and required ancestors in parent-first order; source clips, runtime and geometry are untouched. New rig has no clips, mesh, weights or loader. Positive uniform scales are baked into joint positions and proper world rotations are preserved. Nonuniform scale, shear and reflection return rig_copy_unsupported_rest. Invalid/duplicate authored names are sanitized deterministically and reported in bone_map; forests get a synthetic SceneRoot. One undo command; existing native hierarchy serialization persists the owned copy. Exit Rig Edit before copying. Errors: rig_edit_active, invalid_rig_name, rig_name_conflict, unknown_source_character, rig_copy_requires_meshless_unskinned, rig_copy_requires_skeleton_hierarchy, rig_copy_incomplete_hierarchy, invalid_preview_hierarchy, invalid_preview_pose, rig_copy_unsupported_rest, scene_locked. Anatomy references are preserved/remapped. Reparent may return rig_anatomy_chain_disconnected; delete may return rig_bone_anatomy_referenced until metadata references are removed.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "source_character": {
        "description": "Existing source import/rig name; meshless and unskinned, may contain animation clips"
      },
      "character": {
        "description": "New distinct owned rig name, ASCII letters/digits/underscore/hyphen, 1-128 characters"
      }
    }
  },
  "rig.create": {
    "summary": "Create an owned meshless rig from a canonical template with undo and project persistence",
    "returns": "{ok:true}",
    "related": "rig.add_bone|rig.set_rest_transform|rig.list_bones|project.save|project.open",
    "notes": "Creates NodeHierarchy canonical rest state, BoneData, skeletonNodes and ozz bridge together. Stable joint keys use character_name. Character/name use 1-128 ASCII letters, digits, underscore or hyphen. Rejects existing/overlapping character prefixes and flat-mesh node prefixes. No skin weights or mesh binding. Service failures return named codes (Python ValueError / IPC code): rig_name_conflict, invalid_rig_name/invalid_bone_name, unknown_rig_template, unknown_character, unknown_parent_bone/unknown_bone, bone_name_conflict, rig_not_owned, rig_edit_requires_unskinned, rig_edit_requires_no_clips, invalid_rest_transform, rig_rest_requires_rigid_transform, scene_locked or history_not_bound. Shape/type errors use Python TypeError/ValueError and IPC invalid_parameter. Validation/staging happens before scene changes. Template IDs include humanoid (27 joints), humanoid_detailed v2 (70 joints with a nine-joint pelvis-to-head chain and paired four-joint digits), quadruped (26), insect6 (36) and avian (32), alongside root and chain3. The shared builder creates canonical rest hierarchy plus anatomy roles, symmetry pairs, contiguous chains and optional derived fit rules before existing four-representation finish/undo/persistence. Detailed fingers are geometry and envelope-ready deform segments; grouped finger FK, authored joint axes and twist joints remain separate capabilities. Existing templates and saved hierarchy snapshots remain compatible.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "New character identity; must be unique and not overlap another character prefix."
      },
      "template_id": {
        "enum": [
          "root",
          "chain3",
          "humanoid",
          "humanoid_detailed",
          "quadruped",
          "insect6",
          "avian"
        ]
      },
      "height": {
        "description": "Positive finite rest layout height <=10000; default 1.8. Catalogue gives family-specific UI suggestions."
      }
    }
  },
  "rig.create_mirrored_bone": {
    "summary": "Create one opposite non-root bone and its explicit symmetry pair",
    "returns": "{ok:true}",
    "related": "rig.mirror_rest|rig.get_anatomy|rig.get_next_bone_name",
    "notes": "One bone, not a subtree clone. Reflect source global rest and preserve handedness. Parent is its paired opposite parent when available, otherwise the same shared parent. Add symmetry metadata; anatomy roles/chains remain explicit, not guessed or copied. Existing indices preserved, one new slot, four representations/runtime/revision rebuilt and current selection becomes new key; one production undo/redo and native hierarchy/anatomy persistence. Shared owned/unweighted/meshless/clip-free gates apply. Errors include rig_mirror_invalid_side, rig_mirror_already_paired, rig_root_edit_blocked, invalid_bone_name, bone_name_conflict, rig_mirror_invalid_plane, rig_mirror_limit, rig_mirror_invalid_transform, unknown_bone, rig_edit_stale_revision and common edit/anatomy/scene lock errors.",
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned meshless, unweighted, clip-free rig name"
      },
      "rig_revision": {
        "description": "Expected revision from rig.list_bones; stale operations reject before publication"
      },
      "axis": {
        "description": "Mirror plane normal in canonical rig space: x, y or z"
      },
      "offset": {
        "description": "Finite plane coordinate in rig units; actor placement does not change this coordinate"
      },
      "bone": {
        "description": "One existing unpaired non-root source key"
      },
      "name": {
        "description": "New unprefixed valid bone name; canonical key is character_name"
      },
      "source_side": {
        "description": "left or right; defines pair orientation explicitly"
      }
    }
  },
  "rig.create_pose_clip": {
    "summary": "Create and select an editable native bone-channel clip seeded with rest keys",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional. Creates canonical AnimationData bone channels at rest and selects the clip. Mutations require active pose mode and no render job. FPS controls clip ticks per second; rest transforms and weights are unchanged.",
    "undoable": true,
    "params": {
      "fps": {
        "description": "Clip ticks per second, 1..240; does not change the scene FPS."
      }
    }
  },
  "rig.delete_bone": {
    "summary": "Delete one unweighted leaf bone and select its parent",
    "returns": "{ok: true}",
    "related": "rig.list_bones|rig.set_mode|rig.rename_bone|rig.reparent_bone|rig.delete_bone",
    "notes": "Owned, unskinned, clip-free rigs only. Uses one shared staged core and undo command; other rig selection/edit is blocked in scoped Rig Edit. Shared errors: unknown_character, unknown_bone, rig_not_owned, rig_edit_requires_unskinned, rig_edit_requires_no_clips, rig_edit_character_locked, rig_revision_overflow, scene_locked. Cross-rig references return rig_bone_external_reference. Root or non-leaf deletion is rejected (rig_root_edit_blocked, rig_delete_requires_leaf). Erases canonical node and BoneData entries; surviving scene bone indices stay stable. Native serialization and undo/redo preserve topology. No recursive deletion or weight transfer. Anatomy references are preserved/remapped. Reparent may return rig_anatomy_chain_disconnected; delete may return rig_bone_anatomy_referenced until metadata references are removed. Unskinned staging also scans actual canonical flat scene weight buffers for references to this rig hierarchy indices; references block with rig_edit_requires_unskinned even if weighted metadata is missing, including zero/invalid entries and extra rows. No weighted delete/remap/transfer is implemented.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned rig name"
      },
      "bone": {
        "description": "Existing prefixed bone key"
      }
    }
  },
  "rig.get_anatomy": {
    "summary": "Read canonical rig anatomy roles, symmetry pairs, chains and fit rules",
    "returns": "Versioned anatomy object; v7 may include fit_rules and control_rig definitions",
    "related": "rig.set_anatomy|rig.list_bones",
    "notes": "Returns version 1 anatomy schema. Old projects default to custom family and empty arrays. Read works for any known model; setting requires an owned unskinned clip-free rig. Family is a semantic tag, not auto-rig generation.",
    "access": "Read"
  },
  "rig.get_binding": {
    "summary": "Report canonical bound part identities and current presence for a rig",
    "returns": "RigMeshBindingInfo",
    "related": "rig.preview_bind|rig.bind_mesh|rig.weight_stats",
    "notes": "Read-only authoring binding registry, not imported skin inventory. Returns character, bound, algorithm (nearest_segment, anatomical_capsule_v1 or null), parts[{mesh,present}], rig_revision. anatomical_capsule_v1 also returns persisted envelope_settings. Uses exact persisted names; ownership takes precedence over original source hierarchy. Deleted/missing parts remain recorded with present=false. Errors unknown_character, api_not_bound, scene_locked, rig_bind_failed. Python rt.rig.get_binding(character).",
    "access": "Read"
  },
  "rig.get_fit_setup": {
    "summary": "Prepare read-only coarse alignment and 2D landmark editing data",
    "returns": "RigFitSetup",
    "related": "rig.preflight|rig.preview_fit",
    "notes": "Uses canonical flat mesh preflight and actual rest hierarchy plus actor placement. Produces height/center-based coarse world-space positions and at most 8192 mesh samples. Joint rows report fit_editable and fit_group. Anatomy v6 fit_rules mark deterministic derived joints; clients edit guides while preview_fit recomputes derived positions. Requires editable owned rig and finite volumetric unskinned mesh. +Y up / +Z forward and rest pose require user confirmation. Draft landmarks are request data, not persisted authoring state.",
    "access": "Read",
    "params": {
      "character": {
        "description": "Owned unskinned clip-free rig name"
      },
      "mesh": {
        "description": "Exact flat mesh node name or model:<importName> group target"
      }
    }
  },
  "rig.get_joint_profile": {
    "summary": "Read persistent rest-relative joint rules and rig revision",
    "returns": "JointProfileState",
    "tags": "anatomy|limits|hinge|swing twist|constraints",
    "related": "rig.get_joint_profile|rig.suggest_joint_profile|rig.set_joint_profile|rig.get_pose_state|rig.preview_pose_locals",
    "notes": "Profile schema version 1: joints array of bone/type/enabled/lock_translation/axis/minimum/maximum/swing. Defaults: free, disabled, lock_translation true, unit axis [1,0,0], twist range [-180,180], swing 180 degrees. Types free/hinge/ball/fixed. Axis is in the joint rest-relative local frame. Ranges must contain neutral zero; no stretch or anatomical certainty is implied. Enabled rules project previews and authored playback; Auto Key records the constrained pose. A fully blocked changed request applies as a successful no-op with no keys/history. Read get_pose_state.limit_hits for projected bones. Queries are read-only; suggestions remain disabled. Set requires owned rig and current nonnegative rig_revision, rejects active Pose preview/render job, preserves rest/weights/clip keys, increments rig revision and is undoable. Native anatomy uses version 2 when rules exist, version 1 otherwise. IK integration, soft limits, elliptical cones and muscle deformation remain later work.",
    "params": {
      "character": {
        "description": "Exact character name from rig.list_characters."
      }
    }
  },
  "rig.get_mirrored_landmarks": {
    "summary": "Return a mirrored copy of a world-space fitting landmark draft",
    "returns": "map<bone,array<float,3>>",
    "related": "rig.get_fit_setup|rig.preview_fit|rig.commit_fit|rig.mirror_rest",
    "notes": "Read-only; returns a new complete map, leaving scene/history/selection and input unchanged. Uses the same anatomy pairing and plane service as rest editing; world reflection is actor * rig reflection * inverse(actor), including translated/rotated/scaled actors. Reflects request source positions, not rest defaults. Bounds containment and zero-length validation remain in preview_fit/commit_fit, so drafts can be manually corrected before Apply. Alignment live mirror and directional buttons call this API; UI starts X offset at mesh bounds center converted to rig space. Named errors include rig_mirror_invalid_plane, rig_mirror_invalid_direction, rig_mirror_limit, rig_mirror_unpaired_bone, rig_mirror_ambiguous_pair, rig_mirror_no_pairs, rig_selection_empty, rig_selection_duplicate_bone, unknown_bone, rig_edit_stale_revision, rig_mirror_invalid_transform and shared anatomy/edit/scene lock errors. Also rig_fit_incomplete_landmarks, rig_fit_invalid_landmark and rig_mirror_failed.",
    "access": "Read",
    "params": {
      "character": {
        "description": "Owned meshless, unweighted, clip-free rig name"
      },
      "rig_revision": {
        "description": "Expected revision from rig.list_bones; stale operations reject before publication"
      },
      "axis": {
        "description": "Mirror plane normal in canonical rig space: x, y or z"
      },
      "offset": {
        "description": "Finite plane coordinate in rig units; actor placement does not change this coordinate"
      },
      "landmarks": {
        "description": "Complete finite world XYZ map for every canonical hierarchy node"
      }
    }
  },
  "rig.get_mode": {
    "summary": "Read transient viewport rig interaction mode and locked character",
    "returns": "{mode: scene|edit, character: string}",
    "related": "rig.set_mode",
    "notes": " Pose mode is supported for owned bound rigs. Scene exits Pose and clears transient overrides; Edit requires exiting Pose first.",
    "access": "Read"
  },
  "rig.get_next_bone_name": {
    "summary": "Suggest a free authored bone name for an editable owned rig",
    "returns": "string",
    "related": "rig.add_bone",
    "notes": "Returns seed unchanged when free; otherwise increments its trailing integer (or appends 1). Checks canonical hierarchy and global BoneData keys. Read-only suggestion does not reserve the name. Errors include invalid_bone_name, bone_name_exhausted and shared rig eligibility errors.",
    "access": "Read",
    "params": {
      "seed": {
        "description": "ASCII authored name seed; defaults to Joint1"
      }
    }
  },
  "rig.get_next_name": {
    "summary": "Suggest a collision-free scene rig name from an ASCII seed",
    "returns": "string",
    "related": "rig.create",
    "notes": "Uses the same scene/prefix/global bone/flat mesh name collision checks as rig.create. Returns seed if free, otherwise seed1, seed2, etc. Does not reserve names. Creation of multiple rigs is supported; creation/naming require Scene mode. Errors: invalid_rig_name, rig_name_exhausted, rig_edit_active, scene_locked.",
    "access": "Read"
  },
  "rig.get_overlay_visible": {
    "summary": "Read whether the skeleton viewport overlay is enabled",
    "returns": "bool",
    "tags": "skeleton|joint|meshless",
    "related": "rig.list_characters|rig.list_bones|rig.select_bone|rig.get_selected_bone",
    "notes": "Works with skeleton-only and meshless animated imports. Selection/overlay are transient view state, not authoring edits; no undo. Names are import-prefixed. Joint world transforms are row-major 16 floats from evaluated hierarchy globals, never skin-offset matrices. pose_source is bind, graph, controller or ozz. Bind fallback is explicit per node. Independent skeleton root-motion placement and bind/pose editing are subsequent roadmap work."
  },
  "rig.get_pose_coverage": {
    "summary": "Read flat bound-mesh weight coverage without changing weights",
    "returns": "PoseCoverage",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton. Meshless rigs return skeleton pose capability with no deformation coverage; bound rigs additionally report flat mesh influence coverage. Read-only."
  },
  "rig.get_pose_state": {
    "summary": "Read the evaluated local pose, editable clips and rig revision",
    "returns": "PoseState",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional. Reports the evaluated local pose, editable clips, rig revision, constraint hits and keyed_bones_at_frame for the selected clip/current frame. Matrices are row-major rigid local transforms. Rest transforms and skin weights are unchanged."
  },
  "rig.get_pose_view": {
    "summary": "Read requested and effective evaluation view for one rig",
    "returns": "{mode: rest|animated, effective_mode: rest|animated}",
    "related": "rig.set_pose_view",
    "notes": "effective_mode is rest while scoped Rig Edit owns this character; requested mode is preserved for leaving Edit. Transient default is animated. On import/native reopen, valid skinned characters default to Rest; meshless clips retain prior animated defaults. Explicit per-character views are preserved during append/reinitialization. Rest graphs are excluded from autonomous viewport wake and file-animation reset scheduling. Select animated explicitly to resume existing animation.",
    "access": "Read"
  },
  "rig.get_scene_transform": {
    "summary": "Read the independent placement matrix of an owned meshless rig",
    "returns": "number[16] (row-major)",
    "related": "rig.set_scene_transform|rig.list_bones",
    "notes": "Returns actor placement separately from canonical local rest, sampled joint globals and clips. Available in Scene and Rig Edit. Imported/weighted rigs are not supported by this owned placement service. Errors: unknown_character, rig_placement_requires_meshless_unskinned, scene_locked. Actor placement may include positive uniform scale.",
    "access": "Read",
    "params": {
      "character": {
        "description": "Owned meshless rig name"
      }
    }
  },
  "rig.get_selected_bone": {
    "summary": "Read the currently selected skeleton node or null when selection is absent or stale",
    "returns": "BoneView|null",
    "tags": "skeleton|joint|meshless|overlay",
    "related": "rig.list_characters|rig.list_bones|rig.select_bone|rig.get_selected_bone",
    "notes": "Works with skeleton-only and meshless animated imports. Selection/overlay are transient view state, not authoring edits; no undo. Names are import-prefixed. Joint world transforms are row-major 16 floats from evaluated hierarchy globals, never skin-offset matrices. pose_source is bind, graph, controller or ozz. Bind fallback is explicit per node. Independent skeleton root-motion placement and bind/pose editing are subsequent roadmap work. Returns active joint of multi-selection; rig.get_selection returns full selection set."
  },
  "rig.get_selection": {
    "summary": "Read the canonical selection set, active joint and pivot preference",
    "returns": "{character,bones,active,anchor,pivot}",
    "related": "rig.select_bones|rig.get_selected_bone|rig.set_selection_pivot",
    "notes": "Read-only shared scene selection. bones are unique keys in canonical skeleton order. Empty/absent/stale active yields empty character/active/bones. Missing members are excluded; legacy single active is fallback when older authoring operations changed identity. pivot active or center; choice is transient and remains when selection clears. Active joint drives inspector/gizmo orientation and selected-bone weight map; other selected joints retain highlight. Native save does not persist selection, reopening clears it via existing clearSelection. Errors api_not_bound, scene_locked, rig_selection_failed. Python rt.rig.get_selection(). anchor is the normalized valid range anchor or active fallback; restore via select_bones anchor parameter.",
    "access": "Read"
  },
  "rig.get_template": {
    "summary": "Read a scaled template hierarchy and anatomy without creating scene state",
    "returns": "{template_id,template_version,height,nodeHierarchy,anatomy}",
    "related": "rig.list_templates|rig.create|rig.get_anatomy",
    "notes": "Calls the same template builder as rig.create; returned keys use Template_ prefix for inspection. Hierarchy/anatomy serialization contains the canonical hierarchy and anatomy; actual creation uses the requested character prefix. All joints have identity rest rotation, unit scale and template positions. Height is ground-origin to highest joint endpoint, in scene units; insect height includes antennae. No scene/history/selection/runtime mutations. Errors: unknown_rig_template, invalid_rig_height, invalid_rig_template_definition and anatomy validation errors. template_version is recipe provenance: humanoid is v3, humanoid_detailed and quadruped/insect6 are v2; avian/root/chain3 are v1. The 70-joint detailed humanoid adds three spine joints, two neck joints and paired digits. Anatomy v6 fit_rules identify deterministic joints derived between editable guides. Read and create use identical recipes.",
    "access": "Read",
    "params": {
      "template_id": {
        "enum": [
          "root",
          "chain3",
          "humanoid",
          "humanoid_detailed",
          "quadruped",
          "insect6",
          "avian"
        ]
      },
      "height": {
        "description": "Positive finite layout height, <=10000; default 1.8"
      }
    }
  },
  "rig.get_weight_map": {
    "summary": "Read a selected bone scalar weight field in canonical flat vertex order",
    "returns": "BoneWeightMap",
    "related": "rig.get_weights|rig.weight_stats|rig.set_weight_map_visible",
    "notes": "Exact individual mesh, character and unique bone key required. Shared RigWeights::boneWeightField reads actual DNA skin_weights; verifies unique canonical ownership (authored binding registry takes precedence), hierarchy membership and unambiguous forward bone index. Missing/empty rows contribute zero; matching duplicate entries sum in double; nonfinite/negative matching values are excluded and counted in invalid_entries. Display values clamp to [0,1], no stored weight normalization/repair or sculpt-mask mutation. Reports mesh, character, bone, bone_index, vertex_count, values, invalid_entries, display_clamped=true. Max 2M vertices. Unknown bone/owner/index/foreign errors: unknown_bone, rig_weight_map_unverified_owner, rig_weight_map_foreign_character, rig_weight_map_invalid_index; limit rig_weight_map_limit; exact mesh errors propagate; api_not_bound, scene_locked, rig_weight_map_failed. Numeric/index diagnostics remain rig.weight_stats, this display map is not a contract/deformation certificate. Python rt.rig.get_weight_map(mesh,character,bone).",
    "access": "Read"
  },
  "rig.get_weight_map_visible": {
    "summary": "Read the transient selected-bone weight tint toggle",
    "returns": "bool",
    "related": "rig.set_weight_map_visible",
    "notes": "Same inspector/script/IPC ViewState flag. Defaults false, remains independent of bone overlay visibility and sculpt mask visibility. No mutation/history. Error api_not_bound. Python rt.rig.get_weight_map_visible().",
    "access": "Read"
  },
  "rig.get_weights": {
    "summary": "Read one canonical flat vertex influence list with bone identities",
    "returns": "VertexSkinWeights",
    "related": "rig.weight_stats",
    "notes": "Read-only stored TriangleMesh/DNA skin_weights row, exact individual mesh nodeName only. object parameter names the mesh, vertex is a zero-based nonnegative integer in canonical flat vertex order (not a face/corner selection). Preserves influence order/values, never normalizes. influences records bone_index, unique bone name (null if unknown/ambiguous), weight (null for nonfinite stored values), value_valid, index_known, index_ambiguous, belongs_to_character (null when ownership or index cannot be established). weight_sum includes finite positive values with nonnegative IDs, matching weight_stats finite-sum semantics; values_valid does not certify normalization, duplicate/limit rules or index ownership. Empty/missing row yields empty influences and sum 0; weight_row_present distinguishes them. Reports resolved/unresolved/ambiguous ownership and character, using same core as weight_stats. Nonfinite values are null for safe JSON parity; no scene/rest/pose/selection/history mutation. IPC requires object string and nonnegative integer vertex; bool/float/negative shape errors invalid_parameter. Python uses typed object string/uint64 vertex and may raise binding conversion errors for invalid types or negative/outside uint64 inputs. Shared service errors: invalid_mesh_name, unknown_mesh, ambiguous_mesh_name, mesh_has_no_geometry, rig_vertex_out_of_range, rig_get_weights_failed, api_not_bound, scene_locked. Python rt.rig.get_weights(object, vertex). Initial authored binding registry takes precedence over original source hierarchy/membership; preserves correct owner after multipart binding and native reopen.",
    "access": "Read",
    "params": {
      "object": {
        "description": "Exact individual flat mesh nodeName"
      },
      "vertex": {
        "description": "Zero-based canonical flat vertex index; nonnegative"
      }
    }
  },
  "rig.insert_pose_keys": {
    "summary": "Insert or replace position and quaternion keys for explicit bones at the current frame",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional. Inserts canonical position/quaternion keys at the current frame without changing rest or weights. When the selected clip has timed IK, driven bone insertion fails rig_ik_use_control_keys_or_bake; use insert_ik_key or bake_ik_channels.",
    "undoable": true
  },
  "rig.remove_pose_keys": {
    "summary": "Remove position and quaternion keys for explicit bones at the current frame",
    "returns": "ok",
    "tags": "FK|bone keys|timeline|remove",
    "related": "rig.insert_pose_keys|rig.get_pose_state|rig.remove_ik_key",
    "notes": "Requires active owned bound Pose, no preview/render job and a selected editable clip. Removes exact current-frame position and rotation keys atomically for every requested bone; other frames and clip duration remain unchanged. Duplicate/unknown bones reject without mutation. Missing keys fail rig_edit_no_change. One undo step and native persistence.",
    "undoable": true
  },
  "rig.edit_pose_key": {
    "summary": "Move or replace one canonical authored bone key",
    "returns": "ok",
    "tags": "pose|key|curve|graph|timeline",
    "related": "rig.insert_pose_keys|rig.remove_pose_keys|rig.get_pose_state",
    "notes": "Edits AnimationData through one undoable pose command. Position keys are atomic XYZ vectors and rotation keys are normalized quaternions; Timeline tracks are disposable projections. Null value preserves the key value and changes only time. Destination collisions reject without mutation. A bone driven by timed IK at the source or destination rejects with rig_ik_use_control_keys_or_bake. Requires active Pose, selected editable clip and no pending preview/render job.",
    "undoable": true,
    "params": {
      "character": {
        "description": "Active Pose character",
        "required": true,
        "type": "string"
      },
      "bone": {
        "description": "Existing prefixed bone key",
        "required": true,
        "type": "string"
      },
      "channel": {
        "description": "position or rotation",
        "enum": "position|rotation",
        "required": true,
        "type": "string"
      },
      "source_frame": {
        "description": "Exact source frame in 0..1000000",
        "required": true,
        "type": "int"
      },
      "target_frame": {
        "description": "Destination frame in 0..1000000",
        "required": true,
        "type": "int"
      },
      "value": {
        "description": "Optional [x,y,z] or [w,x,y,z]; null preserves value",
        "required": false,
        "type": "array"
      }
    }
  },
  "rig.list_bones": {
    "summary": "Inspect skeleton joints and helper nodes with their current world transforms",
    "returns": "BoneView[]",
    "tags": "skeleton|joint|meshless|overlay",
    "related": "rig.list_characters|rig.list_bones|rig.select_bone|rig.get_selected_bone",
    "notes": "Works with skeleton-only and meshless animated imports. Selection/overlay are transient view state, not authoring edits; no undo. Names are import-prefixed. Joint world transforms are row-major 16 floats from evaluated hierarchy globals, never skin-offset matrices. pose_source is bind, graph, controller or ozz. Bind fallback is explicit per node. Independent skeleton root-motion placement and bind/pose editing are subsequent roadmap work. Rows now include local_rest_transform, authoring_owned, rig_revision, template_id and separate in_bonedata/in_skeleton_nodes/in_node_hierarchy/in_ozz_skeleton flags. scene_transform reports actor placement; owned meshless world_transform includes it. Scene selection of any joint can place its entire owned rig. template_version is saved recipe provenance; legacy missing versions are 1.",
    "params": {
      "character": {
        "description": "Import name from rig.list_characters"
      }
    }
  },
  "rig.list_characters": {
    "summary": "List imports containing a skeleton, including rigs without mesh or clips",
    "returns": "string[]",
    "tags": "skeleton|joint|meshless|overlay",
    "related": "rig.list_characters|rig.list_bones|rig.select_bone|rig.get_selected_bone",
    "notes": "Works with skeleton-only and meshless animated imports. Selection/overlay are transient view state, not authoring edits; no undo. Names are import-prefixed. Joint world transforms are row-major 16 floats from evaluated hierarchy globals, never skin-offset matrices. pose_source is bind, graph, controller or ozz. Bind fallback is explicit per node. Independent skeleton root-motion placement and bind/pose editing are subsequent roadmap work."
  },
  "rig.list_fit_targets": {
    "summary": "List individual flat mesh and multipart imported-character targets for manual fitting",
    "returns": "[{target,label,kind,part_count}]",
    "related": "rig.preflight|rig.get_fit_setup",
    "notes": "Individual targets are exact flat node names. Multipart groups use model:<importName> and include canonical flat scene meshes associated via model flat members or exact NodeHierarchy keys. Groups preserve separate scene parts; all are sampled into a temporary world-space diagnostic flat snapshot, never inserted into scene or bound. No subset filtering yet; choose an individual body mesh when accessories affect bounds. Existing skin on any group part blocks owned unskinned fitting.",
    "access": "Read"
  },
  "rig.list_templates": {
    "summary": "List shared rig template catalogue with anatomy family, version and joint counts",
    "returns": "[{template_id,template_version,label,family,joint_count,default_height,up_axis,forward_axis,left_axis}]",
    "related": "rig.get_template|rig.create",
    "notes": "Read-only, scene-independent catalogue: root, chain3, humanoid, humanoid_detailed, quadruped, insect6 and avian. Layout is +Y up, +Z forward, +X character-left. default_height is a UI suggestion; rig.create/get_template retain height=1.8 historical default unless supplied. Templates are rest skeleton layouts with neutral rotations; fitting, binding and control creation remain explicit later operations. Humanoid basic v3 has 27 joints; humanoid_detailed v2 has 70 joints with denser spine/neck and paired digits. Its anatomy fit_rules derive intermediate spine, neck and phalanx positions from editable guides. Existing saved rigs retain their concrete hierarchy.",
    "access": "Read"
  },
  "rig.mirror_rest": {
    "summary": "Copy mirrored paired rest transforms in one canonical rig edit",
    "returns": "{ok:true}",
    "related": "rig.get_anatomy|rig.get_mirrored_landmarks|rig.create_mirrored_bone",
    "notes": "Uses anatomy symmetry metadata, never bone-name guessing. Axis/offset are rig-space coordinates. Reflect immutable source joint globals and reflect a basis axis to preserve proper handedness, then solve target locals against updated parents; selected parent/child targets receive reflection once. Untargeted descendants inherit their parent changes. Targets become current selection. Stable existing indices, all derived representations/revision/runtime refresh and one production undo/redo; no new geometry/weights. Rejects weighted/bound/imported/non-owned rigs, clips, stale revision and no-op. Selected both sides of a pair is ambiguous; directional mode filters requested side, [] copies all pairs. Named errors include rig_mirror_invalid_plane, rig_mirror_invalid_direction, rig_mirror_limit, rig_mirror_unpaired_bone, rig_mirror_ambiguous_pair, rig_mirror_no_pairs, rig_selection_empty, rig_selection_duplicate_bone, unknown_bone, rig_edit_stale_revision, rig_mirror_invalid_transform and shared anatomy/edit/scene lock errors.",
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned meshless, unweighted, clip-free rig name"
      },
      "rig_revision": {
        "description": "Expected revision from rig.list_bones; stale operations reject before publication"
      },
      "axis": {
        "description": "Mirror plane normal in canonical rig space: x, y or z"
      },
      "offset": {
        "description": "Finite plane coordinate in rig units; actor placement does not change this coordinate"
      }
    }
  },
  "rig.preflight": {
    "summary": "Inspect canonical flat mesh geometry before anatomical landmark setup",
    "returns": "MeshPreflightReport",
    "related": "rig.create|rig.get_template",
    "notes": "Read-only snapshot over TriangleMesh/DNA viewport positions with object final transform: unskinned local P_orig (fallback P), skinned current P. Static P can be a baked cache and is not transformed again when P_orig exists. No Triangle facades. Requires unique exact mesh nodeName. Reports counts, transformed bounds, nonfinite vertices, invalid/out-of-range/numerically invalid triangles, exact zero-area faces, trailing indices and existing skin. can_start_landmarks only checks finite volumetric unskinned geometry; fit_ready is always false. Axes, rest pose, symmetry, interior and watertightness are not evaluated. No fit, bind, weights, scene/history/selection mutation. Re-run after mesh changes. Errors: invalid_mesh_name, unknown_mesh, ambiguous_mesh_name, mesh_has_no_geometry, mesh_has_no_positions, mesh_position_buffer_incomplete, invalid_mesh_transform, rig_preflight_failed, scene_locked. Targets also accept model:<importName> multipart groups from rig.list_fit_targets. Exact existing mesh names take precedence. Group processing preserves independent scene geometry/transforms and hashes combined viewport world positions/indices for stale preview detection. Group errors: unknown_mesh_group, ambiguous_mesh_group, mesh_group_has_no_flat_parts, mesh_group_invalid_geometry, mesh_group_too_large. Reports blockers and group part_count/parts. Isolated zero-area faces are warnings for manual setup when valid faces remain; nonfinite/out-of-range/trailing indices and no valid faces still block. This does not relax future bind/weight validation.",
    "access": "Read",
    "params": {
      "mesh": {
        "description": "Exact flat mesh node name or model:<importName> group target"
      }
    }
  },
  "rig.preview_bind": {
    "summary": "Preview initial nearest-segment weights for an aligned owned rig and actual flat mesh parts",
    "returns": "RigBindPreview",
    "related": "rig.bind_mesh|rig.get_binding|rig.preview_fit",
    "notes": "Initial bind only: authoring-owned meshless, clip-free rig and static unskinned actual flat mesh parts. Exact mesh name takes precedence over model:<importName> group. Preserves part names/topology/materials/UV; converts flat local P_orig/N_orig (fallback P/N) through inverse(actor placement)*source base into common rig bind space. All parts share actor Transform; inverse joint-global rest offsets and model inverse identity. Root anatomy role is nondeforming; segment weights belong to parent joint, nearest outgoing segment per joint, inverse squared distance regularized by extent*1e-4 (minimum 1e-9), strongest four merged/normalized via common contract, negligible entries pruned. No visibility/interior or alignment certification. Rejects animation, skin, modifiers/geometry graphs/deltas, incomplete or invalid normals/geometry, reflected/singular transforms. Limits: 256 parts, 2M vertices, 100M vertex-segment evaluations, 4096 joints. Existing canEditRig guards apply. Read-only; no scene/history/backend mutation. axes_confirmed defaults false and must be true. Aggregate counts/sums, outside bounds warning and up to 16 sampled rows per part; can_bind true is not a quality certificate. Tokens cover canonical rig/rest/placement/indices/root roles and selected mesh local P_orig/N_orig (fallback P/N)/indices/base. Python rt.rig.preview_bind(character,mesh,axes_confirmed=False). Errors include rig_bind_axes_unconfirmed, rig_bind_limit, rig_bind_requires_segments, rig_bind_requires_unskinned_mesh, rig_bind_requires_static_mesh, rig_bind_source_has_animation, rig_bind_requires_normals, rig_bind_invalid_normals, rig_bind_invalid_geometry, rig_bind_invalid_transform, rig_bind_invalid_rest, rig_bind_invalid_indices, rig_bind_zero_length_segment, mesh_already_bound, rig_bind_mesh_pending_delete, rig_bind_invalid_mesh_name, rig_bind_empty_weights, rig_bind_failed; shared name/rig/preflight errors also propagate.",
    "access": "Read",
    "params": {
      "character": {
        "description": "Exact owned rig character"
      },
      "mesh": {
        "description": "Exact flat part or model: group target"
      },
      "axes_confirmed": {
        "description": "Caller has inspected axes and rest alignment"
      }
    }
  },
  "rig.preview_fit": {
    "summary": "Validate world-space fit guides, resolve derived joints and return a commit preview",
    "returns": "RigFitPreview",
    "related": "rig.get_fit_setup|rig.commit_fit",
    "notes": "Read-only. Legacy anatomy requires complete finite manual landmarks. Anatomy v6 requires every non-derived guide and deterministically replaces fit-rule targets by linear interpolation; supplied derived values cannot affect preview or commit. Reports resolved landmarks, segment lengths and bounds containment, not surface interior or automatic fit residual. can_commit is false for any outside-bounds point. Errors include rig_fit_axes_unconfirmed, rig_fit_incomplete_landmarks, rig_fit_invalid_landmark, rig_fit_zero_length_bone and rig_anatomy_fit_rule_dependency.",
    "access": "Read",
    "params": {
      "character": {
        "description": "Owned unskinned clip-free rig name"
      },
      "mesh": {
        "description": "Exact flat mesh node name or model:<importName> group target"
      },
      "landmarks": {
        "description": "Every canonical node key maps to three finite world-space coordinates"
      },
      "axes_confirmed": {
        "description": "Caller confirms +Y up, +Z forward and rest pose"
      }
    }
  },
  "rig.preview_pose_locals": {
    "summary": "Preview explicit absolute local rigid matrices with revision validation",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional. Absolute row-major rigid local matrices replace the transient preview for supplied bones after revision validation. Rest, weights and committed keys are unchanged until apply/key operations.",
    "params": {
      "local_transforms": {
        "description": "Object mapping bone names to absolute local row-major rigid matrices."
      },
      "rig_revision": {
        "description": "Nonnegative revision from get_pose_state; stale revisions reject without mutation."
      }
    }
  },
  "rig.get_driven_controls": {
    "summary": "Read the data-driven scalar control rig attached to a skeleton",
    "returns": "{character,rig_revision,controls:[RigDrivenControl]}",
    "tags": "control rig|scalar controls|authoring",
    "related": "rig.get_anatomy|rig.preview_control_values|rig.apply_pose_preview",
    "notes": "Read-only. Returns persisted control definitions including display anchor/shape, range/default and explicit local-axis bone drivers. Definitions are template-independent; owned custom rigs may author them through anatomy v7.",
    "access": "Read",
    "params": {
      "character": {
        "description": "Rig character name",
        "required": true,
        "type": "string"
      }
    }
  },
  "rig.preview_control_values": {
    "summary": "Preview sparse scalar control values through the generic control-rig evaluator",
    "returns": "ok",
    "tags": "control rig|scalar controls|bone keys|authoring",
    "related": "rig.get_driven_controls|rig.apply_pose_preview|rig.cancel_pose_preview",
    "notes": "Requires active Pose mode. Values map persisted control IDs to finite values inside each definition's range. Evaluation starts from the committed pose, applies explicit local-axis drivers atomically and then uses the existing IK and joint-limit evaluation. Rest and weights are unchanged. Apply uses the normal undo and Auto Key path. Errors include rig_control_values_invalid, rig_control_values_empty, rig_control_unknown, rig_control_value_out_of_range, rig_control_unknown_bone and rig_edit_stale_revision.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Active Pose-mode rig name",
        "required": true,
        "type": "string"
      },
      "values": {
        "description": "Object mapping control IDs to scalar values",
        "required": true,
        "type": "object"
      },
      "rig_revision": {
        "description": "Nonnegative revision from rig.get_pose_state or rig.get_driven_controls",
        "required": true,
        "type": "uint"
      }
    }
  },
  "rig.preview_pose_transform": {
    "summary": "Preview a rigid world delta on explicit bones with revision validation",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional. Applies a rigid world-space delta to the transient bone preview after revision validation. Rest, weights and committed keys remain unchanged.",
    "params": {
      "world_delta": {
        "description": "Row-major rigid world delta; selected parent/child transforms apply once."
      },
      "rig_revision": {
        "description": "Nonnegative revision from get_pose_state; stale revisions reject without mutation."
      }
    }
  },
  "rig.rename_bone": {
    "summary": "Rename an owned rig bone while preserving its scene index",
    "returns": "{ok: true}",
    "related": "rig.list_bones|rig.set_mode|rig.rename_bone|rig.reparent_bone|rig.delete_bone",
    "notes": "Owned, unskinned, clip-free rigs only. Uses one shared staged core and undo command; other rig selection/edit is blocked in scoped Rig Edit. Shared errors: unknown_character, unknown_bone, rig_not_owned, rig_edit_requires_unskinned, rig_edit_requires_no_clips, rig_edit_character_locked, rig_revision_overflow, scene_locked. Cross-rig references return rig_bone_external_reference. Authored ASCII name, not the prefixed key. Updates canonical hierarchy and BoneData keys/parent references, selects renamed bone and rebuilds skeleton/ozz. Errors: invalid_bone_name, bone_name_conflict, rig_edit_no_change. Anatomy references are preserved/remapped. Reparent may return rig_anatomy_chain_disconnected; delete may return rig_bone_anatomy_referenced until metadata references are removed. Unskinned staging also scans actual canonical flat scene weight buffers for references to this rig hierarchy indices; references block with rig_edit_requires_unskinned even if weighted metadata is missing, including zero/invalid entries and extra rows. No weighted delete/remap/transfer is implemented.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned rig name"
      },
      "bone": {
        "description": "Existing prefixed bone key"
      },
      "name": {
        "description": "New authored name, 1-128 ASCII letters/digits/underscore/hyphen"
      }
    }
  },
  "rig.reparent_bone": {
    "summary": "Change an owned bone parent while preserving world rest pose",
    "returns": "{ok: true}",
    "related": "rig.list_bones|rig.set_mode|rig.rename_bone|rig.reparent_bone|rig.delete_bone",
    "notes": "Owned, unskinned, clip-free rigs only. Uses one shared staged core and undo command; other rig selection/edit is blocked in scoped Rig Edit. Shared errors: unknown_character, unknown_bone, rig_not_owned, rig_edit_requires_unskinned, rig_edit_requires_no_clips, rig_edit_character_locked, rig_revision_overflow, scene_locked. Cross-rig references return rig_bone_external_reference. Parent must belong to the same rig. Selected bone and descendants retain world rest pose; local rest is recomputed. Root moves, self-parenting and descendant cycles are rejected. Errors: unknown_parent_bone, rig_root_edit_blocked, rig_parent_cycle, rig_edit_no_change. Anatomy references are preserved/remapped. Reparent may return rig_anatomy_chain_disconnected; delete may return rig_bone_anatomy_referenced until metadata references are removed. Unskinned staging also scans actual canonical flat scene weight buffers for references to this rig hierarchy indices; references block with rig_edit_requires_unskinned even if weighted metadata is missing, including zero/invalid entries and extra rows. No weighted delete/remap/transfer is implemented.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned rig name"
      },
      "bone": {
        "description": "Existing prefixed bone key"
      },
      "parent": {
        "description": "Existing prefixed parent bone key in the same rig"
      }
    }
  },
  "rig.select_bone": {
    "summary": "Select one named skeleton node in the shared hierarchy and viewport state",
    "returns": "ok",
    "tags": "skeleton|joint|meshless|overlay",
    "related": "rig.list_characters|rig.list_bones|rig.select_bone|rig.get_selected_bone",
    "notes": "Works with skeleton-only and meshless animated imports. Selection/overlay are transient view state, not authoring edits; no undo. Names are import-prefixed. Joint world transforms are row-major 16 floats from evaluated hierarchy globals, never skin-offset matrices. pose_source is bind, graph, controller or ozz. Bind fallback is explicit per node. Independent skeleton root-motion placement and bind/pose editing are subsequent roadmap work. Replaces multi-selection with one active bone through shared RigSelection; no additive behavior. Successful selection releases the selected IK handle and discards its uncommitted preview; committed IK controls, timed channels and contacts remain active. Direct FK edits of IK-driven bones still require returning their control to FK.",
    "params": {
      "character": {
        "description": "Import name from rig.list_characters"
      },
      "bone": {
        "description": "Node name from rig.list_bones; helper nodes are also selectable"
      }
    }
  },
  "rig.select_bones": {
    "summary": "Select multiple unique joints of one rig with an active joint",
    "returns": "{ok: bool}",
    "related": "rig.get_selection|rig.select_bone|rig.set_selection_pivot|rig.transform_rest",
    "notes": "Shared RigSelection service and transient scene ViewState, no geometry edit/history/project dirty mark. Unique bone names from rig.list_bones; max 4096. Modes replace/add/toggle/range; add/toggle on another character start a new single-rig set (scoped Edit rejects other character). Duplicate/unknown bone requests fail atomically. Optional active must belong to resulting set; otherwise default last requested or existing active/last canonical selected when toggled off. Range requires exactly one target; inclusive full depth-first hierarchy order including collapsed branches, from stable selection anchor. Output selection enumeration follows canonical skeleton order. Empty replace clears selection; old rig.select_bone replaces with one active bone. Clear selection clears the complete set. UI Ctrl toggle, Shift range, Rig Edit Shift-blank drag box replaces; Ctrl+Shift box adds. Active orange, other selected green, outgoing segment identity remains its starting joint. Errors rig_selection_invalid_mode, rig_selection_duplicate_bone, rig_selection_active_not_selected, rig_selection_invalid_range, rig_selection_invalid_hierarchy, rig_selection_limit, unknown_bone, unknown_character, rig_edit_character_locked, api_not_bound, scene_locked, rig_selection_failed. Python rt.rig.select_bones(character,bones,active=\"\",mode=\"replace\"). Optional anchor override must be a resulting member (rig_selection_anchor_not_selected); read rig.get_selection.anchor for exact selection restoration. Successful selection releases the selected IK handle and discards its uncommitted preview; committed IK controls, timed channels and contacts remain active. Direct FK edits of IK-driven bones still require returning their control to FK.",
    "access": "SceneWrite",
    "params": {
      "bones": {
        "description": "Array of unique bone name strings"
      },
      "active": {
        "description": "Active member or empty for automatic choice"
      },
      "mode": {
        "enum": [
          "replace",
          "add",
          "toggle",
          "range"
        ]
      },
      "anchor": {
        "description": "Optional anchor member override for restoring a selection snapshot; empty uses click/range behavior"
      }
    }
  },
  "rig.select_pose_clip": {
    "summary": "Select an editable native clip and clear transient pose overrides",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional. Selects only a rigAuthoring clip owned by the character and clears transient FK/IK preview state."
  },
  "rig.set_anatomy": {
    "summary": "Replace a rig anatomy definition through validated shared authoring core",
    "returns": "{ok: true}",
    "related": "rig.get_anatomy|rig.rename_bone|rig.reparent_bone|rig.delete_bone|rig.copy_from",
    "notes": "One undo command; native project persistence stores anatomy beside canonical hierarchy. Versioned schemas 1..7 are accepted with strict unknown-field rejection. V6 fit_rules derive deterministic joints. V7 control_rig adds template-independent scalar controls with explicit display anchors, ranges and local-axis bone drivers. All referenced bones must exist; rename rekeys anchors/drivers and referenced bones cannot be deleted accidentally. Owned unskinned clip-free eligibility and scoped edit lock apply. Errors include rig_anatomy_invalid_fit_rule, rig_anatomy_invalid_driven_control, rig_anatomy_invalid_control_driver and existing anatomy/edit errors.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "anatomy": {
        "description": "Full canonical anatomy object version 1..7; use rig.get_anatomy to preserve existing fields"
      }
    }
  },
  "rig.set_joint_profile": {
    "summary": "Replace persistent joint rules atomically with validation and undo",
    "returns": "ok",
    "tags": "anatomy|limits|hinge|swing twist|constraints",
    "related": "rig.get_joint_profile|rig.suggest_joint_profile|rig.set_joint_profile|rig.get_pose_state|rig.preview_pose_locals",
    "notes": "Profile schema version 1: joints array of bone/type/enabled/lock_translation/axis/minimum/maximum/swing. Defaults: free, disabled, lock_translation true, unit axis [1,0,0], twist range [-180,180], swing 180 degrees. Types free/hinge/ball/fixed. Axis is in the joint rest-relative local frame. Ranges must contain neutral zero; no stretch or anatomical certainty is implied. Enabled rules project previews and authored playback; Auto Key records the constrained pose. A fully blocked changed request applies as a successful no-op with no keys/history. Read get_pose_state.limit_hits for projected bones. Queries are read-only; suggestions remain disabled. Set requires owned rig and current nonnegative rig_revision, rejects active Pose preview/render job, preserves rest/weights/clip keys, increments rig revision and is undoable. Native anatomy uses version 2 when rules exist, version 1 otherwise. IK integration, soft limits, elliptical cones and muscle deformation remain later work.",
    "undoable": true,
    "params": {
      "character": {
        "description": "Exact character name from rig.list_characters."
      },
      "profile": {
        "description": "Complete version 1 profile; omitted existing rows are removed. Unknown fields/bones, duplicate rows, nonfinite numbers, nonunit axes and invalid ranges reject atomically."
      },
      "rig_revision": {
        "description": "Current nonnegative revision from get_joint_profile; stale writes reject."
      }
    }
  },
  "rig.set_mode": {
    "summary": "Set Scene or scoped Rig Edit viewport interaction mode",
    "related": "rig.get_mode|rig.select_bone|rig.set_rest_transform",
    "notes": "Transient, not serialized or undoable. Edit requires an owned unskinned clip-free rig and no active mesh/sculpt/paint edit session. Exit to scene before switching rigs. Errors: unsupported_rig_mode, viewport_edit_mode_conflict, rig_edit_character_locked, rig_not_owned, rig_edit_requires_unskinned, rig_edit_requires_no_clips, unknown_character, scene_locked. Entering Edit also rejects actual flat skin references to owned rig indices even when weighted metadata is missing; rig_edit_requires_unskinned. Pose mode is supported for owned bound rigs. Scene exits Pose and clears transient overrides; Edit requires exiting Pose first.",
    "access": "SceneWrite",
    "params": {
      "mode": {
        "enum": [
          "scene",
          "edit",
          "pose"
        ]
      },
      "character": {
        "description": "Owned rig required when mode is edit"
      }
    }
  },
  "rig.set_overlay_visible": {
    "summary": "Show or hide the skeleton viewport overlay",
    "returns": "ok",
    "tags": "skeleton|joint|meshless",
    "related": "rig.list_characters|rig.list_bones|rig.select_bone|rig.get_selected_bone",
    "notes": "Works with skeleton-only and meshless animated imports. Selection/overlay are transient view state, not authoring edits; no undo. Names are import-prefixed. Joint world transforms are row-major 16 floats from evaluated hierarchy globals, never skin-offset matrices. pose_source is bind, graph, controller or ozz. Bind fallback is explicit per node. Independent skeleton root-motion placement and bind/pose editing are subsequent roadmap work.",
    "params": {
      "visible": {
        "description": "Overlay visibility for all visible skeletons"
      }
    }
  },
  "rig.set_pose_auto_key": {
    "summary": "Enable or disable key insertion when a pose preview is applied",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton; mesh binding is optional. Enabling Auto Key requires a selected editable clip. The setting is transient; generated bone keys are canonical AnimationData."
  },
  "rig.set_pose_frame": {
    "summary": "Pause playback and move to an integer authoring frame",
    "returns": "ok",
    "tags": "FK|bone keys|authoring",
    "related": "rig.set_mode|rig.get_pose_state|rig.preview_pose_locals|rig.apply_pose_preview",
    "notes": "Requires an authoring-owned skeleton in Pose mode; mesh binding is optional. Pauses playback, moves the shared Timeline frame and discards uncommitted frame-local preview state."
  },
  "rig.set_pose_view": {
    "summary": "Switch one rig between stored Rest and Animated evaluation views",
    "related": "rig.get_pose_view|rig.set_mode|anim.sample_clip_binding",
    "notes": "Transient, not serialized or undoable; does not alter clips or bind data. Rest skips graph/controller/ozz playback and root-motion evaluation for the chosen model. Computes skin matrices from canonical hierarchy rest globals and bind offsets; CPU skinning uses flat TriangleMesh only. Other models continue evaluating. Rig Edit overrides effective view to Rest. Animated resumes assigned clips and refreshes CPU/GPU display even when paused. Source must have canonical skeleton hierarchy for Rest. Errors: invalid_rig_pose_view, unknown_character, rig_pose_view_requires_hierarchy, rig_pose_view_incomplete_bind, rig_pose_view_invalid_bind, invalid_preview_hierarchy, invalid_preview_pose, scene_locked. On import/native reopen, valid skinned characters default to Rest; meshless clips retain prior animated defaults. Explicit per-character views are preserved during append/reinitialization. Rest graphs are excluded from autonomous viewport wake and file-animation reset scheduling. Select animated explicitly to resume existing animation.",
    "access": "SceneWrite",
    "params": {
      "mode": {
        "enum": [
          "rest",
          "animated"
        ]
      }
    }
  },
  "rig.set_rest_transform": {
    "summary": "Change an owned unskinned clip-free joint local rest position and rotation with undo",
    "returns": "{ok:true}",
    "related": "rig.add_bone|rig.list_bones|project.save|project.open",
    "notes": "Recomputes descendant rest globals/offsets and BoneData/hierarchy/skeletonNodes/ozz caches from canonical NodeHierarchy. Increments persisted revision. Rest transform must be finite and rigid; existing clips must be absent. Service failures return named codes (Python ValueError / IPC code): rig_name_conflict, invalid_rig_name/invalid_bone_name, unknown_rig_template, unknown_character, unknown_parent_bone/unknown_bone, bone_name_conflict, rig_not_owned, rig_edit_requires_unskinned, rig_edit_requires_no_clips, invalid_rest_transform, rig_rest_requires_rigid_transform, scene_locked or history_not_bound. Shape/type errors use Python TypeError/ValueError and IPC invalid_parameter. Validation/staging happens before scene changes. Unskinned staging also scans actual canonical flat scene weight buffers for references to this rig hierarchy indices; references block with rig_edit_requires_unskinned even if weighted metadata is missing, including zero/invalid entries and extra rows. No weighted delete/remap/transfer is implemented.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "character": {
        "description": "Owned rig identity."
      },
      "bone": {
        "description": "Existing full unique joint key."
      },
      "rest_transform": {
        "description": "Row-major 16-number local rest transform. Translation and proper rotation only; scale/shear/reflection rejected."
      }
    }
  },
  "rig.set_scene_transform": {
    "summary": "Move, rotate or uniformly scale an entire owned meshless rig without editing its rest hierarchy",
    "returns": "{ok: true}",
    "related": "rig.get_scene_transform|rig.set_rest_transform",
    "notes": "Scene mode only. Shared service and undo command used by viewport, inspector, Python and IPC. Clips on meshless owned rigs are allowed; local rest, anatomy, bind offsets and clip curves remain unchanged. Native snapshots persist translation/rotation/uniform scale; legacy snapshots default to identity. Positive uniform actor scale in [0.0001, 10000] is supported. Nonuniform scale/shear/reflection are rejected; individual joint rest transforms remain rigid. Errors: unknown_character, rig_not_owned, rig_edit_active, rig_placement_requires_meshless_unskinned, invalid_rig_scene_transform, rig_scene_requires_uniform_scale, rig_revision_overflow, rig_edit_no_change, scene_locked. Placement staging rejects actual flat weight references to owned rig hierarchy indices even if metadata is missing; rig_placement_requires_meshless_unskinned. Per-frame capability queries do not scan all skin buffers.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "scene_transform": {
        "description": "16 finite row-major numbers describing translation, proper rotation and positive uniform scale (0.0001 to 10000)"
      },
      "character": {
        "description": "Owned meshless rig name"
      }
    }
  },
  "rig.set_selection_pivot": {
    "summary": "Choose active-joint or selection-center pivot for batch rest gizmos",
    "returns": "{ok: bool}",
    "related": "rig.get_selection|rig.transform_rest",
    "notes": "Transient shared view preference, defaults active. center is arithmetic mean of selected world joint origins; orientation remains active joint basis, local/world axes remain existing viewport choice. Used for rest gizmo in scoped Rig Edit; Scene G/R/S still transforms the whole owned meshless actor. Pivot changes during drag cancel pending rest preview. No geometry/history/project dirty mark, not serialized. Errors rig_selection_invalid_pivot, api_not_bound, scene_locked. Python rt.rig.set_selection_pivot(mode).",
    "access": "SceneWrite",
    "params": {
      "mode": {
        "enum": [
          "active",
          "center"
        ]
      }
    }
  },
  "rig.set_weight_map_visible": {
    "summary": "Toggle transient viewport tint for the currently selected bone weights",
    "returns": "{ok: bool}",
    "related": "rig.get_weight_map_visible|rig.get_weight_map|rig.select_bone",
    "notes": "Same shared API as inspector checkbox. Transient ViewState flag, defaults off, no scene geometry/skin/sculpt protection mask change, no history or project dirty mark; not persisted. Selection changes automatically change displayed bone across owned/imported weighted flat parts. Missing/ambiguous ownership is not guessed. Reuses sculpt scalar triangle tint primitive, reads current deformed flat P/N and object transform. ImGui surface overlay works across viewport modes but has no depth buffer (backface culling only); overlapping surfaces can show through. Bounded display (2M vertices and 120k examined faces) may omit dense parts. No weight painting delivered. Errors api_not_bound, scene_locked; IPC expects boolean visible, invalid_parameter on wrong shape. Python rt.rig.set_weight_map_visible(visible).",
    "access": "SceneWrite"
  },
  "rig.suggest_joint_profile": {
    "summary": "Propose disabled limb joint rules from explicit anatomy roles and rest geometry",
    "returns": "JointProfileProposal",
    "tags": "anatomy|limits|hinge|swing twist|constraints",
    "related": "rig.get_joint_profile|rig.suggest_joint_profile|rig.set_joint_profile|rig.get_pose_state|rig.preview_pose_locals",
    "notes": "Profile schema version 1: joints array of bone/type/enabled/lock_translation/axis/minimum/maximum/swing. Defaults: free, disabled, lock_translation true, unit axis [1,0,0], twist range [-180,180], swing 180 degrees. Types free/hinge/ball/fixed. Axis is in the joint rest-relative local frame. Ranges must contain neutral zero; no stretch or anatomical certainty is implied. Enabled rules project previews and authored playback; Auto Key records the constrained pose. A fully blocked changed request applies as a successful no-op with no keys/history. Read get_pose_state.limit_hits for projected bones. Queries are read-only; suggestions remain disabled. Set requires owned rig and current nonnegative rig_revision, rejects active Pose preview/render job, preserves rest/weights/clip keys, increments rig revision and is undoable. Native anatomy uses version 2 when rules exist, version 1 otherwise. IK integration, soft limits, elliptical cones and muscle deformation remain later work.",
    "params": {
      "character": {
        "description": "Exact character name from rig.list_characters."
      }
    }
  },
  "rig.transform_rest": {
    "summary": "Apply one rigid world delta to multiple owned unskinned joints as one undo step",
    "returns": "{ok: bool}",
    "related": "rig.select_bones|rig.get_selection|rig.set_rest_transform|rig.list_bones",
    "notes": "Explicit bone names and expected rig_revision from list_bones required. Owned meshless/unskinned/clip-free rest edit gate, including scan of actual flat skin references. Does not require UI Edit but respects another scoped rig edit lock. World_delta is row-major 16 numeric floats and must be finite rigid affine (no scale/reflection). Pure RigBatchRest helper conjugates delta through actor placement, computes all selected original global targets, then derives locals relative to updated parent globals; selected parent+child do not move twice, unselected descendants inherit hierarchy movement. Uses canonical owned NodeHierarchy; common RigEditing::finish rebuilds BoneData/default/parent/index/inverse rest offset, skeletonNodes, ozz/runtime and revision. Stable scene bone IDs; no flat geometry/skin change allowed. Existing RigEditCommand snapshots bone/model plus complete selection for one-step undo/redo; explicit target list becomes selection, existing selected target active retained else last input. UI uses same helper for immutable preview and same API on release; Escape/camera absence/scene load/revision/selection/pivot/operation changes cancel. No-op rejects rig_edit_no_change, stale rig revision rig_edit_stale_revision; empty/duplicate/unknown list rig_selection_empty/rig_selection_duplicate_bone/unknown_bone; limits4096, invalid delta invalid_rest_transform or rig_rest_requires_rigid_transform, invalid parent rig_batch_rest_invalid_parent; existing rig/ownership/skin/clip/overflow gates propagate. api_not_bound/history_not_bound/scene_locked/rig_edit_failed. IPC bool/float/negative revision or invalid matrix/list shape invalid_parameter; Python typed conversion errors may precede service validation. Rest edits persist via native hierarchy/bone snapshot; transient selection/pivot do not. Mirror/batch topology/weighted rebind/pose authoring are not delivered. Python rt.rig.transform_rest(character,bones,world_delta,rig_revision). Existing valid range anchor is retained if it belongs to the target set.",
    "undoable": true,
    "access": "SceneWrite",
    "params": {
      "bones": {
        "description": "Unique selected joint keys, >=1 and <=4096"
      },
      "world_delta": {
        "description": "16 row-major finite numbers; rigid world transform about caller-chosen pivot"
      },
      "rig_revision": {
        "description": "Expected nonnegative uint64 rig revision from rig.list_bones"
      }
    }
  },
  "rig.weight_stats": {
    "summary": "Inspect stored flat mesh skin influence contract",
    "returns": "SkinWeightStats",
    "related": "rig.preflight|rig.get_weights",
    "notes": "Read-only exact individual TriangleMesh nodeName; no model group or Triangle facade geometry. Existing contract fields retained: max_influences, min/max sums over nonempty rows (null when none), unweighted_vertices, invalid/nonpositive entries, duplicate IDs, descending-order violations, over-four rows, normalization deviations (1e-5), extra rows. contract_valid permits empty rows and checks numeric/top-four format only. fully_weighted is separate. Resolves mesh character through exact canonical nodeHierarchy unique keys or direct flat mesh membership, never authored-name/prefix guessing. ownership=resolved/unresolved/ambiguous; ownership_verified requires a unique owner with hierarchy keys. Checks scene BoneData forward index map, including missing/gap indices and duplicate index assignments; stale reverse caches are not consulted. unknown_bone_entries, ambiguous_bone_entries, foreign_bone_entries are independent; foreign counts require resolved hierarchy ownership. index_range_valid checks known unambiguous indices; bone_indices_verified additionally requires resolved ownership and no foreign entries. weights_valid combines contract_valid, fully_weighted, bone_indices_verified; it does not validate bind matrices or deformation quality. Extra rows fail contract_valid and are not sampled beyond actual DNA vertex count. No mutation/backend invalidation/history. Re-run after edits. Errors: invalid_mesh_name, unknown_mesh, ambiguous_mesh_name, mesh_has_no_geometry, rig_weight_stats_failed, api_not_bound, scene_locked. Python rt.rig.weight_stats(mesh). Initial authored binding registry takes precedence over original source hierarchy/membership; preserves correct owner after multipart binding and native reopen.",
    "access": "Read",
    "params": {
      "mesh": {
        "description": "Exact individual flat mesh nodeName; groups not accepted"
      }
    }
  },
  "viewport.frame_timings": {
    "summary": "Per-pass CPU and GPU cost of the raster viewport frame over the window since the last reset",
    "tags": "timing|profile|gpu|cpu|cost|measure|benchmark|stage|pass",
    "related": "viewport.reset_frame_timings|viewport.frame_telemetry|viewport.rt_shadow",
    "notes": "HOW TO USE IT: viewport.reset_frame_timings, then DRIVE THE CAMERA (camera.set_position/set_target) for as many frames as you want to average, then read this. The raster viewport renders ONLY when it is marked dirty, so a still camera produces an empty window - measured 2026-09-09, 24 s of a still camera gave 0 raster frames while the display loop kept re-presenting one old image, and a wall-clock FPS A/B over it correctly reported 'noise'. The app also draws NOTHING while its window is not focused. Neither is a bug; both make a measurement silently empty, and 'warnings' names them. WHAT THE STAGES MEAN: shadow_atlas, sky, depth_prepass, rt_shadow, main_pass, volume_sdf, transmission, post, overlay - in frame order. GPU marks are written at BOTTOM_OF_PIPE, so a stage is 'time until this boundary retired', NOT 'what this pass would cost alone'. They sum to the frame exactly; they do not attribute an isolated cost, and deleting a 10 ms stage does not promise a 10 ms frame. ZERO IS TWO DIFFERENT ANSWERS: 'frames_ran' 0 with 0 ms means the stage was SKIPPED every frame - which is a RESULT, not missing data. That is exactly what shadow_atlas reports once the RT shadow handoff takes the light (cross-check applied.rt_cascades_replaced). A stage that ran and cost nothing has frames_ran > 0. GPU NUMBERS LAG: marks are read without waiting, so the newest retired slot is one or two frames back and 'frames_with_gpu' trails 'frames'. Over a window that is irrelevant; on the first few frames after a reset it is why gpu_* can be absent. 'gpu_supported' false means this device or queue cannot timestamp at all - every gpu_* is ABSENT, not zero, and the cpu_* half is still real. 'applied' IS PART OF THE MEASUREMENT: it is what the measured frames ACTUALLY did, sampled from those same frames - never the request side. depth_prepass and gpu_culling in particular have both shipped here as requested-but-not-applied. If any of it changed DURING the window, 'warnings' says so and the means average two different machines: reset and measure again rather than comparing them. 'window_wall_ms' far exceeding frames x frame_cpu_mean_ms means these were individually driven frames, not a sustained frame rate - quote them as ms/frame, never as FPS.",
    "requires": "viewport.reset_frame_timings"
  },
  "viewport.raster_depth_prepass": {
    "summary": "Is the raster depth prepass requested?",
    "notes": "This is the REQUEST. viewport.frame_telemetry depth_prepass reports whether the pass actually ran; the two differ when the prepass pipeline failed to build, in which case the viewport silently falls back to the old single-pass path and a warning is logged."
  },
  "viewport.reset_frame_timings": {
    "summary": "Clear the per-pass raster timing window before a measured run",
    "tags": "timing|profile|measure|benchmark",
    "related": "viewport.frame_timings",
    "notes": "Always call this first: the window is a MEASUREMENT of one configuration, and without a reset it still holds frames from whatever the viewport was doing before - a lifetime average across settings changes is the one thing a per-pass timing must never be. It also marks the viewport dirty so the first frame after it is real rather than an empty window. Then drive the camera and read viewport.frame_timings.",
    "next": "viewport.frame_timings"
  },
  "viewport.rt_shadow": {
    "summary": "RT shadow mask state: supported, requested, built, ray count and reason",
    "notes": "'enabled' is the REQUEST; 'ready' is what actually built. They differ whenever ray query is unsupported on the device, rayfusion_rt_shadow.spv is missing, the scene acceleration structure has not been built yet, or a material graph is bound - 'reason' says which. 'rays' is one ray per mask pixel, i.e. the per-frame ray count. 'cascades_replaced' is the number of cascade shadow VIEWS the ray pass took over this frame, and it is the only number that shows the cost actually MOVED rather than being added: rays rising while this stays 0 means both paths ran. Zero with ready=true is usually volumes in the scene - the SDF surface shader, the volume shader and the deep transmittance atlas never read the screen mask, so the cascades stay up for them and nothing is saved. IPC WRITE-THEN-MEASURE: toggling and reading in the same batch reports the PREVIOUS frame; put a frame between the write and the read."
  },
  "viewport.set_raster_depth_prepass": {
    "summary": "Turn the alpha-tested depth prepass in the raster viewport on or off",
    "notes": "ON by default. The prepass does two jobs: early-Z rejects hidden fragments before the 1562-line scene shader runs, and it writes the per-pixel depth a ray-traced shadow pass needs as its ray origin. Measured motivation on a forest scene: fusion 427.6 ms/frame vs solid 17.5 ms at 2.6x MORE triangles, i.e. ~98% of the frame was fragment shading times 18.4 triangles per pixel. Switchable on purpose - a fix that cannot be turned off kills the measurement that would judge it. Read viewport.frame_telemetry depth_prepass for what ACTUALLY ran; the lever is only the request."
  },
  "viewport.set_rt_shadow": {
    "summary": "Turn the screen-space RT shadow mask pass on or off",
    "notes": "OFF by default; turn it on wherever ray query is supported. The mask is consumed by the raster MESH shader (rtPreviewShadow returns the screen visibility for the covered light), and the cascade views for that light are then NOT drawn at all - read 'cascades_replaced' from viewport.rt_shadow to confirm the handoff happened. Exactly ONE light is covered: the first visible directional light, else the Nishita world sun. Point, spot and area lights keep their cascades, which is why they look identical with this on and off. WHAT IS GIVEN UP: fragments the screen mask declines - impostors, transparent replay, and any fragment whose depth does not match the prepass - lose that light's shadow instead of falling back to the atlas. WHEN NOTHING IS SAVED: with volumes in the scene the cascades stay up, because the SDF surface shader, the volume shader and the deep transmittance atlas read the atlas and have no screen mask. Motivation: the cascade shadow term measured 113 ms/frame, while a ray budget on this scene's own TLAS gave >=334 Mrays/s - about 5 ms for one ray per pixel at 1680x945. In a foliage scene the cascade cost is dominated by the alpha pipeline: one opacity texture fetch and discard per leaf fragment, per cascade. Requires a depth prepass and turns one on."
  },
  "rig.set_ik_orientation": {
    "summary": "Preview a world-space hand/foot tip orientation",
    "returns": "ok; transient pose preview",
    "tags": "pose|IK|orientation",
    "related": "rig.get_controls|rig.select_control|rig.apply_pose_preview|rig.cancel_pose_preview",
    "notes": "Active owned bound Pose, matching revision, no render job. orientation_world is a finite unit quaternion [w,x,y,z]; enabled=true activates IK. Rotation blends once with IK/FK blend after positional solving and before joint limits. Apply creates one undo step; Auto Key writes IK control keys; bake writes solved tip bone keys. Live orientation is transient; position contacts retain it while scrubbing. No timed control channels yet. Errors include rig_ik_invalid_orientation, rig_edit_stale_revision, rig_ik_unknown_control.",
    "params": {
      "character": {
        "type": "string",
        "required": true
      },
      "control": {
        "type": "string",
        "required": true
      },
      "orientation_world": {
        "type": "array",
        "required": true
      },
      "enabled": {
        "type": "bool",
        "required": true
      },
      "rig_revision": {
        "type": "integer",
        "required": true
      }
    }
  },
  "viewport.taa": {
    "summary": "Read realtime raster temporal anti-aliasing state plus how many samples have ACTUALLY accumulated",
    "returns": "enabled, target_samples, accumulated_samples, converged, supported, last_ms, inactive_reason",
    "tags": "antialiasing|aa|jitter|temporal|noise|denoise|realtime|raster",
    "related": "viewport.set_taa|viewport.frame_timings|rayfusion.screen_gi|rayfusion.reflections",
    "notes": "'enabled' is the REQUEST and 'accumulated_samples' is the MEASUREMENT; they answer different questions and the pipe can be off in four places that all coexist with enabled=true (raster_taa.spv missing, the compute pipeline failed to build, the history images could not be allocated, or the raster viewport is not the mode on screen). 'inactive_reason' names which one. Reading this right after a camera move gives 0 or 1: that is correct, not a failure -- accumulation restarts whenever the image changes, because a history that no longer matches the image is a ghost, not an average. 'converged' true means the viewport has stopped asking for extra frames, which is also where its GPU cost drops back to idle: TAA is not free while it converges and this field is the honest place to see that. 'target_samples' is a STOPPING CONDITION rather than a quality dial -- raising it buys a cleaner still image and pays for it in how long the GPU keeps working after the camera stops. 'last_ms' is the TAA dispatch plus the full-resolution copy back into the HDR target that the post pass reads."
  },
  "viewport.set_taa": {
    "summary": "Replace realtime raster temporal anti-aliasing settings",
    "returns": "ok",
    "tags": "antialiasing|aa|jitter|temporal|noise|denoise|realtime|raster",
    "related": "viewport.taa|viewport.set_quality|viewport.set_depth_of_field",
    "verify_with": "viewport.taa",
    "notes": "Changing either field RESETS the accumulated history, so the next read of viewport.taa reports accumulated_samples near zero -- that is the reset, not a failure. 'samples' is rejected outside [1, 256] rather than clamped, because a silently clamped dial means the caller records a number it never actually measured. Turning TAA off does not just remove the blending: it also removes the sub-pixel jitter, so edges go back to hard 1:1 aliasing AND the screen-space GI/reflection noise stops being averaged over frames. Those two symptoms have one cause and one switch.",
    "params": {
      "enabled": {
        "description": "Master switch. Off restores the un-jittered, history-free image."
      },
      "samples": {
        "description": "Stopping condition: how many jittered samples to accumulate before the viewport goes idle. 1-256."
      }
    }
  },
  "perf.get_gpu_memory": {
    "summary": "VRAM by device (render / dedicated viewport) and purpose, BLAS compaction savings, and the driver's process-wide usage.",
    "notes": "'vram_usage_bytes' is the driver's VK_EXT_memory_budget number for the WHOLE PROCESS; with a dedicated viewport VkDevice and a render VkDevice on one card it cannot say which is full - that is what 'devices' is for. Each device lists its own allocations by category (geometry, accel_struct, scratch, texture, render_target, other) in device-local and host-visible bytes. 'untracked_bytes' = usage minus the sum of tracked device-local bytes: CUDA/OptiX, simulation compute, the exposure meter and driver overhead live there, and a large value means the bytes are NOT in a VulkanDevice allocation. 'vram_measured' false means the zeros are not a reading. 'tracked' false on a device means it is not Vulkan (OptiX/CPU) and has no categories. 'compaction.bytes_before/bytes_after' cover only the BLAS that were compacted; 'skipped_skinned' counts skinned BLAS, which are rebuilt in place and therefore left at build size (refit-only ALLOW_UPDATE BLAS ARE compacted; their MODE_UPDATE reuses the recorded build flags), and 'failures' counts BLAS kept at build size because the copy could not be made. Read it in Solid, then in Rendered after a frame: the viewport device should lose its accel_struct bytes while Rendered is shown (see rayfusion.scene_as 'yielded').",
    "tags": "perf|memory|vulkan",
    "related": "rayfusion.scene_as|perf.set_blas_compaction"
  },
  "perf.set_blas_compaction": {
    "summary": "Process-wide BLAS compaction switch; applies to acceleration structures built afterwards.",
    "notes": "Compaction copies each static BLAS into an allocation sized to what the build used; PREFER_FAST_TRACE builds are allocated for the worst case. Traversal and the image are unchanged, the cost is one extra copy submit per few BLAS at build time. The switch survives a backend recreate. It does NOT touch existing structures: switch, then force a rebuild (re-enter Rendered after a geometry change, or reopen the project) and compare perf.get_gpu_memory accel_struct bytes. Kept as a switch so a driver problem in the copy path can be ruled out without a rebuild of the app.",
    "tags": "perf|memory|vulkan",
    "related": "perf.get_gpu_memory",
    "params": {
      "enabled": {
        "description": "true compacts static BLAS built from now on; false keeps build-size allocations."
      }
    }
  },
  "mesh.edit.begin": {
    "summary": "Build the editable cache for an object and make it the active edit target",
    "notes": "Every operator does this implicitly; call it only to inspect or select before editing. Returns the same state block as mesh.edit.get_state.",
    "tags": "mesh|edit|begin|topology|select",
    "related": "mesh.edit.get_state|mesh.edit.select",
    "params": {
      "object": {
        "description": "Object to edit",
        "type": "string",
        "required": true
      }
    }
  },
  "mesh.edit.get_state": {
    "summary": "Report topology and selection counts for the mesh edit target",
    "notes": "half_edge_valid false means the operators fall back to the legacy triangle-soup path, where several of them refuse outright - without reading it you cannot tell an unsupported selection from a mesh whose topology never built. An empty object with no active edit target is a valid answer, not an error.",
    "tags": "mesh|edit|state|topology|selection|counts",
    "related": "mesh.edit.select|mesh.edit.select_by_normal|mesh.edit.get_state",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      }
    }
  },
  "mesh.edit.select": {
    "summary": "Select mesh elements by id, or select every element in a domain",
    "notes": "Pass 'all': true instead of 'ids' for a whole domain. Element ids are indices into the editable cache and are INVALIDATED by any operator that changes topology - read mesh.edit.get_state again instead of reusing a list across operations.",
    "tags": "mesh|edit|select|selection|vertex|edge|face",
    "related": "mesh.edit.select_by_normal|mesh.edit.select_by_box|mesh.edit.get_selection",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "domain": {
        "description": "Element domain",
        "enum": "vertex|edge|face",
        "type": "string",
        "required": true
      },
      "ids": {
        "description": "Element ids; required unless 'all' is true",
        "type": "array"
      },
      "mode": {
        "description": "How the ids combine with the current selection",
        "enum": "set|add|remove",
        "type": "string"
      },
      "all": {
        "description": "Select every element in the domain, ignoring 'ids'",
        "type": "bool"
      }
    }
  },
  "mesh.edit.select_by_normal": {
    "summary": "Select faces whose normal points within an angle of a direction",
    "notes": "This is how an automated caller reaches 'the top face' without knowing any face id. Faces only; the normal is area-weighted (Newell) so a non-planar ngon still resolves. 'matched' reports how many faces qualified BEFORE the mode was applied.",
    "tags": "mesh|edit|select|normal|direction|face|top",
    "related": "mesh.edit.select|mesh.edit.select_by_box",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "direction": {
        "description": "Direction the face normal is compared against",
        "type": "vec3",
        "required": true
      },
      "max_angle": {
        "description": "Maximum angle in degrees between normal and direction",
        "type": "float"
      },
      "mode": {
        "description": "How the matches combine with the current selection",
        "enum": "set|add|remove",
        "type": "string"
      }
    }
  },
  "mesh.edit.select_by_box": {
    "summary": "Select elements fully contained in an axis-aligned box",
    "notes": "Containment is total, not overlap: an edge needs both endpoints inside and a face needs every vertex inside, because a partially covered element is one the operators reject. world_space compares against the object's world transform, so a rotated object still selects correctly.",
    "tags": "mesh|edit|select|box|region|volume",
    "related": "mesh.edit.select|mesh.edit.select_by_normal",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "min": {
        "description": "Box minimum corner",
        "type": "vec3",
        "required": true
      },
      "max": {
        "description": "Box maximum corner",
        "type": "vec3",
        "required": true
      },
      "domain": {
        "description": "Element domain",
        "enum": "vertex|edge|face",
        "type": "string"
      },
      "mode": {
        "description": "How the matches combine with the current selection",
        "enum": "set|add|remove",
        "type": "string"
      },
      "world_space": {
        "description": "Compare in world space (default) rather than object space",
        "type": "bool"
      }
    }
  },
  "mesh.edit.clear_selection": {
    "summary": "Clear the mesh edit selection in every domain",
    "notes": "",
    "tags": "mesh|edit|select|clear|deselect",
    "related": "mesh.edit.select|mesh.edit.get_state",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      }
    }
  },
  "mesh.edit.get_selection": {
    "summary": "Return the selected element ids in one domain",
    "notes": "Element ids are indices into the editable cache and are INVALIDATED by any operator that changes topology - read mesh.edit.get_state again instead of reusing a list across operations.",
    "tags": "mesh|edit|select|selection|ids|read",
    "related": "mesh.edit.select|mesh.edit.get_state",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "domain": {
        "description": "Element domain",
        "enum": "vertex|edge|face",
        "type": "string",
        "required": true
      }
    }
  },
  "mesh.extrude": {
    "summary": "Extrude the selected faces along their normal",
    "notes": "Needs a FACE selection. The extruded faces' original triangles are consumed as the new volume's bottom cap rather than left inside it. Returns the post-edit state, so the new element counts arrive without a second call. MEASURED: the distance is in OBJECT-LOCAL units, not metres - it is multiplied by the object's scale on its way to world space, so on an object scaled 0.2 in Y a request of 0.5 moves the face 0.1 m. This matches edit-mode convention; read scene.object_info's world_size if you need the world result.",
    "tags": "mesh|extrude|face|topology|model",
    "undoable": true,
    "related": "mesh.inset|mesh.bevel|mesh.edit.select_by_normal",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "distance": {
        "description": "Extrude distance in OBJECT-LOCAL units; must be non-zero, negative pushes inward",
        "type": "float",
        "required": true
      }
    }
  },
  "mesh.inset": {
    "summary": "Inset the selected faces, producing a border ring and a smaller centre face",
    "notes": "Needs a FACE selection. Typically paired with mesh.extrude to build a rim. MEASURED: the distance is in OBJECT-LOCAL units, not metres - it is multiplied by the object's scale on its way to world space, so on an object scaled 0.2 in Y a request of 0.5 moves the face 0.1 m. This matches edit-mode convention; read scene.object_info's world_size if you need the world result.",
    "tags": "mesh|inset|face|topology|model",
    "undoable": true,
    "related": "mesh.extrude|mesh.bevel",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "amount": {
        "description": "Inset distance in OBJECT-LOCAL units; must be positive",
        "type": "float",
        "required": true
      }
    }
  },
  "mesh.bevel": {
    "summary": "Bevel the selected edges into a chamfer or rounded profile",
    "notes": "Needs an EDGE selection. This is the operator that produces rounded hard edges; subdivision is not a substitute, as catmull_clark at level 2 turns a box into a blob while a bevel keeps the silhouette and softens only the edge. MEASURED: the distance is in OBJECT-LOCAL units, not metres - it is multiplied by the object's scale on its way to world space, so on an object scaled 0.2 in Y a request of 0.5 moves the face 0.1 m. This matches edit-mode convention; read scene.object_info's world_size if you need the world result.",
    "tags": "mesh|bevel|edge|chamfer|round|topology|model",
    "undoable": true,
    "related": "mesh.loop_cut|mesh.edit.select_by_box",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "width": {
        "description": "Bevel width in OBJECT-LOCAL units; must be positive",
        "type": "float",
        "required": true
      },
      "segments": {
        "description": "Profile segments; 1 is a flat chamfer, more rounds it",
        "type": "int"
      },
      "round": {
        "description": "Use a rounded profile instead of a flat chamfer",
        "type": "bool"
      }
    }
  },
  "mesh.loop_cut": {
    "summary": "Insert a loop cut across the selected quad edge ring",
    "notes": "Needs an EDGE selection on a supported quad ring; t is the position along the ring and must lie strictly between 0 and 1.",
    "tags": "mesh|loop|cut|edge|ring|topology|model",
    "undoable": true,
    "related": "mesh.bevel|mesh.dissolve_edges",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "t": {
        "description": "Cut position along the ring, strictly between 0 and 1",
        "type": "float"
      }
    }
  },
  "mesh.dissolve_edges": {
    "summary": "Dissolve the selected interior edges, merging their faces",
    "notes": "Needs an EDGE selection. Edges whose removal would leave invalid topology are refused as a group rather than partially applied.",
    "tags": "mesh|dissolve|edge|merge|cleanup|topology",
    "undoable": true,
    "related": "mesh.dissolve_vertices|mesh.loop_cut",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      }
    }
  },
  "mesh.dissolve_vertices": {
    "summary": "Dissolve the selected vertices, merging the faces around them",
    "notes": "Needs a VERTEX selection.",
    "tags": "mesh|dissolve|vertex|merge|cleanup|topology",
    "undoable": true,
    "related": "mesh.dissolve_edges|mesh.merge_vertices",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      }
    }
  },
  "mesh.merge_vertices": {
    "summary": "Merge the selected vertices into their common centre",
    "notes": "Needs a VERTEX selection. Merges by SELECTION, unlike mesh.weld_vertices which merges by distance.",
    "tags": "mesh|merge|vertex|collapse|cleanup",
    "undoable": true,
    "related": "mesh.weld_vertices|mesh.dissolve_vertices",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      }
    }
  },
  "mesh.weld_vertices": {
    "summary": "Weld selected vertices that lie within a distance of each other",
    "notes": "Needs a VERTEX selection. The threshold is an absolute distance, not a fraction of the mesh. MEASURED: the distance is in OBJECT-LOCAL units, not metres - it is multiplied by the object's scale on its way to world space, so on an object scaled 0.2 in Y a request of 0.5 moves the face 0.1 m. This matches edit-mode convention; read scene.object_info's world_size if you need the world result.",
    "tags": "mesh|weld|vertex|merge|distance|cleanup",
    "undoable": true,
    "related": "mesh.merge_vertices|mesh.dissolve_vertices",
    "params": {
      "object": {
        "description": "Object to edit; empty uses the active mesh edit object",
        "type": "string"
      },
      "distance": {
        "description": "Weld threshold in OBJECT-LOCAL units; must be positive",
        "type": "float",
        "required": true
      }
    }
  },
  "particle.add_preset": {
    "summary": "Create an authored particle preset additively (campfire, explosion, fireball, nuclear, ...)",
    "tags": "particles|gas|preset|authoring|create",
    "notes": "A preset that can only be created from the panel cannot be calibrated by a script. Pair with gas.measure_plume to close the loop: create, step, measure."
  },
  "gas.measure_plume": {
    "summary": "Measure a live gas field: plume extent, cap height above the domain floor, widest slice, peak/mean heat, lid clipping",
    "tags": "gas|measure|verify|volume",
    "notes": "Check `measured` FIRST: every extent reads 0 both for an empty domain and for one that could not be sampled. `touching_ceiling` true means the cap's shape is the domain lid, not the stratification, and the other numbers describe a clipped cloud."
  },
  "gas.step_stats": {
    "summary": "Per-stage timing breakdown of the last gas solver step: every GPU stage, the host residual, the field analysis scan, plus grid size, CFL and field peaks",
    "notes": "Read `measured` FIRST - every timing is 0.0 both for a free stage and for a step that never ran, so an idle domain otherwise records as a 0 ms step, which in an optimisation A/B reads as a total win. Times are HOST WALL TIME around each call, so a GPU row includes the submit/fence the host waited on; that is the cost the frame pays, not isolated kernel time, and reading it as kernel time makes a transfer-bound stage look compute-bound. cfl above 1 means the semi-Lagrangian trace jumps more than one cell per step and smears detail.",
    "tags": "simulation|gas|performance|timing|profiling|optimisation|cfl",
    "related": "particle.stats|gas.get_settings|gas.measure_plume|perf.get_gpu_memory"
  }
}
