{
  "$comment": "Hand-written half of the IPC method descriptors. Parameters, types, defaults and capabilities are extracted from the dispatch code by gen_ipc_descriptors.py - do NOT repeat them here unless you are adding a description, an enum hint or correcting a type the parser could only see as 'any'. Keys: summary (required, one line, present tense), notes, tags (extra search synonyms; the domain and the method name are added automatically), related (pipe-separated), returns, undoable, access, params{name:{description,enum,type,required,default}}.",
  "agent.discover": {
    "summary": "Identify the application and list every capability domain, agent role and coverage metric",
    "notes": "First call for a new agent session. registered_methods is how many methods are dispatched; documented_coverage is the share that carries a hand-written summary. A documented_coverage below 1.0 means agent.describe will answer some methods with parameters but no explanation.",
    "tags": "bootstrap|handshake|capabilities|introspection",
    "related": "agent.list_methods|agent.describe|agent.search_capabilities",
    "returns": "DiscoveryInfo"
  },
  "agent.list_methods": {
    "summary": "List every registered IPC method, optionally filtered to one domain",
    "notes": "The full list is around 30 KB. Cache it once per session instead of calling it per step.",
    "tags": "catalogue|introspection",
    "related": "agent.describe|agent.search_capabilities",
    "returns": "MethodSummary[]",
    "params": {
      "domain": {
        "description": "Domain to restrict the listing to, e.g. 'fluid'. Omit for every method."
      }
    }
  },
  "agent.describe": {
    "summary": "Return the full parameter schema, capability and related methods for one IPC method",
    "notes": "Parameters come from the dispatch code itself, so they are exact. `documented: false` means the schema is real but nobody has written the prose yet - trust the parameters, be careful with intent.",
    "tags": "schema|introspection|parameters",
    "related": "agent.list_methods|agent.get_examples",
    "returns": "MethodDescriptor",
    "params": {
      "method": {
        "description": "Exact method name, e.g. 'fluid.create_domain'"
      }
    }
  },
  "agent.search_capabilities": {
    "summary": "Search workflow recipes and method metadata for a plain-language goal",
    "notes": "Recipes are the valuable half: each one is an ordered list of calls that is known to work end to end. Prefer following a recipe over assembling calls from the method hits.",
    "tags": "search|howto|recipe|workflow|goal",
    "related": "agent.get_examples|agent.describe",
    "returns": "SearchResult",
    "params": {
      "query": {
        "description": "Plain language goal, e.g. 'make a wooden object burn'"
      }
    }
  },
  "agent.get_examples": {
    "summary": "Return runnable call sequences for a method or a named workflow recipe",
    "tags": "example|sample|recipe",
    "related": "agent.search_capabilities|agent.describe",
    "returns": "ExampleSet",
    "params": {
      "method": {
        "description": "Method to show examples for, e.g. 'fluid.create_domain'"
      },
      "workflow": {
        "description": "Recipe id from agent.search_capabilities, e.g. 'combustion_setup'"
      }
    }
  },
  "agent.get_state_summary": {
    "summary": "Compact snapshot of scene, lights, camera, timeline and viewport measurement state",
    "notes": "The viewport block is a MEASUREMENT and is only present when frame capture is on and a frame exists; when it is absent that means 'not measured', never 'measured zero'. Turn capture on with viewport.capture before relying on it.",
    "tags": "state|context|snapshot|scene|verify",
    "related": "viewport.status|render.probe|scene.list_objects",
    "returns": "StateSummary",
    "params": {
      "include_probe": {
        "description": "Sample the last viewport frame for luminance and black/NaN fractions. Costs a full-frame scan.",
        "type": "bool"
      }
    }
  },
  "agent.roles": {
    "summary": "Describe the manager/controller/worker agent roles and their delegation chain",
    "notes": "Informational only. Access is decided by the security capability on the connection token, not by the role an agent claims.",
    "tags": "role|hierarchy|delegation|multi-agent",
    "returns": "RoleSet"
  },
  "agent.chat_send": {
    "summary": "Post a message into the Studio Agent Chat panel",
    "notes": "This WRITES to the user interface, so it needs the AgentChat capability - it is the one agent.* method that is not read-only. Use type='activity' for per-call progress, 'thought' for reasoning, 'error' for failures; the panel colours and filters them separately.",
    "tags": "chat|message|ui|report|progress",
    "related": "agent.chat_poll",
    "returns": "bool",
    "params": {
      "sender": {
        "description": "Display name shown in the panel, e.g. 'RayTrophi Agent'"
      },
      "content": {
        "description": "Message text"
      },
      "type": {
        "description": "Message kind",
        "type": "string",
        "enum": "reply|activity|thought|error",
        "default": "reply"
      },
      "payload": {
        "description": "Optional structured data appended to the message"
      },
      "image_base64": {
        "description": "Optional base64 image; the panel notes the attachment"
      }
    }
  },
  "agent.send_prompt": {
    "summary": "Queue a task for another agent to pick up on its next poll, addressed by agent id or 'all'",
    "notes": "Returns queued:true, NOT delivered - nothing has run yet and the target agent may never poll. Do not report the work as done on the strength of this call; ask for the result, or check the scene yourself. The target must match the id the other agent polls with (agent.chat_poll agent_id), and 'all' broadcasts.",
    "tags": "agent|delegate|multi-agent|prompt|task|handoff",
    "related": "agent.chat_poll|agent.chat_send|agent.roles",
    "verify_with": [
      "agent.get_state_summary"
    ]
  },
  "agent.chat_poll": {
    "summary": "Take the user prompts queued in the Agent Chat panel and mark the agent as alive",
    "notes": "Polling is also the panel's heartbeat: it shows the agent as online for a few seconds after each call. An agent that blocks on a long model turn should keep polling from a second thread, or the panel reports it offline.",
    "tags": "chat|poll|prompt|heartbeat",
    "related": "agent.chat_send",
    "returns": "QueuedPrompt[]"
  },
  "version": {
    "summary": "Return the RayTrophi Studio version string"
  },
  "batch": {
    "summary": "Execute several IPC calls in order and return one result array",
    "notes": "Each child call is authorized on its own; nesting a batch inside a batch is refused. A failing child does not stop the rest.",
    "tags": "sequence|bulk|transaction",
    "params": {
      "calls": {
        "description": "Array of {method, params} objects",
        "type": "array",
        "required": true
      }
    }
  },
  "undo": {
    "summary": "Undo the last recorded scene command",
    "tags": "history",
    "related": "redo|undo_description"
  },
  "redo": {
    "summary": "Redo the last undone scene command",
    "tags": "history",
    "related": "undo|redo_description"
  },
  "undo_description": {
    "summary": "Name of the command that undo would revert",
    "tags": "history"
  },
  "redo_description": {
    "summary": "Name of the command that redo would reapply",
    "tags": "history"
  },
  "request_render": {
    "summary": "Ask the viewport to render another frame",
    "tags": "viewport|refresh"
  },
  "reset_accumulation": {
    "summary": "Restart progressive sample accumulation in the viewport",
    "notes": "Post-process changes must NOT reset accumulation - only changes to scene, camera or lighting should.",
    "tags": "viewport|samples|refresh"
  },
  "scene.list_objects": {
    "summary": "List every object in the scene by name",
    "notes": "Covers both flat SoA meshes and legacy facade objects, so it is the reliable inventory call.",
    "tags": "inventory|objects|browse",
    "related": "scene.object_info|scene.object_exists",
    "returns": "string[]"
  },
  "scene.object_exists": {
    "summary": "Report whether an object with this name exists",
    "returns": "bool",
    "related": "scene.list_objects"
  },
  "scene.object_info": {
    "summary": "Return vertex and triangle counts for one object",
    "returns": "ObjectInfo",
    "related": "scene.list_objects"
  },
  "scene.add_primitive": {
    "summary": "Create a primitive mesh object and return its final name",
    "notes": "The name is made unique by suffix, so the returned name may differ from the requested one - always use the returned name afterwards.",
    "tags": "create|mesh|cube|sphere|plane|cylinder|torus",
    "related": "scene.delete|scene.set_transform|material.assign",
    "returns": "string",
    "undoable": true,
    "params": {
      "type": {
        "description": "Primitive shape",
        "enum": "cube|sphere|plane|cylinder|torus"
      },
      "name": {
        "description": "Requested name; a numeric suffix is added if it is taken"
      },
      "size": {
        "description": "Edge length or radius in metres; must be positive"
      }
    }
  },
  "scene.delete": {
    "summary": "Delete an object from the scene",
    "undoable": true,
    "tags": "remove"
  },
  "scene.duplicate": {
    "summary": "Duplicate an object and return the new object's name",
    "undoable": true,
    "returns": "string",
    "tags": "copy|clone"
  },
  "scene.get_transform": {
    "summary": "Return an object's translation, rotation, scale and full matrix",
    "returns": "TransformInfo",
    "related": "scene.set_transform|scene.get_world_transform",
    "notes": "This is the AUTHORED transform and a solver never writes it: a rigid body can fall for a thousand steps while this keeps returning the spawn pose. To measure physics use scene.get_world_transform."
  },
  "scene.get_world_transform": {
    "summary": "Return an object's SIMULATED world pose: the solver's motion composed onto the authored transform",
    "notes": "The rigid solver bakes its motion into the mesh vertices and never writes a transform, so scene.get_transform reports the spawn pose for a body that has been falling for hundreds of steps. Every physics measurement - free fall, buoyancy draft, restitution, settling - has to read this one. The result carries simulated: false when no solver has contributed, which is not the same as the body having stayed still.",
    "tags": "transform|physics|simulated|pose|measure|verify",
    "related": "scene.get_transform|physics.step|physics.add_body",
    "verify_with": [
      "scene.get_transform"
    ]
  },
  "scene.set_transform": {
    "summary": "Set an object's transform, either as a matrix or as translation/rotation/scale components",
    "notes": "Send `matrix`, or any combination of translation/rotation/scale - components you omit keep their current value. Rotation is in degrees.",
    "tags": "move|rotate|scale|position|placement",
    "related": "scene.get_transform",
    "undoable": true,
    "params": {
      "name": {
        "description": "Object name"
      },
      "matrix": {
        "description": "Row-major 4x4 matrix; wins over the component form when both are sent"
      },
      "translation": {
        "description": "World position in metres"
      },
      "rotation": {
        "description": "Euler angles in degrees"
      },
      "scale": {
        "description": "Per-axis scale factors"
      }
    }
  },
  "scene.import_model": {
    "summary": "Import a model file (glTF/FBX/OBJ) into the scene",
    "tags": "load|asset|mesh"
  },
  "select.object": {
    "summary": "Select an object by name, optionally adding to the current selection",
    "tags": "selection"
  },
  "select.light": {
    "summary": "Select a light by index, optionally adding to the current selection",
    "tags": "selection"
  },
  "select.deselect_object": {
    "summary": "Remove one object from the selection",
    "tags": "selection"
  },
  "select.all_objects": {
    "summary": "Select every object in the scene",
    "tags": "selection"
  },
  "select.clear": {
    "summary": "Clear the selection",
    "tags": "selection"
  },
  "select.list": {
    "summary": "List the currently selected objects and lights",
    "returns": "SelectionEntry[]",
    "tags": "selection"
  },
  "camera.get": {
    "summary": "Return camera position, target, up vector, field of view, focus distance, aperture and the physical exposure state",
    "notes": "EXPOSURE IS A RELATIVE MODEL, NOT AN ABSOLUTE PHOTOMETRIC ONE. The textbook formula 1/(1.2*2^EV100) assumes scene radiance in cd/m2; this engine's light intensities are arbitrary, so the multiplier is a ratio against a calibrated baseline (0.00003125) chosen to avoid a black viewport. Do not 'fix' it into the absolute formula - every scene would go black and the symptom would read as 'too dark', not 'wrong formula'. 'iso_value', 'shutter_seconds', 'f_number' and 'exposure_factor' are DERIVED read-only outputs, not settings: a preset INDEX is not a measurement, and exposure_factor is the number actually handed to the shaders. 'aperture' is the depth-of-field dial and does NOT affect exposure; 'fstop_preset_index' is the exposure/lens dial and does NOT affect depth of field. PRIORITY ORDER, and it is the most common reason these dials look dead: if auto_exposure is ON only ev_compensation is applied and ISO/shutter/f-stop are NOT read; if auto_exposure is OFF but use_physical_exposure is OFF too, again only ev_compensation applies. The call still SUCCEEDS because it really did write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor to see the multiplier actually applied."
  },
  "camera.set_position": {
    "summary": "Move the camera to a world position",
    "tags": "view|move"
  },
  "camera.set_target": {
    "summary": "Aim the camera at a world position",
    "tags": "view|look|aim"
  },
  "camera.set_fov": {
    "summary": "Set the camera field of view in degrees",
    "tags": "view|lens|zoom"
  },
  "camera.set_focus_distance": {
    "summary": "Set the depth-of-field focus distance in metres",
    "tags": "view|lens|dof|blur"
  },
  "camera.set_aperture": {
    "summary": "Set the camera aperture; larger values give shallower depth of field",
    "tags": "view|lens|dof|blur|bokeh"
  },
  "lights.list": {
    "summary": "List the scene lights with index, name, type and position",
    "returns": "LightSummary[]",
    "tags": "lighting"
  },
  "lights.get": {
    "summary": "Return every property of one light",
    "returns": "LightInfo",
    "tags": "lighting"
  },
  "lights.add": {
    "summary": "Add a light of the given type at a world position and return its name",
    "tags": "lighting|create|illuminate",
    "related": "lights.set_intensity|lights.set_color|world.set_mode",
    "undoable": true,
    "params": {
      "type": {
        "description": "Light type",
        "enum": "point|directional|spot|area"
      }
    }
  },
  "lights.delete": {
    "summary": "Delete a light by index",
    "undoable": true,
    "tags": "lighting|remove"
  },
  "lights.rename": {
    "summary": "Rename a light",
    "tags": "lighting"
  },
  "lights.set_position": {
    "summary": "Move a light to a world position",
    "tags": "lighting|move"
  },
  "lights.set_direction": {
    "summary": "Set the direction a directional or spot light points in",
    "tags": "lighting|aim"
  },
  "lights.set_color": {
    "summary": "Set a light's RGB colour",
    "tags": "lighting|colour"
  },
  "lights.set_intensity": {
    "summary": "Set a light's intensity",
    "tags": "lighting|brightness|exposure"
  },
  "lights.set_visible": {
    "summary": "Show or hide a light without deleting it",
    "tags": "lighting|visibility"
  },
  "lights.set_param": {
    "summary": "Set one numeric light parameter by name (radius, spot_angle, spot_falloff, width, height)",
    "notes": "spot_angle and spot_falloff apply to spot lights only; width and height to area lights only. The call fails rather than silently ignoring a mismatch.",
    "tags": "lighting|softness|cone",
    "params": {
      "param": {
        "enum": "intensity|radius|width|height|spot_angle|spot_falloff",
        "description": "Which light property to set. Not every key applies to every light type - a key the light does not have is refused, not ignored."
      }
    }
  },
  "world.get": {
    "summary": "Return world mode, background colour, sun angles and atmosphere settings",
    "returns": "WorldState",
    "tags": "environment|sky"
  },
  "world.set_mode": {
    "summary": "Switch the world background between flat colour, HDRI and physical sky",
    "tags": "environment|sky|background|hdri",
    "params": {
      "mode": {
        "description": "Background model",
        "enum": "solid|hdri|nishita"
      }
    }
  },
  "world.set_background_color": {
    "summary": "Set the flat background colour used in solid world mode",
    "tags": "environment|sky|colour"
  },
  "world.set_sun_elevation": {
    "summary": "Set the sun elevation angle of the physical sky",
    "tags": "environment|sky|sun|time-of-day"
  },
  "world.set_sun_azimuth": {
    "summary": "Set the sun compass angle of the physical sky",
    "tags": "environment|sky|sun|direction"
  },
  "world.set_sun_intensity": {
    "summary": "Set the sun intensity of the physical sky",
    "tags": "environment|sky|sun|brightness"
  },
  "world.set_sun_size": {
    "summary": "Set the sun's angular size; larger values give softer shadows",
    "tags": "environment|sky|sun|shadow|softness"
  },
  "world.set_atmosphere_intensity": {
    "summary": "Set the strength of atmospheric scattering in the physical sky",
    "tags": "environment|sky|haze|fog"
  },
  "world.get_thermal": {
    "summary": "Return the ambient thermal condition every uncoupled substance relaxes toward",
    "notes": "Distinct from world.get: that is the render sky, this is WorldThermalState -- the room temperature (ambient_kelvin), the Kelvin-per-normalized-unit calibration a substance's MSF temperature is read against (kelvin_per_unit), the passive-cooling multiplier (convection_coefficient) and the pyrolysis rate scale (oxygen_availability). Formerly no scripting surface existed for any of this -- see docs/dev/SIMULATION_NODE_OBJECT_MODEL.md section 7 item 1.",
    "returns": "WorldThermalInfo",
    "tags": "simulation|thermal|ambient|world|msf",
    "related": "world.set_thermal|sim_graph.list"
  },
  "world.set_thermal": {
    "summary": "Set the ambient thermal condition every uncoupled substance relaxes toward",
    "notes": "Every field optional: omit a key to leave it unchanged. oxygen_availability is clamped to 0..1 rather than rejected. A domain's own thermal_override_enabled (SimulationGridDomainDesc) still wins over this where it is set -- this call changes only the default a domain has not overridden.",
    "tags": "simulation|thermal|ambient|world|msf",
    "related": "world.get_thermal",
    "params": {
      "ambient_kelvin": {
        "description": "Room temperature everything relaxes toward, in Kelvin. Must be positive."
      },
      "kelvin_per_unit": {
        "description": "Kelvin per normalized solver unit -- the calibration MSF temperature reads through. Must be positive."
      },
      "convection_coefficient": {
        "description": "Scales every substance's passive cooling toward ambient: 1 = authored, higher = draughty, 0 = a perfect thermos."
      },
      "oxygen_availability": {
        "description": "0..1, scales pyrolysis burn rate; 0 smothers combustion entirely. Clamped, not rejected, if out of range."
      }
    }
  },
  "timeline.get_frame": {
    "summary": "Return the current timeline frame",
    "tags": "time|playhead",
    "related": "timeline.set_frame"
  },
  "timeline.set_frame": {
    "summary": "Move the timeline playhead to a frame",
    "notes": "This is how simulations are advanced from a script: stepping the frame runs the solvers that are live for that frame.",
    "tags": "time|playhead|simulate|advance",
    "related": "timeline.get_frame|sim_cache.bake"
  },
  "render.start": {
    "summary": "Render one frame at the given sample count and write it to an image file",
    "notes": "Blocking work runs on the render job; poll render.status for progress. The written image can be read back, so visual checks can be automated.",
    "tags": "output|image|final|save",
    "related": "render.status|render.cancel|render.start_sequence",
    "params": {
      "output_path": {
        "description": "Absolute path of the image file to write"
      },
      "spp": {
        "description": "Samples per pixel"
      }
    }
  },
  "render.start_sequence": {
    "summary": "Render a frame range to an output directory",
    "tags": "animation|output|batch",
    "related": "render.sequence_status|render.cancel_sequence"
  },
  "render.status": {
    "summary": "Report render job state, progress and current sample count",
    "returns": "RenderJobInfo",
    "tags": "progress"
  },
  "render.sequence_status": {
    "summary": "Report sequence render progress and current frame",
    "returns": "SequenceInfo",
    "tags": "progress|animation"
  },
  "render.cancel": {
    "summary": "Cancel the running single-frame render",
    "tags": "abort"
  },
  "render.cancel_sequence": {
    "summary": "Cancel the running sequence render",
    "tags": "abort|animation"
  },
  "render.probe": {
    "summary": "Measure a region of the last viewport frame: mean/min/max luminance, black fraction, NaN fraction and a histogram",
    "notes": "This is the measurement an agent verifies its own work with. `available: false` means no frame was captured - it is not a dark scene. A non-zero nan_fraction means a shader produced invalid pixels.",
    "tags": "measure|verify|luminance|black|nan|check",
    "related": "viewport.capture|viewport.render_frames|viewport.status",
    "returns": "ProbeInfo",
    "params": {
      "x": {
        "description": "Region origin in pixels"
      },
      "y": {
        "description": "Region origin in pixels"
      },
      "width": {
        "description": "Region width in pixels; 0 means to the right edge"
      },
      "height": {
        "description": "Region height in pixels; 0 means to the bottom edge"
      },
      "threshold": {
        "description": "Luminance at or below which a pixel counts as black"
      }
    }
  },
  "render.volume_stats": {
    "summary": "Return volume traversal counters for the last frame",
    "tags": "diagnostics|performance|volume"
  },
  "render.volume_counters": {
    "summary": "Enable or disable volume instrumentation counters",
    "tags": "diagnostics|performance|volume"
  },
  "render.volume_tables": {
    "summary": "Report the published volume SSBO state per backend (render / raster viewport), including whether the simulation compute device is that backend's own device",
    "returns": "VolumeTablesInfo",
    "tags": "diagnostics|volume|backend|verify",
    "notes": "The raster viewport can be a SECOND VulkanBackendAdapter with its own VkDevice and its own volume table. A 'viewport' row with instance_count 0 while 'render' is non-zero means the realtime viewport was never handed the volumes and will draw none — render.volume_stats cannot see this, because a pass that never ran counts zero exactly like a scene with no volumes. sim_device_is_this_backends false means live dense gas addresses are suppressed for that backend and it falls back to the per-adapter NanoVDB upload (a frozen grid, not a fault).",
    "related": "render.volume_stats|viewport.status"
  },
  "viewport.status": {
    "summary": "Report viewport backend, shading mode, resolution, sample count, capture state and whether a frame is available",
    "returns": "ViewportStatusInfo",
    "tags": "measure|verify|backend|samples",
    "related": "viewport.capture|render.probe"
  },
  "viewport.shading": {
    "summary": "Report which viewport shading mode is on screen and whether the interactive raster viewport exists",
    "notes": "interactive_available false means this build has no raster viewport (no Vulkan), so 'rendered' is the only selectable mode - a rejected viewport.set_shading there is the machine, not a bad request.",
    "tags": "viewport|shading|display|measure",
    "related": "viewport.set_shading|viewport.status"
  },
  "viewport.preview_lighting": {
    "summary": "Report realtime/material raster lighting mode, shadow allocation, world background and Nishita direct-sun status",
    "notes": "Canonical modes are 'scene' and 'three_point'. The default 'scene' reads the renderer's own light buffer; 'three_point' is the fixed material-inspection rig. Scene draws canonical world color/HDRI/Nishita behind geometry and uses it for ambient/specular. HDRI uses generated diffuse irradiance, GGX roughness prefilter mips and a split-sum BRDF LUT when world_ibl_ready is true; world_ibl_fallback explicitly reports the bounded raw-environment path. Nishita contributes a direct sun with its own directional atlas tile. 'shadowed_light_count' counts scene lights only; 'world_sun_shadow' reports the reserved sun tile. A gap from 'scene_light_count' means later lights still illuminate without an atlas tile. A gap between 'scene_light_count' and 'scene_light_total' means the preview light loop itself is clamped. 'material_preview_active' false means the mode is stored but the material raster viewport is not on screen. DISPLAY TRANSFORM: Rendered and raster preview use PostProcess/ColorMath.h. display_* reports the GPU transport; post.get reports user gain, while post.get_exposure reports the resolved adaptive gain. None/linear means clipping; Reinhard is explicit. Raster preview uses the resolved EV from the Rendered HDR meter and does not meter its own display-encoded image.",
    "tags": "viewport|preview|lighting|scene|lights|measure",
    "related": "viewport.set_preview_lighting|viewport.set_shading|lights.list"
  },
  "viewport.set_preview_lighting": {
    "summary": "Set realtime/material raster lighting to the default scene path or the three-point inspection rig",
    "notes": "'scene' uses scene lights plus the canonical world background/ambient; 'three_point' isolates material inspection from scene lighting. Directional, point, spot and area lights share a bounded shadow atlas; Nishita adds a direct world sun with one reserved directional tile. Physical Sky uses the Vulkan atmosphere LUT when available and an analytic fallback otherwise. HDRI uses generated irradiance, GGX prefilter mips and a BRDF LUT when ready, with an explicitly reported raw-environment fallback. Resets accumulation and only takes visible effect in 'material' shading mode.",
    "tags": "viewport|preview|lighting|scene|lights|shading",
    "related": "viewport.preview_lighting|viewport.set_shading|viewport.get_screenshot",
    "params": {
      "preset": {
        "enum": "three_point|scene",
        "description": "Canonical mode name. Realtime/real_lights aliases map to scene; legacy classic/studio/outdoor aliases map to three_point."
      }
    },
    "verify_with": [
      "viewport.preview_lighting"
    ]
  },
  "viewport.quality": {
    "summary": "Report the raster viewport quality preset and, as a value, whether scatter proxy substitution is active",
    "notes": "Read values rather than matching on the preset name. 'scatter_lod_split' changes geometry submission. Scene shadows keep one fixed 4096 atlas so preset changes never destroy an image referenced by an in-flight frame; shadow_tile_resolution, shadow_tile_capacity, shadow_light_budget and shadow_pcf_samples report the live bounded contract. directional_shadow_cascades is 2 for Performance and 3 otherwise; directional lights and the Physical Sky sun consume that many atlas tiles and select the smallest camera-centred projection containing the receiver. A scene light beyond the budget still illuminates but has no atlas shadow. 'scene_pbr_shader' reports the comparison shader (GGX for Scene). The material parity strings are an explicit capability contract: opaque_core_parity is RT-aligned; material_graph_surface is bounded because pointiness/named attributes/AO/time inputs are neutral in raster; clearcoat is an iridescent lobe using the RT thin-film model; subsurface is a radius/scale profile approximation; translucency is a bounded thin-surface approximation; surface_anisotropy is unsupported because the current ABI fields are also legacy water wave controls; transparency is unsorted_alpha; transmission is screen_space_thickness with frozen opaque color/depth, bounded screen-space reflection, closed-mesh front/back thickness, RT texture/graph/IOR/Fresnel/Beer/roughness/dispersion semantics and environment fallback; overlapping or off-screen continuation/reflection remains bounded; resin_interior is a procedural bounded approximation; sdf_surface=shared_nanovdb_depth_pbr means realtime reads Vulkan RT's same field/transform/iso threshold and writes real raster depth, while lighting and environment continuation remain bounded rather than recursive. 'raster_viewport_available' false means the preset is stored but nothing on this machine reads it.",
    "tags": "viewport|quality|lod|proxy|scatter|measure",
    "related": "viewport.set_quality|viewport.frame_telemetry"
  },
  "viewport.set_quality": {
    "summary": "Set the raster viewport quality preset; 'full' disables scatter proxy substitution entirely",
    "notes": "The preset controls both geometry and bounded Scene quality. Performance/Balanced/Quality select 256/512/1024 shadow tiles, 4/8/16 shadowed scene lights and 9/9/25 receiver PCF samples; Auto currently maps to Balanced. The atlas remains 4096 and is not reallocated. 'full' additionally turns scatter proxy substitution OFF so every visible instance draws its own mesh; frustum culling stays on. Rebuilds the raster scene and resets accumulation, so render frames again before probing.",
    "tags": "viewport|quality|lod|proxy|scatter|full|performance",
    "related": "viewport.quality|viewport.frame_telemetry|viewport.render_frames",
    "params": {
      "preset": {
        "enum": "auto|performance|balanced|quality|full",
        "description": "Canonical preset name. 'no_proxy' and 'full (no proxy)' are accepted aliases for full because that is what the panel combo reads."
      }
    },
    "verify_with": [
      "viewport.quality",
      "viewport.frame_telemetry"
    ]
  },
  "viewport.frame_telemetry": {
    "summary": "Report raster/Realtime viewport frame presentation telemetry: per-stage host timings, async frame-slot counters and stale-present count",
    "notes": "'available' false means NO raster frame has ever been presented (Rendered mode, no Vulkan viewport backend, or nothing drawn yet) - the missing fields are ABSENCE, not zeros. GEOMETRY SUBMISSION: 'gpu_culling' false while 'global_instance_buffer' is true means the scene is drawn with NO frustum culling and NO scatter proxy - every instance of every mesh, wherever the camera looks. That combination is a FAULT that looks CORRECT on screen and only shows up as cost, so check it before trusting any raster timing. 'visible_triangles'/'full_triangles'/'proxy_triangles'/'full_instances'/'proxy_instances' are read back from the GPU when gpu_culling is on and are therefore ONE FRAME BEHIND; identical on a still camera, one frame late while it moves. 'scatter_triangle_target' is a TARGET, not a hard cap: GPU culling converges a distance threshold toward it across frames, so a fast camera move can exceed it for a frame or two. 'async_present' false means the driver refused persistent frame slots and the old synchronous readback path is live; 'image_readback_ms' is non-zero ONLY there. COUNTERS COUNT BLOCKS, NOT CALLS (corrected 2026-09-02; earlier numbers are not comparable). 'stale_presents' means NO new frame could be consumed during a whole render pass - the viewer really is seeing pixels from before this pass. It used to be incremented by the opportunistic second consume that runs right after submit, which can essentially never succeed, so it tracked frame count in healthy async operation and read as 'the ring is permanently behind'. A one-frame delivery delay is what the async ring BUYS, not a fault. 'slot_waits' likewise counts only waits that actually blocked on an unsignalled fence; read it together with 'slot_wait_ms', which is the honest number and stays 0.0 when nothing blocked. viewport.capture(enabled=true) FORCES synchronous presentation so a probe reads the frame just recorded, so timings taken with capture on are not the interactive timings. 'resource_drains' counts host blocks caused by RESOURCE MUTATION (a buffer/descriptor edit that had to wait for submitted frames), and as of 2026-09-02 it increments ONLY when the wait actually blocked on an unsignalled fence - it used to count every call, including waits on already-signalled fences, so it reported a CALL COUNT and not a cost. Read it per submitted frame: above about 1 per frame means some edit path mutates GPU-read state every frame and the two-slot ring is producing no overlap at all, which shows up as 'stale_presents' tracking 'frames_submitted'. Numbers from before that fix are not comparable. 'host_read_ms' was ALSO looking at the wrong place until 2026-09-02: it timed only the opportunistic post-submit consume (which essentially never succeeds), so it read 0.00 and made the GPU-to-CPU readback look free. It now covers the top-of-pass consume that performs the real invalidate + full-frame memcpy. Any architecture decision about removing the readback needs THIS number, measured at the target resolution - the copy scales with pixel count, so a figure taken at 1680x945 does not describe 4K. DISPLAY PATH (added 2026-09-02): every timing above ends when the backend's renderProgressive returns, and the frame is NOT finished there - the main loop still rebuilds the display surface and uploads it to the SDL texture. Those two passes are 'display_post_ms' and 'display_texture_upload_ms', with 'display_loop_period_ms' as the real main-loop period; they are published ONLY on frames that were actually presented (skipped idle/background frames would otherwise average in as free). They are reported even when 'available' is false, because Rendered mode goes down the SAME display path - check 'display_available'. 'display_post_was_noop_copy' true means color processing was a no-op and the pass degenerated to a plain full-frame copy: still a full-frame cost, but REMOVABLE work rather than needed work. MEASURED 2026-09-02 at 3840x2160, material shading, Vulkan viewport: host_read_ms 3.06 + present_ms 2.79 = 5.84 ms of a 10.57 ms frame (55%), i.e. two 31.6 MB CPU copies per frame; the copy scales with pixel count, so numbers taken at 1080p are ~1/4 of these and must not be compared across resolutions.",
    "tags": "viewport|realtime|telemetry|performance|measure|frame|latency|present",
    "related": "viewport.status|perf.list|render.probe",
    "verify_with": [
      "viewport.status"
    ]
  },
  "viewport.get_screenshot": {
    "summary": "Return the captured viewport frame as a base64 JPEG, for a vision-capable model to look at",
    "notes": "Requires viewport.capture(enabled=true) and at least one rendered frame; an empty result means NOT CAPTURED, never 'the screen is empty'. Looking is not measuring - pair it with render.probe when the question is numeric (is there a black band, did the frame change), because a model reading a JPEG cannot tell 0.001 luminance from 0.",
    "tags": "viewport|screenshot|image|vision|look|see",
    "related": "viewport.capture|render.probe|viewport.render_frames",
    "requires": [
      "viewport.capture",
      "viewport.render_frames"
    ],
    "verify_with": [
      "render.probe"
    ]
  },
  "viewport.set_shading": {
    "summary": "Switch the viewport shading mode (solid, material, rendered, matcap) and optionally the matcap preset",
    "notes": "Resets accumulation like the panel buttons do, so probe AFTER switching - otherwise you measure the frame from the mode you left. Fails loudly when a mode is unavailable instead of silently falling back to rendered.",
    "tags": "viewport|shading|display|solid|rendered|matcap|preview",
    "related": "viewport.shading|viewport.render_frames|render.probe",
    "params": {
      "mode": {
        "enum": "solid|material|rendered|matcap",
        "description": "Canonical mode name. 'preview' is accepted as an alias for material because the panel button reads Preview."
      },
      "matcap_preset": {
        "description": "0..9, meaningful in matcap mode; -1 leaves it unchanged."
      }
    }
  },
  "viewport.capture": {
    "summary": "Turn viewport frame capture on or off",
    "notes": "Capture must be on before render.probe or the state summary can measure anything.",
    "tags": "measure|verify|enable",
    "related": "render.probe|viewport.render_frames"
  },
  "viewport.render_frames": {
    "summary": "Render a fixed number of viewport frames and report timing and convergence",
    "notes": "Use this to converge the viewport deliberately before probing it, instead of guessing how long to wait. Over IPC the captured frame is refreshed between calls by the display loop, so viewport.capture -> viewport.render_frames -> render.probe works. It does NOT work inside a single script.run_file: the script holds the main thread, the display loop never runs, and viewport.status frame_available stays false however many frames were rendered - measured 2026-08-19.",
    "tags": "measure|converge|samples|wait",
    "related": "render.probe|viewport.capture|viewport.status"
  },
  "material.list": {
    "summary": "List every material in the scene",
    "tags": "shading",
    "returns": "string[]"
  },
  "material.create": {
    "summary": "Create a material of the given type and return its name",
    "tags": "shading|create",
    "params": {
      "type": {
        "description": "Material type, e.g. 'principled'"
      }
    }
  },
  "material.assign": {
    "summary": "Assign an existing material to an object",
    "tags": "shading|apply"
  },
  "material.info": {
    "summary": "Return a material's full parameter set",
    "tags": "shading",
    "returns": "MaterialInfo"
  },
  "material.of_object": {
    "summary": "List the materials used by one object",
    "tags": "shading",
    "returns": "string[]"
  },
  "material.get": {
    "summary": "Read one material parameter of an object",
    "tags": "shading"
  },
  "material.set": {
    "summary": "Set one material parameter on an object (base_color, roughness, metallic, emission, ior, transmission, ...)",
    "notes": "Colour parameters take a 3-element array, scalars take a number. An unknown parameter name is an error, not a silent no-op.",
    "tags": "shading|colour|roughness|metallic|emission",
    "related": "material.get|material.info",
    "params": {
      "param": {
        "enum": "base_color|bubble_film|bubble_ior|dust_color_a|dust_color_b|dust_style|emission|emission_strength|ior|is_bubble|metallic|opacity|resin_color|resin_density|resin_dirt|resin_dirt_color|resin_inclusion|resin_inclusion_scale|resin_object_space|resin_roughness|resin_shard|resin_shard_hue|roughness|shard_shape|specular|transmission|uv_offset_x|uv_offset_y|uv_scale_x|uv_scale_y",
        "description": "Which Principled BSDF property to set. Colour keys (base_color, emission, resin_color, dust_color_a/b, resin_dirt_color) need an RGB value; the rest need a scalar. roughness, metallic, specular, transmission and opacity are clamped to [0,1]; ior to [1,10]."
      }
    }
  },
  "material.get_param": {
    "summary": "Read one material parameter directly by material name (not through an object)",
    "notes": "material.get reads through an OBJECT and averages nothing but still names an object; this names the material ASSET, which is what you want when several materials share one flat mesh (e.g. a carafe's glass body + brass cap + rubber foot) and you need the one that actually has that name.",
    "tags": "shading",
    "related": "material.set_param|material.get|material.info"
  },
  "material.set_param": {
    "summary": "Set one material parameter directly by material name (base_color, roughness, metallic, emission, ior, transmission, ...)",
    "notes": "material.set edits through an OBJECT: every Principled BSDF that object's flat meshes reference gets the same value, which is wrong the instant one object carries several DIFFERENT materials — a carafe's glass body + brass cap + rubber foot is one flat mesh, three materials, and object.set would smear transmission=1 onto the cap and foot too. This edits exactly the named material asset and nothing else that happens to share its object. Colour parameters take a 3-element array, scalars take a number; an unknown parameter name is an error, not a silent no-op. Not undoable, unlike material.set.",
    "tags": "shading|colour|roughness|metallic|emission",
    "related": "material.get_param|material.set|material.set_texture",
    "params": {
      "param": {
        "enum": "base_color|bubble_film|bubble_ior|dust_color_a|dust_color_b|dust_style|emission|emission_strength|ior|is_bubble|metallic|opacity|resin_color|resin_density|resin_dirt|resin_dirt_color|resin_inclusion|resin_inclusion_scale|resin_object_space|resin_roughness|resin_shard|resin_shard_hue|roughness|shard_shape|specular|transmission|uv_offset_x|uv_offset_y|uv_scale_x|uv_scale_y",
        "description": "Which Principled BSDF property to set. Colour keys (base_color, emission, resin_color, dust_color_a/b, resin_dirt_color) need an RGB value; the rest need a scalar. roughness, metallic, specular, transmission and opacity are clamped to [0,1]; ior to [1,10]."
      }
    }
  },
  "material.set_texture": {
    "summary": "Bind an image file to a material texture slot",
    "tags": "shading|texture|image"
  },
  "material.clear_texture": {
    "summary": "Clear one material texture slot",
    "tags": "shading|texture"
  },
  "material.textures": {
    "summary": "List a material's populated texture slots with the identity of each bound texture",
    "notes": "Returns {slot, texture} pairs. 'texture' is the texture's identity - a file path, or an 'embedded_<import>_<slot>_<type>' cache key for a texture that lived inside the model file. Two materials reporting the SAME texture share one decoded image: intended when a map is reused, a defect when the two materials came from different source files.",
    "tags": "shading|texture"
  },
  "fluid.create_domain": {
    "summary": "Create an APIC liquid or gas grid domain over a world-space box",
    "notes": "voxel_size sets both resolution and cost: halving it multiplies cell count by eight. A liquid domain does nothing until fluid.seed or a flow_source fills it. Changing voxel_size later invalidates the bake.",
    "tags": "simulation|liquid|water|smoke|fire|create|domain|grid",
    "related": "fluid.seed|fluid.set_param|flow_source.create|fluid.list_domains",
    "returns": "FluidDomainInfo",
    "params": {
      "name": {
        "description": "Domain name; also the object name in the scene"
      },
      "type": {
        "description": "Solver family",
        "enum": "fluid|gas"
      },
      "domain_min": {
        "description": "World-space AABB minimum in metres"
      },
      "domain_max": {
        "description": "World-space AABB maximum in metres"
      },
      "voxel_size": {
        "description": "Grid cell size in metres. Drives resolution, cost and the smallest feature the solver can resolve."
      }
    }
  },
  "fluid.list_domains": {
    "summary": "List every fluid and gas domain with its full settings",
    "tags": "simulation|inventory",
    "related": "fluid.get",
    "returns": "FluidDomainInfo[]"
  },
  "fluid.get": {
    "summary": "Return one fluid or gas domain's complete settings and live counters",
    "tags": "simulation",
    "returns": "FluidDomainInfo"
  },
  "fluid.remove_domain": {
    "summary": "Delete a fluid or gas domain",
    "tags": "simulation|remove"
  },
  "fluid.seed": {
    "summary": "Fill a liquid domain with particles - a given box, or the domain's lower half",
    "notes": "Omit seed_min and seed_max to fill the bottom half of the domain; there is no fixed default box any more, because the old one was derived from nothing and seeded zero particles in any domain that did not contain it. A region that does not overlap the domain is now refused with both boxes in the error, instead of succeeding having created nothing. particles_per_cell of 4 is the standard APIC density; a thin jet seeded below that has no interior cells and therefore no pressure field, which reads as 'water that will not splash'. persistent=true re-seeds the box every reset.",
    "tags": "simulation|liquid|water|fill|particles|initial",
    "related": "fluid.create_domain|fluid.clear|flow_source.create",
    "params": {
      "seed_min": {
        "description": "World-space box minimum in metres; omit to derive from the domain"
      },
      "seed_max": {
        "description": "World-space box maximum in metres; omit to derive from the domain"
      },
      "particles_per_cell": {
        "description": "Particle density per grid cell; 4 is standard"
      },
      "replace": {
        "description": "Clear existing particles first"
      },
      "persistent": {
        "description": "Re-apply this seed after every reset"
      }
    },
    "verify_with": [
      "fluid.get"
    ]
  },
  "fluid.clear": {
    "summary": "Remove the particles from a fluid domain",
    "tags": "simulation|reset"
  },
  "fluid.reset": {
    "summary": "Reset every fluid and gas simulation to frame zero",
    "tags": "simulation|reset|rewind"
  },
  "fluid.step": {
    "summary": "Advance the fluid solver by one timestep",
    "notes": "For scripted work prefer timeline.set_frame, which advances every solver consistently.",
    "tags": "simulation|advance"
  },
  "fluid.set_param": {
    "summary": "Update any field of a fluid or gas domain: bounds, voxel size, solver backend, viscosity, granular constitutive settings, render mode and surface material",
    "notes": "Overlay semantics - fields you do not send keep their value. Changing voxel_size or the bounds invalidates the bake for that domain.",
    "requires": [
      "fluid.create_domain"
    ],
    "next": [
      "timeline.set_frame"
    ],
    "verify_with": [
      "fluid.get",
      "render.probe"
    ],
    "invalidates": [
      "simulation_cache"
    ],
    "tags": "simulation|configure|viscosity|granular|render-mode|backend",
    "related": "fluid.get|fluid.set_substance_material"
  },
  "fluid.set_substance_material": {
    "summary": "Bind a substance (its physical identity, phase and representation) to a fluid domain",
    "notes": "Phase is what the matter IS; representation is how it is solved and drawn. They are separate axes - setting one does not imply the other.",
    "tags": "simulation|substance|material|phase|msf",
    "related": "msf.substances|fluid.set_splat_material|fluid.set_param",
    "params": {
      "domain": {
        "description": "Fluid domain name"
      },
      "substance": {
        "description": "Substance id from msf.substances"
      },
      "phase": {
        "description": "Physical phase",
        "enum": "liquid|solid|gas"
      },
      "representation": {
        "description": "How the substance is solved and rendered"
      }
    }
  },
  "fluid.set_splat_material": {
    "summary": "Set the material used for particle splat rendering of a domain",
    "tags": "simulation|render|appearance"
  },
  "fluid.get_combustion": {
    "summary": "Read a liquid domain's combustion settings",
    "tags": "simulation|fire|burn",
    "related": "fluid.set_combustion"
  },
  "fluid.set_combustion": {
    "summary": "Configure a liquid domain as a combustible fuel: ignition temperature, heat release, smoke yield and evaporation",
    "tags": "simulation|fire|burn|fuel|ignite|combustion",
    "related": "gas.set_settings|fluid.set_substance_material"
  },
  "gas.create_domain": {
    "summary": "Create a gas grid domain over a world-space box",
    "notes": "Same handler as fluid.create_domain with type defaulting to gas.",
    "tags": "simulation|smoke|fire|create|domain",
    "related": "gas.set_settings|flow_source.create",
    "returns": "FluidDomainInfo"
  },
  "gas.list_domains": {
    "summary": "List every gas and fluid domain with its settings",
    "tags": "simulation|inventory"
  },
  "gas.get": {
    "summary": "Return one gas domain's complete settings and live counters",
    "tags": "simulation"
  },
  "gas.remove_domain": {
    "summary": "Delete a gas domain",
    "tags": "simulation|remove"
  },
  "gas.get_settings": {
    "summary": "Read a gas domain's solver settings: fire, buoyancy, turbulence, quality and resource budget",
    "tags": "simulation|fire|smoke",
    "related": "gas.set_settings"
  },
  "gas.set_settings": {
    "summary": "Configure a gas domain's solver: fire and ignition, burn rate, heat release, buoyancy, turbulence, vorticity and resource budget",
    "notes": "This is the SOLVER. Appearance - colour, emission, density scale - lives in gas.set_shader, and the two are read back separately; a fire that simulates but looks wrong is usually a shader setting, not a solver setting.",
    "requires": [
      "gas.create_domain"
    ],
    "next": [
      "gas.set_shader",
      "flow_source.create",
      "timeline.set_frame"
    ],
    "verify_with": [
      "gas.get_settings",
      "render.probe"
    ],
    "invalidates": [
      "simulation_cache"
    ],
    "tags": "simulation|fire|smoke|ignite|burn|buoyancy|turbulence",
    "related": "gas.get_settings|gas.set_shader|flow_source.create",
    "params": {
      "fire_enabled": {
        "description": "Run combustion in this domain"
      },
      "ignition_temperature": {
        "description": "Temperature at which fuel ignites, in Kelvin"
      },
      "burn_rate": {
        "description": "How fast fuel is consumed once lit"
      },
      "heat_release": {
        "description": "Temperature added per unit of fuel burned"
      },
      "smoke_generation": {
        "description": "Smoke produced per unit of fuel burned"
      }
    }
  },
  "gas.get_shader": {
    "summary": "Read a gas domain's volume shading settings",
    "tags": "render|appearance|volume",
    "related": "gas.set_shader"
  },
  "gas.set_shader": {
    "summary": "Set a gas domain's volume appearance: preset, density and absorption/scattering, blackbody emission and the temperature range it maps",
    "notes": "temperature_min/max define the window mapped to emission colour; a flame outside that window renders black however hot it is.",
    "tags": "render|appearance|volume|fire|colour|emission|blackbody",
    "related": "gas.get_shader|gas.set_settings"
  },
  "gas.set_param": {
    "summary": "Update any field of a gas domain (same overlay setter as fluid.set_param)",
    "tags": "simulation|configure"
  },
  "gas.step": {
    "summary": "Advance the gas solver by one timestep",
    "tags": "simulation|advance"
  },
  "gas.reset": {
    "summary": "Reset every gas and fluid simulation to frame zero",
    "tags": "simulation|reset"
  },
  "gas.clear": {
    "summary": "Clear a gas domain's fields",
    "tags": "simulation|reset"
  },
  "gas.pressure_pulse": {
    "summary": "Inject a pressure pulse into a gas domain, as an explosion or blast wave",
    "tags": "simulation|explosion|blast|shockwave|pressure"
  },
  "gas.structural_impulse_stats": {
    "summary": "Report the blast impulses the gas solver handed to the structural/fracture side",
    "tags": "diagnostics|blast|fracture|coupling"
  },
  "flow_source.create": {
    "summary": "Create an emitter that feeds a fluid or gas domain with mass, heat, fuel or particles",
    "notes": "An emitter belongs to a domain by name. With a parent_object it follows that object, and velocity_space decides whether its velocity is read in world or parent-local space.",
    "tags": "simulation|emitter|source|inject|pour|jet|flame|inflow",
    "related": "flow_source.update|flow_source.list|fluid.create_domain|gas.set_settings",
    "returns": "FlowSourceInfo",
    "params": {
      "name": {
        "description": "Emitter name",
        "type": "string"
      },
      "domain": {
        "description": "Target fluid or gas domain name",
        "type": "string"
      },
      "source_mode": {
        "description": "Emission shape/source kind",
        "type": "string"
      },
      "source_object": {
        "description": "Object whose surface or volume emits",
        "type": "string"
      },
      "parent_object": {
        "description": "Object the emitter follows",
        "type": "string"
      },
      "position": {
        "description": "Emitter position; parent-local when parented",
        "type": "vec3"
      },
      "velocity": {
        "description": "Emitted velocity in metres per second",
        "type": "vec3"
      },
      "velocity_space": {
        "description": "Frame the velocity is expressed in",
        "type": "string"
      },
      "radius": {
        "description": "Emission radius in metres",
        "type": "float"
      },
      "temperature": {
        "description": "Injected temperature in Kelvin",
        "type": "float"
      },
      "fuel": {
        "description": "Injected fuel density",
        "type": "float"
      },
      "density": {
        "description": "Injected smoke or liquid density",
        "type": "float"
      },
      "fluid_substance": {
        "description": "Substance id emitted into a liquid domain",
        "type": "string"
      },
      "fluid_particles_per_second": {
        "description": "Liquid particle emission rate",
        "type": "float"
      },
      "enabled": {
        "description": "Emitter active",
        "type": "bool"
      }
    }
  },
  "flow_source.update": {
    "summary": "Update fields of an existing emitter, keeping everything you do not send",
    "notes": "Read-modify-write on purpose: sending only one field must not reset the rest.",
    "tags": "simulation|emitter|configure",
    "related": "flow_source.create|flow_source.get"
  },
  "flow_source.get": {
    "summary": "Return one emitter's full settings",
    "tags": "simulation|emitter",
    "returns": "FlowSourceInfo"
  },
  "flow_source.list": {
    "summary": "List every emitter with its settings",
    "tags": "simulation|emitter|inventory",
    "returns": "FlowSourceInfo[]"
  },
  "flow_source.remove": {
    "summary": "Delete an emitter",
    "tags": "simulation|emitter|remove"
  },
  "msf.substances": {
    "summary": "List the material substance library: every substance an object or fluid can be made of",
    "notes": "Substance ids from here are what fluid.set_substance_material and the combustion path expect.",
    "tags": "substance|material|library|wood|water|metal|thermochemistry",
    "related": "fluid.set_substance_material|msf.fields"
  },
  "msf.fields": {
    "summary": "Report the live material state fields (temperature, moisture, char, mass) per object",
    "notes": "This is the measurement side of burning and melting: mass loss, integrity and mass-conservation error are reported here.",
    "tags": "substance|thermal|burn|melt|measure|verify|temperature|moisture",
    "related": "msf.substances"
  },
  "physics.add_body": {
    "summary": "Give an object a rigid-body with a mass, motion type and collision shape",
    "tags": "simulation|rigid|collision|gravity|mass",
    "related": "physics.set_gravity|physics.get_body|physics.step",
    "params": {
      "object": {
        "description": "Object name"
      },
      "kind": {
        "description": "Body family, e.g. rigid"
      },
      "motion_type": {
        "description": "dynamic, kinematic or static"
      },
      "mass": {
        "description": "Mass in kilograms"
      },
      "shape": {
        "description": "Collision shape, e.g. box, sphere, mesh"
      }
    }
  },
  "physics.get_body": {
    "summary": "Return a rigid body's mass, damping, friction, restitution and motion type",
    "tags": "simulation|rigid"
  },
  "physics.remove_body": {
    "summary": "Remove an object's rigid body",
    "tags": "simulation|rigid|remove"
  },
  "physics.set_gravity": {
    "summary": "Set the world gravity vector in metres per second squared",
    "tags": "simulation|gravity"
  },
  "physics.step": {
    "summary": "Advance the physics solver by dt and move the playhead with it",
    "tags": "simulation|advance",
    "notes": "Advances the solver AND the timeline playhead, and claims the timeline until the user scrubs, plays or stops. It used to advance only the solver: the playhead stayed put, the frame loop found a rigid state that disagreed with the displayed frame, and it reset the runtime to the rest pose - erasing every scripted step while this call returned success. Check sim.control_state around a measurement to learn whether the claim was taken back.",
    "verify_with": [
      "sim.control_state"
    ],
    "next": [
      "scene.get_world_transform"
    ]
  },
  "physics.reset": {
    "summary": "Reset the rigid-body simulation to its initial state",
    "tags": "simulation|reset"
  },
  "physics.fracture_object": {
    "summary": "Shatter an object into Voronoi shards and return the shard objects",
    "notes": "site_count sets how many shards; exact_surface clips shards against the real surface instead of approximating it. The shards are ordinary objects afterwards - group them with physics.make_fracture_group so they hold together until something breaks them.",
    "tags": "destruction|shatter|break|voronoi|shards|debris",
    "related": "physics.make_fracture_group|physics.fracture_cluster_groups|physics.unfracture_object",
    "params": {
      "object": {
        "description": "Object to shatter",
        "type": "string",
        "required": true
      },
      "site_count": {
        "description": "Number of Voronoi sites, i.e. shards"
      },
      "pattern": {
        "description": "Fracture site distribution pattern"
      },
      "cluster_count": {
        "description": "Number of shard clusters to form"
      },
      "exact_surface": {
        "description": "Clip shards against the exact surface rather than an approximation"
      },
      "preview_gap": {
        "description": "Visual gap between shards in metres, for preview only"
      },
      "seed": {
        "description": "Random seed for reproducible shard layouts"
      }
    }
  },
  "physics.unfracture_object": {
    "summary": "Restore a fractured object back to its unbroken form",
    "tags": "destruction|undo|restore"
  },
  "physics.make_fracture_group": {
    "summary": "Bond shards into a fracture group that holds together until the break threshold is exceeded",
    "notes": "break_velocity is a velocity threshold, not an impulse - the group's mass is taken into account when the threshold is evaluated.",
    "tags": "destruction|group|bond|threshold|collapse",
    "related": "physics.fracture_object|physics.break_fracture_group|physics.apply_fracture_impulse"
  },
  "physics.break_fracture_group": {
    "summary": "Break a fracture group apart immediately with a given strength",
    "tags": "destruction|break|collapse"
  },
  "physics.apply_fracture_impulse": {
    "summary": "Apply an impulse at a point on a fracture group and report whether it broke",
    "tags": "destruction|impact|blast|impulse"
  },
  "physics.get_fracture_group": {
    "summary": "Report a fracture group's mass, integrity and break thresholds",
    "tags": "destruction|measure|verify"
  },
  "physics.fracture_cluster_groups": {
    "summary": "List or build the cluster groups of a fractured object",
    "tags": "destruction|group"
  },
  "forcefield.types": {
    "summary": "List the available force-field types",
    "tags": "simulation|wind|vortex|drag"
  },
  "forcefield.create": {
    "summary": "Create a force field of the given type and return its name",
    "tags": "simulation|wind|vortex|turbulence|gravity|create"
  },
  "forcefield.list": {
    "summary": "List the force fields in the scene",
    "tags": "simulation|inventory"
  },
  "forcefield.get": {
    "summary": "Return one force field's full settings",
    "tags": "simulation"
  },
  "forcefield.remove": {
    "summary": "Delete a force field",
    "tags": "simulation|remove"
  },
  "forcefield.set_param": {
    "summary": "Update a force field: shape, strength, falloff, noise, drag and which solvers it affects",
    "notes": "A force field is spatial - it is NOT attached to a domain. Which solvers feel it is decided by the affects_* flags.",
    "tags": "simulation|wind|turbulence|drag|configure",
    "related": "forcefield.evaluate"
  },
  "forcefield.evaluate": {
    "summary": "Evaluate the combined force fields at a world position and return the resulting acceleration",
    "notes": "The way to check a field really reaches a place, rather than inferring it from how a simulation looks.",
    "tags": "measure|verify|probe|force",
    "related": "forcefield.set_param"
  },
  "particle.list_systems": {
    "summary": "List the particle systems with their emitter, domain and collider counts",
    "tags": "particles|inventory"
  },
  "particle.emitters": {
    "summary": "List the particle emitters",
    "tags": "particles|inventory"
  },
  "particle.get_emitter": {
    "summary": "Return one particle emitter's full settings",
    "tags": "particles"
  },
  "particle.add_emitter": {
    "summary": "Add a particle emitter and return it",
    "tags": "particles|create|spawn|emit"
  },
  "particle.set_emitter": {
    "summary": "Update fields of a particle emitter, keeping what you do not send",
    "tags": "particles|configure"
  },
  "particle.remove_emitter": {
    "summary": "Remove a particle emitter",
    "tags": "particles|remove"
  },
  "particle.clear_emitters": {
    "summary": "Remove every particle emitter",
    "tags": "particles|remove"
  },
  "particle.clear_systems": {
    "summary": "Remove every particle system",
    "tags": "particles|remove"
  },
  "particle.clear": {
    "summary": "Delete the live particles without touching the emitters",
    "tags": "particles|reset"
  },
  "particle.spawn": {
    "summary": "Spawn one particle with an explicit position, velocity, size, mass and lifetime",
    "tags": "particles|manual|inject"
  },
  "particle.get_physics": {
    "summary": "Read the particle solver settings",
    "tags": "particles|solver"
  },
  "particle.set_physics": {
    "summary": "Set the particle solver: mode, quality, rest density, viscosity, cohesion, self-collision and grid deposit rates",
    "tags": "particles|solver|configure|sph"
  },
  "particle.stats": {
    "summary": "Report live particle counts and per-stage solver timings",
    "tags": "particles|measure|performance|verify"
  },
  "particle.step": {
    "summary": "Advance the particle solver by one timestep",
    "tags": "particles|advance"
  },
  "terrain.list": {
    "summary": "List the terrain objects",
    "tags": "landscape|inventory"
  },
  "terrain.get": {
    "summary": "Return one terrain's size, resolution, height scale and node-graph state",
    "tags": "landscape"
  },
  "terrain.create": {
    "summary": "Create a terrain heightfield of a given world size and resolution",
    "tags": "landscape|mountain|ground|create|heightmap",
    "related": "terrain.set_mesh_resolution|terrain.get|perf.list",
    "params": {
      "size": {
        "description": "World-space extent in metres"
      },
      "resolution": {
        "description": "Heightmap resolution in samples per side"
      },
      "height_scale": {
        "description": "Vertical scale in metres"
      }
    },
    "notes": "resolution is the FIELD grid: heights and every analysis product (slope, flow, erosion, analysisFields) live at that resolution. mesh_resolution is the separate VERTEX grid; 0 follows the field. Set it at creation rather than decimating afterwards - creating at 4096 and decimating still pays one full-resolution acceleration-structure build (measured 5.6 s Vulkan solid raster + 2.8 s Embree). With a 1024 mesh over a 4096 field the same build is 0.42 s + 0.15 s."
  },
  "terrain.remove": {
    "summary": "Delete a terrain",
    "tags": "landscape|remove"
  },
  "terrain.apply_preset": {
    "summary": "Apply a built-in terrain node preset and report links it could not wire",
    "tags": "landscape|preset|mountain|snow|river",
    "params": {
      "preset": {
        "description": "Preset name",
        "enum": "default|snow_layer|snowy_mountain_valley|river_network|biome_temperate|biome_lush|biome_alpine|biome_arid|biome_boreal|biome_foliage|geology_foundation"
      },
      "add_satmap": {
        "description": "Also add and wire a matching SatMap colorizer",
        "default": false
      }
    },
    "notes": "Returns wiring_faults: links the setup asked for and did not get. A refused link leaves the graph looking complete while the consumer falls back to a synthesized value, so an empty wiring_faults list is the only evidence the setup actually connected everything. wiring_fault_count > 0 means the resulting splat/biome masks are partly synthesized, not driven by the graph.",
    "verify_with": "nodes.list"
  },
  "terrain.list_satmap_presets": {
    "summary": "List data-driven multi-field SatMap preset recipes",
    "tags": "landscape|satmap|color|preset|flow|curvature"
  },
  "terrain.apply_satmap_preset": {
    "summary": "Build a SatMap node recipe from flow, slope, curvature and other terrain fields",
    "tags": "landscape|satmap|color|preset|flow|slope|curvature",
    "related": "terrain.list_satmap_presets|terrain.evaluate"
  },
  "terrain.erode": {
    "summary": "Run an erosion pass over a terrain; the hydraulic model brackets its droplet stages with the fluvial cycle - drainage-area feedback, lake spill and outlet incision, downstream sediment transport",
    "notes": "Every fluvial_* parameter defaults to leaving the solver setting alone, so tuning one dial does not reset the other twenty. The cycle is what produces a river hierarchy and deltas; with fluvial_cycle=0 erosion falls back to the local-slope droplet walk, which is statistically isotropic and leaves closed basins undrained. Check the result with terrain.erosion_stats rather than by eye.",
    "tags": "landscape|erosion|weathering|realism|river|delta|lake|sediment",
    "related": "terrain.erosion_stats|terrain.calculate_flow|terrain.evaluate",
    "verify_with": [
      "terrain.erosion_stats"
    ],
    "params": {
      "type": {
        "description": "Erosion model",
        "enum": "hydraulic|thermal|fluvial|wind"
      },
      "iterations": {
        "description": "Droplet count for the hydraulic model; 0 uses the model's own default"
      },
      "strength": {
        "description": "Erosion strength"
      },
      "talus_angle": {
        "description": "Repose angle for thermal erosion"
      },
      "fluvial_cycle": {
        "description": "Enable the landscape-evolution cycle (1/0); omit to keep the solver setting"
      },
      "fluvial_quality": {
        "description": "Convergence budget preset. Sets iteration count, relaxation passes, transport steps and pyramid depth ONLY - never the shape parameters - so raising it refines the same landscape instead of producing a different one. Applied before any explicit budget parameter in the same call, so you can pick a preset and override one dial of it.",
        "enum": "draft|balanced|high"
      },
      "fluvial_iterations": {
        "description": "Cycle iterations. Refines the solve; does NOT change how much material moves"
      },
      "fluvial_time_step": {
        "description": "Whole-cycle time multiplier: this is the dial for how much material moves"
      },
      "rain_rate": {
        "description": "Runoff depth per unit time; only its ratio to settling_velocity matters"
      },
      "orographic_rain": {
        "description": "Windward/lee rain split, 0..1. The second symmetry breaker after drainage area"
      },
      "rain_wind_degrees": {
        "description": "Prevailing wind bearing in degrees for orographic rain"
      },
      "incision_k": {
        "description": "Stream-power coefficient, in metres of incision at 1 km2 catchment and slope 1"
      },
      "stream_power_m": {
        "description": "Drainage-area exponent, near 0.5. This is what makes rivers cut faster than rills"
      },
      "stream_power_n": {
        "description": "Slope exponent, near 1.0"
      },
      "transport_k": {
        "description": "Sediment transport capacity coefficient, same units as incision_k"
      },
      "sediment_cover": {
        "description": "Cover effect 0..1: a bed already carrying its capacity is armoured"
      },
      "settling_velocity": {
        "description": "How readily suspended load drops. Larger settles sooner and shortens deltas"
      },
      "sediment_route_steps": {
        "description": "Cells of downstream sediment travel per iteration"
      },
      "drainage_refresh_interval": {
        "description": "Iterations between depression-fill and drainage-area re-solves"
      },
      "drainage_fill_passes": {
        "description": "GPU depression-fill budget per pyramid level. Too low leaves spurious lakes"
      },
      "drainage_accumulate_passes": {
        "description": "GPU area-accumulation budget. Too low under-counts long trunk rivers"
      },
      "drainage_coarsest_size": {
        "description": "Coarsest pyramid grid for the GPU drainage solve"
      },
      "flat_gradient": {
        "description": "Slope in m/m assumed for a depression-filled FLAT. A flat has no gradient of its own, so this is what water steers by there; 0 disables it and flat channels go back to straight, grid-locked and unable to incise"
      },
      "flat_resolve_passes": {
        "description": "GPU flat-resolution budget, one cell per pass: the widest flat in cells that resolves. Check unresolved_flat_cells in the stats, not the render"
      },
      "mass_wasting": {
        "description": "Enable in-loop landslides (1/0). Off means valley walls never collapse"
      },
      "repose_angle_degrees": {
        "description": "Angle of repose for mass wasting"
      },
      "mass_wasting_rate": {
        "description": "Fraction of the excess above repose shed per pass, 0..1"
      },
      "mass_wasting_steps": {
        "description": "Talus relaxation passes per iteration"
      },
      "hillslope_diffusion": {
        "description": "Creep coefficient in m2 over the whole cycle; rounds ridges, keeps channels"
      },
      "incision_safety": {
        "description": "Anti-pit limit: max fraction of the drop to the receiver cut in one step"
      },
      "deposition_safety": {
        "description": "Anti-spike limit: max fraction of the rise to the donor built in one step"
      },
      "max_step_meters": {
        "description": "Absolute per-step height change limit; 0 selects half a cell"
      },
      "lake_epsilon_meters": {
        "description": "Fill residue below this is not treated as standing water"
      },
      "headwater_area_km2": {
        "description": "Catchment area at which a cell counts as a channel"
      },
      "backend": {
        "description": "Compute backend; auto picks GPU when available"
      }
    }
  },
  "terrain.erosion_stats": {
    "summary": "Read the sediment mass ledger and drainage diagnostics of the last erosion run",
    "notes": "This is how an erosion result is checked without a screenshot, and the field to read FIRST is max_drainage_area_fraction: the largest catchment as a share of the map. Single digits mean the drainage graph is in fragments and no trunk river exists - a state that still renders as a convincing river network, which is why looking at the picture has repeatedly sent debugging the wrong way. max_drainage_area_km2 alone is NOT interpretable: 0.03 km2 is a shredded network on a 1 km terrain and a healthy trunk on a 100 m one. lake_cells and lake_area_fraction count ANY standing water including the depression-fill ladder's few-ulp lift, so they can report a quarter of the map and mean nothing; act on deep_lake_cells / deep_lake_area_fraction (10 cm threshold) and deepest_lake_meters instead. A high deep_lake_area_fraction on sloping terrain means the surface fed to the solver is genuinely full of pits - suspect the droplet stage, not the fill. mass_error_fraction away from zero means sediment transport is leaking. cycle_iterations == 0 is not a measurement, it is a cleared default.",
    "tags": "landscape|erosion|hydrology|measurement|validation|mass-balance|drainage|lakes",
    "related": "terrain.erode|terrain.calculate_flow|terrain.flow_authority|terrain.evaluate"
  },
  "terrain.evaluate": {
    "summary": "Evaluate the terrain node graph and bake the result into the heightfield",
    "tags": "landscape|bake|apply",
    "related": "terrain.evaluation_status|terrain.cancel_evaluation"
  },
  "terrain.evaluation_status": {
    "summary": "Report terrain evaluation progress and the node being processed",
    "tags": "landscape|progress"
  },
  "terrain.cancel_evaluation": {
    "summary": "Cancel the running terrain evaluation",
    "tags": "landscape|abort"
  },
  "terrain.sample_height": {
    "summary": "Sample terrain height at a world position",
    "tags": "landscape|measure|probe|placement"
  },
  "terrain.carve_river": {
    "summary": "Carve a river channel along a path into the terrain",
    "tags": "landscape|river|water|erosion"
  },
  "terrain.list_rivers": {
    "summary": "List the rivers carved into a terrain",
    "tags": "landscape|river|inventory"
  },
  "terrain.set_layer": {
    "summary": "Bind or edit one terrain layer slot",
    "notes": "Omit a field to leave it unchanged. material=\"\" CLEARS the slot; clearing a semantic overlay restores the built-in shading for that channel. overlay_strength applies to slots 4-7 only and is refused on 0-3, which are normalized against each other and have no independent strength.",
    "tags": "landscape|material|layer|splat|semantic|river",
    "related": "terrain.list_layers",
    "verify_with": "terrain.list_layers",
    "params": {
      "slot": {
        "description": "0-3 splat-weighted, 4-7 semantic overlay (4=Flow 5=Wetness 6=Ice 7=Hardness)"
      },
      "material": {
        "description": "Material name to bind; \"\" clears the slot; omit to leave unchanged"
      },
      "uv_scale": {
        "description": "Tiling scale; omit to leave unchanged"
      },
      "overlay_strength": {
        "description": "Overlay dial [0,1] for slots 4-7; omit to leave unchanged"
      }
    }
  },
  "terrain.slope_area_fit": {
    "summary": "Fit the slope-area relation over the channel network to test for real fluvial form",
    "notes": "Stream-power erosion gives S = k * A^-theta, so on log-log axes channel cells fall on a straight line. Noise, diffusion and sculpted relief scatter instead. This is the only measurement that separates 'an erosion pass ran' from 'the erosion pass produced fluvial form' - both render as plausible terrain. Read r_squared BEFORE concavity_index: a confident theta fitted to scatter is exactly the number that ends a debugging session early. Real landscapes sit near theta 0.4-0.6. MEASURED: the flow field is filled by terrain.calculate_flow ONLY. Evaluating the node graph does not fill it - the buffer ends up allocated, correctly sized and entirely zero, which used to sail past the size guard and answer too_few_channels, an empty input dressed as a statement about the landscape. Call terrain.calculate_flow first; status flow_field_empty now names that case and flow_peak reports what the fit actually read. status no_flow_field means the buffer is missing or the wrong size.",
    "tags": "landscape|hydrology|erosion|measurement|validation",
    "related": "terrain.calculate_flow|terrain.erosion_stats|terrain.flow_authority"
  },
  "terrain.flow_authority": {
    "summary": "Report which discharge field the graph's Flow node classified",
    "notes": "Two quantities were both called flow: DISCHARGE, the physical field the erosion sim measures against the terrain it is carving, and CHANNEL, a 0-1 selection of which cells read as a watercourse. Flow is now the single authority - measured discharge goes into it and every consumer reads out of it. source == 'derived_erosion_unwired' is the state nothing else reports: the graph HAS an erosion sim whose discharge is not wired into Flow, so channels come from bare geometry (no lakes, no infiltration, no erosion history) while the render shows the eroded surface, and the result still looks like a river network.",
    "tags": "landscape|hydrology|flow|measurement|authority",
    "related": "terrain.calculate_flow|terrain.erosion_stats|terrain.apply_preset"
  },
  "terrain.landform_stats": {
    "summary": "Measure the realised shape of a terrain: slope distribution, landform width, local-relief spread and hypsometry",
    "tags": "landscape|terrain|measure|diagnostic|shape|slope|relief",
    "related": "terrain.slope_area_fit|terrain.flow_authority|terrain.get",
    "notes": "Measured on the BAKED heightfield, never read back from a generator's own settings, so it can see the gap between what a node was asked for and what the terrain became. relief_window_meters / relief_window_relief are the relief-vs-window ladder itself; broad_growth is the relief added by its last doubling and is the sharpest read on whether the tile has landforms at its own scale - near 1.0 the field is already uncorrelated at a quarter of the tile and the map is one texture repeated (measured 1.05 with Feature Size 600 m on a 4096 m terrain, against 1.37 at 2048 m). landform_scale_meters is the ladder's growth knee, octave-quantised and NOT expected to equal the authored Feature Size. cliff_fraction is the area over 40 degrees - ground too steep to hold soil, so the share that must read as bare rock - and roughness_slope_ratio is cell-scale roughness on the steepest fifth over the gentlest fifth. Read those two TOGETHER: a terrain with no steep ground still scores a high ratio because its steepest fifth is only its least flat fifth. local_relief_ratio near 1 means every part of the map is equally rugged - no massifs, no basins. lowland_fraction below midland_fraction means the hypsometry is gaussian rather than depositional. spectrum_kink is macro-micro coherence in one number: the largest departure of any octave from the fitted power law. A landscape whose layers agree has ONE law from the grid to the landform, so a detail pass that does not match what it sits on, or a band guard quietly dropping octaves, shows up here and ONLY here - slope, relief and hypsometry all stay healthy while it is there. Under about 0.05 the scales are one landscape; spectrum_kink_meters names the window and spectrum_kink_signed says whether that scale is starved of detail (negative) or carrying too much (positive). realised_hurst is fitted only below landform_scale_meters; include the saturated windows and any exponent is dragged toward 0.5 regardless of the field.",
    "params": {
      "name": {
        "description": "Terrain name"
      }
    }
  },
  "terrain.calculate_flow": {
    "summary": "Reconstruct final-height flow diagnostics and report likely inland stalls",
    "notes": "Reconstructs a diagnostic accumulation field from the final heightmap, then returns channel cells with no greater-accumulation neighbour as inland_terminations. This does not inspect Watershed Analysis's authoritative direction/receiver raster (Hydraulic Erosion stopped publishing one when the compact port contract moved flow direction to its authoritative owner), so it detects wholesale depression-routing failures but is not an exact river-topology measurement. Use terrain.flow_authority to identify the graph's real source.",
    "tags": "landscape|water|hydrology|flow|measurement",
    "related": "terrain.erosion_stats|terrain.erode",
    "verify_with": "terrain.erosion_stats"
  },
  "terrain.import_heightmap": {
    "summary": "Import a heightmap image into a terrain",
    "tags": "landscape|import|heightmap"
  },
  "terrain.export_heightmap": {
    "summary": "Export a terrain heightfield to an image file",
    "tags": "landscape|export|heightmap"
  },
  "scatter.list_groups": {
    "summary": "List the scatter groups with their sources, counts and placement settings",
    "tags": "instancing|vegetation|inventory"
  },
  "scatter.create_group": {
    "summary": "Create a scatter group that instances objects over a target surface",
    "tags": "instancing|vegetation|forest|rocks|create"
  },
  "scatter.add_source": {
    "summary": "Add a source object that a scatter group may instance",
    "tags": "instancing|vegetation"
  },
  "scatter.fill": {
    "summary": "Populate a scatter group and report how many instances were spawned",
    "tags": "instancing|vegetation|populate|spawn"
  },
  "scatter.clear": {
    "summary": "Remove the instances of a scatter group, keeping the group",
    "tags": "instancing|reset"
  },
  "scatter.delete_group": {
    "summary": "Delete a scatter group",
    "tags": "instancing|remove"
  },
  "hair.list": {
    "summary": "List the hair grooms",
    "tags": "hair|fur|grass|inventory"
  },
  "hair.get": {
    "summary": "Return one hair groom's full settings",
    "tags": "hair"
  },
  "hair.create": {
    "summary": "Create a hair groom bound to a mesh",
    "tags": "hair|fur|grass|create"
  },
  "hair.remove": {
    "summary": "Delete a hair groom",
    "tags": "hair|remove"
  },
  "hair.rename": {
    "summary": "Rename a hair groom",
    "tags": "hair"
  },
  "hair.update": {
    "summary": "Update hair groom settings, keeping what you do not send",
    "tags": "hair|configure"
  },
  "hair.grow": {
    "summary": "Scale hair length by a factor",
    "tags": "hair|length|style"
  },
  "hair.trim": {
    "summary": "Trim hair strands",
    "tags": "hair|length|style"
  },
  "hair.comb": {
    "summary": "Comb hair strands towards a direction with a root stiffness",
    "tags": "hair|style|direction"
  },
  "hair.smooth": {
    "summary": "Smooth hair strand shapes",
    "tags": "hair|style"
  },
  "hair.restyle": {
    "summary": "Restyle a hair groom",
    "tags": "hair|style"
  },
  "hair.apply_preset": {
    "summary": "Apply a built-in hair preset to a groom",
    "tags": "hair|preset|style"
  },
  "hair.list_presets": {
    "summary": "List the available hair presets",
    "tags": "hair|preset"
  },
  "hair.bake": {
    "summary": "Bake a hair groom to its cached geometry",
    "tags": "hair|bake"
  },
  "hair.reset_simulation": {
    "summary": "Reset hair dynamics to the rest pose",
    "tags": "hair|simulation|reset"
  },
  "paint.ensure": {
    "summary": "Create or return the paint target (texture set) for an object",
    "tags": "texture|paint|setup"
  },
  "paint.get": {
    "summary": "Return an object's paint target: layers, channels and resolution",
    "tags": "texture|paint"
  },
  "paint.add_layer": {
    "summary": "Add a paint layer to an object's texture set",
    "tags": "texture|paint|layer"
  },
  "paint.remove_layer": {
    "summary": "Remove a paint layer",
    "tags": "texture|paint|layer"
  },
  "paint.duplicate_layer": {
    "summary": "Duplicate a paint layer",
    "tags": "texture|paint|layer"
  },
  "paint.move_layer": {
    "summary": "Reorder a paint layer in the stack",
    "tags": "texture|paint|layer"
  },
  "paint.update_layer": {
    "summary": "Set a paint layer's name, opacity, blend mode, visibility or lock",
    "tags": "texture|paint|layer"
  },
  "paint.merge_down": {
    "summary": "Merge a paint layer into the one below it",
    "tags": "texture|paint|layer"
  },
  "paint.flatten": {
    "summary": "Flatten every paint layer into one",
    "tags": "texture|paint|layer"
  },
  "paint.fill": {
    "summary": "Fill a paint layer channel with a value or colour",
    "tags": "texture|paint|fill"
  },
  "paint.clear_channel": {
    "summary": "Clear one channel of a paint layer",
    "tags": "texture|paint"
  },
  "paint.apply_mask": {
    "summary": "Apply a mask preset to a paint layer",
    "tags": "texture|paint|mask|wear|dirt"
  },
  "paint.list_mask_presets": {
    "summary": "List the available paint mask presets",
    "tags": "texture|paint|mask"
  },
  "paint.bake_height_to_normal": {
    "summary": "Bake a paint height channel into the normal channel",
    "tags": "texture|paint|bake|normal"
  },
  "paint.import_channel": {
    "summary": "Import an image into a paint layer channel",
    "tags": "texture|paint|import"
  },
  "paint.export_channel": {
    "summary": "Export a paint layer channel to an image file",
    "tags": "texture|paint|export"
  },
  "sculpt.get": {
    "summary": "Report an object's sculpt state: vertex count and mask range",
    "tags": "sculpt|mesh"
  },
  "sculpt.stroke": {
    "summary": "Apply a sculpt brush stroke to a mesh",
    "tags": "sculpt|mesh|deform|brush",
    "params": {
      "points": {
        "type": "array",
        "required": true,
        "description": "Stroke path: list of [x, y, z] world points. Required - a stroke with no points does nothing."
      }
    }
  },
  "sculpt.paint_mask": {
    "summary": "Paint the sculpt mask on a mesh",
    "tags": "sculpt|mask"
  },
  "sculpt.mask_operation": {
    "summary": "Run an operation over the sculpt mask (invert, clear, grow, ...)",
    "tags": "sculpt|mask"
  },
  "modifiers.get_stack": {
    "summary": "List an object's modifier stack",
    "tags": "mesh|modifier"
  },
  "modifiers.add": {
    "summary": "Add a modifier to an object and return the new stack entry",
    "tags": "mesh|modifier|subdivide|create"
  },
  "modifiers.remove": {
    "summary": "Remove a modifier from an object",
    "tags": "mesh|modifier|remove"
  },
  "modifiers.apply": {
    "summary": "Apply a modifier destructively into the mesh",
    "tags": "mesh|modifier|bake"
  },
  "modifiers.set_param": {
    "summary": "Set a modifier parameter: enabled, levels, render_levels or smooth_angle",
    "tags": "mesh|modifier|configure"
  },
  "nodes.graphs": {
    "summary": "List the node graphs that exist",
    "tags": "nodes|graph|inventory"
  },
  "nodes.create_graph": {
    "summary": "Create a node graph in a domain",
    "tags": "nodes|graph|create"
  },
  "nodes.remove_graph": {
    "summary": "Delete a node graph",
    "tags": "nodes|graph|remove"
  },
  "nodes.types": {
    "summary": "List every node type with its category and description",
    "tags": "nodes|catalogue"
  },
  "nodes.list": {
    "summary": "List the nodes of a graph with their inputs and outputs",
    "tags": "nodes|graph"
  },
  "nodes.add": {
    "summary": "Add a node of the given type to a graph",
    "tags": "nodes|graph|create"
  },
  "nodes.remove": {
    "summary": "Remove a node from a graph",
    "tags": "nodes|graph|remove"
  },
  "nodes.link": {
    "summary": "Connect one node's output to another node's input",
    "tags": "nodes|graph|connect|wire"
  },
  "nodes.link_by_key": {
    "summary": "Connect nodes using stable input and output port keys",
    "tags": "nodes|graph|connect|wire|port"
  },
  "nodes.list_ports": {
    "summary": "List stable node ports, exposure tiers, visibility and connection state",
    "tags": "nodes|graph|ports"
  },
  "nodes.set_port_visible": {
    "summary": "Show or hide an optional node port without changing evaluation",
    "tags": "nodes|graph|ports|configure"
  },
  "nodes.list_params": {
    "summary": "List a node's parameters, their types, values and whether they are driven by a link",
    "tags": "nodes|graph"
  },
  "nodes.get_param": {
    "summary": "Read one node parameter",
    "tags": "nodes|graph"
  },
  "nodes.set_param": {
    "summary": "Set one node parameter",
    "tags": "nodes|graph|configure"
  },
  "nodes.list_properties": {
    "summary": "List a node's non-parameter properties",
    "tags": "nodes|graph"
  },
  "nodes.get_property": {
    "summary": "Read one node property",
    "tags": "nodes|graph"
  },
  "nodes.set_property": {
    "summary": "Set one node property",
    "tags": "nodes|graph|configure"
  },
  "nodes.apply": {
    "summary": "Evaluate a node graph and apply its result, reporting errors and warnings",
    "tags": "nodes|graph|evaluate|bake"
  },
  "sim_graph.list": {
    "summary": "List the simulation node graphs with their scope, owner and whether the owner still exists",
    "notes": "owner_missing means the graph outlived the entity it was written for - it will not run.",
    "tags": "simulation|nodes|graph|inventory|scope"
  },
  "sim_graph.create": {
    "summary": "Create a simulation node graph for a scope and owner",
    "notes": "Scope and owner are mandatory and identify what the graph drives; a graph is not global.",
    "tags": "simulation|nodes|graph|create|scope"
  },
  "sim_graph.delete": {
    "summary": "Delete a simulation node graph",
    "tags": "simulation|nodes|remove"
  },
  "sim_graph.clear": {
    "summary": "Remove every node from a simulation graph",
    "tags": "simulation|nodes|reset"
  },
  "sim_graph.nodes": {
    "summary": "List a simulation graph's nodes, their channels, sources and restart requirements",
    "tags": "simulation|nodes|graph"
  },
  "sim_graph.add_node": {
    "summary": "Add a node of the given type to a simulation graph",
    "tags": "simulation|nodes|create"
  },
  "sim_graph.connect": {
    "summary": "Connect two nodes in a simulation graph",
    "tags": "simulation|nodes|connect|wire"
  },
  "sim_graph.set_node": {
    "summary": "Set a text-valued key on a simulation node",
    "tags": "simulation|nodes|configure"
  },
  "sim_graph.set_node_value": {
    "summary": "Set a numeric key on a simulation node",
    "tags": "simulation|nodes|configure"
  },
  "sim_graph.evaluate": {
    "summary": "Evaluate a simulation graph and report the commands it would issue, without applying them",
    "notes": "The graph DECLARES; the solver REPORTS. Evaluate first, then apply - that split is what makes a graph inspectable.",
    "tags": "simulation|nodes|evaluate|dry-run|inspect",
    "related": "sim_graph.apply|sim_graph.couplings"
  },
  "sim_graph.apply": {
    "summary": "Apply an evaluated simulation graph, reporting what was applied, refused or held",
    "tags": "simulation|nodes|apply|commit",
    "related": "sim_graph.evaluate"
  },
  "sim_graph.clear_overrides": {
    "summary": "Drop the overrides a simulation graph is holding",
    "tags": "simulation|nodes|reset"
  },
  "sim_graph.couplings": {
    "summary": "Report declared versus actually running couplings between simulation domains",
    "notes": "declared_not_running and running_not_declared are the two ways a graph and the live solvers can disagree.",
    "tags": "simulation|nodes|coupling|verify|diagnostics"
  },
  "sim_graph.domain_intersections": {
    "summary": "Report which force fields and colliders geometrically overlap a domain's box",
    "notes": "A MEASUREMENT, not a declared link - neither force fields nor colliders carry a domain field, because force is spatial. Bounding-volume overlap, not exact clipping: an obb collider's box ignores rotation (over-reports only, never hides a real intersection); mesh_sdf/convex/mesh_bvh colliders report measurable=false rather than a guessed non-intersection.",
    "tags": "simulation|nodes|forcefield|collider|measure|diagnostics"
  },
  "attr.list": {
    "summary": "List the attribute names that exist for an object/domain/world scope",
    "notes": "Unifies the old sim_graph.attributes (domain) and sim_graph.surface_attributes (object) into one surface that also covers world.",
    "tags": "simulation|attributes|discover",
    "related": "attr.stats"
  },
  "attr.stats": {
    "summary": "Measure one named attribute directly: count, min, max, mean",
    "notes": "available=false means the attribute could NOT be measured (no live particles, no MSF field, unknown name) - not the same as a value that measured zero. A world field is a single scalar: min/max/mean all read the same number.",
    "tags": "simulation|attributes|measure",
    "related": "attr.list"
  },
  "sim_cache.status": {
    "summary": "Report the simulation cache: valid range, frames in RAM, cache directory and the config signature",
    "notes": "The config signature is what decides whether a bake is still valid; when it changes, cached frames belong to a different setup.",
    "tags": "simulation|cache|bake|verify"
  },
  "sim_cache.bake": {
    "summary": "Bake a frame range of the simulation to the cache",
    "tags": "simulation|cache|bake"
  },
  "sim_cache.clear": {
    "summary": "Clear the simulation cache",
    "tags": "simulation|cache|reset"
  },
  "debris.configure": {
    "summary": "Enable and configure the ash/debris particle system",
    "tags": "fire|ash|debris|particles"
  },
  "debris.emit_ash": {
    "summary": "Emit ash debris of a given mass at a point",
    "tags": "fire|ash|debris|emit"
  },
  "debris.stats": {
    "summary": "Report ash debris counts, accepted mass and budget rejections",
    "tags": "fire|ash|measure"
  },
  "anim.characters": {
    "summary": "List the animated characters",
    "tags": "animation|inventory"
  },
  "anim.character": {
    "summary": "Return one character's animation state",
    "tags": "animation"
  },
  "anim.clips": {
    "summary": "List a character's animation clips with frame ranges and loop flags",
    "tags": "animation|clip"
  },
  "anim.source_clips": {
    "summary": "List the RAW imported animation clips with channel and key counts",
    "notes": "SceneData::animationDataList as the LOADER produced it - not a character's AnimationController state (that is anim.clips). This is the acceptance instrument for replacing the Assimp importer: the same file must yield identical channel and key counts on the old and the new path, and 'it still animates' is not a measurement. A channel is one animated node name; position_keys/rotation_keys/scaling_keys are summed over all channels. Times are in TICKS (duration_ticks, first_key_time, last_key_time); divide by ticks_per_second for seconds. first_key_time > last_key_time would mean a reader emitted unsorted keys - the sampling code assumes time-sorted keys.",
    "tags": "animation|clip|import|acceptance|assimp|gltf|channels|keys",
    "related": "anim.source_channels|anim.clips|scene.import_model",
    "returns": "Array of objects with name, model_name, duration_ticks, ticks_per_second, duration_seconds, start_frame, end_frame, position_channels, rotation_channels, scaling_channels, position_keys, rotation_keys, scaling_keys, first_key_time, last_key_time",
    "undoable": false
  },
  "anim.source_channels": {
    "summary": "Per-node key counts for one raw imported clip",
    "notes": "The breakdown behind anim.source_clips, sorted by node name so two runs diff without post-processing. Kept separate because a rigged character has hundreds of nodes and the clip totals answer most questions; reach for this when the totals match but the pose does not, which means the keys landed on different NODES.",
    "tags": "animation|clip|channels|bones|import|acceptance",
    "related": "anim.source_clips",
    "returns": "Array of objects with node_name, position_keys, rotation_keys, scaling_keys",
    "undoable": false,
    "params": {
      "clip": {
        "description": "Clip name; empty or omitted uses the first imported clip"
      }
    }
  },
  "anim.play": {
    "summary": "Play an animation clip on a character layer with a blend time",
    "tags": "animation|play|clip"
  },
  "anim.stop": {
    "summary": "Stop playback on a character layer with a blend-out time",
    "tags": "animation|stop"
  },
  "anim.set_paused": {
    "summary": "Pause or resume a character's animation",
    "tags": "animation|pause"
  },
  "anim.set_time": {
    "summary": "Set playback time in seconds on a character layer",
    "tags": "animation|time|scrub"
  },
  "anim.set_speed": {
    "summary": "Set playback speed on a character layer",
    "tags": "animation|speed"
  },
  "anim.set_loop": {
    "summary": "Set whether a character layer loops",
    "tags": "animation|loop"
  },
  "anim.status": {
    "summary": "Report a character layer's playback state",
    "tags": "animation|progress"
  },
  "anim.graph_status": {
    "summary": "Report a character's animation graph state",
    "tags": "animation|graph|statemachine"
  },
  "anim.set_graph_param": {
    "summary": "Set a float or bool parameter on a character's animation graph",
    "tags": "animation|graph|parameter"
  },
  "anim.trigger_graph_param": {
    "summary": "Fire a trigger parameter on a character's animation graph",
    "tags": "animation|graph|trigger"
  },
  "anim.insert_key": {
    "summary": "Insert a keyframe on an object channel at a frame",
    "tags": "animation|keyframe|channel",
    "params": {
      "channel": {
        "description": "Animated channel",
        "enum": "location|rotation|scale"
      }
    }
  },
  "anim.remove_key": {
    "summary": "Remove an object's keyframe at a frame",
    "tags": "animation|keyframe"
  },
  "anim.list_keys": {
    "summary": "List an object's keyframes",
    "tags": "animation|keyframe"
  },
  "post.get": {
    "summary": "Read the post-processing settings: exposure, gamma, tone mapping, saturation, vignette and stylize",
    "tags": "post|grade|look"
  },
  "post.set_exposure": {
    "summary": "Set post-process exposure",
    "notes": "Linear gain in [0,65504]. Multiplies the EV/physical/auto exposure selected by post.configure_exposure. Does not reset sample accumulation.",
    "tags": "post|brightness|grade"
  },
  "post.set_gamma": {
    "summary": "Set post-process gamma",
    "tags": "post|grade"
  },
  "post.set_tone_mapping": {
    "summary": "Select AgX, ACES Fitted, Uncharted, Hejl Filmic, Linear or Reinhard",
    "notes": "Shared CPU/CUDA/GLSL scene-linear Rec.709 implementation. ACES is a fitted approximation, not a full ACES/OCIO output transform. None/linear clips without tone mapping. Old AGX/ACES looks are intentionally replaced.",
    "tags": "post|tonemap|color",
    "params": {
      "type": {
        "enum": "agx|aces_fitted|uncharted|filmic|linear|reinhard"
      }
    }
  },
  "post.set_saturation": {
    "summary": "Set post-process saturation",
    "tags": "post|grade|colour"
  },
  "post.set_color_temperature": {
    "summary": "Set scene-linear Bradford white balance",
    "notes": "Finite [4000,25000] Kelvin, daylight-locus source white adapted to D65. 6500 K is exactly neutral. Applied before the view transform. Higher Kelvin warms.",
    "tags": "post|whitebalance|color"
  },
  "post.set_vignette_enabled": {
    "summary": "Enable or disable the vignette",
    "tags": "post|look"
  },
  "post.set_vignette_strength": {
    "summary": "Set vignette strength",
    "tags": "post|look"
  },
  "post.set_stylize_enabled": {
    "summary": "Enable or disable the stylize pass",
    "tags": "post|look|npr"
  },
  "post.set_stylize_strength": {
    "summary": "Set stylize pass strength",
    "tags": "post|look|npr"
  },
  "project.path": {
    "summary": "Return the current project file path",
    "tags": "project|file"
  },
  "project.open": {
    "summary": "Open a project file, replacing the current scene",
    "tags": "project|file|load"
  },
  "project.save": {
    "summary": "Save the project, optionally to a new path",
    "tags": "project|file|save"
  },
  "script.run_file": {
    "summary": "Run a Python script file inside the application",
    "tags": "script|python|automation"
  },
  "templates.list": {
    "summary": "List the available scene templates",
    "tags": "template|starter|scene"
  },
  "templates.get": {
    "summary": "Return one template's metadata",
    "tags": "template"
  },
  "templates.open": {
    "summary": "Open a template as the current scene",
    "tags": "template|load|start"
  },
  "templates.prepare": {
    "summary": "Run a template's preflight without opening it",
    "tags": "template|preflight|validate"
  },
  "templates.validate": {
    "summary": "Validate a template and report the errors that would block it",
    "tags": "template|validate"
  },
  "templates.refresh": {
    "summary": "Rescan the template directories",
    "tags": "template|reload"
  },
  "templates.save_user": {
    "summary": "Save the current scene as a user template",
    "tags": "template|save"
  },
  "templates.delete_user": {
    "summary": "Delete a user template",
    "tags": "template|remove"
  },
  "templates.show_hub": {
    "summary": "Show the Template Hub window",
    "tags": "template|ui"
  },
  "templates.hide_hub": {
    "summary": "Hide the Template Hub window",
    "tags": "template|ui"
  },
  "templates.is_hub_visible": {
    "summary": "Report whether the Template Hub is visible",
    "tags": "template|ui"
  },
  "editor.get_state": {
    "summary": "Report which editors are open, the node editor's domain and the simulation graph scope being shown",
    "notes": "Editor state is a VALUE, so it travels over IPC. Panel drawing does not - that is the rt.ui exemption.",
    "tags": "ui|editor|state|context"
  },
  "editor.set_bottom_editor": {
    "summary": "Choose which editor fills the bottom dock",
    "tags": "ui|editor"
  },
  "editor.set_node_domain": {
    "summary": "Choose which domain's graph the node editor shows",
    "tags": "ui|editor|nodes",
    "params": {
      "name": {
        "description": "Node editor domain",
        "enum": "material|geometry|terrain"
      }
    }
  },
  "editor.set_sim_graph_scope": {
    "summary": "Choose which scope and owner the simulation node editor shows",
    "tags": "ui|editor|simulation|scope"
  },
  "addons.list": {
    "summary": "List the installed addons with their load state",
    "tags": "plugin|extension"
  },
  "addons.enable": {
    "summary": "Enable an addon module",
    "tags": "plugin|extension"
  },
  "addons.disable": {
    "summary": "Disable an addon module",
    "tags": "plugin|extension"
  },
  "addons.reload": {
    "summary": "Reload an addon module from disk",
    "tags": "plugin|extension|develop"
  },
  "ipc.admin.tokens.list": {
    "summary": "List the remote IPC access tokens and their capabilities",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.create": {
    "summary": "Create a remote IPC token with a capability mask and return the raw secret once",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.update": {
    "summary": "Update a token's name, capabilities, expiry or address allowlist",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.rotate": {
    "summary": "Rotate a token's secret and return the new one once",
    "tags": "security|admin|token"
  },
  "ipc.admin.tokens.revoke": {
    "summary": "Revoke a remote IPC token",
    "tags": "security|admin|token"
  },
  "ipc.admin.sessions.list": {
    "summary": "List the connected IPC sessions with traffic counters",
    "tags": "security|admin|session"
  },
  "ipc.admin.sessions.get": {
    "summary": "Return one IPC session's details",
    "tags": "security|admin|session"
  },
  "ipc.admin.sessions.disconnect": {
    "summary": "Disconnect one IPC session",
    "tags": "security|admin|session"
  },
  "ipc.admin.sessions.disconnect_all": {
    "summary": "Disconnect every IPC session except the caller",
    "tags": "security|admin|session"
  },
  "ipc.admin.audit.list": {
    "summary": "Return recent IPC audit events: method, caller, outcome and duration",
    "tags": "security|admin|audit|log"
  },
  "ipc.admin.audit.clear": {
    "summary": "Clear the IPC audit log",
    "tags": "security|admin|audit"
  },
  "ipc.admin.audit.export": {
    "summary": "Export the IPC audit log to a JSONL file",
    "tags": "security|admin|audit|export"
  },
  "sim.control_state": {
    "summary": "Who is currently driving the solvers, and an epoch that changes whenever anything re-poses them",
    "notes": "Read epoch before and after a measurement. If it changed, the solvers were re-posed under you - the user scrubbed, playback ran, or a reset fired - and the numbers you just read are not a physics result. Without this a reverted pose and a body that never moved read exactly the same. driver says which of those it was; script_driving is true while physics.step holds the timeline, which it keeps only until the user scrubs, plays or stops. dropped_seeds names any fluid domain whose one-shot fluid.seed() (no persistent=true) the most recent auto-reset wiped and did NOT refill -- that auto-reset fires from a config-signature change ANYWHERE in the scene (e.g. an unrelated fluid.create_domain), so a domain's particles can go to zero with no error at all; check this field when that happens instead of assuming a reader fault.",
    "tags": "simulation|control|epoch|driver|timeline|measure|verify|fluid|seed",
    "related": "physics.step|timeline.set_frame|timeline.get_frame|scene.get_world_transform|fluid.seed|fluid.create_domain"
  },
  "perf.list": {
    "summary": "List every recorded build/render timing section, newest write first",
    "notes": "Sections are named by the code that performs the work. BUILD sections: terrain.graph.evaluate/.height/.aux_outputs/.finalize_mesh, terrain.mesh_fill/.create/.update/.publish_fields, terrain.splat_resize, Renderer::rebuildBVH(...), Renderer::rebuildBackendGeometry(GPU), accel.vulkan_solid.raster_geometry. PER-FRAME sections (added 2026-09-03), all prefixed loop.: loop.frame is the whole main-loop iteration and the others are its parts - loop.events (SDL event pump), loop.ui_draw (building every ImGui panel), loop.viewport_render (the backend's renderProgressive; loop.viewport_render_cpu for the legacy CPU path), loop.display_post (original_surface to display surface), loop.imgui_render (building draw lists), loop.present (SDL_RenderClear/Copy/RenderPresent - a vsync wait, if any, lands HERE, so a large value must be read as 'work OR waiting'), loop.throttle_sleep and loop.frame_tail (deferred geometry/BLAS/TLAS rebuilds and the rest of the iteration). WHY THIS EXISTS: before it, only the backend frame and two display copies were timed. Measured on a 695k-triangle interior at 1680x945, the backend frame was 1.2 ms while the loop period was 17.8-26 ms - about 95% of the frame was in code no instrument looked at, so a 142 ms frame could not be decomposed at all. READ loop.throttle_sleep BEFORE CONCLUDING ANYTHING: the main loop deliberately sleeps 16 ms (tier1) or 48 ms (tier2) when the app is not in tier0, and tier0 requires camera_moved, which is set ONLY from mouse/keyboard. An IPC-driven session therefore never reaches tier0, so every frame rate a script measures includes that sleep and is NOT the engine's throughput. Use max_ms to catch the rare slow frame - the mean dissolves it - and call perf.reset first to open a clean window. Each section carries last/total/max ms and a call count. 'rss_measured' says whether the working-set fields mean anything: the loop.* sections set it false because a per-frame scope must not pay for two GetProcessMemoryInfo calls, and their last_rss_delta_mb/rss_after_mb are ABSENCE, not a measurement of zero.",
    "tags": "performance|timing|profile|measure|slow|memory",
    "related": "perf.get|perf.reset|perf.set_logging",
    "returns": "PerfSection[]"
  },
  "perf.get": {
    "summary": "Read one timing section by name",
    "notes": "Returns found:false when the section has never been recorded. It never answers with zeros: a zeroed timing reads as 'measured, and it was free', which turns a missing measurement into a false one. 'rss_measured' false means the working-set fields are absence rather than zero - per-frame sections (loop.*) skip the working-set query so the instrument does not distort the section it measures. See perf.list for the loop.* section map and for why loop.throttle_sleep must be read before any frame-rate conclusion.",
    "tags": "performance|timing|profile|measure",
    "related": "perf.list",
    "returns": "PerfSection"
  },
  "perf.reset": {
    "summary": "Clear the timing counters before a measured run",
    "notes": "Write ordering (seq) stays monotonic across a reset, so a section recorded after it cannot be confused with a stale one. Changes no scene, render or file state - which is why it carries the read capability. Call it before a measured run: max_ms never decays, so one scene-load frame keeps dominating the table until it is cleared.",
    "tags": "performance|timing|profile|reset",
    "related": "perf.list|perf.get"
  },
  "perf.set_logging": {
    "summary": "Also mirror completed timing sections into the Scene Log",
    "notes": "Off by default. The registry is the readable surface - script and IPC callers cannot read the Scene Log, which is exactly why the previous profiler produced no usable numbers.",
    "tags": "performance|timing|profile|log",
    "related": "perf.list"
  },
  "terrain.set_mesh_resolution": {
    "summary": "Set the terrain vertex-grid resolution independently of the field (analysis) resolution",
    "notes": "0 means the mesh follows the field, which is the historical behaviour. The field grid (heights, slope, flow, erosion, every analysisFields entry) is untouched; only the triangle count changes, and analysis fields are resampled onto the coarser vertex grid rather than dropped. Measured at 4096^2: mesh fill is 380 ms but the acceleration structures built from its 33.5 M triangles cost 6.4 s (Vulkan solid raster) + 2.6 s (Embree), and that cost is linear in triangle count - a 1024 mesh over a 4096 field is roughly 18x cheaper end to end. Normals are still sampled from the field, so shading detail largely survives; the silhouette does not. A value above the field resolution is REFUSED rather than clamped.",
    "tags": "terrain|mesh|resolution|lod|performance|triangles|decimate",
    "related": "terrain.get|terrain.create|perf.list",
    "returns": "TerrainInfo",
    "params": {
      "name": {
        "description": "Terrain name",
        "required": true,
        "type": "string"
      },
      "mesh_resolution": {
        "description": "Vertices per side for the mesh grid, or 0 to follow the field resolution",
        "required": true,
        "type": "int"
      }
    }
  },
  "spline.list": {
    "summary": "List editable 2D spline sources and their curve types",
    "notes": "Returns names, plane, closed state and control-point counts. This is a read-only scene query.",
    "tags": "spline|curve|profile|control-point|authoring",
    "related": "spline.get|spline.set|spline.insert_point|spline.subdivide|spline.extrude",
    "returns": "SplineInfo[]"
  },
  "spline.create": {
    "summary": "Create an editable spline source through the canonical scene-object service",
    "notes": "Returns the unique scene name. The operation is undoable and produces the same SplineObject used by the viewport UI, Python and Geometry Nodes. primitive also accepts 'empty' (no points) and plane also accepts 'free' - the pair that starts a drawn route, since a profile is planar but a path is not.",
    "tags": "spline|curve|create|authoring|undo",
    "related": "spline.list|spline.get|scene.delete|nodes.create_graph",
    "returns": "string",
    "undoable": true,
    "params": {
      "primitive": {
        "description": "Initial spline shape",
        "type": "string",
        "enum": "circle|rectangle|open_line|open_arc",
        "default": "open_line"
      },
      "name": {
        "description": "Requested object name; made unique when necessary",
        "type": "string",
        "default": "Spline"
      },
      "plane": {
        "description": "Authoring plane",
        "type": "string",
        "enum": "xy|xz|yz",
        "default": "xy"
      }
    }
  },
  "spline.get": {
    "summary": "Read the versioned JSON authoring payload of a spline",
    "notes": "The payload includes curve type, plane, closed state, render transform, pivot_offset, optional cubic B-Spline knots and all point/handle data.",
    "tags": "spline|serialize|profile|curve",
    "related": "spline.set|spline.list",
    "returns": "SplinePayload",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      }
    }
  },
  "spline.set": {
    "summary": "Replace a spline authoring payload with validated JSON data",
    "notes": "Rejects unknown curve types, malformed points, invalid transforms, pivot_offset or knot vectors, and B-Splines with fewer than four controls.",
    "tags": "spline|serialize|edit|profile",
    "related": "spline.get|spline.insert_point|spline.subdivide",
    "returns": "bool",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "spline": {
        "description": "Versioned spline payload object",
        "required": true,
        "type": "object"
      }
    }
  },
  "spline.insert_point": {
    "summary": "Insert a control point on a spline segment at normalized parameter t",
    "notes": "Bezier insertion preserves the curve with De Casteljau; Linear insertion splits the segment; cubic B-Spline insertion uses shape-preserving Boehm knot insertion.",
    "tags": "spline|insert|subdivide|bezier|linear",
    "related": "spline.subdivide|spline.get",
    "returns": "{index:int}",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "segment": {
        "description": "Zero-based segment start index",
        "required": true,
        "type": "int"
      },
      "t": {
        "description": "Segment parameter in [0,1]",
        "required": true,
        "type": "float"
      }
    }
  },
  "spline.subdivide": {
    "summary": "Subdivide one or more spline segments with a fixed cut count",
    "notes": "Segments are processed in descending index order so a batch request remains stable. The operation is validated as one mutation.",
    "tags": "spline|subdivide|batch|curve",
    "related": "spline.insert_point|spline.get",
    "returns": "{last_index:int}",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "segments": {
        "description": "Segment start indices",
        "required": true,
        "type": "int[]"
      },
      "cuts": {
        "description": "Cuts per segment, from 1 to 128",
        "required": true,
        "type": "int"
      }
    }
  },
  "spline.extrude": {
    "summary": "Extrude an open spline endpoint to a new control point",
    "notes": "Only endpoint 0 or the final point is accepted, and closed splines are rejected.",
    "tags": "spline|extrude|endpoint|profile",
    "related": "spline.get|spline.insert_point",
    "returns": "{index:int}",
    "params": {
      "name": {
        "description": "Spline object name",
        "required": true,
        "type": "string"
      },
      "endpoint": {
        "description": "0 for first endpoint or last point index",
        "required": true,
        "type": "int"
      },
      "position": {
        "description": "New control point position",
        "required": true,
        "type": "float[3]"
      }
    }
  },
  "spline.keyframe.insert": {
    "summary": "Capture spline object transform and control-point deformation at a timeline frame",
    "notes": "Point keys include Linear, Bezier and B-Spline controls, Bezier handles, radius/user data and color. Point count, curve type, open/closed state and knot topology must remain stable between keys.",
    "tags": "spline|animation|keyframe|deform|hose|cable|radius",
    "related": "spline.keyframe.list|spline.keyframe.remove|timeline.set_frame|spline.get",
    "returns": "bool",
    "params": {
      "name": {
        "description": "Spline object name",
        "type": "string",
        "required": true
      },
      "frame": {
        "description": "Non-negative timeline frame",
        "type": "int",
        "required": true
      },
      "object_transform": {
        "description": "Capture the spline object's location, rotation and scale",
        "type": "bool",
        "default": true
      },
      "points": {
        "description": "Capture all control positions, handles, radius/user data and colors",
        "type": "bool",
        "default": true
      }
    }
  },
  "spline.keyframe.remove": {
    "summary": "Remove spline transform and/or point channels from one timeline key",
    "notes": "Other keyframe domains stored at the same frame are preserved.",
    "tags": "spline|animation|keyframe|remove",
    "related": "spline.keyframe.insert|spline.keyframe.list",
    "returns": "bool",
    "params": {
      "name": {
        "description": "Spline object name",
        "type": "string",
        "required": true
      },
      "frame": {
        "description": "Timeline frame containing the key",
        "type": "int",
        "required": true
      },
      "object_transform": {
        "description": "Remove the object transform portion",
        "type": "bool",
        "default": true
      },
      "points": {
        "description": "Remove the control-point deformation portion",
        "type": "bool",
        "default": true
      }
    }
  },
  "spline.keyframe.list": {
    "summary": "List spline animation keys and the channels captured at each frame",
    "tags": "spline|animation|keyframe|list",
    "related": "spline.keyframe.insert|spline.keyframe.remove|timeline.set_frame",
    "returns": "SplineKeyInfo[]",
    "params": {
      "name": {
        "description": "Spline object name",
        "type": "string",
        "required": true
      }
    }
  },
  "spline.animation.self_test": {
    "summary": "Run the deterministic spline transform, control-point and radius interpolation self-test",
    "tags": "spline|animation|selftest|validation",
    "related": "spline.keyframe.insert|spline.keyframe.list",
    "returns": "{ok:bool,details:string}"
  },
  "mesh.profile.revolve.preview": {
    "summary": "Preview a partial or full revolve around a selected axis",
    "notes": "The radial side profile may be open or closed. start_angle/end_angle are radians; partial ranges keep their angular seams open.",
    "tags": "mesh|profile|revolve|screw|axis|preview",
    "related": "mesh.profile.revolve.commit|mesh.profile.revolve.self_test",
    "params": {
      "preset": {
        "description": "Built-in radial profile preset: cup or bottle",
        "type": "string"
      },
      "axis": {
        "description": "Revolution axis: x, y or z",
        "type": "string"
      },
      "start_angle": {
        "description": "Start angle in radians",
        "type": "float"
      },
      "end_angle": {
        "description": "End angle in radians; span must be <= 2*pi",
        "type": "float"
      },
      "radius_offset": {
        "description": "Added to profile X before revolving; use a positive value for torus-style closed profiles",
        "type": "float"
      },
      "axis_pivot": {
        "description": "Geometry-space revolution axis pivot [x, y, z]",
        "type": "float[3]"
      },
      "angle_segments": {
        "description": "Angular subdivisions",
        "type": "int"
      },
      "profile_samples": {
        "description": "Samples along the radial profile",
        "type": "int"
      }
    }
  },
  "mesh.profile.revolve.commit": {
    "summary": "Commit an undoable partial or full revolve mesh",
    "notes": "Uses the same open/closed profile, axis and angle contract as preview.",
    "tags": "mesh|profile|revolve|screw|axis|commit",
    "undoable": true,
    "related": "mesh.profile.revolve.preview|mesh.profile.revolve.self_test",
    "params": {
      "preset": {
        "description": "Built-in radial profile preset: cup or bottle",
        "type": "string"
      },
      "object": {
        "description": "Destination mesh object name",
        "type": "string"
      },
      "axis": {
        "description": "Revolution axis: x, y or z",
        "type": "string"
      },
      "start_angle": {
        "description": "Start angle in radians",
        "type": "float"
      },
      "end_angle": {
        "description": "End angle in radians; span must be <= 2*pi",
        "type": "float"
      },
      "radius_offset": {
        "description": "Added to profile X before revolving; use a positive value for torus-style closed profiles",
        "type": "float"
      },
      "axis_pivot": {
        "description": "Geometry-space revolution axis pivot [x, y, z]",
        "type": "float[3]"
      },
      "angle_segments": {
        "description": "Angular subdivisions",
        "type": "int"
      },
      "profile_samples": {
        "description": "Samples along the radial profile",
        "type": "int"
      }
    }
  },
  "mesh.profile.revolve.self_test": {
    "summary": "Run full closed and partial open-profile revolve core tests",
    "tags": "mesh|profile|revolve|screw|test"
  },
  "mesh.profile.loft.preview": {
    "summary": "Preview a loft between multiple closed spline sections",
    "tags": "mesh|profile|loft|spline|preview",
    "related": "mesh.profile.loft.commit|mesh.profile.loft.self_test",
    "params": {
      "sections": {
        "description": "Ordered spline object names",
        "required": true,
        "type": "string[]"
      },
      "samples_per_section": {
        "description": "Samples per loft ring",
        "type": "int"
      },
      "cap_start": {
        "description": "Generate the first cap",
        "type": "bool"
      },
      "cap_end": {
        "description": "Generate the final cap",
        "type": "bool"
      }
    }
  },
  "mesh.profile.loft.commit": {
    "summary": "Commit an undoable flat DNA SoA mesh generated from multiple spline sections",
    "tags": "mesh|profile|loft|spline|commit",
    "undoable": true,
    "related": "mesh.profile.loft.preview|mesh.profile.loft.self_test",
    "params": {
      "sections": {
        "description": "Ordered spline object names",
        "required": true,
        "type": "string[]"
      },
      "object": {
        "description": "Destination mesh object name",
        "required": true,
        "type": "string"
      },
      "samples_per_section": {
        "description": "Samples per loft ring",
        "type": "int"
      },
      "cap_start": {
        "description": "Generate the first cap",
        "type": "bool"
      },
      "cap_end": {
        "description": "Generate the final cap",
        "type": "bool"
      }
    }
  },
  "mesh.profile.loft.self_test": {
    "summary": "Run the deterministic profile loft core self-test",
    "tags": "mesh|profile|loft|test"
  },
  "geometry_cache.bake": {
    "summary": "Bake a fixed-topology mesh deformation clip from the canonical evaluated geometry",
    "notes": "Stores only local vertex positions. Bake is rejected if vertex count or index connectivity changes.",
    "tags": "geometry|cache|animation|bake|deformation|vertex",
    "related": "geometry_cache.status|geometry_cache.set_enabled|geometry_cache.clear|nodes.apply",
    "params": {
      "object_name": {
        "description": "Flat mesh / Geometry Graph host object",
        "required": true,
        "type": "string"
      },
      "start_frame": {
        "description": "First sampled timeline frame",
        "required": true,
        "type": "int"
      },
      "end_frame": {
        "description": "Last sampled timeline frame",
        "required": true,
        "type": "int"
      },
      "frame_step": {
        "description": "Distance between stored samples",
        "type": "int",
        "default": 1
      }
    }
  },
  "spline.skin.create": {
    "summary": "Create or update a non-destructive skin display on an open spline",
    "notes": "Reuses one linked preview host while parameters change. Builds Spline Object to Taper to Twist to Wave/Noise to Curve to Mesh to Output; an optional closed custom_profile drives the cross-section.",
    "tags": "spline|skin|bevel|cable|tube|curve-to-mesh|geometry-nodes",
    "related": "spline.get|spline.keyframe.insert|geometry_cache.bake|nodes.set_property",
    "params": {
      "spline": {
        "description": "Open spline path object",
        "required": true,
        "type": "string"
      },
      "output": {
        "description": "Requested output mesh name; blank derives one from the spline",
        "type": "string",
        "default": ""
      },
      "radius": {
        "description": "Global bevel radius",
        "type": "float",
        "default": 0.1
      },
      "path_samples": {
        "description": "Samples along the spline",
        "type": "int",
        "default": 48
      },
      "radial_segments": {
        "description": "Circular cross-section segments",
        "type": "int",
        "default": 12
      },
      "cap_start": {
        "description": "Cap the first endpoint",
        "type": "bool",
        "default": true
      },
      "cap_end": {
        "description": "Cap the final endpoint",
        "type": "bool",
        "default": true
      },
      "use_point_radius": {
        "description": "Multiply bevel radius by interpolated per-point Curve Radius",
        "type": "bool",
        "default": true
      },
      "custom_profile": {
        "description": "Optional closed spline used as the swept cross-section",
        "type": "string",
        "default": ""
      },
      "taper_start": {
        "description": "Radius scale at the first control",
        "type": "float",
        "default": 1.0
      },
      "taper_end": {
        "description": "Radius scale at the final control",
        "type": "float",
        "default": 1.0
      },
      "taper_falloff": {
        "description": "Taper interpolation exponent",
        "type": "float",
        "default": 1.0
      },
      "twist_start_degrees": {
        "description": "Profile rotation at the first control",
        "type": "float",
        "default": 0.0
      },
      "twist_end_degrees": {
        "description": "Profile rotation at the final control",
        "type": "float",
        "default": 0.0
      },
      "wave_amplitude": {
        "description": "Wave displacement amplitude",
        "type": "float",
        "default": 0.0
      },
      "wave_cycles": {
        "description": "Wave cycles along the controls",
        "type": "float",
        "default": 1.0
      },
      "wave_phase_degrees": {
        "description": "Wave phase in degrees",
        "type": "float",
        "default": 0.0
      },
      "wave_noise": {
        "description": "Deterministic noise amplitude",
        "type": "float",
        "default": 0.0
      },
      "wave_seed": {
        "description": "Deterministic noise seed",
        "type": "int",
        "default": 0
      },
      "wave_axis": {
        "description": "Local offset axis: 0=X, 1=Y, 2=Z",
        "type": "int",
        "default": 1
      }
    }
  },
  "spline.skin.finalize": {
    "summary": "Convert a spline's live skin display into an ordinary mesh",
    "notes": "Evaluates the current frame, removes the procedural Geometry Graph link and keeps the existing flat preview host as the final mesh.",
    "tags": "spline|skin|preview|convert|apply|mesh",
    "related": "spline.skin.create|spline.skin.clear|geometry_cache.bake",
    "params": {
      "spline": {
        "description": "Open spline source owning the live skin display",
        "required": true,
        "type": "string"
      }
    }
  },
  "spline.skin.clear": {
    "summary": "Remove a spline's non-destructive skin display",
    "notes": "Deletes only the linked preview host and graph; the editable spline source remains unchanged.",
    "tags": "spline|skin|preview|remove|clear",
    "related": "spline.skin.create|spline.skin.finalize",
    "params": {
      "spline": {
        "description": "Spline source owning the live skin display",
        "required": true,
        "type": "string"
      }
    }
  },
  "geometry_cache.status": {
    "summary": "Inspect deformation-cache range, memory, topology validity and source staleness",
    "tags": "geometry|cache|animation|status|memory",
    "related": "geometry_cache.bake|geometry_cache.set_enabled|geometry_cache.clear",
    "params": {
      "object_name": {
        "description": "Cached flat mesh object",
        "required": true,
        "type": "string"
      }
    }
  },
  "geometry_cache.set_enabled": {
    "summary": "Switch an existing geometry cache between cached and live procedural playback",
    "tags": "geometry|cache|animation|playback|toggle",
    "related": "geometry_cache.status|geometry_cache.bake",
    "params": {
      "object_name": {
        "description": "Cached flat mesh object",
        "required": true,
        "type": "string"
      },
      "enabled": {
        "description": "Use cached positions when true",
        "required": true,
        "type": "bool"
      }
    }
  },
  "geometry_cache.clear": {
    "summary": "Remove a baked deformation clip and restore live Geometry Graph evaluation",
    "tags": "geometry|cache|animation|clear",
    "related": "geometry_cache.bake|geometry_cache.status",
    "params": {
      "object_name": {
        "description": "Cached flat mesh object",
        "required": true,
        "type": "string"
      }
    }
  },
  "geometry_cache.self_test": {
    "summary": "Run fixed-topology position capture, interpolation and memory-accounting tests",
    "tags": "geometry|cache|animation|selftest|validation",
    "related": "geometry_cache.bake|geometry_cache.status"
  },
  "terrain.list_layers": {
    "summary": "List the eight terrain layer slots and what drives each one",
    "notes": "Slots 0-3 are weighted by the splat map and normalized against each other - they partition the surface. Slots 4-7 are weighted by the semantic map (Flow, Wetness, Ice, Hardness) and composited OVER that blend by their own unnormalized weight. bound=false on a semantic slot means the built-in shading still applies to that channel. TWO dead cases to check on a bound overlay: channel_coverage 0 means the graph never fills that channel (Auto Splat's semantic output writes Flow and hard-zeroes the other three, while Surface Composer writes all four); channel_constant true means min==max, a flat fill that reports full coverage yet selects nothing and washes the material evenly over the terrain - this is how an unwired Surface Composer Hardness input presents, falling back to a constant 0.45. Hardness is produced by Lithology or Strata.",
    "tags": "landscape|material|layer|splat|semantic",
    "related": "terrain.set_layer|material.list"
  },
  "terrain.sculpt": {
    "summary": "Run a terrain sculpt stroke (raise|lower|flatten|smooth|stamp) over a list of world-space dabs",
    "tags": "landscape|brush|authoring",
    "notes": "The panel brush and this method share TerrainManager::sculpt, so this is the only way to regression-test terrain sculpting. Read height_delta, NOT ok: the height field is metres/scale_y and a graph-authored landform routinely exceeds 1.0, so a bug that clamps the field reports success while flattening the ground under the brush. dt is the seconds one dab represents (the panel feeds 1/60); strength is metres per second at full falloff, so a visible bump needs either a large strength or many dabs. flatten samples its target from the surface under the first dab unless use_fixed_height is set.",
    "params": {
      "dabs": {
        "type": "array",
        "required": true,
        "description": "Stroke path: list of [world_x, world_z] pairs. One dab is a click, several are a drag; every dab must land on the tile or the call is refused."
      },
      "mode": {
        "type": "string",
        "enum": "raise|lower|flatten|smooth|stamp"
      },
      "dt": {
        "description": "Seconds one dab represents; the panel feeds one frame",
        "default": "0.0166667"
      },
      "strength": {
        "description": "Metres of height change per second at full falloff"
      },
      "radius": {
        "description": "Brush radius in metres"
      },
      "curve": {
        "description": "Falloff exponent, 0.25..4"
      }
    }
  },
  "terrain.paint_splat": {
    "summary": "Paint one terrain splat channel (layer) along a list of world-space dabs",
    "tags": "landscape|brush|paint",
    "notes": "Fails rather than initialising when the terrain has no splat map or layers: an auto-created layer stack would hide the missing setup step. coverage_before/after are the mean weight of the painted channel over the whole splat map, which is what separates 'the stroke ran' from 'the stroke painted'; a stroke fully outside the tile is refused, not silently ignored.",
    "params": {
      "dabs": {
        "type": "array",
        "required": true,
        "description": "Stroke path: list of [world_x, world_z] pairs, all of which must land on the tile."
      },
      "channel": {
        "description": "Splat channel / layer index 0..3"
      },
      "dt": {
        "description": "Seconds one dab represents; the panel feeds one frame",
        "default": "0.0166667"
      }
    }
  },
  "scatter.set_settings": {
    "summary": "Patch a scatter group's placement rules and its density / exclusion / scale masks",
    "notes": "Only the keys you pass are written; the rest are left alone. The two mask roles are NOT symmetric: density_mask INCLUDES probabilistically (its value is a placement probability, so 0.3 thins a stand), while exclusion_mask FORBIDS placement wherever the field is >= exclusion_threshold. exclusion_threshold is shared by exclusion_mask and splat_exclude_channel. Mask names are terrain analysis fields - get the ones a terrain actually has from terrain.list_fields, and build a combined mask upstream with a Publish Field node rather than expecting more mask slots here. splat channels are -1 (off) or 0..3 (RGBA); an out-of-range channel is refused, not clamped. Read the result back with scatter.list_groups.",
    "tags": "scatter|foliage|mask|density|exclusion|placement",
    "related": "scatter.list_groups|terrain.list_fields|scatter.fill",
    "params": {
      "group": {
        "type": "string",
        "description": "Scatter group id or name"
      },
      "target_count": {
        "type": "int",
        "description": "How many instances to aim for on the next fill"
      },
      "seed": {
        "type": "int",
        "description": "Placement seed; the same seed reproduces the same layout"
      },
      "min_distance": {
        "type": "float",
        "description": "Minimum spacing between instances, in metres"
      },
      "slope_max": {
        "type": "float",
        "description": "Reject ground steeper than this, in degrees"
      },
      "height_min": {
        "type": "float",
        "description": "Lowest world height that accepts an instance, in metres"
      },
      "height_max": {
        "type": "float",
        "description": "Highest world height that accepts an instance, in metres"
      },
      "density_mask": {
        "type": "string",
        "description": "INCLUDE field name; its value is a placement PROBABILITY, so 0.3 thins rather than cuts. Empty string turns it off"
      },
      "exclusion_mask": {
        "type": "string",
        "description": "EXCLUDE field name; placement is forbidden where the field is >= exclusion_threshold. Empty string turns it off"
      },
      "exclusion_threshold": {
        "type": "float",
        "description": "Reject at or above this value; drives BOTH exclusion_mask and splat_exclude_channel"
      },
      "scale_mask": {
        "type": "string",
        "description": "Field name that scales instances. Empty string turns it off"
      },
      "scale_mask_influence": {
        "type": "float",
        "description": "0 ignores scale_mask, 1 lets it drive scale fully"
      },
      "splat_include_channel": {
        "type": "int",
        "description": "Splat map channel used as an include weight: -1 off, 0..3 = RGBA"
      },
      "splat_exclude_channel": {
        "type": "int",
        "description": "Splat map channel used as an exclusion: -1 off, 0..3 = RGBA"
      }
    }
  },
  "terrain.list_fields": {
    "summary": "List the named analysis fields a terrain is currently publishing",
    "notes": "Measures the LIVE terrain: a name appears only because an output node (Terrain Fields Output, or a Publish Field node) actually wrote it during this evaluation. It is not a catalogue of what the node library could produce, so an empty result means the graph has not published anything yet - not that the feature is missing. These are the valid mask names for scatter.set_settings.",
    "tags": "terrain|field|mask|introspection",
    "related": "scatter.set_settings|terrain.list_layers",
    "params": {
      "terrain": {
        "type": "string",
        "description": "Terrain name, as reported by terrain.list"
      }
    }
  },
  "terrain.field_stats": {
    "summary": "Measure the values and coverage of one live terrain analysis field",
    "notes": "Reads the published field grid itself, not node settings or mesh attributes. Use this before judging a mask visually: min/max/mean and nonzero_fraction expose empty, saturated and constant fields; non_finite_count exposes poisoned data. histogram_bins is 0 to omit or 2..256. samples contains integer [x, y] field-grid coordinates and out-of-range coordinates are refused.",
    "tags": "terrain|field|mask|measurement|histogram|sample|diagnostic",
    "related": "terrain.list_fields|terrain.get|scatter.set_settings",
    "params": {
      "terrain": {
        "type": "string",
        "description": "Terrain name, as reported by terrain.list"
      },
      "field": {
        "type": "string",
        "description": "Live field name, as reported by terrain.list_fields"
      },
      "histogram_bins": {
        "type": "int",
        "description": "0 omits the histogram; otherwise 2..256 bins over the finite min/max range"
      },
      "samples": {
        "type": "array",
        "description": "Optional list of integer [x, y] field-grid coordinates"
      }
    }
  },
  "scene.raycast": {
    "summary": "Reports what a ray hits: scene geometry, terrain, or the Y=0 ground plane. This is the VALUE behind the viewport's draw-on-surface tools - without it that workflow is reachable only by clicking, and therefore not testable.",
    "params": {
      "origin": {
        "type": "vec3",
        "description": "Ray origin in world space"
      },
      "direction": {
        "type": "vec3",
        "description": "Ray direction; normalized internally, must be non-zero"
      },
      "filter": {
        "type": "string",
        "description": "mesh_and_terrain (default) | terrain_only | ground_plane. An unknown value is REFUSED, not defaulted: silently widening a terrain_only query would report a mesh hit that looks correct"
      }
    },
    "notes": "Returns hit, kind (mesh|terrain|ground_plane|none), object (hit mesh name; empty for terrain and the ground plane), position, normal and distance. The mesh and terrain hits are compared by distance, so a mesh behind the terrain does not win.",
    "related": "spline.append_point|terrain.sample_height|scene.list_objects"
  },
  "spline.append_point": {
    "summary": "Appends a control point at the tail of an open spline, valid from ZERO points. spline.extrude needs two existing points to derive a tangent, so it can extend a curve but never build one.",
    "params": {
      "name": {
        "type": "string",
        "description": "Spline object name, as reported by spline.list"
      },
      "position": {
        "type": "vec3",
        "description": "Point position in the spline's LOCAL space"
      }
    },
    "notes": "Returns the inserted index. Pair with scene.raycast to reproduce the viewport draw-on-surface flow from script. Keyframe topology propagation runs only once the curve has more than two points, because the first two create the curve rather than change its shape.",
    "related": "spline.create|spline.extrude|scene.raycast|spline.get"
  },
  "terrain.road.list_profiles": {
    "summary": "Lists the built-in road profiles. 'Path' and 'road' are the SAME measurement under a different profile, not two solvers - a footpath is narrow, steep-capable and barely moves earth; a main road buys a shallow grade with earthworks.",
    "notes": "Returns id, display_name and the carve parameters each profile sets: road_width, shoulder_width, grading_falloff, foliage_margin, max_grade_percent, max_cut_meters, max_fill_meters. Ids: footpath, dirt_road, main_road.",
    "related": "terrain.road.assign_profile|terrain.road.list_assignments"
  },
  "terrain.road.assign_profile": {
    "summary": "Marks an existing SplineObject as a road under the named profile. Stores NO curve geometry - the curve stays owned by the spline.* surface.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name, as reported by spline.list"
      },
      "profile": {
        "type": "string",
        "description": "Profile id: footpath | dirt_road | main_road. An unknown id is REFUSED, never substituted - carving a main road where a footpath was asked for looks entirely plausible"
      }
    },
    "notes": "A spline that does not exist is refused: an assignment pointing at a missing curve would look identical to a working one in every listing. This is also what removes the graph explosion - Road Network reads the registry, so N roads are one node.",
    "related": "terrain.road.clear_profile|terrain.road.get_assignment|spline.list"
  },
  "terrain.road.clear_profile": {
    "summary": "Removes a road assignment. The SplineObject itself is untouched.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      }
    },
    "related": "terrain.road.assign_profile|terrain.road.list_assignments"
  },
  "terrain.road.get_assignment": {
    "summary": "Reads one road assignment back, including whether its curve still exists.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      }
    },
    "notes": "Returns spline_object, profile_id, crossing_mode, enabled, has_override and curve_exists. A setter with no readback cannot be tested.",
    "related": "terrain.road.assign_profile|terrain.road.get_diagnostics"
  },
  "terrain.road.list_assignments": {
    "summary": "Lists every road assignment in the scene.",
    "notes": "Same fields as terrain.road.get_assignment, one row per assignment.",
    "related": "terrain.road.get_assignment|terrain.road.get_diagnostics"
  },
  "terrain.road.set_crossing_mode": {
    "summary": "Sets how a road resolves where the ground cannot carry it. The solver applies the mode PER ROUTE SAMPLE, so only the span that needs a crossing gets one.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      },
      "mode": {
        "type": "string",
        "description": "auto | terrain | bridge | ford | tunnel. An unknown mode is refused. auto bridges over water when the Road Network node's Water pin is connected and otherwise behaves exactly as terrain; bridge also spans wherever the required fill exceeds max_fill_meters; tunnel bores wherever the required cut exceeds max_cut_meters; ford follows the bed and needs a Water field, reporting crossing_diagnostic when it has none"
      }
    },
    "related": "terrain.road.get_route|terrain.road.get_assignment",
    "notes": "Bridge and tunnel spans leave the terrain untouched and are straightened between their abutments; a tunnel also publishes no surface masks at all, so a forest still grows over it. Measure the result with terrain.road.get_route."
  },
  "terrain.road.set_enabled": {
    "summary": "Enables or disables one road without losing its profile assignment.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name"
      },
      "enabled": {
        "type": "bool",
        "description": "false leaves the assignment in place but stops it carving"
      }
    },
    "related": "terrain.road.get_assignment|terrain.road.get_diagnostics"
  },
  "terrain.road.get_diagnostics": {
    "summary": "Reports assignment health: counts, assignments whose SplineObject is gone, and assignments naming a profile this build does not have.",
    "notes": "Returns assignment_count, enabled_count, dangling and unknown_profiles. A road that silently stops grading because its curve was deleted is the failure nobody files, so it is surfaced here and on the Road Network node.",
    "related": "terrain.road.list_assignments|terrain.field_stats"
  },
  "terrain.road.get_route": {
    "summary": "Reads the SOLVED route of one assigned road out of the terrain graph: sample positions, road and ground height, and the crossing each sample resolved to.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name of an assigned road"
      },
      "max_samples": {
        "type": "int",
        "description": "0 (default) returns only the counters. A positive value returns that many samples, decimated with both endpoints kept"
      }
    },
    "notes": "This is the measurement surface for crossings: bridge_samples, ford_samples and tunnel_samples are how a script proves a declared crossing actually resolved, and crossing_diagnostic names one that could not. It fails explicitly when no Road Network node has carved this assignment yet - an empty route and an unevaluated graph must not read the same.",
    "returns": "spline_object, terrain, sample_count, bridge_samples, ford_samples, tunnel_samples, length_meters, peak_cut_meters, peak_fill_meters, revision, crossing_diagnostic, samples[]",
    "tags": "terrain|road|measurement",
    "related": "terrain.road.build_mesh|terrain.road.set_crossing_mode|terrain.field_stats"
  },
  "terrain.road.build_mesh": {
    "summary": "Generates the optional road surface mesh from the solved route - the very samples that carved the terrain, never a second sampling of the curve.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name of an assigned road"
      },
      "object": {
        "type": "string",
        "description": "Scene object to write. Empty reuses the object this assignment already owns, or creates <spline>_RoadSurface"
      },
      "include_shoulder": {
        "type": "bool",
        "description": "Include the shoulder in the ribbon. The ditch is never meshed - a ditch is terrain"
      },
      "surface_offset": {
        "type": "float",
        "description": "Metres above the graded surface. At zero the continuous ribbon and the discretised heightfield interpenetrate and the road reads as torn"
      },
      "uv_meters_per_tile": {
        "type": "float",
        "description": "Metres of road per V tile"
      },
      "skip_tunnels": {
        "type": "bool",
        "description": "Split the ribbon at tunnel spans instead of drawing a deck through the mountain"
      }
    },
    "notes": "A rebuild REPLACES the geometry of the object the assignment owns, so repeated generation cannot leave a stack of stale roads behind. Publishes flat TriangleMesh / DNA SoA geometry. The terrain-only road stays valid without it: the mesh is a view of the route, never the authority for it.",
    "returns": "object, vertex_count, triangle_count, span_count, length_meters, replaced_existing",
    "tags": "terrain|road|geometry",
    "related": "terrain.road.clear_mesh|terrain.road.get_route"
  },
  "terrain.road.clear_mesh": {
    "summary": "Deletes the generated road surface and forgets the ownership record.",
    "params": {
      "spline": {
        "type": "string",
        "description": "SplineObject name of an assigned road"
      }
    },
    "notes": "The ownership record is cleared even when the object is already gone, so the next build is not blocked by a pointer to nothing. The terrain-only road is unaffected.",
    "related": "terrain.road.build_mesh"
  },
  "world.get_atmosphere": {
    "summary": "Return the physical atmosphere the sky LUT is baked from",
    "notes": "Separate from world.get on purpose: world.get is the SUN plus the two intensities, this is the MEDIUM. Every field here feeds makeAtmosphereLUTParamsGPU, so every one of them dirties the transmittance/SkyView LUT when it changes.",
    "returns": "WorldAtmosphereInfo",
    "tags": "environment|sky|atmosphere|nishita",
    "related": "world.set_atmosphere|world.get"
  },
  "world.set_atmosphere": {
    "summary": "Set the physical atmosphere parameters of the Nishita sky",
    "notes": "Every field optional: omit a key to leave it unchanged. Out-of-range values are REJECTED, not clamped, because a silently clamped planet radius or scale height reads as 'the sky looks odd' rather than as an error. Any changed field marks the atmosphere LUT dirty; the runtime rebuilds it on the GPU compute path (throttled to about 15 Hz while a value is being dragged) and falls back to a CPU bake only when that pipeline is missing. UNITS ARE ABSOLUTE: planet_radius/atmosphere_height/altitude are METRES, rayleigh_density and mie_density are SCALE HEIGHTS in metres (NOT densities, despite the field names), temperature is CELSIUS and scales both scale heights.",
    "tags": "environment|sky|atmosphere|nishita|scattering",
    "related": "world.get_atmosphere|world.set_atmosphere_intensity|world.set_sun_elevation",
    "params": {
      "air_density": {
        "description": "Rayleigh scattering multiplier (Blender 'Air'). 1 = Earth. Must be >= 0."
      },
      "dust_density": {
        "description": "Mie/aerosol scattering multiplier (Blender 'Dust'). Must be >= 0."
      },
      "ozone_density": {
        "description": "Ozone column multiplier; drives blue-hour saturation. Must be >= 0."
      },
      "ozone_absorption_scale": {
        "description": "Scales the ozone absorption coefficients on top of ozone_density. Must be >= 0."
      },
      "humidity": {
        "description": "0..1, dry to hazy. Rejected outside that range."
      },
      "temperature": {
        "description": "Air temperature in CELSIUS. Scales BOTH Rayleigh and Mie scale heights by (T+273.15)/288.15, so it moves the whole atmosphere profile, not just a tint."
      },
      "altitude": {
        "description": "Viewer height above sea level in METRES; the SkyView LUT is baked from this camera altitude."
      },
      "mie_anisotropy": {
        "description": "Henyey-Greenstein g for the Mie phase function; forward scattering as it approaches 1. Must be within (-1, 1)."
      },
      "planet_radius": {
        "description": "Ground sphere radius in METRES (Earth = 6360000). Must be >= 1000."
      },
      "atmosphere_height": {
        "description": "Atmosphere shell thickness above the ground in METRES (Earth = 60000). Must be >= 1000."
      },
      "rayleigh_scattering": {
        "description": "Per-channel Rayleigh scattering coefficients as [r, g, b], in 1/metre."
      },
      "mie_scattering": {
        "description": "Per-channel Mie scattering coefficients as [r, g, b], in 1/metre."
      },
      "rayleigh_density": {
        "description": "Rayleigh SCALE HEIGHT in metres (Earth = 8000). The name says density, the unit is a height. Must be >= 1."
      },
      "mie_density": {
        "description": "Mie SCALE HEIGHT in metres (Earth = 1200). Same naming trap as rayleigh_density. Must be >= 1."
      }
    }
  },
  "camera.set_auto_exposure": {
    "summary": "Turn auto exposure on; while on, only EV compensation is applied",
    "notes": "PRIORITY ORDER, and it is the most common reason these dials look dead: if auto_exposure is ON only ev_compensation is applied and ISO/shutter/f-stop are NOT read; if auto_exposure is OFF but use_physical_exposure is OFF too, again only ev_compensation applies. The call still SUCCEEDS because it really did write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor to see the multiplier actually applied."
  },
  "camera.set_use_physical_exposure": {
    "summary": "Enable the ISO/shutter/f-stop preset chain (needs auto_exposure off)",
    "notes": "PRIORITY ORDER, and it is the most common reason these dials look dead: if auto_exposure is ON only ev_compensation is applied and ISO/shutter/f-stop are NOT read; if auto_exposure is OFF but use_physical_exposure is OFF too, again only ev_compensation applies. The call still SUCCEEDS because it really did write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor to see the multiplier actually applied."
  },
  "camera.set_iso_preset": {
    "summary": "Select the ISO preset by index; camera.get returns the resolved iso_value",
    "notes": "The index is not a measurement - read 'iso_value' from camera.get for the actual ISO. PRIORITY ORDER, and it is the most common reason these dials look dead: if auto_exposure is ON only ev_compensation is applied and ISO/shutter/f-stop are NOT read; if auto_exposure is OFF but use_physical_exposure is OFF too, again only ev_compensation applies. The call still SUCCEEDS because it really did write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor to see the multiplier actually applied."
  },
  "camera.set_shutter_preset": {
    "summary": "Select the shutter-speed preset by index; camera.get returns shutter_seconds",
    "notes": "Index, not seconds. Read 'shutter_seconds' from camera.get for the resolved exposure time. This dial does NOT drive motion blur. PRIORITY ORDER, and it is the most common reason these dials look dead: if auto_exposure is ON only ev_compensation is applied and ISO/shutter/f-stop are NOT read; if auto_exposure is OFF but use_physical_exposure is OFF too, again only ev_compensation applies. The call still SUCCEEDS because it really did write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor to see the multiplier actually applied."
  },
  "camera.set_fstop_preset": {
    "summary": "Select the f-stop preset by index; drives exposure and Cinema lens imperfections, NOT depth of field",
    "notes": "Depth of field still comes from the separate 'aperture' dial. On a real camera one f-number would do both; they are separate here because unifying them would silently change the blur of every existing scene - a recorded debt. PRIORITY ORDER, and it is the most common reason these dials look dead: if auto_exposure is ON only ev_compensation is applied and ISO/shutter/f-stop are NOT read; if auto_exposure is OFF but use_physical_exposure is OFF too, again only ev_compensation applies. The call still SUCCEEDS because it really did write the setting - the image not moving is the model's precedence, not a fault. Read camera.get exposure_factor to see the multiplier actually applied."
  },
  "camera.set_ev_compensation": {
    "summary": "Set exposure compensation in stops; this one applies in EVERY mode",
    "notes": "Unlike the preset dials this is always read, so it is the reliable way to move exposure without first arranging auto_exposure/use_physical_exposure. Rejected outside [-10, +10]."
  },
  "scene.export_gltf": {
    "summary": "Export the scene to glTF 2.0 (.glb or .gltf + sidecar .bin) and report measured cost",
    "notes": "Written directly from the in-memory flat SoA geometry - no Assimp round trip, no per-triangle facade. Blocks the main thread until the file is closed. The reply carries what was actually written plus a per-phase time breakdown and the writer's own peak heap (peak_writer_mb, NOT process RSS), so export cost is regressable from a script. .glb is capped at 4 GB by its 32-bit chunk length; use .gltf for anything larger.",
    "tags": "export|gltf|glb|save|interchange|asset",
    "related": "scene.import_model|project.save",
    "returns": "Object with path, meshes, primitives, triangles, vertices, nodes, instances, instanced_groups, materials, images, file_bytes, peak_writer_mb and seconds_total/collect/materials/plan/write",
    "undoable": false,
    "params": {
      "path": {
        "description": "Output file path; extension must be .glb or .gltf"
      },
      "geometry": {
        "description": "Write meshes"
      },
      "materials": {
        "description": "Write materials and textures"
      },
      "cameras": {
        "description": "Write the active camera as a glTF camera node"
      },
      "lights": {
        "description": "Write scene lights via KHR_lights_punctual"
      },
      "animations": {
        "description": "Write animation clips as glTF samplers/channels"
      },
      "skinning": {
        "description": "Write the skeleton, JOINTS_0/WEIGHTS_0 and inverse bind matrices"
      },
      "selected_only": {
        "description": "Export only the current multi-selection; fails if nothing is selected"
      },
      "bake_terrain_materials": {
        "description": "Flatten splat-blended terrain layers into export textures"
      },
      "terrain_bake_resolution": {
        "description": "Square resolution of each baked terrain texture"
      },
      "gpu_instancing": {
        "description": "Collapse scattered objects sharing one source mesh into EXT_mesh_gpu_instancing nodes"
      }
    }
  },
  "scene.export_estimate": {
    "summary": "Report what an export WOULD cost, without writing anything",
    "notes": "The same pre-export estimate the Export Settings panel shows, computed by the same function - so a script can catch the panel drifting away from the writer. It already did: the writer was fixed to read scatter from InstanceManager (on Vulkan scatter is NEVER expanded into world.objects), the panel's estimate was not, and it reported instances=0 for a scene the exporter then wrote a thousand instances of. Compare `instances` here against scene.export_gltf's measured `instances`; a gap means they have drifted again. Note `triangles` counts non-instanced geometry only - add `instance_triangles` for the total. `estimated_peak_mb` covers only geometry the writer must MATERIALISE (legacy Triangle facades); flat SoA meshes stream from where they live and cost no extra heap.",
    "tags": "export|gltf|estimate|cost|preflight|scatter|instances",
    "related": "scene.export_gltf|scatter.list_groups",
    "returns": "Object with objects, triangles, legacy_triangles, instances, unique_instance_sources, instance_triangles, materialised_instance_triangles, estimated_peak_mb",
    "undoable": false,
    "params": {
      "geometry": {
        "description": "Count meshes"
      },
      "materials": {
        "description": "Count materials and textures"
      },
      "cameras": {
        "description": "Count the active camera as a glTF camera node"
      },
      "lights": {
        "description": "Count scene lights"
      },
      "animations": {
        "description": "Count animation clips"
      },
      "skinning": {
        "description": "Count skinning data"
      },
      "selected_only": {
        "description": "Estimate the current multi-selection only"
      },
      "bake_terrain_materials": {
        "description": "Assume terrain layers get baked"
      },
      "terrain_bake_resolution": {
        "description": "Terrain bake resolution assumed by the estimate"
      },
      "gpu_instancing": {
        "description": "Assume EXT_mesh_gpu_instancing collapses scatter nodes"
      }
    }
  },
  "post.get_exposure": {
    "summary": "Read exposure settings, applied EV and HDR histogram",
    "notes": "Relative scene-linear luminance, not photometric EV100. GPU meters use a deterministic 128x128 sample grid and 256 weighted log2 bins. Raster preview shares the last resolved EV; it does not provide an HDR meter. meter_valid and meter_source report available data.",
    "returns": "object",
    "tags": "post|exposure|histogram",
    "related": "post.configure_exposure|post.reset_exposure"
  },
  "post.configure_exposure": {
    "summary": "Atomically configure manual, physical-camera or GPU-histogram exposure",
    "notes": "UI and Python call the same service. Mode is manual|physical|auto_histogram. EV in [-24,24]; min_ev <= max_ev. 0 <= low_percent < high_percent <= 100. key in [0.001,1], speed_up/down in (0,20] per second, center_weight in [0,1]. Non-finite values and unknown fields fail without mutation. locked=true captures current applied EV unless locked_ev is supplied. Final render freezes adaptation and rejects edits. No accumulation reset.",
    "params": {
      "settings": {
        "type": "object",
        "required": true,
        "description": "Exposure patch: mode, ev, min_ev, max_ev, low_percent, high_percent, key, speed_up, speed_down, center_weight, locked, locked_ev"
      }
    },
    "tags": "post|exposure|histogram",
    "verify_with": "post.get_exposure",
    "invalidates": "display",
    "returns": "object"
  },
  "post.reset_exposure": {
    "summary": "Reset exposure histogram and adaptation history",
    "notes": "Invalidates in-flight measurements by generation. Does not change settings or the explicit locked EV. Rejected while a final render is active.",
    "returns": "object",
    "tags": "post|exposure|reset",
    "verify_with": "post.get_exposure",
    "invalidates": "display"
  }
}
