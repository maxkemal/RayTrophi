# RayTrophi — engineering notes index

This folder holds the working documents behind RayTrophi Studio: roadmaps,
architecture decisions, acceptance-test protocols and root-cause postmortems.
`docs/` itself is the user-facing HTML manual; everything an implementer needs
lives here.

**Most of these are written in Turkish.** The project has been developed by one
person for about two and a half years, and the notes were written to be useful
during that work rather than to be published. They are kept in the repository
anyway, because a roadmap explains *why* and *what is next* — which the code
cannot. If you are reading these to contribute and a document matters to you,
open an issue and it will be translated.

## How to read the status line

Every document starts with a `> **Durum:**` line. Four values:

| Durum | Meaning |
|---|---|
| **AKTIF** | Live plan. Work is in progress or queued here. Read before touching that area. |
| **REFERANS** | Implemented. Records a contract, rule or architecture that is in force. Still binding. |
| **ARSIV** | Finished or closed. Historical record — usually a postmortem, kept because the root cause is worth remembering. |
| **TASLAK** | Proposed. Not implemented; the design may still change. |
| **CANLI** | Rewritten every work batch. Only the latest revision is meaningful. |

`REFERANS` is not "old". A completed postmortem is often the most useful file in
this folder: the root cause was paid for once, and the note is what stops it
being paid for twice.

---

## Active plans (AKTIF)

| Document | Area |
|---|---|
| [PARTICLE_SYSTEM_GPU_ROADMAP.md](PARTICLE_SYSTEM_GPU_ROADMAP.md) | **ACTIVE 2026-09-24. Canonical particle-system target.** GPU-resident simulation with an explicit CPU reference/fallback, multi-entry Geometry Sets promoted from the existing weighted `mesh_sources`, reusable Appearance Profiles, composable billboard/ribbon/mesh/light/domain outputs, high-quality domain-optional fire/explosion/water effects, GPU collision/events, migration gates, and phased UI/Python/IPC delivery. A domain remains an optional output and is not required for particle effects. Review pass: Phase 1.5 campfire vertical slice, load-only legacy migration, Surface State Deposit into MSF, field-masked surface sources, deterministic event ordering, RT instance budget, mud/snow and volcanic scope tables. |
| [DOMAIN_TASIMA_EMITTERLERI_TASIMIYORDU.md](DOMAIN_TASIMA_EMITTERLERI_TASIMIYORDU.md) | **AKTIF 2026-09-24. RUNTIME DOGRULANMADI.** Domain tasininca emitterler geride kaliyordu; yeniden simule edilen duman eski yerinde belirdigi icin belirti "cache silinmemis" gibi okunuyordu. **Cache SILINIYORDU** -- imza bounds'u hash'liyor, iki yol da clearSimFrameCache cagiriyor. Bayat olan cache degil EMITTERLERDI: iki belirti tek sebep, ve yanlis hikaye daha inandirici cunku cache zaten supheli bir seydir. Hicbir testin yakalamamasinin sebebi domain tasimanin IPC'de HIC OLMAMASI (kural 1). Cozum: translateGridDomain + carryGridDomainAnchors; keyframe pozisyonlari da tasiniyor (yoksa duzeltme play'e basana kadar yasar), parent'li kaynak TASINMIYOR (tek konumun iki otoritesi olamaz). Gaz ve sivi tek govde. Yeni: `sim.move_domain` + `sim.` namespace yetkisi (yoksa fail-closed). |
| [IZGARA_HACMIN_ONUNDE.md](IZGARA_HACMIN_ONUNDE.md) | **AKTIF 2026-09-24. TESHIS EDILDI, DUZELTILMEDI.** Ana izgara kati geometride dogru tikaniyor ama hacimsel verinin (duman/gaz) ONUNDE kaliyor. Cizim sirasi dogru; sorun hacmin HICBIR varyantta derinlik YAZMAMASI (`previewPipeline` testi bile kapali, `solidPipeline` test ediyor ama yazmiyor), o yuzden sonradan cizilen izgara hic reddedilmiyor. "Tek satir" gorunuyor ama degil: shader zaten `gl_FragDepth` yaziyor, ancak `firstContributionT`den -- ilk KATKI noktasi, opaklastigi yer degil. Yazmayi acmak en ince dumanin bile izgarayi tamamen gizlemesi demek; opaklik esigi gerekir, esik altinda uzak deger yazmak ise ancak derinlik TESTI acikken guvenli, oysa previewPipeline`da test kapali. Uc degisiklik birlikte, ve bu deponun en cok sessiz ariza uretmis alaninda. Sirasi: shader esigi, iki pipeline`in depth durumu, esigin KADRAN+IPC olmasi. Kontrol: hacmin ICINDEKI partikul/hair overlay kaybolmamali. |
| [KAMERA_ODAK_KILIDI.md](KAMERA_ODAK_KILIDI.md) | **AKTIF 2026-09-23. RUNTIME DOGRULANMADI.** Orbit pivotu ile lens odak duzlemi AYNI SAYIYDI: `setLookDirection` `lookat`i `focus_dist` mesafesine koyuyordu, yani her fare rotasyonu navigasyon capasini lensin odak duzlemine tasiyordu. Odagi 0,5 m'ye cekmek orbit yaricapini 0,5 m'ye dusuruyor, pan hizi ve dolly adimi o yaricapla orantili oldugu icin viewport cevap vermez oluyordu - hatasiz, logsuz, ekran goruntusunde izsiz. Ikinci kuplaj: dolly her adimda `focus_dist` yaziyordu, yani zoom sinematik odagi suruklüyordu. Cozum: `Camera::orbit_pivot` + `pivot_mode` (Free|Selection) + `navDistance()`; navigasyon yolunda hicbir yer artik `focus_dist` yazmiyor. **Secim modunda pivot SAKLANAN degil TURETILEN bir degerdir** - her hareketin basinda secimden yeniden hesaplanir, boylece bayatlamasi yapisal olarak imkansiz (bayat pivot, bu isin kaldirdigi arizanin ta kendisiydi: Frame Selected secim merkezini yazardi, sonraki orta-tik imlecin altindaki yuzeyi yazardi). Yeni IPC: `camera.get_pivot` (nav_distance ve focus_distance YAN YANA - kuplajin geri gelmediginin tek kaniti), `set_pivot_mode`, `orbit`, `dolly`, `pan`, `frame_selected`. Dogrulama: `scripts/ipc/Probe-CameraPivotLock.ps1`. |
| [GAZ_DERSLERI_VE_FLUID_DEVRI.md](GAZ_DERSLERI_VE_FLUID_DEVRI.md) | **AKTIF 2026-09-21. DEVIR NOTU.** Gaz adimi 203->157 ms'ye indi; bu not sayilari degil YONTEMI devreder. Sirali is listesi: once gaz tarafinda BELLEK MUHASEBESI (perf.get_gpu_memory izlenen 672 MB, **izlenmeyen 1001 MB** - gaz domain'inin ~340 MB'lik compute tamponlari hicbir kategoride gorunmuyor, yani optimize edilecek sey olcu aletinin disinda), sonra performans (scalar advection handle takasi, pressure projection'in YENIDEN olculmesi, yapisal geri okuma), sonra ayni yontemin APIC fluid yoluna tasinmasi. Devredilen dersler: (1) zamanlama tablosundan hedef secmek satir adlarinin dogru oldugunu varsaymaktir - bu oturumda UC kez yanlis cikti, `cpu_boundary_ms`in altinda 23 ms'lik `clampVelocity`, `gpu_publish_ms`in altinda 20 ms'lik `sanitizeProjectedVelocity`, `cpu_surface_dust_ms`in altinda collider yokken calisan yogun kati taramasi; (2) bayragin SET edildigi yer ile OKUNDUGU yer arasindaki bosluk - dort ornegi, ikisini ajan kendisi uretti; (3) her TUKETICI baglanmadan hicbir URETICI indirmeyi birakamaz, ve bir asamanin yalnizca OKUDUGU alan da o alanin tuketicisidir (turbulence density'yi maske olarak aliyordu ve combustion'in cihazdaki sonucunu ezdi: f120'de 959364 yerine 96826 hucre); (4) gecici A/B anahtari evet, kalici ikinci kod yolu hayir - ve anahtarin CANLI degerini oku, varsayilani varsayma; (5) bir tampon her zaman ayrildigi icin 'var mi' sorusu size()==cell_count ile sorulunca cevap hep evet cikar. Kayan nokta kurali: aritmetik KALDIRILDIGINDA birebir aynilik beklenir, TASINDIGINDA beklenmez (Vulkan sqrt 2.5 ULP) - ayirt edici, sapmanin buyuyen mi yoksa ilk kareden itibaren sistematik mi oldugudur. |
| [GAZ_ADIMI_TASIMA_MALIYETI.md](GAZ_ADIMI_TASIMA_MALIYETI.md) | **AKTIF 2026-09-21. Cihaz yerlesikligi KURULDU ve DOGRULANDI.** Gaz adiminin maliyeti hesap degil TASIMA idi: her asama upload -> dispatch -> synchronize -> download kalibinda kosuyordu. Olcum bunun donanim siniri olmadigini gosterdi - cihazdan hic cikmayan majorant cekirdegi 125 GB/s, tur atan asamalar ayni GPU'da ayni adimda 4,7-7,3 GB/s. Cozum alan basina UC DURUMLU bir defter (host-only / ikisi-esit / CIHAZ-ONLY); tek bitlik surum yalnizca gereksiz YUKLEMEYI atlatabiliyordu, cunku 'tek kopya cihazda' diye bir durumu ifade edemiyordu. Sonuc olculdu: `gpu_combustion_ms` 14,09 -> 6,79 ms, `total_ms` 218 -> 213 (%2,4) - odul kucuk ve sebebi kalici: `boundaries + solids` host'ta ve zincirin ORTASINDA oldugu surece `gasSyncGridToHost` combustion'in biraktigi indirmeyi geri ekliyor. **En pahali ders bu partide iki kez ayni sekilde geldi: bir bayragin SET edildigi yer ile OKUNDUGU yer arasindaki bosluk** (`gpu_resident_fields_valid` hicbir yerde true yapilmiyordu; `scalar_inputs_resident` okundugu anda her zaman false'tu) - ve ucuncusunu ben urettim: `runGpuTurbulence` density/temperature/interaction'i sadece MASKE olarak okudugu icin tuketici sayilmadi ve combustion'in cihazdaki sonucunu bayat host kopyasiyla ezdi; f120'de 959 364 yerine 96 826 aktif hucre. Onu yakalayan sey gecici `device_resident_chain` anahtariyla yapilan A/B idi (bake imzasina KASTEN dahil edilmemisti: acik ve kapali bake AYNI olmali). Anahtar gorevini bitirince kural 5 geregi sokuldu. Sirada: `boundaries + solids` GPU'ya - degeri artik `gpu_host_sync_ms` satiriyla olculebilir. |
| [NUKLEER_PRESET.md](NUKLEER_PRESET.md) | **WRITTEN, UNBUILT 2026-09-20.** Two nuclear detonation presets (cinematic and kilometre-scale physical) and the one solver parameter they exist for: `gas_ambient_stratification`. Gas buoyancy compared temperature against a SINGLE scalar ambient, so a rising plume never lost lift and climbed until the domain LID stopped it - meaning a mushroom cap's shape was the box talking, and it changed when the domain was resized. That failure is invisible by construction: a lid flattens a cap exactly the way a real inversion layer does, so the wrong result looks right. The fix is a POTENTIAL-temperature gradient (positive with height, the opposite sign to the lapse-rate intuition - this solver has no adiabatic cooling, so a parcel conserves its value and the ENVIRONMENT must rise), bounded at `-|anomaly|` so still air feels exactly nothing instead of acquiring a domain-wide phantom downdraft that would read as 'the smoke settles nicely'. Cap altitude is now h* = anomaly / stratification, a number the author picks and a script can assert. Shipped with the testability half, because the presets were panel-only and a twenty-parameter recipe cannot be calibrated through a human pressing a button: `particle.add_preset` creates any authored preset by slug, `gas.measure_plume` measures the live field (extent, cap height above the domain floor, widest slice AND its height, peak/mean heat, lid clipping). Its `measured` flag is the load-bearing field - every extent reads 0 both for an empty domain and for one that could not be sampled, so without it a script watching the cloud would read 'height 0' and conclude the detonation failed. No render backend was touched and none needs to be: buoyancy is in the SIMULATION compute path, which RayFusion/VulkanRT/OptiX/CPU all consume. Acceptance: `docs/dev/NEXT_BUILD_CHECKS.md`, `scripts/ipc/Test-NuclearPreset.ps1`; the result nobody should assume is `touching_ceiling` being false - a run that hits the lid still LOOKS like a good mushroom. |
| [MESH_EDIT_IPC_ACILDI.md](MESH_EDIT_IPC_ACILDI.md) | **WRITTEN, UNBUILT 2026-09-20.** The eight polygon edit operators (extrude, inset, bevel, loop cut, dissolve edges/vertices, merge, weld) had been implemented on SceneUI for a long time while `mesh.tools.list` advertised them as `"scriptable": true, "ipc_exposed": true` and NO method existed to call them - the catalogue measured its own intent, the same failure class as the hand-kept IPC catalogue that came back 299 rows empty. The same field lied in the other direction too: `edit.edge_bevel` was registered `Planned` while `bevelSelectedEdges` was a full implementation, and `Planned` tools are hidden from the default listing, so the edit workspace's only real rounding operator was invisible to every caller. Opening the operators alone would not have been enough: all eight consume `editable_mesh_cache.selection`, so without a selection API they would return "nothing selected" forever and read as BROKEN operators rather than missing ones - and an id list is unreachable in practice for an agent that knows "the top face", not face 4, hence `select_by_normal` / `select_by_box`. Shipped alongside: `scene.object_info` now returns local and world bounds, because an agent assembling geometry could only eyeball a screenshot to judge whether a leg reached a rail; bounds are ABSENT rather than zeroed when there is no geometry, and a new `meshes` field exposes that the old first-hit read reported a slice of a multi-material object as the whole. Not closed: `editable_mesh_cache` still lives on SceneUI, so core state still sits in the UI - this batch made it readable and writable from outside so core and panel can no longer diverge unobserved. Acceptance: `docs/dev/NEXT_BUILD_CHECKS.md`, probe `scripts/ipc/Probe-MeshEdit.ps1`; the one result nobody should assume is bevel actually mutating topology rather than returning ok as a silent no-op. |
| [ANIM_STATE_MACHINE_FROZEN.md](ANIM_STATE_MACHINE_FROZEN.md) | **VERIFIED LIVE 2026-09-13.** An animation state machine never left its current state, and selecting its node in the AnimGraph inspector froze the character outright. Three independent silent roots. (1) `StateMachineNode` SERIALIZED its runtime state, and the node inspector mirrors the selected asset node into the live runtime node every frame through `onSave`/`onLoad` - so a completed transition was undone on the next frame and the machine re-fired the same transition forever; permanently transitioning is visually identical to frozen. (2) The same mirror calls `onLoad` alone, which REBUILDS the node's dynamic pins with `id = 0`; `loadFromJson` already re-applies pin ids after `onLoad` for exactly this reason, but the second call site never inherited that fix, so every link into the state machine was orphaned and each state evaluated to an EMPTY pose. (3) `BlendNode::blendPoses` dropped `wasUpdated`, and the renderer only writes bone matrices when that flag is set - so the character froze for the whole blend even with 1 and 2 fixed. Two dead paths removed on the way: blend nodes applied the bind-pose offset in LOCAL space (would double-apply, never fired because its lookup key was empty), and `PoseData::boneNames` was consumed by three sites and written by none, which silently reduced `LayeredBlendNode`'s bone mask to "affects nothing". The whole class survived because a state machine's live state was visible ONLY in a panel: new `anim.state_machines` / `anim.force_state` make it scriptable, and `pose_connected` is the field that matters - a state with no pose is not an error at any layer. Probe: `scripts/ipc/Probe-AnimStateMachine.ps1`. Verified after the build: `pose_connected` true on both states, state stable, clip time advancing. The follow-up report that "the transition happens once and then the second clip just plays" was measured in ONE call and was NOT a bug - the graph holds a single `w -> d` transition with no `d -> w`, plus `has_exit_time=false` (so it fires on the first frame and the first clip never plays) and a 5 s blend time. That is what the instrument was for: the same symptom could equally have been the three roots returning, and the panel shows both the same way. A fourth, smaller root was found in the same function and is fixed but UNVERIFIED: `Transition::evaluate()` CONSUMES a Trigger parameter and the condition used to be asked before the exit-time gate, so a trigger fired on an ineligible frame was silently eaten - the symptom is not "triggers are broken" but "triggers work sometimes". Still open, scope not code: state machine AUTHORING is panel-only, so no regression test can build one from a script. |
| [RAYFUSION_SPECULAR_REFLECTIONS.md](RAYFUSION_SPECULAR_REFLECTIONS.md) | **WRITTEN, UNBUILT 2026-09-13 (slices A+B+C). Every shader compiles; the C++ has not.** How low-roughness reflectivity and emissive near-field light reach the realtime viewport. Opens on a source finding that reframes the whole question: an opaque metal gets NO local reflection today, because the SSR call is inside a `drawPhase == PREVIEW_PHASE_TRANSMISSION` gate - opaque surfaces see only the sky lookup, and that gate cannot simply be opened because SSR reads the opaque snapshot, which is copied AFTER the opaque pass. Two designs are rejected with reasons worth keeping. The probe field cannot carry a mirror: a probe texel is a hemisphere average at a grid-cell centre and a reflection needs surface-origin parallax, so a specular lobe there would produce the plausible-but-wrong class. And the existing per-pixel RT passes cannot grow one either, because they hold only DEPTH: `giSurface()` reconstructs a GEOMETRIC normal, the reflection direction error is 2x the normal error, and at roughness 0.05 the lobe is ~3 degrees - narrower than the error. The screen-GI consumer already pays for that split with its `dot < 0.5` bail-out. Chosen architecture: a thin G-buffer (oct shading normal + reflectionRoughness + split-sum weight) written by the pass that already computes them, then a reflection compute after it, composited into HDR before DoF - producer EQUALS consumer, separately measurable, and gateable by indirect dispatch so a scene with no shiny surface costs nothing. The framing is what makes "not metals only" free: the pass REPLACES the environment radiance lookup rather than adding a metallic term, so dielectrics inherit it through the Fresnel they already use, and the gate is on the LOBE (`roughness < t` AND split-sum weight) - a `metallic > x` gate would have excluded exactly the varnished-wood and painted-floor surfaces the question was about. Three traps named: double occlusion if `rfSpecularSkyVisibility` also stays (the image just gets darker and nobody calls it a bug), a visible seam unless the ray miss falls back to the SAME env lookup with the SAME filtering, and clearcoat being a second lobe one ray cannot serve. Emissive near-field is a separate parallel slice: it already works structurally (`rfBounceRadiance` returns emission), the gap is the ESTIMATOR - emissive geometry is not a sampleable light (the table is point+directional only, Spot/Area counted unsupported), so a lamp is found only by chance hemisphere hits. Predicts the symptom in advance: not flicker but a STABLE stippled pattern (the seed is deliberately frame-independent) smeared by a 5x5 blur with no luminance clamp. |
| [RAYFUSION_SKINNED_SHADOW_STALE.md](RAYFUSION_SKINNED_SHADOW_STALE.md) | **ROOT CAUSE FOUND 2026-09-13, fix WRITTEN, UNBUILT.** A skinned character animated in the raster image while its RayFusion ray shadow stayed at the pose of the frame the acceleration structure was built in. The gate could not see it by construction: GPU skinning rewrites the CONTENTS of the vertex buffer the BLAS borrows, so the handle, the device address and the vertex count all stay put and BOTH scene-AS signatures match - `ready=true`, `builds` correctly flat, every counter green. Second layer: refitting the BLAS alone is not enough, because each TLAS instance carries the world AABB the BLAS had at build time and rejects the ray on that box first, so a limb leaving its old box reads as a shadow with a bite taken out of it. Fix: a skin generation bumped only where a write actually happened, ALLOW_UPDATE on skinned BLASes only, a device-side refit (no CPU upload - `updateBLAS()` would push a base pose over the skinned result) with a periodic MODE_BUILD reset, and a TLAS refresh. New counters `blas_skinned` / `skin_refits` split POSSIBLE from HAPPENING; `Probe-RayFusionSkinnedShadow.ps1` measures both, including the quiet failure where the counter climbs on a parked timeline. |
| [RASTER_FRAME_COST_2026_09_10.md](RASTER_FRAME_COST_2026_09_10.md) | **MEASURED LIVE 2026-09-10, no code written. Pick the next optimization from here.** The foliage frame is now ~81 ms (was 324) and splits almost exactly in half: geometry throughput 38.9 ms (1.29 ms/Mtri across the prepass and main pass) and a triangle-count-INDEPENDENT shading constant of 38.0 ms, fitted from three quality presets and reproduced in a second independent run by the new `scripts/ipc/Probe-GeometrySlope.ps1`. Four results a next agent must not re-derive: the depth prepass costs 19.4 ms and SAVES 162 ms, which overturns the 2026-09-08 "net negative, default off" record; `viewport.set_raster_depth_prepass(false)` is silently overridden while RT shadows are on, so an A/B on that lever can measure two identical arms and report "no difference"; the RT shadow mask costs 2.3 ms against the cascade atlas' 74.3 ms, so soft shadows (cone sampling + a depth-aware filter + PCSS) fit in the existing budget and the light `radius`/`width`/`height` fields already exist unread; and RayFusion 2b was silent for two RUNTIME reasons, not a structural one: the probe window was still the 12x6x12 m build constant covering no visible surface, and the traced producer was never requested. Fixed live (120x48x120 m, 400 probes, traced): hit_fraction 0 -> 0.741, shaded bounce hits 1 -> 76, image change 369x the repeat-arm noise floor, cost unchanged at 84 ms. `core_status.gi_active=false` is WRONG and produced that misdiagnosis - the shader consumer `rfSampleProbeField` is wired and working. The result is not yet correct either: occluded ground goes near-BLACK because the bounce gate rejects 69 layered/transparent foliage materials, so occlusion arrives with no indirect light to put back - connect occlusion and bounce in the SAME batch. Carries the measured raster-vs-RT image gap that decides the visual roadmap: near/far luminance ratio 1.18 (raster) vs 2.02 (RT) and a ground mid-tone fraction of 1.8% vs 33.5%, so "flat lighting" and "hard shadows" are numbers, not impressions; and RT costs 61 ms PER SAMPLE against an 81 ms raster frame, so the ray budget for a sky-visibility (AO) term is affordable at ~2-3 ms. Also records why `viewport.set_automatic_cutout` measures ZERO here: it patches the GPU material copy only, so CPU-side consumers such as transmission-replay eligibility never see it, and the live 324->82 ms win came from the AUTHORED flag on four needle materials - which is NOT saved to the project. |
| [BUG_DELETE_LEAVES_STALE_GEOMETRY_SNAPSHOT.md](BUG_DELETE_LEAVES_STALE_GEOMETRY_SNAPSHOT.md) | MEASURED 2026-09-07: scene.delete drops the mesh from the drawn image but leaves a dangling raster instance AND does not bump g_scene_geometry_generation, so any consumer that snapshots geometry on that counter keeps a DELETED object with no error. RayFusion now gates on content signatures instead; the general path is still open. |
| [RAYFUSION_PROBE_BOUNCE.md](RAYFUSION_PROBE_BOUNCE.md) | Step 1b-beta, written and unverified on GPU: one diffuse probe bounce reading real raster material IDs, sky plus point/directional visibility. Carries the 2026-09-07 measurement finding: the panel reported 31 of 31 materials unsupported in a scene whose table holds exactly one qualifying material, and a live IPC A/B showed a textureless opaque material is rejected too. The gate is unchanged; the counter now names the CLAUSE that rejected each material, because a bare "31 unsupported" reads identically for a fully textured scene and for a gate that rejects everything. |
| [RAYFUSION_RT_SHADOWS_AND_TRANSPARENCY.md](RAYFUSION_RT_SHADOWS_AND_TRANSPARENCY.md) | **STEP 2a WRITTEN, UNBUILT 2026-09-08.** RayFusion is a render path: hand RT the terms raster is worst at while keeping same-quality-every-frame with no accumulation wait. Measured split at ~29M triangles: geometry ~8 ms, shadows 113 ms, scene ambient ~160 ms, material/opacity ~139 ms - and raster pushes 104M triangles at 56.8 ms in a 9.9-BILLION-triangle scene, so geometry is never the wall. Shadows are the clearest hand-off: rtPreviewShadow() costs up to 6 cascade matrix multiplies plus 9 PCF taps per fragment across ~86M invocations, and one ray per pixel replaces all of it on an AS that already exists. The ray budget was MEASURED on this scene's own TLAS before building anything: 65,536 rays in 0.196 ms, still dispatch-overhead-bound, so >=334 Mrays/s and about 5 ms for 1.59M rays. Step 2a ships the PRODUCER only - the mask is written and measured but nothing reads it, and the audit enforces that the consumer did not change, because if both move at once the measurement cannot separate cost from quality. Two known blockers before 2a can be consumed, both deliberate: the pass runs AFTER the main pass so the mask is one frame late (fixing it means splitting the render pass, which needs no new pipelines since load/store ops do not affect pipeline compatibility), and the ray uses OpaqueEXT so foliage cards block light entirely - the symptom of that one is not "no shadows" but "the forest is too dark". Also records that the depth prepass, written first as the ray-origin source, measured NET NEGATIVE on its own (~22 ms, no gain) and is defaulted off. |
| [GENERAL_MESH_LOD_DESIGN.md](GENERAL_MESH_LOD_DESIGN.md) | **DESIGN, 2026-09-08, no code.** How to fill the 98,500-vs-96 LOD cliff WITHOUT tying the solution to specific assets. The first sketch selected needle/twig triangles by material name; the user rejected it because it would do nothing for their own objects, and they were right. What survives from that analysis is the structural fact, not the material one: 84% of the tree is small, disconnected, alpha-tested cards. So the design classifies by structure and never by content - weld into connectivity (reusing the flat_soa_to_unique cache pattern from the melt path), find connected components, then QEM edge-collapse the large connected surfaces but DELETE whole small components with area compensation for the many-small case, because a 2-triangle alpha card has no intermediate collapse state. Selection moves from a distance threshold to a screen-error metric, which is also the front-end a cluster pipeline would need later, so it is not throwaway work. Blocker: the cull chain is hardwired to exactly two levels (one proxy, a bool in classify()). |
| [BUG_WORLD_SKY_CHANGE_SOMETIMES_NOT_PRESENTED.md](BUG_WORLD_SKY_CHANGE_SOMETIMES_NOT_PRESENTED.md) | **OPEN, parked 2026-09-08.** Sky changes sometimes do not reach the screen; the scene stays as the project saved it while RT responds correctly. Intermittent, and camera motion hides it because motion retriggers the render. One concrete measured finding survives the parking: solid and hdri hash to the SAME RayFusion producer signature (10267504932950959680, twice, at different hit_fractions), so the probe field cannot tell those two modes apart and never re-applies between them. That alone does not explain everything - publishes still increment and sun elevation propagates correctly - so the remaining suspect is the deliberately deferred setWorld() not always being serviced by the main loop, the same shape as the earlier cleared-rebuild-flag bugs. The first hypothesis (the UI panel not invalidating) was measured FALSE: it sets g_world_dirty and resets accumulation. |
| [RASTER_MICROTRIANGLE_WALL.md](RASTER_MICROTRIANGLE_WALL.md) | **FIXED AND MEASURED LIVE 2026-09-08.** The LOD triangle budget was never enforced: `raster_cull.comp` solved every mesh's distance threshold against the WHOLE-SCENE target, so all 35 cull meshes each claimed the full 29.6M and the effective cap was 35x - the feedback ratio pinned at 1.5 every frame and nothing went proxy (28 of 1033). Fixed by sharing the target proportional to per-mesh demand; verified on a 10,030-instance forest: proxies 28 -> 4,965, submitted 45.9M -> 32.1M against a 29.6M target. ★★★★★ The measurements then found the REAL bottleneck, and it is not primitive rate (solid pushes 84.9M triangles in 97.9 ms = 867M tri/s): LOD is BINARY at 98,500 triangles per full instance vs 96 per proxy, a 1:1026 cliff with nothing in between, so the 292 instances that must stay full carry 98.4% of the triangles while 4,965 proxies carry 1.6%. That single fact explains all three user observations - proxies look bad, proxies barely help in fusion (they are exhausted, not ineffective), and solid mode is fine (1.15 vs 15 ms/Mtri). Also carries the full per-layer cost split at ~29M triangles (geometry 42 / material+opacity +139 / scene ambient +162 / directional light + 3 cascades +113 = 456 ms measured 455.6) and closes two measurement gaps: RayFusion is noise here, and the diffuse probe consumer cannot be the cost because it early-outs on 94.5% of fragments. |
| [RASTER_SCENE_LIGHTING_FRAGMENT_COST.md](RASTER_SCENE_LIGHTING_FRAGMENT_COST.md) | **MEASURED 2026-09-08, no fix written.** Why "fusion" (scene) lighting suddenly costs so much in a foliage scene, separated by live A/B: it is NOT the light loop (0 scene lights; three_point runs 3 iterations and is 3x faster) and NOT geometry (solid shading draws 45.0M triangles in 98.9 ms; scene draws 44.3M in 316.9 ms). Cost is linear in TRIANGLE count, not pixel count - 7.2 ms/Mtri in scene vs 2.2 in solid - which points at overdraw from overlapping foliage cards, not resolution. RayFusion's specular-visibility consumer measured at ~28 ms of it (the "cost not measured" note in its own doc is now closed); the diffuse probe consumer has no off switch so its share is still unknown. Next structural candidate: a depth prepass, which the raster viewport does not have. |
| [RASTER_GPU_CULLING_NEVER_ENABLED.md](RASTER_GPU_CULLING_NEVER_ENABLED.md) | **MEASURED 2026-09-08, fix written and unbuilt.** The realtime viewport's `buildRasterGeometry` override carried a COPY of the base class's tail and dropped `rebuildRasterInstanceLayout()`, so the global instance buffer never turned on there. One missing call cost two things: GPU culling and scatter LOD proxies never engaged (45.9M triangles submitted per frame, proxies carrying 30k, 55% over the LOD target) AND every frame paid a full frame-ring drain (0.98/frame measured) because the drain-free direct write lives behind the same flag. The consumer side was already complete. Third victim of the same override trap - the two tails are now ONE body, and the audit rejects a second copy. No visual symptom exists: culling off draws correctly, only slower. |
| [RAYFUSION_FOLIAGE_FRAME_COST.md](RAYFUSION_FOLIAGE_FRAME_COST.md) | Written, unbuilt: in a 1000-instance foliage scene the raster viewport crawled while the same scene was fast in RT. One root cause found and fixed - the bounce table build re-hashed the whole scene's per-vertex material-ID stream (264 MB, byte-serial) on EVERY frame, for every TLAS entry, and was reported nowhere. Carries the reading lesson: "CPU 6%" on a 16-thread machine is ONE SATURATED CORE, not an idle CPU. Second candidate (camera motion flipping the instance signature into a per-frame TLAS rebuild + full frame-ring drain) is named but NOT measured; `tlas_only_refreshes` is the number that decides it. |
| [RAYFUSION_PROBE_GRID.md](RAYFUSION_PROBE_GRID.md) | Written, unbuilt: the probe window (cell counts, cell size, placement) became a RUNTIME value reachable from UI, Python and IPC instead of a build constant. It was a 12x6x12 box nailed to the world origin, and the measured room scene put 6 of its 16 useful probes outdoors (hit_fraction 0.105) - so every RayFusion image question so far was confounded by coverage that could not be varied without a rebuild. Defaults are unchanged, so nothing gets slower until someone asks for a different window. Records the debt this slice did NOT pay: a full frame-ring drain plus a whole-window upload per publish, which stays invisible until continuous camera following makes publishing per-frame. |
| [RAYFUSION_PROBE_CORE.md](RAYFUSION_PROBE_CORE.md) | Phase 0.1 written, unbuilt: world-cell probe cache, bounded scheduling, epoch/ticket validation and native control-plane checks exposed through UI/Python/IPC. GPU producer and raster GI consumer are not connected. |
| [RAYFUSION_RENDERER.md](RAYFUSION_RENDERER.md) | User-approved target: RayFusion hybrid raster + RT renderer, with the phase table. Work is routed by workload class, not by a fixed raster/RT split; the seam is the ambient lookup, so the producer can change without touching the consumer. Step 0 (Physical Sky prefiltered ambient) written, unbuilt. |
| [REALTIME_PERF_FIRST_FIXES.md](REALTIME_PERF_FIRST_FIXES.md) | Pending build: reject provably opaque transmission replay before texture work, skip zero-contribution sky lobes, and reuse unchanged selection matrix uploads without draining each camera frame. Physical Sky prefilter remains separate work. |
| [REALTIME_CAMERA_MOTION_PERF.md](REALTIME_CAMERA_MOTION_PERF.md) | **MEASURED 2026-09-07: ~60% of the frame is Physical Sky's ambient cone integral** (102 -> 38.6 ms with the sky off). Shadows, transmission replay, textures, glass and DoF together are ~3 ms - the 2026-09-06 candidate list is kept but eliminated. Root cause: the prefiltered IBL cascade is gated to `worldMode == 1u` (HDRI), so Nishita pays a 9-tap cone per lobe per fragment. Also records why `viewport.render_frames` cannot measure the raster viewport. |
| [BUG_VIEWPORT_DEVICE_LOST_ON_PROJECT_OPEN.md](BUG_VIEWPORT_DEVICE_LOST_ON_PROJECT_OPEN.md) | OPEN, root cause NOT found. `VK_ERROR_DEVICE_LOST` on raster submit when a project is opened over an already-loaded heavy scene **while realtime is active**; loading the same scene into an empty app is fine, and switching to Solid first is fine. The user's A/B killed three separate static hypotheses (cost/TDR, bad project data, VRAM eviction). The rule added the same day made the crash UNREACHABLE, which also silenced the tripwire -- so the guard was made switchable (`viewport.set_scene_load_guard`) and the outcome is published as values (`stale_descset_rebuilds`, `device_lost`). Next step is `scripts/ipc/Probe-DeviceLostOnProjectOpen.ps1`, then validation-layer output. Carries the method lesson: ask for the NEGATIVE arm first. |
| [REALTIME_HAIR_AUDIT.md](REALTIME_HAIR_AUDIT.md) | Realtime hair is a fixed-color, 1-pixel line overlay; canonical hair material is not consumed. CPU child expansion/upload and RT work are performance candidates, not measured bottlenecks. Current live scene had no hair. |
| [IMPORT_EXPORT_OPEN_DEBTS.md](IMPORT_EXPORT_OPEN_DEBTS.md) | **Open debts left by Faz 3.** Assimp is gone and verified; this is what was deliberately left out. Top item is a rule-1 breach: `scene.import_model` returns nothing although ImportStats already measures every phase. Also 4.39M duplicate triangles from shared meshes, unread KHR_texture_transform, and the writer's non-conformant skin contract that the reader compensates for by sniffing `generator`. |
| [REALTIME_VOLUME_TABLE_NEVER_PUBLISHED.md](REALTIME_VOLUME_TABLE_NEVER_PUBLISHED.md) | Realtime viewport drew no volume because the volume packet was published to the render backend only — the raster viewport is a second VulkanBackendAdapter with its own VkDevice, and the SSBO is per device. Also: why the tripwire's `volumeCount=0` was misread |
| [API_SCRIPTING_ROADMAP.md](API_SCRIPTING_ROADMAP.md) | Scripting/IPC API waves and their status table |
| [PHYSICS_VALIDATION.md](PHYSICS_VALIDATION.md) | Is the solver RIGHT, not merely running — analytical cases, and why a script test is blind to the frame loop |
| [IPC_TEST_CHANNEL.md](IPC_TEST_CHANNEL.md) | The second test channel: drives the app from outside so it can see the frame loop — and the first thing it found is that physics.step is reverted 100% before you can read it |
| [SIMULATION_NODE_CONCEPTUAL_MODEL.md](SIMULATION_NODE_CONCEPTUAL_MODEL.md) | What the simulation node layer IS — read this before the object-model report |
| [PROFILE_SPLINE_NEXT_PHASE_ROADMAP.md](PROFILE_SPLINE_NEXT_PHASE_ROADMAP.md) | 2D profile spline authoring core; Faz 3.6 generalizes it into a shared 3D curve for road/scatter-mask/mesh-surface consumers, and Faz 3.7 makes drawing on a surface a shared service so River becomes a consumer of `SplineObject` instead of owning a private curve |
| [SIMULATION_NODE_OBJECT_MODEL.md](SIMULATION_NODE_OBJECT_MODEL.md) | Scope-based object model for the simulation node layer — steps 1-6 done, compiled, and live-verified |
| [AGENT_VIEWPORT_MEASUREMENT_PLAN.md](AGENT_VIEWPORT_MEASUREMENT_PLAN.md) | Driving the viewport and reading render DATA over IPC — the substrate agents verify with |
| [AGENT_DISCOVERY_LAYER_PLAN.md](AGENT_DISCOVERY_LAYER_PLAN.md) | Self-describing API layer — agent.* discovery, method registry, workflow recipes |
| [DESCRIPTOR_TRUTH_VERIFICATION.md](DESCRIPTOR_TRUTH_VERIFICATION.md) | Whether the descriptor prose is TRUE — claim grounding, capability mirror drift |
| [LOCAL_MODEL_HORIZON.md](LOCAL_MODEL_HORIZON.md) | Where a local 8B model breaks down on a long task, and what the loop does about it |
| [BUG_DELETED_NAME_REUSE_GHOST.md](BUG_DELETED_NAME_REUSE_GHOST.md) | OPEN: re-adding a deleted object name yields a half-existing object |
| [TEMPLATE_HUB_UX_ROADMAP.md](TEMPLATE_HUB_UX_ROADMAP.md) | Canonical product direction for startup / template / guided-scene UX |
| [GRANULAR_SIMULATION_ROADMAP.md](GRANULAR_SIMULATION_ROADMAP.md) | Sand and granular MPM/APIC constitutive solver |
| [GRANULAR_GPU_FIRST_PLAN.md](GRANULAR_GPU_FIRST_PLAN.md) | GPU-first execution plan for the granular solver |
| [GRANULAR_GPU_TEST_PROTOCOL.md](GRANULAR_GPU_TEST_PROTOCOL.md) | Vulkan granular acceptance test |
| [GRANULAR_COHESIVE_DAMAGE_TEST.md](GRANULAR_COHESIVE_DAMAGE_TEST.md) | Bonded/cohesive granular damage acceptance test |
| [material_transformation_fracture_roadmap.md](material_transformation_fracture_roadmap.md) | Burning, melting, mass transfer, fracture |
| [NODE_SIMULATION_ARCHITECTURE_PLAN.md](NODE_SIMULATION_ARCHITECTURE_PLAN.md) | Node-based simulation and thermochemistry layer |
| [VULKAN_PRODUCTION_VOLUMETRICS_ROADMAP.md](VULKAN_PRODUCTION_VOLUMETRICS_ROADMAP.md) | Production volumetric path and its release gates |
| [VULKAN_GPU_FORCE_FIELD_SIMULATION_ROADMAP.md](VULKAN_GPU_FORCE_FIELD_SIMULATION_ROADMAP.md) | Force-field evaluation on Vulkan Compute |
| [VOLUME_SHADER_GRAPH_ROADMAP.md](VOLUME_SHADER_GRAPH_ROADMAP.md) | Volume output of the unified material graph |
| [volumetric_cloud_layer_roadmap.md](volumetric_cloud_layer_roadmap.md) | Layered procedural cloud volumes |
| [gpu_foliage_scatter_roadmap.md](gpu_foliage_scatter_roadmap.md) | GPU foliage scatter migration |
| [hydraulic_multipass_next_steps.md](hydraulic_multipass_next_steps.md) | Terrain hydraulic erosion multi-pass |
| [ASSIMP_IMPORT_REPLACEMENT_BRIEF.md](ASSIMP_IMPORT_REPLACEMENT_BRIEF.md) | Assimp import'tan çıkış. **Faz 0 YAZILDI, DERLENMEDİ (2026-09-05):** animasyon anahtar tipi Assimp'ten ayrıştırıldı (`RayTrophi::VectorKey`/`QuatKey`), sarkan `aiNode*` alanı ve ölü `AnimatedObject` söküldü, `.rtp` şeması değişmedi. Faz 1 (doğrudan glTF okuyucu) başlamadı. Kabul aleti: `anim.source_clips` + `scripts/probe_import_export_parity.py` |
| [SCENE_EXPORT_DIRECT_GLTF.md](SCENE_EXPORT_DIRECT_GLTF.md) | Why the Assimp export round trip cost minutes and 15 GB on one giant mesh, and the plan-then-write glTF writer that replaced it — plus `scene.export_gltf`, the script surface export never had |
| [TERRAIN_NODE_CONTRACT_REDESIGN.md](TERRAIN_NODE_CONTRACT_REDESIGN.md) | Compact terrain ports, authoritative field ownership, node pruning and setup migration |
| [TERRAIN_LANDFORM_SHAPE.md](TERRAIN_LANDFORM_SHAPE.md) | Measured: the Noise Generator was producing a homogeneous fractal, not a landscape - no landform wider than a quarter tile, 4.4% flat ground, gaussian hypsometry. Feature Size sized to the terrain, dissection driven by an authored lowland area, and terrain.landform_stats to judge it |
| [TERRAIN_BUILD_CAP_SCOPE.md](TERRAIN_BUILD_CAP_SCOPE.md) | Measured: maxDepositionMeters bounds only the route pass, talus escapes it and never saturates — plus the two instruments that were reporting zeros as success |
| [TERRAIN_PERF_HANDOFF.md](TERRAIN_PERF_HANDOFF.md) | Terrain build cost: what was measured, what is verified, what is NOT, and the ordered next steps — start here before touching terrain performance |
| [TERRAIN_SATMAP_COLORIZER_ROADMAP.md](TERRAIN_SATMAP_COLORIZER_ROADMAP.md) | Gaea-style SatMap macro colour over the existing 4-layer splat blend — why it must modulate rather than replace the albedo, and the Faz 0 split of field / mesh / paint resolution that has to land first |
| [refactoring_implementation_plan.md](refactoring_implementation_plan.md) | DNA / flat-SoA data-oriented core migration |
| [flat_mesh_facade_audit.md](flat_mesh_facade_audit.md) | Remaining `Triangle` facade call sites |
| [REALTIME_HDR_AND_DOF.md](REALTIME_HDR_AND_DOF.md) | **Realtime raster went from one pass to three.** Depth of field needs an HDR intermediate: bokeh is a bright point opening into a disc, and the raster viewport used to tone map inside every scene shader, so there was nothing un-clipped left to blur. Records the pass split, the CoC formula shared with the path tracer, why there is deliberately no separate realtime blur dial, and the four gates that `viewport.get_depth_of_field` reports by name. |
| [CAMERA_DOF_SWITCH.md](CAMERA_DOF_SWITCH.md) | **`aperture == 0` could not stay the off switch.** It was a sentinel, not a value: no f-number produces a zero opening, so wiring the f-stop dial to the aperture destroyed the only way back and depth of field could never be turned off again. Records the separate `depth_of_field` switch, the single `effectiveLensRadius()` every consumer reads, the THREE disagreeing f-number conversions found on the way (one of which pinned the panel to f/128), the unscaled CPU lens sample, and why old projects default to `aperture > 0.001`. |

## Rules and architecture in force (REFERANS)

Volume shadow implementation: [REALTIME_VOLUME_SHADOWS.md](REALTIME_VOLUME_SHADOWS.md) — user confirmed it works; detailed GPU timing and full regression coverage were not separately reported. GI remains open.

| Document | Area |
|---|---|
| [VULKAN_RT_SSS_ANALIZ_VE_IYILESTIRME_PLANI.md](VULKAN_RT_SSS_ANALIZ_VE_IYILESTIRME_PLANI.md) | **REFERANS 2026-09-24. Kullanici Vulkan RT'de dogruladi.** Ek: SSS Method (Random Walk/Fast) + Walk Max Steps (8-256) Vulkan'a acildi; RayFusion RT albedo paritesi + egrilik sarmasi. Vulkan RT random-walk SSS. Planin vaatleri koda karsilik gelmiyordu (cift renk carpimi, radius ton uretemiyordu, IOR oluydu, probe en yakin cikisi bulmuyordu, cikis normali iceri bakiyordu) ve hicbiri olculemiyordu cunku SSS parametreleri yalnizca paneldeydi. Duzeltildi + `material.set` SSS anahtarlari + `Probe-SssResponse.ps1`. |
| [OPTIX_DONDURULDU.md](OPTIX_DONDURULDU.md) | OptiX is FROZEN: no new work, but NOT removable. TDR resets the Vulkan driver only — the CUDA/OptiX context survives it, so OptiX is the real lifeboat. The obvious assumption ("same GPU, it dies too") is wrong; read this before proposing to delete or default-disable it |
| [BUG_CHANGE_GATE_HALF_UPDATED.md](BUG_CHANGE_GATE_HALF_UPDATED.md) | Adding a field to a change-detection gate changes TWO places: the comparison and the memo. Only the comparison was updated, so the gate never closed again — setRenderParams every frame, and on OptiX that zeroed the accumulation buffer every frame while the sample counter kept climbing. Also: why the first instrument (wipe_count) read healthy while the bug was raging |
| [BUG_719_WAS_A_FABRICATED_ERROR_CODE.md](BUG_719_WAS_A_FABRICATED_ERROR_CODE.md) | Solid/RayFusion -> OptiX crash: the 719 "unspecified launch failure" was never measured, it was ASSIGNED (`false ? cudaSuccess : cudaErrorLaunchFailure`, and cudaErrorLaunchFailure IS 719) by a benign null-buffer guard. CUDA_CHECK calls std::terminate() rather than throwing, and skipped the first-error latch, so the terminate handler printed a POSITIVE but wrong diagnosis. Also: a failed cudaMalloc left a freed pointer non-null |
| [DEVELOPMENT_PRINCIPLES.md](DEVELOPMENT_PRINCIPLES.md) | Binding engineering rules (file size, ownership, splitting) |
| [BUG_GLASS_LOBE_CANNOT_SEE_ANALYTIC_LIGHTS.md](BUG_GLASS_LOBE_CANNOT_SEE_ANALYTIC_LIGHTS.md) | Glass showed no specular highlight from any lamp: the glass lobe skips NEE (correct) and scene lights carry no geometry in the TLAS (correct) — together they make the reflection structurally unreachable, not merely dim. Water had already grown a private estimator for this; it is now the shared `addDielectricDirectLighting`. Read before deciding any lobe may skip NEE |
| [BUG_FOLIAGE_ASSET_TEXTURE_ALIASING.md](BUG_FOLIAGE_ASSET_TEXTURE_ALIASING.md) | Why two Asset Library plants wore each other's textures on every backend: one shared import name disarmed the embedded-texture ABA guard. Plus the material-ID generation rule for anything that caches a resolved ID |
| [FOLIAGE_MASK_CONTRACT.md](FOLIAGE_MASK_CONTRACT.md) | Foliage placement masks: why include and exclude are deliberately asymmetric, why the slot count does NOT grow, and the Publish Field node that makes composing masks in the graph the answer instead |
| [VULKAN_SIMULATION_RESET_SAFETY.md](VULKAN_SIMULATION_RESET_SAFETY.md) | Reset/rewind safety rule — TDR avoidance |
| [BUG_SKINNING_DESCRIPTOR_POOL_LEAK.md](BUG_SKINNING_DESCRIPTOR_POOL_LEAK.md) | Why GPU skinning fell back to the CPU the moment you added an object — and why Vulkan RT lost it at the same time: destroyRasterMesh dropped the skinning descriptor set instead of freeing it, and raster + RT share one fixed pool. Also: the RT path is slower by construction (BLAS refit), and the `dispatchSkinningAll` batching it advertised never existed |
| [BUG_TIMELINE_PLAYBACK_LOCKED_TO_RENDER_RATE.md](BUG_TIMELINE_PLAYBACK_LOCKED_TO_RENDER_RATE.md) | Why the timeline played at whatever speed the viewport computed: the 1-frame cap banked a time debt it could never repay, so playback pinned itself to the render rate permanently. The cap was the deliberate part; the carry underneath it was the bug |
| [simulation_physics_foundation_plan.md](simulation_physics_foundation_plan.md) | Field-first physics stack direction |
| [IPC_SECURITY_PERFORMANCE.md](IPC_SECURITY_PERFORMANCE.md) | IPC transport and local security model |
| [AGENT_RUNTIME_PHASE4_REVIEW.md](AGENT_RUNTIME_PHASE4_REVIEW.md) | Review of the agent runtime + discovery layer before Phase 4 — what is real, what only looks real |
| [REMOTE_IPC_GATEWAY.md](REMOTE_IPC_GATEWAY.md) | Remote gateway boundary (see also `docs/remote_ipc_gateway_openapi.yaml`) |
| [TEMPLATE_REGISTRY_API.md](TEMPLATE_REGISTRY_API.md) | Template Registry script/IPC surface |
| [INTERIOR_VOLUME.md](INTERIOR_VOLUME.md) | Procedural interior appearance model |
| [VULKAN_GAS_FLUID_LAYERING.md](VULKAN_GAS_FLUID_LAYERING.md) | Coincident gas + fluid surface layering |
| [VOLUME_BOX_REENTRY_POSTMORTEM.md](VOLUME_BOX_REENTRY_POSTMORTEM.md) | Volume-box re-entry: black band + cost explosion, and why a distance epsilon cannot fix it |
| [VULKAN_VOLUME_TEMPORAL_INSTRUMENTATION.md](VULKAN_VOLUME_TEMPORAL_INSTRUMENTATION.md) | Volume temporal stability and instrumentation |
| [FOLIAGE_WIND_PIPELINE.md](FOLIAGE_WIND_PIPELINE.md) | Foliage wind runtime modes |
| [terrain_river_lake_snow_notes.md](terrain_river_lake_snow_notes.md) | Snowmelt water budget and river visibility |
| [fire_burns_down_a_structure_recipe.md](fire_burns_down_a_structure_recipe.md) | End-to-end scenario recipe, built from panels only |
| [raytrophi_manifesto_and_plan.md](raytrophi_manifesto_and_plan.md) | Project direction manifesto — the "world kernel" idea |
| [TERRAIN_EROSION_FLUVIAL_REWRITE.md](TERRAIN_EROSION_FLUVIAL_REWRITE.md) | Landscape-evolution cycle: drainage-area feedback, lake spill and sediment transport — live-tested and closed 2026-08-23 |
| [TERRAIN_FLAT_DRAINAGE.md](TERRAIN_FLAT_DRAINAGE.md) | Why flat and pit-floor channels came out straight and angular — a filled flat has no gradient, so the conditioning ladder WAS the drainage pattern, and a flat could never incise to change it |
| [TERRAIN_DEPOSITION_MODEL.md](TERRAIN_DEPOSITION_MODEL.md) | Why the LEM transport pass was kept over the redundant channel carvers, and the avulsion + alluvial-spreading passes added on top of it |

| [POST_PROCESS_V2.md](POST_PROCESS_V2.md) | **The exposure/color contract in force.** The exposure MODE lives in the post chain, not on the camera: camera ISO/shutter/f-stop are read ONLY under `physical`. Last section records 2026-09-06, when the viewport exposure triangle, the camera panel and `camera.get.exposure_factor` were wired to that gate (they were still writing the camera's dead flags), plus the three formula copies removed and the one path left out: OptiX. |

## Closed — postmortems and audits (ARSIV)

| Document | What it records |
|---|---|
| [HANDOFF_2026_09_09_RASTER_FRAME_COST.md](HANDOFF_2026_09_09_RASTER_FRAME_COST.md) | **ARCHIVED 2026-09-10 - both open targets closed and measured; see RASTER_FRAME_COST_2026_09_10.md. DEVIR NOTU, 2026-09-09.** Foliage sahnesinde cakilmanin sebebi RayFusion DEGIL: RT golge dispatch'i karenin %0,4'u (1,17 ms). Kare tamamen GPU-bound (CPU 0,69 ms/kare): main_pass 147,8 + transmission 103,2 ms. Dort kok neden kapatildi ve hepsi CANLI dogrulandi - derinlik on gecisi ana gecisle INVARIANT degildi (`VP*M*p` vs `VP*(M*p)`, maskenin 2 ULP'lik derinlik kapisi tutmuyordu), ekran isin golgesinde tmin SABIT 0,02 idi (belirsizlik z^2 ile buyur; zeminde yatay acne, ekran goruntusuyle teshis edildi), maske tek isik kapsiyordu oysa dunya gunesi ile senkron directional AYNI yon (shadow_atlas 38 ms -> SKIPPED), ve olcum aletinin kendi iki kusuru. ACIK BIRINCI HEDEF: alpha-test `discard` ERKEN DERINLIK TESTINI kapatiyor - depoda hicbir shader early_fragment_tests beyan etmiyor, ana pipeline LESS_OR_EQUAL + depth-write ON, yani ortulu foliage katmanlari TAM materyal shader'indan geciyor; kullanicinin 'kameraya birkac agac girse cakiliyor' gozlemi ve olculen ters iliski (ucgen %29 artarken main_pass %31 dustu) bunu isaret ediyor. On gecis zaten kosuyor, yani EQUAL + depth-write OFF + early_fragment_tests mumkun - ve on kosulu (iki gecisin bit bazinda ayni derinligi) bu oturumda saglandi. IKINCI HEDEF: transmission replay 72.030 ucgenlik su icin 27,3 M ucgen ciziyor (~379x); sahne seviyesindeki yeni kapi DOGRU calisiyor ama bu sahne icin yanlis granulerlik, mesh basina ayiklama gerekiyor - bayatlama yonu 'cam kaybolur' oldugu icin onbellek dikkat ister. Ayrica uc IPC olcum tuzagi kayitli: yanlis parametre adi sessiz bos kume uretir (bir 'sifir gecirgen materyal' raporu boyle uretildi, gercek 40) ve material.info ozetinin vaat ettigi parametre setini DONDURMUYOR. |
| [BUG_LOADER_THREAD_RACES_ITS_OWN_FRAME.md](BUG_LOADER_THREAD_RACES_ITS_OWN_FRAME.md) | **KOK NEDEN KAPANDI, kullanici CANLI dogruladi 2026-09-09.** Proje acilisinda `DrawVolumePerformancePanel`de erisim ihlali. Yukleyici ipligi `ui.draw`in ICINDE dogar (Template Hub `drawMainMenuBar` icinde cizilir), ama Main.cpp'deki yukleme kapisi dongunun BASINDA bakar - yani karenin geri yarisi (drawPanels, overlay, render+submit+present blogunun tamami) `newProject` -> `InstanceManager::clearAll()` ile YARISIR ve panel serbest birakilmis ucgen vektorunu yineler. Teshisin devredilebilir kismi: **"sadece su iki proje, su sirada" bir icerik ipucu degil YARIS ipucudur** - sira, iki pencerenin cakismasini ayarliyordu (onceki sahne agir foliage olmali ki panel taramasi uzun surssun, yeni proje ayristirmasi kisa olmali ki clearAll panel hala gezerken dussun). Uc kapi eklendi. ★ HALA ACIK olan tahmin bu notta DEGIL, device-lost notunda yasiyor: ayni pencere oradaki arizanin da adayi ve sinamasi kalkanin kapatilmasini gerektiriyor. Yan duzeltme: panel `flat_meshes` kaynaklara kordu, dosyadan acilan her sahnede foliage VRAM tahminini AZ gosteriyordu (ve bu hic sikayet uretmedi). |
| [REALTIME_VOLUME_FINITE_RAYS.md](REALTIME_VOLUME_FINITE_RAYS.md) | User verified realtime gas/VDB visibility, speed and UI controls after replacing far-plane division by finite-depth rays. Volume cast shadows remain separate work; full camera/SDF regression coverage was not separately reported. |
| [GLASS_LOBE_CHECKS_ARCHIVE.md](GLASS_LOBE_CHECKS_ARCHIVE.md) | Preserved glass-lobe acceptance checklist from 2026-09-06; archiving this checklist does not mark its tests passed. |
| [FAZ3_DEVIR_NOTU.md](FAZ3_DEVIR_NOTU.md) | **ARCHIVE — Faz 3 is done.** Assimp was removed on 2026-09-06 after the user verified FBX, GLB and OBJ through the direct readers. §3 is still live reference: the traps already paid for when writing an importer here. |
| [UFBX_INCREMENT1.md](UFBX_INCREMENT1.md) | **ARCHIVE.** Static FBX reader and its dispatch. Superseded by increments 2 (skinning + animation) and 3 (OBJ + Assimp removal), both user-verified. |
| [IMPORT_GLTF_CHECKS_ARCHIVE.md](IMPORT_GLTF_CHECKS_ARCHIVE.md) | glTF import/export acceptance history through batch 9; §7/§8/§9(a) verified, §9(b) shared geometry storage remains a separate optimization. |
| [POSTMORTEM_CPU_PICK_P_VS_PORIG.md](POSTMORTEM_CPU_PICK_P_VS_PORIG.md) | CPU seçimi yanlış obje seçiyordu: `TriangleMesh::hit` ile `EmbreeBVH` farklı tampon okuyordu (`"P"` vs `P_orig`) |
| [GRANULAR_STABILITY_POSTMORTEM.md](GRANULAR_STABILITY_POSTMORTEM.md) | Granular blow-up at low Young modulus — four root causes, and the CPU/Vulkan stage contract |
| [VULKAN_HAIR_PAUSE_DOUBLE_FREE_POSTMORTEM.md](VULKAN_HAIR_PAUSE_DOUBLE_FREE_POSTMORTEM.md) | Double-free on the second pause/play, Vulkan RT hair |
| [VULKAN_PARTICLE_PRESET_PAUSE_TDR.md](VULKAN_PARTICLE_PRESET_PAUSE_TDR.md) | Particle preset pause/resume TDR |
| [VOLUME_RAYMARCH_FOUNDATION_AUDIT.md](VOLUME_RAYMARCH_FOUNDATION_AUDIT.md) | Three-backend volume parity audit, 2026-07-23 |
| [fluid_material_coordinate_quality_notes.md](fluid_material_coordinate_quality_notes.md) | Fluid UVW quality — why the remaining difference is the method, not a bug |
| [oidn_optimization_report.md](oidn_optimization_report.md) | OIDN denoiser CPU fallback and CUDA path |

## Proposed, not implemented (TASLAK)

| Document | Area |
|---|---|
| [KINEMATIC_COLLIDER_SOURCES.md](KINEMATIC_COLLIDER_SOURCES.md) | **2026-09-24.** Bone-attached collider proxies as one solver-neutral source for fluid, gas, granular (mud/snow footprints) and particles. Gap: today every collider is ONE rigid velocity, so a skinned character reads as a rigid block. Traps: skinning matrix ≠ bone world pose; pose must be sampled per substep. |
| [REALTIME_RENDERER_ROADMAP.md](REALTIME_RENDERER_ROADMAP.md) | Vulkan realtime viewport: product contract, frame graph and bounded quality profiles. **Separate Realtime mode was removed 2026-08-31** — the analysis holds, the ORDER was wrong; read the decision section first. Raster speed-up (presentation copies, GPU-driven culling) comes before scene lighting, which lands as an option in material preview. **2026-09-01**: carries the measured answer to "continuous LOD or scene lighting" — reaching pathtrace speed is the wrong target (RT cost is ~independent of instance count, raster's is linear), there is no mesh decimator in this repo, and the dominant cause of "crude proxy" was that proxies never reached the material pipeline at all. **2026-09-01**: material preview gained a `scene` lighting preset that reads the SAME light buffer the renderer reads (no second light list, so the two cannot disagree about which lights exist); shadows and world ambient are still absent and are reported as values rather than left to be inferred |
| [TERRAIN_ROAD_NETWORK_ROADMAP.md](TERRAIN_ROAD_NETWORK_ROADMAP.md) | Road/path üst katmanı: Faz 3.6 Curve to Mask + Road Carve üzerine profil, ölçülebilir field publication, foliage, crossing ve otomatik routing |
| [TERRAIN_ROAD_BUILD_CHECKS.md](TERRAIN_ROAD_BUILD_CHECKS.md) | Curve Input, Curve to Mask ve Road Carve faz-sonu derleme/canlı IPC doğrulama paketi |
| [AGENT_PIPELINE_ARCHITECTURE.md](AGENT_PIPELINE_ARCHITECTURE.md) | Multi-instance agent/human production pipeline — which decisions are irreversible |
| [photon_caustics_plan.md](photon_caustics_plan.md) | Photon caustic pass, Vulkan RT first |
| [stylize_mode_architecture.md](stylize_mode_architecture.md) | Stylize mode as an AOV-driven post layer |
| [stylize_pipeline_notes.md](stylize_pipeline_notes.md) | Stylize working notes |
| [mesh_paint_fill_layer_smart_masks_report.md](mesh_paint_fill_layer_smart_masks_report.md) | Non-destructive fill layers and smart masks |
| [mesh_paint_tablet_support_plan.md](mesh_paint_tablet_support_plan.md) | Tablet/pen pressure input |

## Rewritten every batch (CANLI)

- [NEXT_BUILD_CHECKS.md](NEXT_BUILD_CHECKS.md) — the ordered check list for the
  most recent batch of work. It is overwritten each time, so it describes only
  what is waiting to be verified right now.

---

Related material outside this folder:

- [`docs/`](../) — the user-facing HTML manual
- [`docs/template_hub/`](../template_hub/) — template manifest schema and examples
- [`CLAUDE.md`](../../CLAUDE.md) / [`AGENTS.md`](../../AGENTS.md) — working rules for agents and contributors
